<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>慧聪科技网上面经总结</title>
      <link href="/2026/02/25/%E6%85%A7%E8%81%AA%E7%A7%91%E6%8A%80%E7%BD%91%E4%B8%8A%E9%9D%A2%E7%BB%8F%E6%80%BB%E7%BB%93/"/>
      <url>/2026/02/25/%E6%85%A7%E8%81%AA%E7%A7%91%E6%8A%80%E7%BD%91%E4%B8%8A%E9%9D%A2%E7%BB%8F%E6%80%BB%E7%BB%93/</url>
      
        <content type="html"><![CDATA[<h1 id="慧聪科技面经总结"><a href="#慧聪科技面经总结" class="headerlink" title="慧聪科技面经总结"></a>慧聪科技面经总结</h1><p>3、HashMap 扩容后数据是如何移动的？</p><p>4、有什么办法保证HashMap的线程安全？ConcurrentHashMap、HashTable</p><p>5、如何保证共享变量在多线程的可见性。synchronized、volatile的了解</p><p>6、JVM内存区域划分，栈溢出的场景，堆的区域划分</p><p>7、Java中垃圾回收机制(GC)，常用的垃圾回收算法有哪几种？</p><p>8、数据库索引为什么加速查询？索引原理，索引类别，InnoDB聚簇索引</p><p>9、B+树与普通二叉树的区别有哪些？回表问题</p><p>10、最左匹配原则，如何分析sql语句执行较慢—explain分析语句（全表扫描、命中率索引）<br>12、更新数据时会如果设置的是唯一索引，会发生什么？消极更新时判断是否会破坏唯一性条件</p><p>13、事务的隔离级别</p><p>14、Spring常用注解，AOP与IOC的理解</p><p>15、拷打项目，项目介绍，遇到的问题及解决方案，使用MQ解决什么问题、</p><h3 id="16-Redis如何部署的，主从模式中，从节点能写入数据吗？"><a href="#16-Redis如何部署的，主从模式中，从节点能写入数据吗？" class="headerlink" title="16. Redis如何部署的，主从模式中，从节点能写入数据吗？"></a><strong>16. Redis如何部署的，主从模式中，从节点能写入数据吗？</strong></h3><h4 id="✅-Redis部署方式："><a href="#✅-Redis部署方式：" class="headerlink" title="✅ Redis部署方式："></a>✅ <strong>Redis部署方式：</strong></h4><ol><li><p><strong>单机部署</strong>：直接启动<code>redis-server</code>，适用于开发测试。</p></li><li><p><strong>主从部署（Master-Slave）</strong>：</p><ul><li><p>主节点负责读写。</p></li><li><p>从节点默认<strong>只读</strong>，用于备份和读扩展。</p></li><li><p>配置方式：</p><ul><li><p>在从节点<code>redis.conf</code>中配置：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">replicaof &lt;主节点IP&gt; &lt;主节点端口&gt;</span><br></pre></td></tr></table></figure></li><li><p>或者在运行时使用命令：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">REPLICAOF &lt;主节点IP&gt; &lt;主节点端口&gt;</span><br></pre></td></tr></table></figure></li></ul></li></ul></li><li><p><strong>哨兵模式（Sentinel）</strong>：用于主从自动故障转移。</p></li><li><p><strong>集群模式（Cluster）</strong>：数据分片，支持高可用和水平扩展。</p></li></ol><h4 id="❗-从节点能否写入数据？"><a href="#❗-从节点能否写入数据？" class="headerlink" title="❗ 从节点能否写入数据？"></a>❗ <strong>从节点能否写入数据？</strong></h4><ul><li><strong>默认情况下，从节点是只读的</strong>，不能写入。</li><li>如果强制写入（通过配置<code>replica-read-only no</code>），写入的数据不会同步到主节点，也不会同步到其他从节点，会造成数据不一致，<strong>生产环境不建议</strong>。</li></ul><h3 id="17-ConcurrentHashMap，区别，怎么实现线程安全？（JDK1-8前后）"><a href="#17-ConcurrentHashMap，区别，怎么实现线程安全？（JDK1-8前后）" class="headerlink" title="17. ConcurrentHashMap，区别，怎么实现线程安全？（JDK1.8前后）"></a><strong>17. ConcurrentHashMap，区别，怎么实现线程安全？（JDK1.8前后）</strong></h3><h4 id="✅-JDK1-7-vs-JDK1-8-区别："><a href="#✅-JDK1-7-vs-JDK1-8-区别：" class="headerlink" title="✅ JDK1.7 vs JDK1.8 区别："></a>✅ <strong>JDK1.7 vs JDK1.8 区别：</strong></h4><p>表格</p><p>复制</p><table><thead><tr><th align="left">特性</th><th align="left">JDK1.7</th><th align="left">JDK1.8</th></tr></thead><tbody><tr><td align="left"><strong>数据结构</strong></td><td align="left">分段锁（Segment + HashEntry）</td><td align="left">数组 + 链表 + 红黑树</td></tr><tr><td align="left"><strong>锁机制</strong></td><td align="left">每个Segment一把锁</td><td align="left">使用CAS + synchronized锁住链表头节点</td></tr><tr><td align="left"><strong>并发度</strong></td><td align="left">受Segment数量限制（默认16）</td><td align="left">更高，支持更多线程并发</td></tr><tr><td align="left"><strong>查询性能</strong></td><td align="left">链表过长时性能下降</td><td align="left">链表长度&gt;8转红黑树，查询O(log n)</td></tr></tbody></table><h4 id="✅-线程安全实现原理："><a href="#✅-线程安全实现原理：" class="headerlink" title="✅ 线程安全实现原理："></a>✅ <strong>线程安全实现原理：</strong></h4><ul><li><strong>JDK1.7</strong>：<ul><li>使用<strong>分段锁（Segment）</strong>，每个Segment继承<code>ReentrantLock</code>，锁粒度较粗。</li></ul></li><li><strong>JDK1.8</strong>：<ul><li>使用<strong>CAS（Compare-And-Swap）</strong>进行无锁插入。</li><li>使用<strong>synchronized</strong>锁住链表或红黑树的<strong>头节点</strong>，锁粒度更细，并发性能更高。</li></ul></li></ul><h4 id="✅-volatile的作用："><a href="#✅-volatile的作用：" class="headerlink" title="✅ volatile的作用："></a>✅ <strong>volatile的作用：</strong></h4><ul><li><code>Node.val</code>和<code>Node.next</code>使用<code>volatile</code>修饰，保证可见性。</li><li><code>table</code>数组也使用<code>volatile</code>，确保扩容时的可见性</li></ul><p>18.学生成绩表，学生姓名，科目，成绩，班级，查询那些学生总分大于600，sql语句</p><p>1、JDK1.8 的新的特性<br>2、Stream 流常用操作<br>3、ConcurrentHashMap 用来解决什么问题？什么时候会用？<br>4、你刚才提到了线程安全，什么现象表示出现了线程安全问题？<br>5、Java 里有哪些锁？这些锁有什么异同呢？<br>6、这些都是单机锁，分布式系统要加锁需要采取什么方案？<br>7、SETNX 本身有超时时间吗？<br>8、那如果方法执行时间很长，那锁不就提前被释放掉了吗？还有什么方案吗？ 不会，不太清楚别的方案了，面试官提到也是基于 Redis 的。<br>9、对 synchronized 关键字了解吗？原理是什么？如何加锁的？<br>10、什么叫做偏向锁？偏向是体现在哪里了？ 不会，回答了线程id<br>11、你刚才提到了对象头，对象头中会存储哪些数据呢？ 不会<br>12、Spring 中有单例模式，除了单例模式你还知道什么模式？<br>13、你有用过原型模式吗？什么情况下可能会用原型模式？ 不熟悉，面试官提示到是否有 “状态”<br>14、Spring 中，哪些类是单例的？<br>15、Spring 的 Bean 的生命周期分为几个阶段呢<br>16、它的实例化的过程是怎样的？Bean 销毁的时候想调用一些函数要怎么做？<br>17、Transactional 参数有哪些？<br>18、标注了 Transactional 的方法，并没有回滚，是因为什么呢？回答 代理对象。异常那块答错了。<br>19、为什么内部调用会导致事务失效呢？<br>20、代理对象本质用的是 Spring 什么特性？反射中的核心方法是什么？<br>21、如果想要内部调用，又想要事务生效要怎么做？<br>22、用 getBean 来得到可不可以？开始回答错了，后面拐回来了 容器里面放的就是 代理对象<br>23、Transactional 注解使用了 AOP，那么 AOP 体现在哪里呢？不会，面试官举了个例子：开启事务、回滚、提交 的这些点。<br>24、Nacos 的集群部署了解吗？不会<br>25、CAP 理论了解吗？Nacos 是 AP 还是 CP ？不知道（感觉最近面试好多都问我 Nacos 的）<br>26、如果让你来实现 服务的注册中心，那么它需要哪些功能呢？<br>27、Nacos 配置中心的功能用过吗？动态刷新是如何实现的？<br>28、如何确定一个 SQL 是否使用了索引？如何知道这个 SQL 扫描了多少行？答道用 EXPLAIN，但是有哪些信息我有点忘了<br>29、给你一个慢 SQL，你会从哪些方面去优化？答得不太好，后续好好补上<br>30、事务的隔离级别？少答了一个<br>31、JVM 常用启动参数用过吗？答了Xmx Xms<br>32、什么是 Full GC？什么情况下会出现？答得不太好，JVM 有段时间没复习了。<br>33、FullGC 会对 方法区和原空间 进行回收吗？同上<br>34、OOM 你有遇到过吗？提到了 内存泄漏、内存很大的对象<br>35、定位 内存泄漏&#x2F;溢出 有哪些方式？面试官提到了 内存分析工具</p><p>下面我<strong>逐条精讲</strong>你列出的<strong>35个高频面试题</strong>，每个问题都给出：<strong>一句话答案 + 面试话术 + 扩展提示</strong>，帮你<strong>快速补齐盲区</strong>，现场不卡壳。</p><hr><h3 id="1-JDK1-8-新特性"><a href="#1-JDK1-8-新特性" class="headerlink" title="1. JDK1.8 新特性"></a><strong>1. JDK1.8 新特性</strong></h3><ul><li><strong>Lambda 表达式</strong>、<strong>Stream API</strong>、<strong>方法引用</strong>（<code>::</code>）</li><li><strong>接口默认方法</strong>（<code>default</code>）、<strong>Optional</strong>、<strong>新的日期 API</strong>（<code>LocalDateTime</code>）</li><li><strong>元空间</strong>（替代永久代）、<strong>重复注解</strong>（<code>@Repeatable</code>）</li></ul><p><strong>面试话术</strong>：  </p><blockquote><p>“JDK1.8 最大的变化是函数式编程（Lambda+Stream），以及用元空间解决永久代 OOM。”</p></blockquote><hr><h3 id="2-Stream-流常用操作"><a href="#2-Stream-流常用操作" class="headerlink" title="2. Stream 流常用操作"></a><strong>2. Stream 流常用操作</strong></h3><ul><li><strong>中间操作</strong>：<code>filter()</code>、<code>map()</code>、<code>distinct()</code>、<code>sorted()</code>、<code>limit()</code></li><li><strong>终端操作</strong>：<code>forEach()</code>、<code>collect()</code>、<code>reduce()</code>、<code>count()</code>、<code>anyMatch()</code></li></ul><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">list.stream()</span><br><span class="line">    .filter(u -&gt; u.getAge() &gt; <span class="number">18</span>)</span><br><span class="line">    .map(User::getName)</span><br><span class="line">    .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><hr><h3 id="3-ConcurrentHashMap-用来解决什么问题？"><a href="#3-ConcurrentHashMap-用来解决什么问题？" class="headerlink" title="3. ConcurrentHashMap 用来解决什么问题？"></a><strong>3. ConcurrentHashMap 用来解决什么问题？</strong></h3><ul><li><strong>解决 HashMap 的线程安全问题</strong>，替代 <code>Hashtable</code>。</li><li><strong>高并发读写</strong>场景（如缓存、计数器）。</li></ul><p><strong>面试话术</strong>：  </p><blockquote><p>“ConcurrentHashMap 通过 CAS + synchronized 锁桶头，实现高并发安全。”</p></blockquote><hr><h3 id="4-线程安全问题的现象"><a href="#4-线程安全问题的现象" class="headerlink" title="4. 线程安全问题的现象"></a><strong>4. 线程安全问题的现象</strong></h3><ul><li><strong>数据不一致</strong>（如计数器少加）</li><li><strong>死循环</strong>（如 HashMap 死链）</li><li><strong>NullPointerException</strong>（未同步初始化）</li></ul><hr><h3 id="5-Java-有哪些锁？"><a href="#5-Java-有哪些锁？" class="headerlink" title="5. Java 有哪些锁？"></a><strong>5. Java 有哪些锁？</strong></h3><table><thead><tr><th>锁类型</th><th>特点</th><th>场景</th></tr></thead><tbody><tr><td><strong>synchronized</strong></td><td>JVM 内置，可重入</td><td>低并发</td></tr><tr><td><strong>ReentrantLock</strong></td><td>可重入、可中断、公平锁</td><td>高并发</td></tr><tr><td><strong>ReadWriteLock</strong></td><td>读写分离</td><td>读多写少</td></tr><tr><td><strong>StampedLock</strong></td><td>乐观读锁</td><td>读远大于写</td></tr><tr><td><strong>自旋锁</strong></td><td>循环等待，不阻塞</td><td>短任务</td></tr></tbody></table><hr><h3 id="6-分布式锁方案"><a href="#6-分布式锁方案" class="headerlink" title="6. 分布式锁方案"></a><strong>6. 分布式锁方案</strong></h3><ul><li><strong>Redis 分布式锁</strong>（SETNX + EXPIRE）</li><li><strong>RedLock</strong>（Redis 集群）</li><li><strong>ZooKeeper</strong>（临时有序节点）</li><li><strong>数据库锁</strong>（悲观锁 <code>FOR UPDATE</code>）</li></ul><hr><h3 id="7-SETNX-有超时时间吗？"><a href="#7-SETNX-有超时时间吗？" class="headerlink" title="7. SETNX 有超时时间吗？"></a><strong>7. SETNX 有超时时间吗？</strong></h3><ul><li><strong>没有</strong>，必须配合 <code>EXPIRE</code> 或使用 <code>SET key value NX EX 10</code>。</li></ul><hr><h3 id="8-锁提前释放怎么办？"><a href="#8-锁提前释放怎么办？" class="headerlink" title="8. 锁提前释放怎么办？"></a><strong>8. 锁提前释放怎么办？</strong></h3><ul><li><strong>Redisson 看门狗</strong>：自动续期（默认 30s 续一次）</li><li><strong>方案</strong>：业务执行完手动释放（<code>DEL key</code>），或设置足够长的超时。</li></ul><hr><h3 id="9-synchronized-原理"><a href="#9-synchronized-原理" class="headerlink" title="9. synchronized 原理"></a><strong>9. synchronized 原理</strong></h3><ul><li><strong>对象头 Mark Word</strong>：存储锁标志位、线程 ID。</li><li><strong>Monitor</strong>：每个对象关联一个 Monitor，通过 <code>monitorenter/monitorexit</code> 指令加锁。</li><li><strong>锁升级</strong>：无锁 → 偏向锁 → 轻量级锁 → 重量级锁。</li></ul><hr><h3 id="10-偏向锁"><a href="#10-偏向锁" class="headerlink" title="10. 偏向锁"></a><strong>10. 偏向锁</strong></h3><ul><li><strong>偏向第一个获取锁的线程</strong>，后续该线程再次进入无需 CAS。</li><li><strong>体现在对象头</strong>：记录线程 ID，无竞争时性能最高。</li></ul><hr><h3 id="11-对象头存储的数据"><a href="#11-对象头存储的数据" class="headerlink" title="11. 对象头存储的数据"></a><strong>11. 对象头存储的数据</strong></h3><ul><li><strong>Mark Word</strong>：锁状态、hashCode、GC 分代年龄、偏向线程 ID。</li><li><strong>Klass Pointer</strong>：指向类元数据（开启压缩指针后 4 字节）。</li></ul><hr><h3 id="12-Spring-单例以外的模式"><a href="#12-Spring-单例以外的模式" class="headerlink" title="12. Spring 单例以外的模式"></a><strong>12. Spring 单例以外的模式</strong></h3><ul><li><strong>原型模式</strong>（<code>@Scope(&quot;prototype&quot;)</code>）</li><li><strong>请求作用域</strong>（<code>request</code>）</li><li><strong>会话作用域</strong>（<code>session</code>）</li><li><strong>全局会话</strong>（<code>globalSession</code>）</li></ul><hr><h3 id="13-原型模式使用场景"><a href="#13-原型模式使用场景" class="headerlink" title="13. 原型模式使用场景"></a><strong>13. 原型模式使用场景</strong></h3><ul><li><strong>有状态的 Bean</strong>（如每次请求需独立副本）</li><li><strong>示例</strong>：<code>@Scope(&quot;prototype&quot;)</code> + 多线程任务中避免共享状态。</li></ul><hr><h3 id="14-Spring-默认单例的类"><a href="#14-Spring-默认单例的类" class="headerlink" title="14. Spring 默认单例的类"></a><strong>14. Spring 默认单例的类</strong></h3><ul><li><strong>所有 Bean 默认单例</strong>（<code>@Component</code>、<code>@Service</code>、<code>@Repository</code>）。</li></ul><hr><h3 id="15-Bean-生命周期阶段"><a href="#15-Bean-生命周期阶段" class="headerlink" title="15. Bean 生命周期阶段"></a><strong>15. Bean 生命周期阶段</strong></h3><ol><li><strong>实例化</strong>（构造函数）</li><li><strong>属性赋值</strong>（DI）</li><li><strong>初始化</strong>（<code>@PostConstruct</code>、<code>InitializingBean</code>）</li><li><strong>使用</strong></li><li><strong>销毁</strong>（<code>@PreDestroy</code>、<code>DisposableBean</code>）</li></ol><hr><h3 id="16-销毁时自定义逻辑"><a href="#16-销毁时自定义逻辑" class="headerlink" title="16. 销毁时自定义逻辑"></a><strong>16. 销毁时自定义逻辑</strong></h3><ul><li><strong>注解</strong>：<code>@PreDestroy</code></li><li><strong>接口</strong>：实现 <code>DisposableBean.destroy()</code></li><li><strong>配置</strong>：<code>destroy-method=&quot;cleanup&quot;</code></li></ul><hr><h3 id="17-Transactional-参数"><a href="#17-Transactional-参数" class="headerlink" title="17. @Transactional 参数"></a><strong>17. @Transactional 参数</strong></h3><ul><li><code>propagation</code>（传播行为）</li><li><code>isolation</code>（隔离级别）</li><li><code>timeout</code>（超时时间）</li><li><code>readOnly</code>（只读）</li><li><code>rollbackFor</code>（指定回滚异常）</li></ul><hr><h3 id="18-Transactional-未回滚原因"><a href="#18-Transactional-未回滚原因" class="headerlink" title="18. @Transactional 未回滚原因"></a><strong>18. @Transactional 未回滚原因</strong></h3><ul><li><strong>异常未抛出</strong>（被 catch）</li><li><strong>非 RuntimeException</strong>（需指定 <code>rollbackFor</code>）</li><li><strong>内部调用</strong>（绕过代理）</li></ul><hr><h3 id="19-内部调用事务失效原因"><a href="#19-内部调用事务失效原因" class="headerlink" title="19. 内部调用事务失效原因"></a><strong>19. 内部调用事务失效原因</strong></h3><ul><li><strong>Spring 事务基于代理</strong>，同类内部方法调用不走代理，事务失效。</li></ul><hr><h3 id="20-代理对象核心机制"><a href="#20-代理对象核心机制" class="headerlink" title="20. 代理对象核心机制"></a><strong>20. 代理对象核心机制</strong></h3><ul><li><strong>Spring AOP</strong>：基于 <strong>动态代理</strong>（JDK&#x2F;CGLIB）</li><li><strong>核心方法</strong>：<code>Proxy.newProxyInstance()</code> 或 <code>Enhancer.create()</code></li></ul><hr><h3 id="21-内部调用事务生效方案"><a href="#21-内部调用事务生效方案" class="headerlink" title="21. 内部调用事务生效方案"></a><strong>21. 内部调用事务生效方案</strong></h3><ul><li><strong>注入自身</strong>（<code>@Autowired</code> 代理对象）</li><li>**AopContext.currentProxy()**（需开启暴露代理）</li></ul><hr><h3 id="22-getBean-获取的是代理吗？"><a href="#22-getBean-获取的是代理吗？" class="headerlink" title="22. getBean() 获取的是代理吗？"></a><strong>22. getBean() 获取的是代理吗？</strong></h3><ul><li><strong>是的</strong>，Spring 容器中存的是代理对象（单例池）。</li></ul><hr><h3 id="23-Transactional-的-AOP-体现"><a href="#23-Transactional-的-AOP-体现" class="headerlink" title="23. @Transactional 的 AOP 体现"></a><strong>23. @Transactional 的 AOP 体现</strong></h3><ul><li><strong>切面</strong>：事务开启、提交、回滚。</li><li><strong>通知</strong>：<code>@Around</code> 拦截方法，执行 <code>TransactionInterceptor</code>。</li></ul><hr><h3 id="24-Nacos-集群部署"><a href="#24-Nacos-集群部署" class="headerlink" title="24. Nacos 集群部署"></a><strong>24. Nacos 集群部署</strong></h3><ul><li><strong>至少 3 个节点</strong>，持久化配置存 MySQL。</li><li><strong>集群模式</strong>：<code>cluster.conf</code> 配置所有节点 IP。</li></ul><hr><h3 id="25-CAP-理论与-Nacos"><a href="#25-CAP-理论与-Nacos" class="headerlink" title="25. CAP 理论与 Nacos"></a><strong>25. CAP 理论与 Nacos</strong></h3><ul><li><strong>CAP</strong>：一致性（C）、可用性（A）、分区容错（P）。</li><li><strong>Nacos</strong>：默认 <strong>AP</strong>（牺牲一致性保证高可用），可切换 <strong>CP</strong>（Raft）。</li></ul><hr><h3 id="26-注册中心需要的功能"><a href="#26-注册中心需要的功能" class="headerlink" title="26. 注册中心需要的功能"></a><strong>26. 注册中心需要的功能</strong></h3><ul><li><strong>服务注册&#x2F;发现</strong></li><li><strong>健康检查</strong>（心跳）</li><li><strong>负载均衡</strong>（客户端&#x2F;服务端）</li><li><strong>故障剔除</strong>（超时剔除）</li></ul><hr><h3 id="27-Nacos-动态刷新"><a href="#27-Nacos-动态刷新" class="headerlink" title="27. Nacos 动态刷新"></a><strong>27. Nacos 动态刷新</strong></h3><ul><li><strong>@RefreshScope</strong> 或 <strong>@NacosValue</strong></li><li><strong>原理</strong>：监听配置变更事件（长轮询），触发 Bean 重新绑定。</li></ul><hr><h3 id="28-EXPLAIN-关键字段"><a href="#28-EXPLAIN-关键字段" class="headerlink" title="28. EXPLAIN 关键字段"></a><strong>28. EXPLAIN 关键字段</strong></h3><table><thead><tr><th>字段</th><th>含义</th></tr></thead><tbody><tr><td><strong>type</strong></td><td>查询类型（ALL&#x2F;index&#x2F;ref&#x2F;range）</td></tr><tr><td><strong>key</strong></td><td>实际使用的索引</td></tr><tr><td><strong>rows</strong></td><td>扫描行数</td></tr><tr><td><strong>Extra</strong></td><td>额外信息（Using index&#x2F;filesort）</td></tr></tbody></table><hr><h3 id="29-慢-SQL-优化方向"><a href="#29-慢-SQL-优化方向" class="headerlink" title="29. 慢 SQL 优化方向"></a><strong>29. 慢 SQL 优化方向</strong></h3><ul><li><strong>索引优化</strong>（覆盖索引、最左匹配）</li><li>**避免 SELECT ***（减少回表）</li><li><strong>分页优化</strong>（延迟关联）</li><li><strong>JOIN 优化</strong>（小表驱动大表）</li><li><strong>子查询转 JOIN</strong></li></ul><hr><h3 id="30-事务隔离级别（补全）"><a href="#30-事务隔离级别（补全）" class="headerlink" title="30. 事务隔离级别（补全）"></a><strong>30. 事务隔离级别（补全）</strong></h3><ol><li><strong>READ UNCOMMITTED</strong></li><li><strong>READ COMMITTED</strong></li><li><strong>REPEATABLE READ</strong>（MySQL 默认）</li><li><strong>SERIALIZABLE</strong></li></ol><hr><h3 id="31-JVM-常用启动参数"><a href="#31-JVM-常用启动参数" class="headerlink" title="31. JVM 常用启动参数"></a><strong>31. JVM 常用启动参数</strong></h3><ul><li><code>-Xmx</code>（最大堆）</li><li><code>-Xms</code>（初始堆）</li><li><code>-Xss</code>（栈大小）</li><li><code>-XX:+PrintGCDetails</code>（打印 GC 日志）</li><li><code>-XX:MetaspaceSize</code>（元空间初始值）</li></ul><hr><h3 id="32-Full-GC-触发条件"><a href="#32-Full-GC-触发条件" class="headerlink" title="32. Full GC 触发条件"></a><strong>32. Full GC 触发条件</strong></h3><ul><li><strong>老年代空间不足</strong></li><li><strong>System.gc()</strong></li><li><strong>元空间不足</strong></li><li><strong>晋升失败</strong>（Survivor 放不下）</li></ul><hr><h3 id="33-Full-GC-会回收方法区-元空间吗？"><a href="#33-Full-GC-会回收方法区-元空间吗？" class="headerlink" title="33. Full GC 会回收方法区&#x2F;元空间吗？"></a><strong>33. Full GC 会回收方法区&#x2F;元空间吗？</strong></h3><ul><li><strong>会</strong>，Full GC 会清理 <strong>元空间</strong>中无用的类加载器。</li></ul><hr><h3 id="34-OOM-场景"><a href="#34-OOM-场景" class="headerlink" title="34. OOM 场景"></a><strong>34. OOM 场景</strong></h3><ul><li><strong>内存泄漏</strong>（如 ThreadLocal 未清理）</li><li><strong>大对象</strong>（如一次性加载大文件）</li><li><strong>递归过深</strong>（栈溢出）</li></ul><hr><h3 id="35-内存泄漏定位工具"><a href="#35-内存泄漏定位工具" class="headerlink" title="35. 内存泄漏定位工具"></a><strong>35. 内存泄漏定位工具</strong></h3><ul><li><strong>jmap</strong>（生成 dump）</li><li><strong>MAT</strong>（Memory Analyzer Tool）</li><li><strong>VisualVM</strong>（实时监控）</li><li><strong>Arthas</strong>（在线诊断）</li></ul><hr><h3 id="✅-终极建议"><a href="#✅-终极建议" class="headerlink" title="✅ 终极建议"></a>✅ <strong>终极建议</strong></h3><ul><li><strong>JVM + MySQL + Spring</strong>：刷 <strong>《深入理解JVM》+《MySQL技术内幕》+《Spring实战》</strong></li><li><strong>项目</strong>：提前准备 <strong>MQ 场景 + 分布式锁 + 事务失效</strong> 的坑。</li><li><strong>工具</strong>：<strong>Arthas + MAT + EXPLAIN</strong> 现场演示更加分！</li></ul><p>需要<strong>某部分展开</strong>或<strong>项目细节深挖</strong>，随时告诉我！</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>中科全安笔试题</title>
      <link href="/2026/02/10/%E4%B8%AD%E7%A7%91%E5%85%A8%E5%AE%89%E7%AC%94%E8%AF%95%E9%A2%98/"/>
      <url>/2026/02/10/%E4%B8%AD%E7%A7%91%E5%85%A8%E5%AE%89%E7%AC%94%E8%AF%95%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h2 id="中科全安笔试题答案"><a href="#中科全安笔试题答案" class="headerlink" title="中科全安笔试题答案"></a>中科全安笔试题答案</h2><p>1、clickhouse建表语句</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建数据库</span></span><br><span class="line"><span class="keyword">CREATE</span> DATABASE IF <span class="keyword">NOT</span> <span class="keyword">EXISTS</span> netflow_db;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 创建NetFlow原始数据表（五元组 + 纠正后字段）</span></span><br><span class="line"><span class="keyword">CREATE TABLE</span> netflow_db.flow_records</span><br><span class="line">(</span><br><span class="line">    <span class="comment">-- 原始五元组（从CSV读取）</span></span><br><span class="line">    raw_src_ip String,</span><br><span class="line">    raw_dst_ip String,</span><br><span class="line">    raw_src_port UInt16,</span><br><span class="line">    raw_dst_port UInt16,</span><br><span class="line">    start_time DateTime64(<span class="number">3</span>),</span><br><span class="line">    </span><br><span class="line">    <span class="comment">-- 纠正后的五元组（程序处理后的标准化数据）</span></span><br><span class="line">    src_ip String,</span><br><span class="line">    dst_ip String,</span><br><span class="line">    src_port UInt16,</span><br><span class="line">    dst_port UInt16,</span><br><span class="line">    </span><br><span class="line">    <span class="comment">-- 方向标记：0=内对内, 1=内对外, 2=外对内, 3=未知</span></span><br><span class="line">    direction UInt8,</span><br><span class="line">    </span><br><span class="line">    <span class="comment">-- 内外网标记（0=公网, 1=私网）</span></span><br><span class="line">    src_is_private UInt8,</span><br><span class="line">    dst_is_private UInt8,</span><br><span class="line">    </span><br><span class="line">    <span class="comment">-- 会话ID（用于关联双向流）</span></span><br><span class="line">    session_id String,</span><br><span class="line">    </span><br><span class="line">    <span class="comment">-- 入库时间</span></span><br><span class="line">    insert_time DateTime <span class="keyword">DEFAULT</span> now()</span><br><span class="line">)</span><br><span class="line">ENGINE <span class="operator">=</span> MergeTree()</span><br><span class="line"><span class="keyword">PARTITION</span> <span class="keyword">BY</span> toYYYYMMDD(start_time)</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> (start_time, src_ip, dst_ip, src_port, dst_port)</span><br><span class="line">SETTINGS index_granularity <span class="operator">=</span> <span class="number">8192</span>;</span><br></pre></td></tr></table></figure><p>2、数据入库后的截图</p><p><img src="/img_posts/image-20260210140401034.png" alt="image-20260210140401034"></p><p>4、Netflow为什么分不清源和目的？</p><p>Netflow是网络设备（路由器&#x2F;交换机）采集的，它基于数据包的方向定义源和目的，这表明数据包的方向在业务方可能是相反的，例如，假设客户端–》服务端是正常的数据包方向，而服务端–》客户端则是想反的，但对于采集的数据包来说，源和目的是正确的。</p><p>5、如何区分Netflow的源和目的？</p><ul><li>目的服务端提供的服务通常是知名端口即端口号&lt;1024和知名服务的端口号，例如ssh22端口，Mysql3306端口，redis6379端口。</li><li>公网IP访问私网时，公网是客户端，私网IP通常是服务端</li><li>服务端端口通常不会改变，而客户端端口号来自不同区域即是不同的ip地址</li></ul><p>6、入库时做了那些操作用来区分源和目的？</p><p>首先判断源ip是否正确，不正确则需反转ip源和目的ip</p><p><img src="/img_posts/image-20260210141404862.png" alt="image-20260210141404862"></p><p>7、如何区分扫描、暴力破解IP；</p><p><strong>扫描行为特征：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1. 横向扫描：1个IP → N个不同IP的相同端口（如扫描整个网段的22端口）</span><br><span class="line">2. 端口扫描：1个IP → 1个目标IP的N个不同端口</span><br><span class="line">3. 综合扫描：1个IP → N个IP的N个端口</span><br><span class="line">4. 高频特征：短时间内大量新建连接（源端口变化快）</span><br></pre></td></tr></table></figure><p><strong>暴力破解特征：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">1. 目标明确：针对特定服务端口（22/3389/3306等）</span><br><span class="line">2. 频率稳定：持续尝试，间隔规律</span><br><span class="line">3. 单目标深度：对单个目标IP+端口有大量连接</span><br><span class="line">4. 失败特征：连接时间短、数据量小（认证失败后断开）</span><br></pre></td></tr></table></figure><p>8、查询扫描、暴力破解IP的SQL并列出扫描、暴力破解的IP？</p><p><strong>扫描IP：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">10.20.30.200</span><br><span class="line">10.20.30.201</span><br><span class="line">10.20.30.202</span><br></pre></td></tr></table></figure><p><strong>暴力破解的ip</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">10.10.100.100</span><br><span class="line">10.10.100.200</span><br><span class="line">10.10.100.300</span><br></pre></td></tr></table></figure><p><strong>查询sql有两种方式</strong></p><ul><li><p>一是从原表中直接查询</p><ul><li><p><strong>扫描Ip，sql</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">&lt;</span><span class="keyword">select</span> id<span class="operator">=</span>&quot;selectScanIps&quot; resultMap<span class="operator">=</span>&quot;AttackResultMap&quot;<span class="operator">&gt;</span></span><br><span class="line">       <span class="keyword">SELECT</span></span><br><span class="line">           src_ip <span class="keyword">as</span> attacker_ip,</span><br><span class="line">           <span class="string">&#x27;&#x27;</span> <span class="keyword">as</span> victim_ip,</span><br><span class="line">           <span class="number">0</span> <span class="keyword">as</span> target_port,</span><br><span class="line">           <span class="string">&#x27;&#x27;</span> <span class="keyword">as</span> service_type,</span><br><span class="line">           toStartOfFiveMinute(start_time) <span class="keyword">as</span> time_window,</span><br><span class="line">           <span class="built_in">count</span>() <span class="keyword">as</span> attempt_count,</span><br><span class="line">           <span class="number">0</span> <span class="keyword">as</span> unique_attackers,</span><br><span class="line">           uniqExact(dst_ip) <span class="keyword">as</span> unique_hosts,</span><br><span class="line">           uniqExact(dst_port) <span class="keyword">as</span> unique_ports,</span><br><span class="line">           uniqExact((dst_ip, dst_port)) <span class="keyword">as</span> unique_targets,</span><br><span class="line">           <span class="string">&#x27;SCAN&#x27;</span> <span class="keyword">as</span> attack_type</span><br><span class="line">       <span class="keyword">FROM</span> flow_records</span><br><span class="line">       <span class="operator">&lt;</span>if test<span class="operator">=</span>&quot;startTime != null&quot;<span class="operator">&gt;</span></span><br><span class="line">           <span class="keyword">AND</span> start_time <span class="operator">&gt;=</span> parseDateTimeBestEffort(#&#123;startTime&#125;)</span><br><span class="line">       <span class="operator">&lt;</span><span class="operator">/</span>if<span class="operator">&gt;</span></span><br><span class="line">       <span class="keyword">GROUP</span> <span class="keyword">BY</span> src_ip, time_window</span><br><span class="line">       <span class="keyword">HAVING</span> unique_targets <span class="operator">&gt;=</span> #&#123;targetThreshold&#125;</span><br><span class="line">       <span class="keyword">ORDER</span> <span class="keyword">BY</span> unique_targets <span class="keyword">DESC</span></span><br><span class="line">   <span class="operator">&lt;</span><span class="operator">/</span><span class="keyword">select</span><span class="operator">&gt;</span></span><br></pre></td></tr></table></figure><p>最后对结果集按照src_ip进行去重</p></li><li><p><strong>暴力破解的ip，sql</strong></p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br></pre></td><td class="code"><pre><span class="line"><span class="operator">&lt;</span><span class="keyword">select</span> id<span class="operator">=</span>&quot;selectBruteForceIps&quot; resultMap<span class="operator">=</span>&quot;AttackResultMap&quot;<span class="operator">&gt;</span></span><br><span class="line">    <span class="keyword">SELECT</span></span><br><span class="line">    src_ip <span class="keyword">as</span> attacker_ip,</span><br><span class="line">    dst_ip <span class="keyword">as</span> victim_ip,</span><br><span class="line">    dst_port <span class="keyword">as</span> target_port,</span><br><span class="line">    <span class="keyword">CASE</span></span><br><span class="line">        <span class="comment">-- 远程管理（高风险爆破目标）</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">22</span>, <span class="number">23</span>, <span class="number">3389</span>, <span class="number">5900</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">22</span> <span class="keyword">THEN</span> <span class="string">&#x27;SSH&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">23</span> <span class="keyword">THEN</span> <span class="string">&#x27;Telnet&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">3389</span> <span class="keyword">THEN</span> <span class="string">&#x27;RDP&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">5900</span> <span class="keyword">THEN</span> <span class="string">&#x27;VNC&#x27;</span></span><br><span class="line">            <span class="keyword">END</span></span><br><span class="line">        <span class="comment">-- 数据库服务（高风险）</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">3306</span>, <span class="number">5432</span>, <span class="number">6379</span>, <span class="number">11211</span>, <span class="number">27017</span>, <span class="number">9200</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">3306</span> <span class="keyword">THEN</span> <span class="string">&#x27;MySQL&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">5432</span> <span class="keyword">THEN</span> <span class="string">&#x27;PostgreSQL&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">6379</span> <span class="keyword">THEN</span> <span class="string">&#x27;Redis&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">11211</span> <span class="keyword">THEN</span> <span class="string">&#x27;Memcached&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">27017</span> <span class="keyword">THEN</span> <span class="string">&#x27;MongoDB&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">9200</span> <span class="keyword">THEN</span> <span class="string">&#x27;Elasticsearch&#x27;</span></span><br><span class="line">             <span class="keyword">END</span></span><br><span class="line">            <span class="comment">-- 文件传输</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">20</span>, <span class="number">21</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">20</span> <span class="keyword">THEN</span> <span class="string">&#x27;FTP-DATA&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">21</span> <span class="keyword">THEN</span> <span class="string">&#x27;FTP&#x27;</span></span><br><span class="line">            <span class="keyword">END</span></span><br><span class="line">        <span class="comment">-- 邮件服务</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">25</span>, <span class="number">110</span>, <span class="number">143</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">25</span> <span class="keyword">THEN</span> <span class="string">&#x27;SMTP&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">110</span> <span class="keyword">THEN</span> <span class="string">&#x27;POP3&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">143</span> <span class="keyword">THEN</span> <span class="string">&#x27;IMAP&#x27;</span></span><br><span class="line">             <span class="keyword">END</span></span><br><span class="line">        <span class="comment">-- Web 服务</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">80</span>, <span class="number">443</span>, <span class="number">8080</span>, <span class="number">8443</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">80</span> <span class="keyword">THEN</span> <span class="string">&#x27;HTTP&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">443</span> <span class="keyword">THEN</span> <span class="string">&#x27;HTTPS&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">8080</span> <span class="keyword">THEN</span> <span class="string">&#x27;HTTP-ALT&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">8443</span> <span class="keyword">THEN</span> <span class="string">&#x27;HTTPS-ALT&#x27;</span></span><br><span class="line">            <span class="keyword">END</span></span><br><span class="line">        <span class="comment">-- Windows 网络</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="keyword">IN</span> (<span class="number">135</span>, <span class="number">445</span>) <span class="keyword">THEN</span></span><br><span class="line">            <span class="keyword">CASE</span> dst_port</span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">135</span> <span class="keyword">THEN</span> <span class="string">&#x27;MSRPC&#x27;</span></span><br><span class="line">                <span class="keyword">WHEN</span> <span class="number">445</span> <span class="keyword">THEN</span> <span class="string">&#x27;SMB&#x27;</span></span><br><span class="line">            <span class="keyword">END</span></span><br><span class="line">        <span class="comment">-- 基础设施</span></span><br><span class="line">        <span class="keyword">WHEN</span> dst_port <span class="operator">=</span> <span class="number">53</span> <span class="keyword">THEN</span> <span class="string">&#x27;DNS&#x27;</span></span><br><span class="line">        <span class="keyword">ELSE</span> <span class="string">&#x27;Unknown&#x27;</span></span><br><span class="line">    <span class="keyword">END</span> <span class="keyword">as</span> service_type,</span><br><span class="line">    toStartOfFiveMinute(start_time) <span class="keyword">as</span> time_window,</span><br><span class="line">    <span class="built_in">count</span>() <span class="keyword">as</span> attempt_count,</span><br><span class="line">    uniqExact(src_ip) <span class="keyword">as</span> unique_attackers,</span><br><span class="line">    <span class="number">0</span> <span class="keyword">as</span> unique_hosts,</span><br><span class="line">    <span class="number">0</span> <span class="keyword">as</span> unique_ports,</span><br><span class="line">    <span class="number">0</span> <span class="keyword">as</span> unique_targets,</span><br><span class="line">    <span class="string">&#x27;BRUTE_FORCE&#x27;</span> <span class="keyword">as</span> attack_type</span><br><span class="line">    <span class="keyword">FROM</span> flow_records</span><br><span class="line">    <span class="keyword">WHERE</span> dst_port <span class="keyword">IN</span> (<span class="number">20</span>, <span class="number">21</span>, <span class="number">22</span>, <span class="number">23</span>, <span class="number">25</span>, <span class="number">53</span>, <span class="number">80</span>, <span class="number">110</span>, <span class="number">143</span>, <span class="number">443</span>, <span class="number">445</span>,</span><br><span class="line">    <span class="number">3306</span>, <span class="number">3389</span>, <span class="number">5432</span>, <span class="number">6379</span>, <span class="number">8080</span>, <span class="number">8443</span>, <span class="number">9200</span>, <span class="number">27017</span>, <span class="number">11211</span>, <span class="number">5900</span>, <span class="number">135</span>)</span><br><span class="line">    <span class="operator">&lt;</span>if test<span class="operator">=</span>&quot;startTime != null&quot;<span class="operator">&gt;</span></span><br><span class="line">        <span class="keyword">AND</span> start_time <span class="operator">&gt;=</span> parseDateTimeBestEffort(#&#123;startTime&#125;)</span><br><span class="line">    <span class="operator">&lt;</span><span class="operator">/</span>if<span class="operator">&gt;</span></span><br><span class="line">    <span class="keyword">GROUP</span> <span class="keyword">BY</span> src_ip, dst_ip, dst_port, time_window</span><br><span class="line">    <span class="keyword">HAVING</span> attempt_count <span class="operator">&gt;=</span> #&#123;attemptThreshold&#125;</span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> attempt_count <span class="keyword">DESC</span></span><br><span class="line"><span class="operator">&lt;</span><span class="operator">/</span><span class="keyword">select</span><span class="operator">&gt;</span></span><br></pre></td></tr></table></figure><p>最后对结果集按照dst_ip进行去重</p></li></ul></li><li><p>二是在创建表时创建对应的物化视图(必须在创建表时同时创建，物化视图不会从历史数据中更新视图)，这样就能在插入数据后，直接从物化视图中查询</p><p>阈值取5</p><ul><li><strong>扫描Ip</strong></li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建物化视图：扫描检测（按源IP聚合）</span></span><br><span class="line"><span class="keyword">CREATE</span> MATERIALIZED <span class="keyword">VIEW</span> netflow_db.mv_scan</span><br><span class="line">ENGINE <span class="operator">=</span> SummingMergeTree()</span><br><span class="line"><span class="keyword">PARTITION</span> <span class="keyword">BY</span> toYYYYMMDD(time_window)</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> (src_ip, time_window)</span><br><span class="line"><span class="keyword">AS</span> <span class="keyword">SELECT</span></span><br><span class="line">    src_ip,</span><br><span class="line">    toStartOfFiveMinute(start_time) <span class="keyword">as</span> time_window,</span><br><span class="line">    <span class="built_in">count</span>() <span class="keyword">as</span> flow_count,</span><br><span class="line">    uniqExact(dst_ip) <span class="keyword">as</span> unique_hosts,</span><br><span class="line">    uniqExact(dst_port) <span class="keyword">as</span> unique_ports,</span><br><span class="line">    uniqExact((dst_ip, dst_port)) <span class="keyword">as</span> unique_targets</span><br><span class="line"><span class="keyword">FROM</span> netflow_db.flow_records</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> src_ip, time_window;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> src_ip <span class="keyword">from</span> mv_scan <span class="keyword">where</span> unique_targets <span class="operator">&gt;=</span> <span class="number">5</span> <span class="keyword">group</span> <span class="keyword">by</span> src_ip;</span><br></pre></td></tr></table></figure><ul><li><strong>暴力破解的ip</strong></li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 创建物化视图：扫描检测（按源IP聚合）</span></span><br><span class="line"><span class="keyword">CREATE</span> MATERIALIZED <span class="keyword">VIEW</span> netflow_db.mv_scan</span><br><span class="line">ENGINE <span class="operator">=</span> SummingMergeTree()</span><br><span class="line"><span class="keyword">PARTITION</span> <span class="keyword">BY</span> toYYYYMMDD(time_window)</span><br><span class="line"><span class="keyword">ORDER</span> <span class="keyword">BY</span> (src_ip, time_window)</span><br><span class="line"><span class="keyword">AS</span> <span class="keyword">SELECT</span></span><br><span class="line">    src_ip,</span><br><span class="line">    toStartOfFiveMinute(start_time) <span class="keyword">as</span> time_window,</span><br><span class="line">    <span class="built_in">count</span>() <span class="keyword">as</span> flow_count,</span><br><span class="line">    uniqExact(dst_ip) <span class="keyword">as</span> unique_hosts,</span><br><span class="line">    uniqExact(dst_port) <span class="keyword">as</span> unique_ports,</span><br><span class="line">    uniqExact((dst_ip, dst_port)) <span class="keyword">as</span> unique_targets</span><br><span class="line"><span class="keyword">FROM</span> netflow_db.flow_records</span><br><span class="line"><span class="keyword">GROUP</span> <span class="keyword">BY</span> src_ip, time_window;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">select</span> dst_ip <span class="keyword">from</span> mv_brute_force <span class="keyword">where</span> attempt_count <span class="operator">&gt;=</span> <span class="number">5</span> <span class="keyword">group</span> <span class="keyword">by</span> dst_ip;</span><br></pre></td></tr></table></figure></li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>同行者科技笔试题</title>
      <link href="/2026/02/10/%E5%90%8C%E8%A1%8C%E8%80%85%E7%A7%91%E6%8A%80%E7%AC%94%E8%AF%95%E9%A2%98/"/>
      <url>/2026/02/10/%E5%90%8C%E8%A1%8C%E8%80%85%E7%A7%91%E6%8A%80%E7%AC%94%E8%AF%95%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="同行者科技笔试题"><a href="#同行者科技笔试题" class="headerlink" title="同行者科技笔试题"></a>同行者科技笔试题</h1><h2 id="1-字符拼接和组合"><a href="#1-字符拼接和组合" class="headerlink" title="1.字符拼接和组合"></a><strong>1.字符拼接和组合</strong></h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> Test12;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.ArrayList;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">StringSplitCompose</span> &#123;</span><br><span class="line">    <span class="comment">//1) 字符串拆分成数组，如”ab&amp;&amp;2”通过”&amp;&amp;”做分隔符，分割得到字符串数组[“ab”,”2”]</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String[] question1(String arr) &#123;</span><br><span class="line">        ArrayList&lt;String&gt; res = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> -<span class="number">1</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; arr.length()) &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr.charAt(i) == <span class="string">&#x27;&amp;&#x27;</span> &amp;&amp; arr.charAt(i + <span class="number">1</span>) == <span class="string">&#x27;&amp;&#x27;</span>) &#123;</span><br><span class="line">                <span class="type">StringBuilder</span> <span class="variable">temp</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">                <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">k</span> <span class="operator">=</span> j + <span class="number">1</span>; k &lt; i; k++) &#123;</span><br><span class="line">                    temp.append(arr.charAt(k));</span><br><span class="line">                &#125;</span><br><span class="line">                res.add(temp.toString());</span><br><span class="line">                j = i + <span class="number">1</span>;</span><br><span class="line">                i = i + <span class="number">2</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">        res.add(arr.substring(j + <span class="number">1</span>));</span><br><span class="line">        <span class="keyword">return</span> res.toArray(<span class="keyword">new</span> <span class="title class_">String</span>[<span class="number">0</span>]);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">//2) 实现字符串组合，如[“ab”,”2”]通过”&amp;&amp;”分隔符，组合成字符串”ab&amp;&amp;2”</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> String <span class="title function_">question2</span><span class="params">(String arr[])</span> &#123;</span><br><span class="line">        <span class="type">StringBuilder</span> <span class="variable">res</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">StringBuilder</span>();</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; arr.length - <span class="number">1</span>; i++) &#123;</span><br><span class="line">            res.append(arr[i] + <span class="string">&quot;&amp;&amp;&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        res.append(arr[arr.length - <span class="number">1</span>]);</span><br><span class="line">        <span class="keyword">return</span> res.toString();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="2-1000个整数，数值的范围是-0-999-，有且只有2个相同的数，请编写程序找出来-异或和问题"><a href="#2-1000个整数，数值的范围是-0-999-，有且只有2个相同的数，请编写程序找出来-异或和问题" class="headerlink" title="2.1000个整数，数值的范围是[0,999]，有且只有2个相同的数，请编写程序找出来(异或和问题)"></a><strong>2.1000个整数，数值的范围是[0,999]，有且只有2个相同的数，请编写程序找出来(异或和问题)</strong></h2><p><strong>异或（XOR）满足“可交换、可结合、自身消去”这三条定律</strong></p><p>异或的三条基本定律<br>a ⊕ a &#x3D; 0        （自身消去）<br>a ⊕ 0 &#x3D; a        （0 是单位元）<br>a ⊕ b &#x3D; b ⊕ a    （可交换）<br>于是可以把任意序列随意打乱、随意括号，结果不变。</p><p><strong>解题思路</strong></p><ol><li>取 xor 最右侧的 1 位（lowbit），把原 1000 个数和 0…999 按这一位是 0&#x2F;1 分成两组，分别再异或一次。</li><li>在其中一组里会得到 d(<strong>重复的那个数</strong>)，另一组得到 miss(<strong>0到999未出现的那个数</strong>)——因为同一组内 d 和 miss 必然落在不同侧，其余所有数都成对出现被消掉。<br>这样总共两遍扫描、两个整型变量，仍是 O(n) 时间、O(1) 空间，而且<strong>同时</strong>给出重复数和缺席数。</li></ol><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">import</span> java.util.Scanner;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FindDupAndMiss</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="type">Scanner</span> <span class="variable">sc</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Scanner</span>(System.in);</span><br><span class="line">        <span class="type">int</span> <span class="variable">n</span> <span class="operator">=</span> <span class="number">1000</span>;</span><br><span class="line">        <span class="type">int</span>[] arr = <span class="keyword">new</span> <span class="title class_">int</span>[n];</span><br><span class="line">        <span class="type">int</span> <span class="variable">xorAll</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; n; i++) &#123;</span><br><span class="line">            arr[i] = sc.nextInt();</span><br><span class="line">            xorAll ^= arr[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> <span class="variable">xorOnce</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++) xorOnce ^= i;</span><br><span class="line">        <span class="type">int</span> <span class="variable">diff</span> <span class="operator">=</span> xorAll ^ xorOnce;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">mask</span> <span class="operator">=</span> diff &amp; -diff;</span><br><span class="line">        <span class="type">int</span> <span class="variable">group</span> <span class="operator">=</span> <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> v : arr) <span class="keyword">if</span> ((v &amp; mask) != <span class="number">0</span>) group ^= v;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; <span class="number">1000</span>; i++) <span class="keyword">if</span> ((i &amp; mask) != <span class="number">0</span>) group ^= i;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">a</span> <span class="operator">=</span> group;</span><br><span class="line">        <span class="type">int</span> <span class="variable">b</span> <span class="operator">=</span> diff ^ a;</span><br><span class="line"></span><br><span class="line">        <span class="type">int</span> <span class="variable">cntA</span> <span class="operator">=</span> <span class="number">0</span>, cntB = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> v : arr) &#123;</span><br><span class="line">            <span class="keyword">if</span> (v == a) cntA++;</span><br><span class="line">            <span class="keyword">if</span> (v == b) cntB++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="type">int</span> <span class="variable">dup</span> <span class="operator">=</span> cntA == <span class="number">2</span> ? a : b;</span><br><span class="line">        <span class="type">int</span> <span class="variable">miss</span> <span class="operator">=</span> dup == a ? b : a;</span><br><span class="line"></span><br><span class="line">        System.out.println(dup + <span class="string">&quot; &quot;</span> + miss);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="3-同步和异步进程关系逻辑实现"><a href="#3-同步和异步进程关系逻辑实现" class="headerlink" title="3.同步和异步进程关系逻辑实现"></a>3.同步和异步进程关系逻辑实现</h2><p><img src="https://www.kimi.com/apiv2-files/sign-obj/kimi-fs%2Ffiles%2Fblob%2F4b6e82669ff764c996ae5d89cc64c6a18615f007da97f9ba9aff286a4b3437dc?filename=image.png&sig=SXu_h3Xt6nBBghip3vy6bEbNkSFpc8-wNlDdlutqfjk=&t=t" alt="img"></p><ul><li>用 <code>CompletableFuture</code> 做异步任务；</li><li>读操作用 <code>thenAcceptBoth</code> 做“全部结束”型并行；</li><li>写操作用 <code>acceptEither</code> 做“任一结束”型并行；</li><li>任务之间用 <code>thenCompose</code> 串成“请求流程”。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.*;</span><br><span class="line"><span class="keyword">import</span> java.util.function.Consumer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncFlowDemo</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Executor</span> <span class="variable">POOL</span> <span class="operator">=</span> ForkJoinPool.commonPool();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> CompletableFuture&lt;Void&gt; <span class="title function_">read</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> CompletableFuture.runAsync(() -&gt; &#123;</span><br><span class="line">            sleep(<span class="number">50</span> + (<span class="type">int</span>) (Math.random() * <span class="number">100</span>));</span><br><span class="line">            System.out.println(name + <span class="string">&quot; 读操作 完成&quot;</span>);</span><br><span class="line">        &#125;, POOL);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> CompletableFuture&lt;Void&gt; <span class="title function_">write</span><span class="params">(String name)</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> CompletableFuture.runAsync(() -&gt; &#123;</span><br><span class="line">            sleep(<span class="number">50</span> + (<span class="type">int</span>) (Math.random() * <span class="number">100</span>));</span><br><span class="line">            System.out.println(name + <span class="string">&quot; 写操作 完成&quot;</span>);</span><br><span class="line">        &#125;, POOL);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">sleep</span><span class="params">(<span class="type">int</span> ms)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123; Thread.sleep(ms); &#125; <span class="keyword">catch</span> (InterruptedException ignore) &#123;&#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        CompletableFuture&lt;Void&gt; c = read(<span class="string">&quot;任务C&quot;</span>);</span><br><span class="line">        CompletableFuture&lt;Void&gt; h = read(<span class="string">&quot;任务H&quot;</span>);</span><br><span class="line"></span><br><span class="line">        CompletableFuture&lt;Void&gt; a = c.thenAcceptBoth(h, (__, ___) -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;C、H 全部结束 → 启动任务A&quot;</span>);</span><br><span class="line">        &#125;).thenCompose(__ -&gt; write(<span class="string">&quot;任务A&quot;</span>));</span><br><span class="line"></span><br><span class="line">        CompletableFuture&lt;Void&gt; x = read(<span class="string">&quot;任务X&quot;</span>);</span><br><span class="line"></span><br><span class="line">        CompletableFuture&lt;Void&gt; w = read(<span class="string">&quot;任务W&quot;</span>);</span><br><span class="line">        CompletableFuture&lt;Void&gt; y = write(<span class="string">&quot;任务Y&quot;</span>);</span><br><span class="line"></span><br><span class="line">        CompletableFuture&lt;Void&gt; either = w.acceptEither(y, <span class="keyword">new</span> <span class="title class_">Consumer</span>&lt;Void&gt;() &#123;</span><br><span class="line">            <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">accept</span><span class="params">(Void v)</span> &#123;</span><br><span class="line">                System.out.println(<span class="string">&quot;W、Y 任一结束 → 流程继续&quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">        CompletableFuture&lt;Void&gt; p = read(<span class="string">&quot;任务P&quot;</span>);</span><br><span class="line"></span><br><span class="line">        CompletableFuture.allOf(a, x, either, p).join();</span><br><span class="line">        System.out.println(<span class="string">&quot;=== 整个请求流程 结束 ===&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>原生事件vue2中的.native</title>
      <link href="/2025/10/10/%E5%8E%9F%E7%94%9F%E4%BA%8B%E4%BB%B6vue2%E4%B8%AD%E7%9A%84.native/"/>
      <url>/2025/10/10/%E5%8E%9F%E7%94%9F%E4%BA%8B%E4%BB%B6vue2%E4%B8%AD%E7%9A%84.native/</url>
      
        <content type="html"><![CDATA[<h2 id="原生事件vue2中的-native"><a href="#原生事件vue2中的-native" class="headerlink" title="原生事件vue2中的.native"></a>原生事件vue2中的.native</h2><p>“原生事件”就是 <strong>DOM 本身自带、浏览器直接派发</strong> 的那种最底层事件，<br>与 <strong>Vue 自定义事件</strong>（<code>$emit(&#39;my-event&#39;)</code>）相对。</p><hr><p>一、两种事件对比（代码一眼看懂）</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 1. 原生事件：浏览器说了算 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">button</span> <span class="attr">onclick</span>=<span class="string">&quot;console.log(&#x27;浏览器直接点我&#x27;)&quot;</span>&gt;</span>原生 button<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 2. Vue 自定义事件：组件自己说了算 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">my-button</span> @<span class="attr">my-click</span>=<span class="string">&quot;handleCustom&quot;</span>&gt;</span>组件内部会 $emit(&#x27;my-click&#x27;)<span class="tag">&lt;/<span class="name">my-button</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><p>二、原生事件长什么样？</p><table><thead><tr><th>元素</th><th>原生事件名</th><th>由谁触发</th></tr></thead><tbody><tr><td><code>button</code></td><td><code>click</code></td><td>用户点击</td></tr><tr><td><code>input</code></td><td><code>input</code> &#x2F; <code>change</code></td><td>用户输入</td></tr><tr><td><code>form</code></td><td><code>submit</code></td><td>用户回车或点提交</td></tr><tr><td><code>window</code></td><td><code>resize</code> &#x2F; <code>scroll</code></td><td>浏览器窗口</td></tr><tr><td><code>document</code></td><td><code>DOMContentLoaded</code></td><td>浏览器解析完 DOM</td></tr></tbody></table><p>这些事件 <strong>不需要 Vue 干预</strong>，浏览器自动派发，可直接 <code>addEventListener</code>。</p><hr><p>三、为什么组件需要 <code>.native</code>？</p><p>Vue 把 <strong>自定义组件</strong> 当成 <strong>“黑盒”</strong>，默认只会监听 <strong>“它显式 $emit 出来的事件”</strong>。<br>如果你写了：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">my-component</span> @<span class="attr">click</span>=<span class="string">&quot;doTheThing&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">my-component</span>&gt;</span></span><br></pre></td></tr></table></figure><p>Vue 会理解为：<br>“等 my-component 内部执行 <code>this.$emit(&#39;click&#39;)</code> 再回调”——<strong>不是监听根节点原生的点击</strong>。</p><p>而加上 <code>.native</code> 就是告诉 Vue：</p><blockquote><p>“别等它 $emit，直接把 click 事件绑到组件 <strong>根元素</strong> 上，像普通 DOM 一样监听浏览器原生 click。”</p></blockquote><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">my-component</span> @<span class="attr">click.native</span>=<span class="string">&quot;doTheThing&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">my-component</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><p>四、一个例子</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// MyButton.vue</span></span><br><span class="line">&lt;template&gt;</span><br><span class="line">  <span class="language-xml"><span class="tag">&lt;<span class="name">button</span> <span class="attr">class</span>=<span class="string">&quot;btn&quot;</span>&gt;</span>点我<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span></span><br><span class="line">&lt;/template&gt;</span><br></pre></td></tr></table></figure><p>使用：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">my-button</span> @<span class="attr">click.native</span>=<span class="string">&quot;handle&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">my-button</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li>用户点击 <strong>button</strong> → 浏览器派发 <strong>原生 click</strong> → Vue 自动触发 <code>handle</code>。  </li><li>如果去掉 <code>.native</code>，<code>handle</code> 永远不会执行，因为 <strong>组件内部没有</strong> <code>$emit(&#39;click&#39;)</code>。</li></ul><hr><p>五、Vue3 的变化（重要）</p><p><code>.native</code> 在 <strong>Vue3 已被移除</strong>！<br>Vue3 默认会把 <strong>所有未声明的事件</strong> 直接绑到根节点，无需 <code>.native</code>。<br>如果不想让事件透传，用 <code>inheritAttrs: false</code> 即可。</p><hr><p>一句话总结</p><blockquote><p><strong>原生事件</strong>就是浏览器自己派发、<strong>不需要 Vue $emit</strong> 的底层 DOM 事件；<br>在 Vue2 自定义组件上想直接监听这种底层事件，就得加 <code>.native</code> 修饰符；<br>Vue3 已废弃该修饰符，事件会自动透传到根节点。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>因为 text-danger 里包含 连字符（-），而 连字符在 JavaScript 对象键名里是不合法标识符；</title>
      <link href="/2025/10/10/%E5%9B%A0%E4%B8%BA-text-danger-%E9%87%8C%E5%8C%85%E5%90%AB-%E8%BF%9E%E5%AD%97%E7%AC%A6%EF%BC%88-%EF%BC%89%EF%BC%8C%E8%80%8C-%E8%BF%9E%E5%AD%97%E7%AC%A6%E5%9C%A8-JavaScript-%E5%AF%B9%E8%B1%A1%E9%94%AE%E5%90%8D%E9%87%8C%E6%98%AF%E4%B8%8D%E5%90%88%E6%B3%95%E6%A0%87%E8%AF%86%E7%AC%A6%EF%BC%9B/"/>
      <url>/2025/10/10/%E5%9B%A0%E4%B8%BA-text-danger-%E9%87%8C%E5%8C%85%E5%90%AB-%E8%BF%9E%E5%AD%97%E7%AC%A6%EF%BC%88-%EF%BC%89%EF%BC%8C%E8%80%8C-%E8%BF%9E%E5%AD%97%E7%AC%A6%E5%9C%A8-JavaScript-%E5%AF%B9%E8%B1%A1%E9%94%AE%E5%90%8D%E9%87%8C%E6%98%AF%E4%B8%8D%E5%90%88%E6%B3%95%E6%A0%87%E8%AF%86%E7%AC%A6%EF%BC%9B/</url>
      
        <content type="html"><![CDATA[<p>因为 <code>text-danger</code> 里包含 <strong>连字符（-）</strong>，而 <strong>连字符在 JavaScript 对象键名里是不合法标识符</strong>；<br>不加引号会被解析成 <strong>“减号表达式”</strong>，直接报错。</p><hr><p>一、演示区别</p><table><thead><tr><th>写法</th><th>是否合法</th><th>结果</th></tr></thead><tbody><tr><td><code>text-danger: true</code></td><td>❌</td><td>报错：Uncaught SyntaxError: Unexpected token ‘-‘</td></tr><tr><td><code>&#39;text-danger&#39;: true</code></td><td>✅</td><td>正确，键名就是字符串 <code>&quot;text-danger&quot;</code></td></tr><tr><td><code>textDanger: true</code></td><td>✅</td><td>合法标识符，可省略引号</td></tr></tbody></table><hr><p>二、对象字面量规则速记</p><ul><li><strong>纯字母、数字、下划线、( 开头且不含关键字</strong> → 可省略引号  </li><li><strong>一旦含 <code>-</code>、空格、数字开头、关键字</strong> → 必须加引号（单双引均可）</li></ul><hr><p>三、在 Vue 模板里更常见的等价写法</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">:class</span>=<span class="string">&quot;&#123; &#x27;text-danger&#x27;: true, active: true &#125;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>同样，**<code>text-danger</code> 必须加引号**，<code>active</code> 可以不加。</p><hr><p>一句话：<br><strong>引号不是 Vue 的要求，是 JavaScript 语法的要求</strong>——键名里出现 <strong>连字符</strong> 就得当字符串写。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>c语言指针</title>
      <link href="/2025/09/30/c%E8%AF%AD%E8%A8%80%E6%8C%87%E9%92%88/"/>
      <url>/2025/09/30/c%E8%AF%AD%E8%A8%80%E6%8C%87%E9%92%88/</url>
      
        <content type="html"><![CDATA[<h2 id="c语言指针"><a href="#c语言指针" class="headerlink" title="c语言指针"></a>c语言指针</h2><p>下面把 5 道“自测题”一口气讲透，每题都给：<br>① 结论一句话 ② 内存图&#x2F;规则 ③ 能直接跑的小例子 ④ 常见踩坑。</p><hr><h3 id="1-int-p-3-与-int-p-3-区别？"><a href="#1-int-p-3-与-int-p-3-区别？" class="headerlink" title="1. int *p[3]; 与 int (*p)[3]; 区别？"></a>1. <code>int *p[3];</code> 与 <code>int (*p)[3];</code> 区别？</h3><table><thead><tr><th>声明</th><th>类型</th><th>含义</th><th>宽度（64 位）</th></tr></thead><tbody><tr><td><code>int *p[3];</code></td><td>数组 → 元素是 <code>int *</code></td><td>存 3 个 <code>int *</code> 指针</td><td>3×8 &#x3D; 24 B</td></tr><tr><td><code>int (*p)[3];</code></td><td>指针 → 指向含 3 个 <code>int</code> 的数组</td><td>一次偏移跳 3 个 <code>int</code></td><td>8 B</td></tr></tbody></table><p><strong>助记</strong>：括号 <code>()</code> 改变命运。<br>“下标优先级高” → <code>*p[3]</code> 先组合成数组；<code>(*p)[3]</code> 先组合成指针。</p><p><strong>代码体验</strong>：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> a[<span class="number">2</span>][<span class="number">3</span>] = &#123;&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;,&#123;<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>&#125;&#125;;</span><br><span class="line"><span class="type">int</span> (*row)[<span class="number">3</span>] = a;          <span class="comment">// 指向一行 3 个 int</span></span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, row[<span class="number">1</span>][<span class="number">2</span>]);  <span class="comment">// 6</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> *ptrs[<span class="number">3</span>];               <span class="comment">// 存 3 个 int*</span></span><br><span class="line"><span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;<span class="number">3</span>;++i) ptrs[i] = &amp;a[<span class="number">0</span>][i];</span><br><span class="line"><span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, *ptrs[<span class="number">2</span>]);   <span class="comment">// 3</span></span><br></pre></td></tr></table></figure><hr><h3 id="2-void-能否直接解引用？"><a href="#2-void-能否直接解引用？" class="headerlink" title="2. void * 能否直接解引用？"></a>2. <code>void *</code> 能否直接解引用？</h3><p><strong>不能</strong>。编译器不知道“解引用后该拿几个字节”，会报错：</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">void</span> *vp = <span class="built_in">malloc</span>(<span class="number">4</span>);</span><br><span class="line">*vp = <span class="number">10</span>;     <span class="comment">// error: dereferencing ‘void *’ pointer</span></span><br></pre></td></tr></table></figure><p><strong>正确姿势</strong>：先强制转换成具体类型再解引用。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">*(<span class="type">int</span>*)vp = <span class="number">10</span>;</span><br></pre></td></tr></table></figure><p><strong>例外</strong>：<code>gcc -pedantic</code> 下即使强转后写再转回 <code>void *</code> 完全合法，是通用数据交换的惯用手段。</p><hr><h3 id="3-free-p-后-p-还能不能用？应该怎样做？"><a href="#3-free-p-后-p-还能不能用？应该怎样做？" class="headerlink" title="3. free(p) 后 p 还能不能用？应该怎样做？"></a>3. <code>free(p)</code> 后 p 还能不能用？应该怎样做？</h3><ul><li><strong>语法上</strong>：能访问，但属于 <strong>Undefined Behaviour</strong>（可能立即崩溃，也可能暂时正常）。  </li><li><strong>正确做法</strong>：马上置空，防止“野指针”二次释放。<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">free</span>(p);</span><br><span class="line">p = <span class="literal">NULL</span>;   <span class="comment">// 后续可用 if(p) 做判断</span></span><br></pre></td></tr></table></figure><strong>二次 free 演示</strong>：<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">free</span>(p);</span><br><span class="line"><span class="built_in">free</span>(p);    <span class="comment">// 大概率 abort：double free or corruption</span></span><br></pre></td></tr></table></figure>置空后即使误 <code>free(NULL)</code> 也是安全空操作。</li></ul><hr><h3 id="4-char-s-abc-s-0-A-为什么崩溃？"><a href="#4-char-s-abc-s-0-A-为什么崩溃？" class="headerlink" title="4. char *s = &quot;abc&quot;; s[0] = &#39;A&#39;; 为什么崩溃？"></a>4. <code>char *s = &quot;abc&quot;; s[0] = &#39;A&#39;;</code> 为什么崩溃？</h3><ul><li><code>&quot;abc&quot;</code> 是<strong>字符串字面量</strong>，放在<strong>只读段（.rodata）</strong>。  </li><li><code>s</code> 拿到的是 <strong>const 地址</strong>，写操作触发 <strong>SIGSEGV</strong>（段错误）。<br><strong>修正</strong>：用可写数组<figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">char</span> s[] = <span class="string">&quot;abc&quot;</span>;  <span class="comment">// 栈上复制一份</span></span><br><span class="line">s[<span class="number">0</span>] = <span class="string">&#x27;A&#x27;</span>;        <span class="comment">// OK</span></span><br></pre></td></tr></table></figure>编译器警告：<code>warning: deprecated conversion from string constant to &#39;char*&#39;</code> —— 现代 C 应写成 <code>const char *s = &quot;abc&quot;;</code> 直接禁止写。</li></ul><hr><h3 id="5-用函数指针实现-C-版“策略模式”"><a href="#5-用函数指针实现-C-版“策略模式”" class="headerlink" title="5. 用函数指针实现 C 版“策略模式”"></a>5. 用函数指针实现 C 版“策略模式”</h3><p><strong>思路</strong>：把“行为”抽成函数指针，运行期动态切换。</p><p><strong>示例</strong>：两个排序策略（升序 &#x2F; 降序），同一接口 <code>sort()</code>。</p><figure class="highlight c"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="keyword">include</span> <span class="string">&lt;stdlib.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="title function_">int</span> <span class="params">(*<span class="type">cmp_t</span>)</span><span class="params">(<span class="type">int</span>, <span class="type">int</span>)</span>;   <span class="comment">// 策略签名</span></span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">asc</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span>  &#123; <span class="keyword">return</span> a - b; &#125;</span><br><span class="line"><span class="type">int</span> <span class="title function_">desc</span><span class="params">(<span class="type">int</span> a, <span class="type">int</span> b)</span> &#123; <span class="keyword">return</span> b - a; &#125;</span><br><span class="line"></span><br><span class="line"><span class="type">void</span> <span class="title function_">sort</span><span class="params">(<span class="type">int</span> *a, <span class="type">int</span> n, <span class="type">cmp_t</span> cmp)</span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;n<span class="number">-1</span>;++i)</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">0</span>;j&lt;n<span class="number">-1</span>-i;++j)</span><br><span class="line">            <span class="keyword">if</span>(cmp(a[j], a[j+<span class="number">1</span>]) &gt; <span class="number">0</span>)&#123;</span><br><span class="line">                <span class="type">int</span> t = a[j]; a[j] = a[j+<span class="number">1</span>]; a[j+<span class="number">1</span>] = t;</span><br><span class="line">            &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="type">int</span> <span class="title function_">main</span><span class="params">(<span class="type">void</span>)</span>&#123;</span><br><span class="line">    <span class="type">int</span> a[] = &#123;<span class="number">3</span>,<span class="number">1</span>,<span class="number">4</span>,<span class="number">2</span>&#125;;</span><br><span class="line">    sort(a, <span class="number">4</span>, asc);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;<span class="number">4</span>;++i) <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, a[i]); <span class="comment">// 1 2 3 4</span></span><br><span class="line">    <span class="built_in">putchar</span>(<span class="string">&#x27;\n&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    sort(a, <span class="number">4</span>, desc);</span><br><span class="line">    <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;<span class="number">4</span>;++i) <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, a[i]); <span class="comment">// 4 3 2 1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>扩展</strong>：把 <code>cmp_t</code> 放进结构体可做成“对象 + 虚表”风格，实现更复杂的插件体系。</p><hr><h3 id="30-秒速记卡"><a href="#30-秒速记卡" class="headerlink" title="30 秒速记卡"></a>30 秒速记卡</h3><ol><li><code>[]</code> 优先级高 → <code>*p[3]</code> 是数组；<code>(*p)[3]</code> 是指向数组。  </li><li><code>void *</code> 必须强转后解引用。  </li><li><code>free(p); p = NULL;</code> 防二次释放。  </li><li>字符串字面量只读；想改就写成 <code>char s[] = &quot;...&quot;;</code>  </li><li>函数指针 &#x3D; C 的“多态”，把行为当参数传。</li></ol>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>聚集索引和辅助索引(非聚集索引)</title>
      <link href="/2025/09/29/%E8%81%9A%E9%9B%86%E7%B4%A2%E5%BC%95%E5%92%8C%E8%BE%85%E5%8A%A9%E7%B4%A2%E5%BC%95(%E9%9D%9E%E8%81%9A%E9%9B%86%E7%B4%A2%E5%BC%95)/"/>
      <url>/2025/09/29/%E8%81%9A%E9%9B%86%E7%B4%A2%E5%BC%95%E5%92%8C%E8%BE%85%E5%8A%A9%E7%B4%A2%E5%BC%95(%E9%9D%9E%E8%81%9A%E9%9B%86%E7%B4%A2%E5%BC%95)/</url>
      
        <content type="html"><![CDATA[<h1 id="聚集索引和辅助索引-非聚集索引"><a href="#聚集索引和辅助索引-非聚集索引" class="headerlink" title="聚集索引和辅助索引(非聚集索引)"></a>聚集索引和辅助索引(非聚集索引)</h1><p>在 MySQL（InnoDB）里，“聚集索引”与“辅助索引”是两种<strong>物理存储方式</strong>完全不同的索引。<br>理解它们差异的核心只有一句话：<br><strong>聚集索引的叶子节点就是整行数据，辅助索引的叶子节点只是“主键值”——拿到主键后还得再回表。</strong></p><p>下面从 10 个维度逐层拆解，看完即可 100% 区分。</p><hr><ol><li>定义与存在性</li></ol><hr><ul><li><p>聚集索引（Clustered Index）<br>一张表<strong>只能有 1 个</strong>，InnoDB 把它当成数据本身来存放。<br>如果表定义了 PK，则 PK 就是聚集索引；<br>没 PK 就选第一个 UNIQUE+NOT NULL 键；<br>再没有就隐式生成 6 字节的 RowID 作为聚集索引。</p></li><li><p>辅助索引（Secondary Index）<br>又叫二级索引，<strong>可以建多个</strong>，包括普通 INDEX、UNIQUE、FULLTEXT、SPATIAL。<br>它们与数据行分离存放，仅起“指路”作用。</p></li></ul><hr><ol start="2"><li>叶子节点内容</li></ol><hr><ul><li>聚集索引叶子：完整的行记录（所有列的值都在）。  </li><li>辅助索引叶子：索引列的值 + 对应行的<strong>聚集索引键值</strong>（通常是主键值）。<br>因此通过辅助索引查非索引列需要<strong>回表（lookup）</strong>：先走辅助索引拿到主键，再走聚集索引拿整行。</li></ul><hr><ol start="3"><li>逻辑结构图</li></ol><hr><p>聚集索引<br>├─ 非叶子节点：主键值 + 页指针<br>└─ 叶子节点：整行数据（主键顺序物理相邻）</p><p>辅助索引<br>├─ 非叶子节点：索引列值 + 页指针<br>└─ 叶子节点：索引列值 + 主键值<br>（没有行数据）</p><hr><ol start="4"><li>是否存储整行</li></ol><hr><ul><li>聚集索引：是，它就是数据文件。  </li><li>辅助索引：否，只存索引列+主键。</li></ul><hr><ol start="5"><li>插入性能</li></ol><hr><ul><li><p>聚集索引：<br>插入速度与主键顺序高度相关。<br>顺序主键（AUTO_INCREMENT） → 页分裂少，顺序写，性能高。<br>随机主键（UUID） → 页分裂频繁，产生碎片，性能差。</p></li><li><p><strong>辅助索引：<br>插入时除了要写数据页，还要每建一个索引就多写一棵 B+ 树，随机 IO 成倍增加。<br>因此“索引越多，插入越慢”主要指辅助索引。</strong></p></li></ul><hr><ol start="6"><li>范围扫描性能</li></ol><hr><ul><li><p>聚集索引：<br>叶子节点上数据<strong>物理连续</strong>，范围扫描（BETWEEN、&gt;、ORDER BY PK）只需顺序读盘，极快。</p></li><li><p>辅助索引：<br>叶子节点只给主键，范围扫描后拿到一批主键，再<strong>随机回表</strong>（离散读聚集索引），随机 IO 多，性能下降。<br>覆盖索引（Using index）可避免回表，但前提是查询列全部包含在该辅助索引中。</p></li></ul><hr><ol start="7"><li>回表（lookup）成本</li></ol><hr><ul><li>只有辅助索引才需要回表。  </li><li>回表次数 &#x3D; 辅助索引叶子节点匹配到的行数。  </li><li>大偏移分页（LIMIT 100000,10）下，辅助索引先扫 100010 个主键再回表，性能暴跌；<br>此时让查询走<strong>覆盖索引</strong>或直接利用聚集索引排序可根治。</li></ul><hr><ol start="8"><li>覆盖索引（Using index）</li></ol><hr><ul><li>辅助索引如果能满足查询的全部列（包括 WHERE、SELECT、ORDER BY），就<strong>不需要回表</strong>，Extra 显示 “Using index”。  </li><li>聚集索引天然覆盖所有列，但优化器不会把 “Using index” 写进 Extra，因为它就是数据。</li></ul><hr><ol start="9"><li>存储空间</li></ol><hr><ul><li>聚集索引：1 份，大小 &#x3D; 表数据本身。  </li><li>辅助索引：每建 1 个就多 1 棵 B+ 树，空间 ≈ (索引列长度 + 主键长度) × 行数 × 1.2（页头指针开销）。<br>主键越长，辅助索引膨胀越厉害——这是“UUID 当主键”最致命的副作用之一。</li></ul><hr><ol start="10"><li>典型 SQL 对比</li></ol><hr><p>表：user(id PK, name VARCHAR(30), age INT, addr VARCHAR(100), INDEX(name))</p><ol><li><p>SELECT * FROM user WHERE id&#x3D;5;<br>走聚集索引，1 次主键查找，0 回表，最快。</p></li><li><p>SELECT * FROM user WHERE name&#x3D;’Jack’;<br>先走辅助索引 name → 拿到主键 id → 再回表取 addr、age，<strong>2 次索引查找</strong>。</p></li><li><p>SELECT id, name FROM user WHERE name&gt;’Tom’ ORDER BY name;<br>辅助索引 name 已包含 (name, id)，覆盖索引，<strong>无需回表</strong>，性能 ≈ 聚集索引。</p></li></ol><hr><h2 id="一句话口诀"><a href="#一句话口诀" class="headerlink" title="一句话口诀"></a>一句话口诀</h2><p>“聚集即数据，辅助只指路；<br>  顺序插入快，随机插入慢；<br>  范围扫连续，辅助多回表；<br>  主键越短，辅助越瘦。”</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>java中的四种函数式接口</title>
      <link href="/2025/09/28/java%E4%B8%AD%E7%9A%84%E5%9B%9B%E7%A7%8D%E5%87%BD%E6%95%B0%E5%BC%8F%E6%8E%A5%E5%8F%A3/"/>
      <url>/2025/09/28/java%E4%B8%AD%E7%9A%84%E5%9B%9B%E7%A7%8D%E5%87%BD%E6%95%B0%E5%BC%8F%E6%8E%A5%E5%8F%A3/</url>
      
        <content type="html"><![CDATA[<h2 id="java中的四种函数式接口"><a href="#java中的四种函数式接口" class="headerlink" title="java中的四种函数式接口"></a>java中的四种函数式接口</h2><p>在 Java 中，函数式接口（Functional Interface）是指<strong>只包含一个抽象方法</strong>的接口，通常用于 Lambda 表达式或方法引用。Java 8 引入了 <code>java.util.function</code> 包，其中定义了四种最常用的函数式接口，分别是：</p><table><thead><tr><th>接口名</th><th>抽象方法</th><th>输入类型</th><th>输出类型</th><th>用途说明</th></tr></thead><tbody><tr><td><strong>Consumer<T></strong></td><td><code>void accept(T t)</code></td><td>T</td><td>void</td><td>接收一个参数，执行操作，无返回</td></tr><tr><td><strong>Supplier<T></strong></td><td><code>T get()</code></td><td>无</td><td>T</td><td>不接收参数，返回一个值</td></tr><tr><td><strong>Function&lt;T, R&gt;</strong></td><td><code>R apply(T t)</code></td><td>T</td><td>R</td><td>接收一个参数，返回一个结果</td></tr><tr><td><strong>Predicate<T></strong></td><td><code>boolean test(T t)</code></td><td>T</td><td>boolean</td><td>接收一个参数，返回布尔值，用于判断</td></tr></tbody></table><hr><h3 id="✅-示例代码"><a href="#✅-示例代码" class="headerlink" title="✅ 示例代码"></a>✅ 示例代码</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.function.*;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">FunctionalDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// Consumer 示例</span></span><br><span class="line">        Consumer&lt;String&gt; printer = s -&gt; System.out.println(<span class="string">&quot;打印: &quot;</span> + s);</span><br><span class="line">        printer.accept(<span class="string">&quot;Hello&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Supplier 示例</span></span><br><span class="line">        Supplier&lt;Double&gt; randomSupplier = () -&gt; Math.random();</span><br><span class="line">        System.out.println(<span class="string">&quot;随机数: &quot;</span> + randomSupplier.get());</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Function 示例</span></span><br><span class="line">        Function&lt;Integer, String&gt; intToString = i -&gt; <span class="string">&quot;数字是: &quot;</span> + i;</span><br><span class="line">        System.out.println(intToString.apply(<span class="number">42</span>));</span><br><span class="line"></span><br><span class="line">        <span class="comment">// Predicate 示例</span></span><br><span class="line">        Predicate&lt;Integer&gt; isEven = n -&gt; n % <span class="number">2</span> == <span class="number">0</span>;</span><br><span class="line">        System.out.println(<span class="string">&quot;是偶数吗？ &quot;</span> + isEven.test(<span class="number">10</span>));</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="🔍-小结口诀（记忆用）："><a href="#🔍-小结口诀（记忆用）：" class="headerlink" title="🔍 小结口诀（记忆用）："></a>🔍 小结口诀（记忆用）：</h3><ul><li><strong>Consumer</strong> 消费不产出（有进无出）  </li><li><strong>Supplier</strong> 供应不摄入（无进有出）  </li><li><strong>Function</strong> 转化有进出（有进有出）  </li><li><strong>Predicate</strong> 判断真假（有进有布尔）</li></ul>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Thread.currentThread().interrupt()</title>
      <link href="/2025/09/28/Thread.currentThread().interrupt()/"/>
      <url>/2025/09/28/Thread.currentThread().interrupt()/</url>
      
        <content type="html"><![CDATA[<h2 id="Thread-currentThread-interrupt"><a href="#Thread-currentThread-interrupt" class="headerlink" title="Thread.currentThread().interrupt()"></a><code>Thread.currentThread().interrupt()</code></h2><p><code>Thread.currentThread().interrupt()</code> 干的事情只有一件：</p><blockquote><p><strong>把“当前线程”的中断标志位重新设为 true</strong>。</p></blockquote><hr><h3 id="1-为什么“重新”设？"><a href="#1-为什么“重新”设？" class="headerlink" title="1 为什么“重新”设？"></a>1 为什么“重新”设？</h3><ul><li><code>InterruptedException</code> 被抛出时，JVM 会<strong>自动清除</strong>中断标志（置 false）。  </li><li>如果此时代码捕获了 <code>InterruptedException</code> 却<strong>不重新中断</strong>，那么<strong>上层调用者</strong>就永远不知道“曾经被中断过”，<strong>任务可能无法优雅停止</strong>。</li></ul><hr><h3 id="2-使用模板"><a href="#2-使用模板" class="headerlink" title="2 使用模板"></a>2 使用模板</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">try</span> &#123;</span><br><span class="line">    Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">&#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">    <span class="comment">// 1. 立即恢复中断标志</span></span><br><span class="line">    Thread.currentThread().interrupt();</span><br><span class="line">    <span class="comment">// 2. 自己的清理逻辑</span></span><br><span class="line">    <span class="keyword">return</span>;                 <span class="comment">// 或抛出运行时异常</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h3 id="3-不恢复的后果"><a href="#3-不恢复的后果" class="headerlink" title="3 不恢复的后果"></a>3 不恢复的后果</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">    logger.error(<span class="string">&quot;sleep interrupted&quot;</span>, e);</span><br><span class="line">    <span class="comment">// 忘记 Thread.currentThread().interrupt()</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>中断标志丢失 → 调用者检查 <code>Thread.currentThread().isInterrupted()</code> 永远返回 false  </li><li><strong>线程池</strong>、<strong>可取消任务</strong>、<strong>框架循环</strong> 无法退出 → 变成“漏不掉的僵尸线程”</li></ul><hr><h3 id="4-一句话总结"><a href="#4-一句话总结" class="headerlink" title="4 一句话总结"></a>4 一句话总结</h3><p><strong>捕获 <code>InterruptedException</code> 后，最正确的做法就是：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Thread.currentThread().interrupt();</span><br></pre></td></tr></table></figure><p><strong>把“曾被打断”的事实传递出去，让上层有机会优雅地结束任务。</strong></p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>java内省机制</title>
      <link href="/2025/09/25/java%E5%86%85%E7%9C%81%E6%9C%BA%E5%88%B6/"/>
      <url>/2025/09/25/java%E5%86%85%E7%9C%81%E6%9C%BA%E5%88%B6/</url>
      
        <content type="html"><![CDATA[<h2 id="java内省机制"><a href="#java内省机制" class="headerlink" title="java内省机制"></a>java内省机制</h2><h1 id="Java-内省机制（Introspection）速览"><a href="#Java-内省机制（Introspection）速览" class="headerlink" title="Java 内省机制（Introspection）速览  "></a>Java 内省机制（Introspection）速览  </h1><p>一、一句话定位<br>“只认 getter&#x2F;setter”的官方属性扫描器——把 JavaBean 变成<strong>属性名⇄读写方法</strong>的映射，无需手写反射。</p><hr><p>二、基础使用（3 个类 4 行代码）</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 拿到 BeanInfo（缓存全局复用）</span></span><br><span class="line"><span class="type">BeanInfo</span> <span class="variable">info</span> <span class="operator">=</span> Introspector.getBeanInfo(User.class, Object.class); <span class="comment">// 忽略 Object 的属性</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 遍历属性</span></span><br><span class="line"><span class="keyword">for</span> (PropertyDescriptor pd : info.getPropertyDescriptors()) &#123;</span><br><span class="line">    <span class="type">String</span>     <span class="variable">name</span>   <span class="operator">=</span> pd.getName();          <span class="comment">// 属性名</span></span><br><span class="line">    <span class="type">Method</span>     <span class="variable">getter</span> <span class="operator">=</span> pd.getReadMethod();    <span class="comment">// getXxx()</span></span><br><span class="line">    <span class="type">Method</span>     <span class="variable">setter</span> <span class="operator">=</span> pd.getWriteMethod();   <span class="comment">// setXxx()</span></span><br><span class="line">    Class&lt;?&gt;   type   = pd.getPropertyType();  <span class="comment">// 属性类型</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (getter != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="type">Object</span> <span class="variable">value</span> <span class="operator">=</span> getter.invoke(user);    <span class="comment">// 读</span></span><br><span class="line">        System.out.println(name + <span class="string">&quot; = &quot;</span> + value);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>依赖包：仅 <code>java.beans.*</code>，零第三方。</p><hr><p>三、核心 API 脑图</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">Introspector.getBeanInfo(Class&lt;?&gt; beanClass, Class&lt;?&gt; stopClass)</span><br><span class="line">    ↓</span><br><span class="line">BeanInfo</span><br><span class="line">    ├─ PropertyDescriptor[]   读写方法对</span><br><span class="line">    ├─ MethodDescriptor[]     普通方法（很少用）</span><br><span class="line">    └─ EventSetDescriptor[]   事件（已过时）</span><br><span class="line">PropertyDescriptor</span><br><span class="line">    ├─ getName()</span><br><span class="line">    ├─ getReadMethod()</span><br><span class="line">    ├─ getWriteMethod()</span><br><span class="line">    └─ getPropertyType()</span><br></pre></td></tr></table></figure><hr><p>四、工作原理（源码级顺口溜）</p><ol><li><p><strong>缓存优先</strong><br><code>Introspector</code> 内部有全局 <code>SoftReference</code> 缓存——同一个 Class 只解析一次，性能高于原生反射。</p></li><li><p><strong>命名规约硬编码</strong><br>把 <code>getXxx/setXxx/isXxx</code> 按 JavaBean 规范拆出属性名；<strong>不认 public 字段</strong>。</p></li><li><p><strong>低层还是反射</strong><br>解析完成后返回的 <code>Method</code> 对象就是 <code>java.lang.reflect.Method</code>——内省只是“反射的封装 + 缓存 + 规约”。</p></li><li><p><strong>可插拔：BeanInfo 定制</strong><br>若类提供 <code>XXXBeanInfo</code>（同名包内），<code>Introspector</code> 会优先加载它，可隐藏&#x2F;重命名属性，实现<strong>细粒度控制</strong>。</p></li></ol><hr><p>五、常见场景</p><table><thead><tr><th>场景</th><th>原因</th></tr></thead><tbody><tr><td>早期 JSP EL <code>${user.name}</code></td><td>容器用内省找 <code>getName()</code></td></tr><tr><td>GUI 表单绑定（Swing）</td><td>自动把 JTextField 绑定到 bean 属性</td></tr><tr><td>轻量级 Map↔Bean 转换</td><td>代码量低，不引入 Spring&#x2F;Apache 依赖</td></tr><tr><td>写脚手架代码生成器</td><td>快速拿到所有可写属性</td></tr></tbody></table><hr><p>六、与反射快速对比</p><table><thead><tr><th>维度</th><th>反射</th><th>内省</th></tr></thead><tbody><tr><td>入口</td><td><code>Class.getMethod</code></td><td><code>Introspector.getBeanInfo</code></td></tr><tr><td>范围</td><td>所有成员</td><td>仅 JavaBean 属性</td></tr><tr><td>缓存</td><td>无</td><td>全局软引用缓存</td></tr><tr><td>性能</td><td>慢</td><td>快（首次后）</td></tr><tr><td>代码量</td><td>多</td><td>少</td></tr></tbody></table><hr><p>七、小结（背下来）</p><blockquote><p>内省 &#x3D; <strong>官方出品的 JavaBean 扫描器</strong>；<br>给类→<code>Introspector</code>→<code>PropertyDescriptor</code>→<code>getReadMethod / getWriteMethod</code>；<br>缓存 + 规约，写工具类时比反射更轻更快。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Vue事件修饰符</title>
      <link href="/2025/09/18/Vue%E4%BA%8B%E4%BB%B6%E4%BF%AE%E9%A5%B0%E7%AC%A6/"/>
      <url>/2025/09/18/Vue%E4%BA%8B%E4%BB%B6%E4%BF%AE%E9%A5%B0%E7%AC%A6/</url>
      
        <content type="html"><![CDATA[<h2 id="Vue事件修饰符"><a href="#Vue事件修饰符" class="headerlink" title="Vue事件修饰符"></a>Vue事件修饰符</h2><p>在 Vue 中，事件修饰符用于便捷处理事件行为，除了 <code>.stop</code> 之外，常用的还有以下这些，我将包含 <code>.stop</code> 一起整理：</p><h3 id="1-stop"><a href="#1-stop" class="headerlink" title="1. .stop"></a>1. <code>.stop</code></h3><p><strong>作用</strong>：阻止事件冒泡（事件不会向父元素传播）。<br><strong>场景</strong>：嵌套元素中，避免子元素事件触发父元素同名事件。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">click</span>=<span class="string">&quot;parentClick&quot;</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 点击按钮只会触发 childClick，不会触发 parentClick --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> @<span class="attr">click.stop</span>=<span class="string">&quot;childClick&quot;</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="2-prevent"><a href="#2-prevent" class="headerlink" title="2. .prevent"></a>2. <code>.prevent</code></h3><p><strong>作用</strong>：阻止事件的默认行为（如表单提交刷新页面、链接跳转）。<br><strong>场景</strong>：需要自定义处理逻辑，替代浏览器默认行为。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 阻止表单默认提交（不刷新页面） --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">form</span> @<span class="attr">submit.prevent</span>=<span class="string">&quot;handleSubmit&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> <span class="attr">type</span>=<span class="string">&quot;submit&quot;</span>&gt;</span>提交<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">form</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 阻止链接跳转 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">href</span>=<span class="string">&quot;https://example.com&quot;</span> @<span class="attr">click.prevent</span>=<span class="string">&quot;handleClick&quot;</span>&gt;</span>点击不跳转<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="3-capture"><a href="#3-capture" class="headerlink" title="3. .capture"></a>3. <code>.capture</code></h3><p><strong>作用</strong>：启用事件捕获模式（事件从父元素向子元素传播，而非默认的冒泡模式）。<br><strong>场景</strong>：需要优先处理父元素事件时。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">click.capture</span>=<span class="string">&quot;parentClick&quot;</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 点击按钮时，先触发 parentClick，再触发 childClick --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> @<span class="attr">click</span>=<span class="string">&quot;childClick&quot;</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="4-self"><a href="#4-self" class="headerlink" title="4. .self"></a>4. <code>.self</code></h3><p><strong>作用</strong>：事件仅在<strong>直接触发于当前元素本身</strong>时才执行（忽略冒泡&#x2F;捕获的事件）。<br><strong>场景</strong>：避免父元素响应子元素传递的事件。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">click.self</span>=<span class="string">&quot;parentClick&quot;</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- 点击按钮时，事件冒泡到 div，但 div 的事件不会触发（事件源是按钮） --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">button</span> @<span class="attr">click</span>=<span class="string">&quot;childClick&quot;</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="5-once"><a href="#5-once" class="headerlink" title="5. .once"></a>5. <code>.once</code></h3><p><strong>作用</strong>：事件仅触发一次，触发后自动解绑。<br><strong>场景</strong>：限制操作次数（如提交按钮防重复点击）。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">button</span> @<span class="attr">click.once</span>=<span class="string">&quot;submitForm&quot;</span>&gt;</span>提交（仅一次）<span class="tag">&lt;/<span class="name">button</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="6-passive"><a href="#6-passive" class="headerlink" title="6. .passive"></a>6. <code>.passive</code></h3><p><strong>作用</strong>：告知浏览器不会阻止事件默认行为，优化滚动&#x2F;触摸事件性能。<br><strong>场景</strong>：<code>scroll</code>、<code>touchmove</code> 等高频事件，避免卡顿。  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 优化移动端滚动性能 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">scroll.passive</span>=<span class="string">&quot;onScroll&quot;</span>&gt;</span>长列表内容<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="7-键盘事件修饰符"><a href="#7-键盘事件修饰符" class="headerlink" title="7. 键盘事件修饰符"></a>7. 键盘事件修饰符</h3><p>针对 <code>@keyup</code>、<code>@keydown</code> 等键盘事件，指定特定按键触发：  </p><ul><li><code>.enter</code>：回车键  </li><li><code>.tab</code>：Tab 键  </li><li><code>.delete</code>：删除键（含 Backspace）  </li><li><code>.esc</code>：Esc 键  </li><li><code>.up</code>&#x2F;<code>.down</code>&#x2F;<code>.left</code>&#x2F;<code>.right</code>：方向键</li></ul><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 按下回车键触发搜索 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> @<span class="attr">keyup.enter</span>=<span class="string">&quot;search&quot;</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 按下 Esc 键清空输入 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">input</span> @<span class="attr">keyup.esc</span>=<span class="string">&quot;clearInput&quot;</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="8-鼠标按钮修饰符"><a href="#8-鼠标按钮修饰符" class="headerlink" title="8. 鼠标按钮修饰符"></a>8. 鼠标按钮修饰符</h3><p>针对鼠标事件，指定按键触发：  </p><ul><li><code>.left</code>：左键  </li><li><code>.right</code>：右键  </li><li><code>.middle</code>：中键（滚轮）</li></ul><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 右键点击显示菜单 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">click.right</span>=<span class="string">&quot;showMenu&quot;</span>&gt;</span>右键点击我<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="修饰符组合使用"><a href="#修饰符组合使用" class="headerlink" title="修饰符组合使用"></a>修饰符组合使用</h3><p>多个修饰符可组合（顺序可能影响效果）：  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 阻止冒泡 + 阻止默认行为 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">a</span> @<span class="attr">click.stop.prevent</span>=<span class="string">&quot;handleClick&quot;</span> <span class="attr">href</span>=<span class="string">&quot;...&quot;</span>&gt;</span>点击<span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 捕获模式 + 仅自身触发 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> @<span class="attr">click.capture.self</span>=<span class="string">&quot;handle&quot;</span>&gt;</span>...<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>这些修饰符能大幅简化事件处理代码，避免在方法中手动调用 <code>event.stopPropagation()</code> 等原生 API，让逻辑更清晰。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>启动类上各注解的作用</title>
      <link href="/2025/09/18/%E5%90%AF%E5%8A%A8%E7%B1%BB%E4%B8%8A%E5%90%84%E6%B3%A8%E8%A7%A3%E7%9A%84%E4%BD%9C%E7%94%A8/"/>
      <url>/2025/09/18/%E5%90%AF%E5%8A%A8%E7%B1%BB%E4%B8%8A%E5%90%84%E6%B3%A8%E8%A7%A3%E7%9A%84%E4%BD%9C%E7%94%A8/</url>
      
        <content type="html"><![CDATA[<h2 id="启动类上各注解的作用"><a href="#启动类上各注解的作用" class="headerlink" title="启动类上各注解的作用"></a>启动类上各注解的作用</h2><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.boot.SpringApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.SpringBootApplication;</span><br><span class="line"><span class="keyword">import</span> org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.cloud.client.discovery.EnableDiscoveryClient;</span><br><span class="line"><span class="keyword">import</span> org.springframework.cloud.openfeign.EnableFeignClients;</span><br><span class="line"><span class="keyword">import</span> org.springframework.retry.annotation.EnableRetry;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.EnableAsync;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.EnableScheduling;</span><br><span class="line"><span class="keyword">import</span> org.springframework.transaction.annotation.EnableTransactionManagement;</span><br><span class="line"></span><br><span class="line"><span class="meta">@SpringBootApplication(exclude = DataSourceAutoConfiguration.class,scanBasePackages = &quot;com.huice.hjl&quot;)</span></span><br><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="meta">@EnableScheduling</span></span><br><span class="line"><span class="meta">@EnableRetry</span></span><br><span class="line"><span class="meta">@EnableDiscoveryClient</span></span><br><span class="line"><span class="meta">@EnableFeignClients</span></span><br><span class="line"><span class="meta">@EnableTransactionManagement(proxyTargetClass = true)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AnalysisApplication</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">       SpringApplication.run(AnalysisApplication.class, args);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>你提到的这些注解都是 Spring Boot 开发中常用的核心注解，用于开启特定功能或配置应用行为。下面逐个解释它们的作用：</p><h3 id="1-SpringBootApplication"><a href="#1-SpringBootApplication" class="headerlink" title="1. @SpringBootApplication"></a>1. <code>@SpringBootApplication</code></h3><p>这是 Spring Boot 的<strong>核心注解</strong>，用于标识一个 Spring Boot 应用的主类，是以下三个注解的组合：</p><ul><li><code>@Configuration</code>：标识该类为配置类，可定义 Bean。</li><li><code>@EnableAutoConfiguration</code>：开启自动配置（Spring Boot 的核心特性），根据依赖自动配置 Bean（如引入 <code>spring-boot-starter-web</code> 会自动配置 Tomcat、Spring MVC 等）。</li><li><code>@ComponentScan</code>：扫描当前类所在包及子包下的组件（<code>@Component</code>、<code>@Service</code>、<code>@Controller</code> 等）。</li></ul><p><strong>属性说明</strong>：</p><ul><li><code>exclude = DataSourceAutoConfiguration.class</code>：排除 Spring 自动配置的数据源（<code>DataSource</code>）。通常在不需要默认数据源（如纯接口服务、不连接数据库的场景）或自定义数据源时使用，避免自动配置冲突。</li><li><code>scanBasePackages = &quot;com.huice.hjl&quot;</code>：指定组件扫描的基础包路径（替代默认的当前类所在包），Spring 会扫描该路径下的所有组件并注册到容器中。</li></ul><h3 id="2-EnableAsync"><a href="#2-EnableAsync" class="headerlink" title="2. @EnableAsync"></a>2. <code>@EnableAsync</code></h3><p>开启<strong>异步方法执行</strong>的支持。  </p><ul><li>配合 <code>@Async</code> 注解使用：在方法上标注 <code>@Async</code> 后，该方法会在独立的线程中执行，不阻塞主线程。  </li><li>用途：适合处理耗时操作（如文件上传、邮件发送），提高应用并发能力。</li></ul><h3 id="3-EnableScheduling"><a href="#3-EnableScheduling" class="headerlink" title="3. @EnableScheduling"></a>3. <code>@EnableScheduling</code></h3><p>开启<strong>定时任务</strong>的支持。  </p><ul><li>配合 <code>@Scheduled</code> 注解使用：在方法上标注 <code>@Scheduled</code> 并指定执行时间（如 <code>@Scheduled(cron = &quot;0 0 1 * * ?&quot;)</code> 表示每天凌晨 1 点执行），Spring 会自动按规则触发方法。  </li><li>用途：实现周期性任务（如数据备份、报表生成、定时清理）。</li></ul><h3 id="4-EnableRetry"><a href="#4-EnableRetry" class="headerlink" title="4. @EnableRetry"></a>4. <code>@EnableRetry</code></h3><p>开启<strong>方法重试</strong>的支持。  </p><ul><li>配合 <code>@Retryable</code> 注解使用：在可能失败的方法（如远程接口调用、数据库操作）上标注 <code>@Retryable</code>，可配置重试次数、重试间隔、触发重试的异常类型等。  </li><li>用途：应对临时故障（如网络波动），提高接口稳定性。例如：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Retryable(value = &#123;IOException.class&#125;, maxAttempts = 3, backoff = @Backoff(delay = 1000))</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">callRemoteApi</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 可能抛出 IOException 的远程调用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="5-EnableDiscoveryClient"><a href="#5-EnableDiscoveryClient" class="headerlink" title="5. @EnableDiscoveryClient"></a>5. <code>@EnableDiscoveryClient</code></h3><p>开启<strong>服务发现</strong>功能，使当前应用能注册到服务注册中心（如 Eureka、Nacos、Consul 等），并能发现其他服务。  </p><ul><li>用途：在微服务架构中，实现服务间的注册与发现，支持服务调用（配合 <code>@LoadBalanced</code> 实现负载均衡）。  </li><li>注意：Spring Cloud 2020 之后的版本可省略此注解，只需引入注册中心依赖即可自动开启服务发现。</li></ul><h3 id="6-EnableFeignClients"><a href="#6-EnableFeignClients" class="headerlink" title="6. @EnableFeignClients"></a>6. <code>@EnableFeignClients</code></h3><p>开启<strong>Feign 客户端</strong>的支持。  </p><ul><li>Feign 是声明式 HTTP 客户端，通过接口 + 注解的方式定义远程服务调用（无需手动编写 <code>RestTemplate</code> 代码）。  </li><li>用途：简化微服务间的远程调用，例如：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@FeignClient(name = &quot;user-service&quot;)</span> <span class="comment">// 声明调用的服务名</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserServiceClient</span> &#123;</span><br><span class="line">    <span class="meta">@GetMapping(&quot;/users/&#123;id&#125;&quot;)</span></span><br><span class="line">    User <span class="title function_">getUserById</span><span class="params">(<span class="meta">@PathVariable(&quot;id&quot;)</span> Long id)</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="7-EnableTransactionManagement-proxyTargetClass-true"><a href="#7-EnableTransactionManagement-proxyTargetClass-true" class="headerlink" title="7. @EnableTransactionManagement(proxyTargetClass = true)"></a>7. <code>@EnableTransactionManagement(proxyTargetClass = true)</code></h3><p>开启<strong>声明式事务</strong>的支持，配合 <code>@Transactional</code> 注解使用，使方法在事务中执行（保证 ACID 特性）。  </p><ul><li>属性 <code>proxyTargetClass = true</code>：指定事务代理方式为<strong>CGLIB 代理</strong>（默认是 JDK 动态代理，仅支持接口）。设置为 <code>true</code> 后，可对类（非接口）的方法增强事务，更灵活。  </li><li>用途：确保数据库操作的原子性（如转账时“扣钱”和“加钱”要么同时成功，要么同时失败）。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>这些注解共同作用是：<strong>搭建一个支持异步、定时任务、重试、服务注册发现、远程调用和事务管理的 Spring Boot 微服务应用</strong>，并通过 <code>@SpringBootApplication</code> 定制基础配置（如扫描路径、排除自动配置）。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>var，let,  const的区别</title>
      <link href="/2025/09/17/var%EF%BC%8Clet,-const%E7%9A%84%E5%8C%BA%E5%88%AB/"/>
      <url>/2025/09/17/var%EF%BC%8Clet,-const%E7%9A%84%E5%8C%BA%E5%88%AB/</url>
      
        <content type="html"><![CDATA[<h2 id="var，let-const的区别"><a href="#var，let-const的区别" class="headerlink" title="var，let,  const的区别"></a>var，let,  const的区别</h2><p>在 JavaScript 中，<code>var</code>、<code>let</code>、<code>const</code> 都是用于声明变量的关键字，但它们在<strong>作用域、变量提升、可修改性</strong>等方面有显著区别，以下是详细对比：</p><h3 id="1-作用域（Scope）"><a href="#1-作用域（Scope）" class="headerlink" title="1. 作用域（Scope）"></a>1. 作用域（Scope）</h3><ul><li><p><strong><code>var</code><strong>：<br>仅支持</strong>函数作用域</strong>（function scope），在函数外声明的 <code>var</code> 变量是全局变量，在代码块（如 <code>if</code>、<code>for</code> 循环的 <code>{}</code>）中声明的 <code>var</code> 变量会“穿透”代码块，在外部仍可访问。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">  <span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(a); <span class="comment">// 10（变量穿透了 if 代码块）</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>let</code> 和 <code>const</code><strong>：<br>支持</strong>块级作用域</strong>（block scope），变量仅在声明它的代码块（<code>{}</code>）内有效，外部无法访问。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">if</span> (<span class="literal">true</span>) &#123;</span><br><span class="line">  <span class="keyword">let</span> b = <span class="number">20</span>;</span><br><span class="line">  <span class="keyword">const</span> c = <span class="number">30</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(b); <span class="comment">// 报错：b is not defined</span></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(c); <span class="comment">// 报错：c is not defined</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="2-变量提升（Hoisting）"><a href="#2-变量提升（Hoisting）" class="headerlink" title="2. 变量提升（Hoisting）"></a>2. 变量提升（Hoisting）</h3><ul><li><p><strong><code>var</code><strong>：<br>会发生</strong>变量提升</strong>，即变量声明会被提升到当前作用域的顶部，但赋值不会提升。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(x); <span class="comment">// undefined（声明被提升，赋值未提升）</span></span><br><span class="line"><span class="keyword">var</span> x = <span class="number">5</span>;</span><br></pre></td></tr></table></figure></li><li><p><strong><code>let</code> 和 <code>const</code><strong>：<br>存在</strong>暂时性死区（Temporal Dead Zone, TDZ）</strong>，变量声明不会被提升，在声明前访问会直接报错。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(y); <span class="comment">// 报错：Cannot access &#x27;y&#x27; before initialization</span></span><br><span class="line"><span class="keyword">let</span> y = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="variable language_">console</span>.<span class="title function_">log</span>(z); <span class="comment">// 报错：Cannot access &#x27;z&#x27; before initialization</span></span><br><span class="line"><span class="keyword">const</span> z = <span class="number">15</span>;</span><br></pre></td></tr></table></figure></li></ul><h3 id="3-可修改性（Mutability）"><a href="#3-可修改性（Mutability）" class="headerlink" title="3. 可修改性（Mutability）"></a>3. 可修改性（Mutability）</h3><ul><li><p><strong><code>var</code> 和 <code>let</code><strong>：<br>声明的是</strong>可重新赋值的变量</strong>，可以多次修改其值。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">1</span>;</span><br><span class="line">a = <span class="number">2</span>; <span class="comment">// 允许</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">let</span> b = <span class="number">3</span>;</span><br><span class="line">b = <span class="number">4</span>; <span class="comment">// 允许</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>const</code><strong>：<br>声明的是</strong>常量</strong>，<strong>必须在声明时赋值</strong>，且赋值后<strong>不能重新赋值</strong>（但如果是对象&#x2F;数组，其内部属性可以修改）。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> c = <span class="number">5</span>;</span><br><span class="line">c = <span class="number">6</span>; <span class="comment">// 报错：Assignment to constant variable</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 特殊情况：对象/数组的内部修改允许</span></span><br><span class="line"><span class="keyword">const</span> obj = &#123; <span class="attr">name</span>: <span class="string">&#x27;张三&#x27;</span> &#125;;</span><br><span class="line">obj.<span class="property">name</span> = <span class="string">&#x27;李四&#x27;</span>; <span class="comment">// 允许（仅禁止重新赋值整个对象）</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="4-重复声明（Duplicate-Declaration）"><a href="#4-重复声明（Duplicate-Declaration）" class="headerlink" title="4. 重复声明（Duplicate Declaration）"></a>4. 重复声明（Duplicate Declaration）</h3><ul><li><p><strong><code>var</code><strong>：<br>允许在同一作用域内</strong>重复声明同一变量</strong>，后声明的会覆盖前面的。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> a = <span class="number">10</span>;</span><br><span class="line"><span class="keyword">var</span> a = <span class="number">20</span>; <span class="comment">// 允许，a 最终为 20</span></span><br></pre></td></tr></table></figure></li><li><p><strong><code>let</code> 和 <code>const</code><strong>：<br>不允许在同一作用域内</strong>重复声明同一变量</strong>（包括与 <code>var</code> 声明的变量重名）。  </p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> b = <span class="number">30</span>;</span><br><span class="line"><span class="keyword">let</span> b = <span class="number">40</span>; <span class="comment">// 报错：Identifier &#x27;b&#x27; has already been declared</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">var</span> c = <span class="number">50</span>;</span><br><span class="line"><span class="keyword">let</span> c = <span class="number">60</span>; <span class="comment">// 报错：Identifier &#x27;c&#x27; has already been declared</span></span><br></pre></td></tr></table></figure></li></ul><h3 id="总结对比表"><a href="#总结对比表" class="headerlink" title="总结对比表"></a>总结对比表</h3><table><thead><tr><th>特性</th><th><code>var</code></th><th><code>let</code></th><th><code>const</code></th></tr></thead><tbody><tr><td>作用域</td><td>函数作用域</td><td>块级作用域</td><td>块级作用域</td></tr><tr><td>变量提升</td><td>有（声明提升）</td><td>无（有暂时性死区）</td><td>无（有暂时性死区）</td></tr><tr><td>可重新赋值</td><td>允许</td><td>允许</td><td>不允许（声明时必须赋值）</td></tr><tr><td>重复声明</td><td>允许</td><td>不允许</td><td>不允许</td></tr><tr><td>适用场景</td><td>（不推荐）旧代码兼容</td><td>需修改值的变量</td><td>不修改值的常量、对象等</td></tr></tbody></table><h3 id="最佳实践"><a href="#最佳实践" class="headerlink" title="最佳实践"></a>最佳实践</h3><ul><li>优先使用 <code>const</code>，除非确定变量需要被重新赋值（此时用 <code>let</code>）。  </li><li>避免使用 <code>var</code>，因其作用域和提升特性容易导致意外的 bugs（如变量泄露、重复声明覆盖）。  </li><li>对于对象&#x2F;数组，即使使用 <code>const</code> 声明，也应通过 <code>Object.freeze()</code> 冻结内部属性（如需完全不可变）。</li></ul>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>limit.sync,page.sync 和v-model的区别？</title>
      <link href="/2025/09/17/limit.sync,page.sync-%E5%92%8Cv-model%E7%9A%84%E5%8C%BA%E5%88%AB%EF%BC%9F/"/>
      <url>/2025/09/17/limit.sync,page.sync-%E5%92%8Cv-model%E7%9A%84%E5%8C%BA%E5%88%AB%EF%BC%9F/</url>
      
        <content type="html"><![CDATA[<h2 id="limit-sync-page-sync-和v-model的区别？"><a href="#limit-sync-page-sync-和v-model的区别？" class="headerlink" title=":limit.sync,:page.sync 和v-model的区别？"></a>:limit.sync,:page.sync 和v-model的区别？</h2><p><code>&lt;pagination v-show=&quot;total&gt;0&quot; :total=&quot;total&quot; :page.sync=&quot;current&quot; :limit.sync=&quot;limit&quot; @pagination=&quot;getList&quot; /&gt;</code></p><p>在 Vue 组件中，<code>limit.sync</code> 是 <strong>带 <code>.sync</code> 修饰符的双向绑定语法</strong>，用于实现父子组件间数据的「双向同步」，核心作用是让子组件（这里的 <code>pagination</code> 分页组件）能修改父组件（当前页面）的 <code>limit</code> 变量，同时保持数据响应式。</p><h3 id="1-基础概念拆解"><a href="#1-基础概念拆解" class="headerlink" title="1. 基础概念拆解"></a>1. 基础概念拆解</h3><h4 id="（1）limit-是什么？"><a href="#（1）limit-是什么？" class="headerlink" title="（1）limit 是什么？"></a>（1）<code>limit</code> 是什么？</h4><p>在分页逻辑中，<code>limit</code> 表示「<strong>每页显示的数据条数</strong>」（比如每页显示 10 条、20 条）。<br>在你当前的代码里，父组件的 <code>data</code> 中已定义：</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="title function_">data</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    <span class="attr">limit</span>: <span class="number">10</span>, <span class="comment">// 默认每页显示 10 条数据</span></span><br><span class="line">    <span class="comment">// 其他变量...</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="（2）-sync-修饰符的作用"><a href="#（2）-sync-修饰符的作用" class="headerlink" title="（2）.sync 修饰符的作用"></a>（2）<code>.sync</code> 修饰符的作用</h4><p>Vue 中默认的 props 是「单向数据流」：父组件传值给子组件，子组件不能直接修改父组件的 props（否则会报错，且不符合单向数据流原则）。  </p><p>而 <code>.sync</code> 是 Vue 提供的「语法糖」，本质是简化了「父组件传值 + 子组件触发事件修改父值」的流程，让双向同步更简洁。  </p><p>它的底层逻辑等价于：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- .sync 语法糖 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> <span class="attr">:limit.sync</span>=<span class="string">&quot;limit&quot;</span> /&gt;</span></span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 等价于手动写 props + 事件监听 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> <span class="attr">:limit</span>=<span class="string">&quot;limit&quot;</span> @<span class="attr">update:limit</span>=<span class="string">&quot;newLimit =&gt; limit = newLimit&quot;</span> /&gt;</span></span><br></pre></td></tr></table></figure><h3 id="2-在你的分页组件中如何工作？"><a href="#2-在你的分页组件中如何工作？" class="headerlink" title="2. 在你的分页组件中如何工作？"></a>2. 在你的分页组件中如何工作？</h3><p>假设 <code>pagination</code> 是封装的分页组件（基于 Element UI 的 <code>el-pagination</code>），它的核心逻辑是：</p><ol><li><strong>父组件传值</strong>：通过 <code>:limit.sync=&quot;limit&quot;</code>，将父组件的 <code>limit</code>（初始 10）传给子组件，子组件用 props 接收（比如 <code>props: { limit: { type: Number, default: 10 } }</code>）。  </li><li><strong>用户操作触发修改</strong>：当用户在分页组件中修改「每页条数」（比如从 10 条改成 20 条）时，子组件会触发一个 <code>update:limit</code> 事件，并把新的条数（20）作为参数传递出去。  </li><li><strong>数据双向同步</strong>：由于父组件用了 <code>.sync</code>，会自动监听 <code>update:limit</code> 事件，把父组件的 <code>limit</code> 更新为子组件传递的新值（20）。  </li><li><strong>重新加载数据</strong>：分页组件通常还会触发 <code>@pagination</code> 事件（你代码中的 <code>@pagination=&quot;getList&quot;</code>），此时 <code>getList</code> 方法会用更新后的 <code>current</code>（当前页）和 <code>limit</code>（新的每页条数）重新请求数据，实现「切换每页条数后刷新表格」的效果。</li></ol><h3 id="3-为什么不用普通的单向绑定？"><a href="#3-为什么不用普通的单向绑定？" class="headerlink" title="3. 为什么不用普通的单向绑定？"></a>3. 为什么不用普通的单向绑定？</h3><p>如果不用 <code>.sync</code>，而是用普通的 <code>:limit=&quot;limit&quot;</code>，子组件无法直接修改父组件的 <code>limit</code>，需要手动写事件监听，代码会更繁琐：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 不用 .sync 的写法（等价效果，但代码更长） --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">:limit</span>=<span class="string">&quot;limit&quot;</span> </span></span><br><span class="line"><span class="tag">  @<span class="attr">update:limit</span>=<span class="string">&quot;handleLimitChange&quot;</span>  &lt;!<span class="attr">--</span> <span class="attr">手动监听子组件事件</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">  @pagination=&quot;getList&quot; </span><br><span class="line">/&gt;</span><br><span class="line"></span><br><span class="line"><span class="comment">&lt;!-- 父组件中需要写对应的处理方法 --&gt;</span></span><br><span class="line">methods: &#123;</span><br><span class="line">  handleLimitChange(newLimit) &#123;</span><br><span class="line">    this.limit = newLimit; // 手动更新父组件的 limit</span><br><span class="line">  &#125;,</span><br><span class="line">  getList() &#123;</span><br><span class="line">    // 用更新后的 limit 请求数据</span><br><span class="line">    tenantApi.fetchList(this.current, this.limit, this.listQuery);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>而 <code>.sync</code> 直接简化了这个过程，不用手动写 <code>handleLimitChange</code> 方法，让代码更简洁。</p><h3 id="4-注意事项"><a href="#4-注意事项" class="headerlink" title="4. 注意事项"></a>4. 注意事项</h3><ul><li>子组件必须遵循约定：修改 <code>sync</code> 绑定的属性时，必须触发 <code>update:属性名</code> 格式的事件（比如修改 <code>limit</code> 要触发 <code>update:limit</code>），否则双向同步会失效。  </li><li><code>.sync</code> 不是“双向绑定”的唯一方式：也可以用 <code>v-model</code>（但 <code>v-model</code> 通常用于表单组件，且一个组件只能有一个 <code>v-model</code>），而 <code>.sync</code> 可以用于多个属性的双向绑定（比如分页组件可能同时有 <code>current.sync</code> 和 <code>limit.sync</code>）。</li></ul><p>总结：<code>limit.sync</code> 的核心是「让分页组件能修改父组件的“每页条数”，并自动同步数据」，是实现分页功能中“切换每页显示数量”的常用且简洁的语法。</p><h2 id="为什么分页不用v-mdoel双向绑定数据"><a href="#为什么分页不用v-mdoel双向绑定数据" class="headerlink" title="为什么分页不用v-mdoel双向绑定数据"></a>为什么分页不用v-mdoel双向绑定数据</h2><p>在分页组件场景中不用 <code>v-model</code> 而用 <code>.sync</code>，核心原因是 <strong><code>v-model</code> 的设计限制</strong> 与 <strong>分页组件的功能需求</strong> 不匹配。我们可以从 <code>v-model</code> 的本质、分页组件的多属性需求、语法灵活性三个维度具体分析：</p><h3 id="1-先明确：v-model-是什么？"><a href="#1-先明确：v-model-是什么？" class="headerlink" title="1. 先明确：v-model 是什么？"></a>1. 先明确：<code>v-model</code> 是什么？</h3><p><code>v-model</code> 是 Vue 提供的「双向绑定语法糖」，但它有 <strong>固定的语法规则</strong>，并非万能的双向绑定：  </p><ul><li>本质等价于：<code>v-bind:value=&quot;xxx&quot; + v-on:input=&quot;xxx = $event&quot;</code>（针对输入类组件）。  </li><li>核心限制：一个组件 **只能有一个 <code>v-model</code>**（因为它默认绑定的是 <code>value</code> 属性和 <code>input</code> 事件，多 <code>v-model</code> 会冲突）。</li></ul><h3 id="2-分页组件的需求：需要「多个属性双向同步」"><a href="#2-分页组件的需求：需要「多个属性双向同步」" class="headerlink" title="2. 分页组件的需求：需要「多个属性双向同步」"></a>2. 分页组件的需求：需要「多个属性双向同步」</h3><p>分页组件（如你代码中的 <code>pagination</code>）通常需要同步 <strong>两个核心属性</strong>：  </p><ul><li><code>current</code>：当前页码（比如从第 1 页切换到第 3 页）。  </li><li><code>limit</code>：每页显示条数（比如从 10 条切换到 20 条）。</li></ul><p>如果用 <code>v-model</code>，会遇到两个无法解决的问题：  </p><h4 id="问题1：一个组件只能绑定一个-v-model"><a href="#问题1：一个组件只能绑定一个-v-model" class="headerlink" title="问题1：一个组件只能绑定一个 v-model"></a>问题1：一个组件只能绑定一个 <code>v-model</code></h4><p>假设尝试用 <code>v-model</code> 绑定 <code>current</code>，那 <code>limit</code> 就无法用 <code>v-model</code> 绑定了，只能手动写事件监听，代码会变得混乱：  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 错误示例：无法同时给 current 和 limit 绑定 v-model --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">v-model</span>=<span class="string">&quot;current&quot;</span>  &lt;!<span class="attr">--</span> <span class="attr">绑定当前页</span> <span class="attr">--</span>&gt;</span></span><br><span class="line">  v-model=&quot;limit&quot;   <span class="comment">&lt;!-- 报错！一个组件只能有一个 v-model --&gt;</span></span><br><span class="line">/&gt;</span><br></pre></td></tr></table></figure><h4 id="问题2：v-model-的默认事件-属性不匹配分页逻辑"><a href="#问题2：v-model-的默认事件-属性不匹配分页逻辑" class="headerlink" title="问题2：v-model 的默认事件&#x2F;属性不匹配分页逻辑"></a>问题2：<code>v-model</code> 的默认事件&#x2F;属性不匹配分页逻辑</h4><p><code>v-model</code> 默认监听 <code>input</code> 事件（或组件自定义的 <code>model.event</code>），但分页组件切换「页码」和「每页条数」是两个不同的操作，需要触发不同的逻辑（虽然最终都要调用 <code>getList</code>，但属性修改的源头不同）。<br>如果强行用 <code>v-model</code> 绑定其中一个属性，另一个属性仍需用 <code>.sync</code> 或手动事件，反而不如统一用 <code>.sync</code> 更清晰。</p><h3 id="3-sync-更适合分页场景的核心优势"><a href="#3-sync-更适合分页场景的核心优势" class="headerlink" title="3. .sync 更适合分页场景的核心优势"></a>3. <code>.sync</code> 更适合分页场景的核心优势</h3><p>对比 <code>v-model</code>，<code>.sync</code> 完全契合分页组件的需求，主要有两个优势：  </p><h4 id="优势1：支持「多属性双向同步」"><a href="#优势1：支持「多属性双向同步」" class="headerlink" title="优势1：支持「多属性双向同步」"></a>优势1：支持「多属性双向同步」</h4><p><code>.sync</code> 没有数量限制，只要遵循「子组件触发 <code>update:属性名</code> 事件」的约定，就能同时给多个属性做双向绑定。<br>你代码中正是这么用的：  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- 同时同步 current（当前页）和 limit（每页条数），语法简洁统一 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">:current.sync</span>=<span class="string">&quot;current&quot;</span>  </span></span><br><span class="line"><span class="tag">  <span class="attr">:limit.sync</span>=<span class="string">&quot;limit&quot;</span>       </span></span><br><span class="line"><span class="tag">/&gt;</span></span><br></pre></td></tr></table></figure><h4 id="优势2：语义更清晰，与业务逻辑匹配"><a href="#优势2：语义更清晰，与业务逻辑匹配" class="headerlink" title="优势2：语义更清晰，与业务逻辑匹配"></a>优势2：语义更清晰，与业务逻辑匹配</h4><p><code>.sync</code> 的语义是「<strong>父组件允许子组件修改这个属性</strong>」，而分页组件的 <code>current</code> 和 <code>limit</code> 本质就是「需要子组件（分页器）修改的父组件属性」，语义完全对齐。<br>而 <code>v-model</code> 的语义更偏向「<strong>用户输入驱动的数据绑定</strong>」（如输入框、单选框），用在分页组件上会显得语义不匹配（分页切换不是“输入”，而是“选择操作”）。</p><h3 id="4-特殊情况：Vue-3-的-v-model-支持多绑定，但仍不推荐"><a href="#4-特殊情况：Vue-3-的-v-model-支持多绑定，但仍不推荐" class="headerlink" title="4. 特殊情况：Vue 3 的 v-model 支持多绑定，但仍不推荐"></a>4. 特殊情况：Vue 3 的 <code>v-model</code> 支持多绑定，但仍不推荐</h3><p>Vue 3 对 <code>v-model</code> 做了优化，支持通过「参数」绑定多个属性（如 <code>v-model:current=&quot;current&quot;</code>、<code>v-model:limit=&quot;limit&quot;</code>），语法上可以实现分页需求：  </p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">&lt;!-- Vue 3 示例：多 v-model 绑定 --&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">pagination</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">v-model:current</span>=<span class="string">&quot;current&quot;</span> </span></span><br><span class="line"><span class="tag">  <span class="attr">v-model:limit</span>=<span class="string">&quot;limit&quot;</span> </span></span><br><span class="line"><span class="tag">/&gt;</span></span><br></pre></td></tr></table></figure><p>但即便如此，在分页场景中仍更推荐 <code>.sync</code>，原因是：  </p><ul><li><strong>兼容性</strong>：如果项目是 Vue 2，<code>v-model</code> 不支持多绑定，而 <code>.sync</code> 在 Vue 2&#x2F;3 中语法一致，无需修改。  </li><li><strong>语义一致性</strong>：<code>.sync</code> 明确表示「属性修改的双向同步」，而 <code>v-model</code> 即便支持多绑定，语义上仍更偏向「用户输入」，分页组件用 <code>.sync</code> 更符合开发者的直觉。</li></ul><h3 id="总结：核心区别与选择"><a href="#总结：核心区别与选择" class="headerlink" title="总结：核心区别与选择"></a>总结：核心区别与选择</h3><table><thead><tr><th>维度</th><th><code>v-model</code></th><th><code>.sync</code></th></tr></thead><tbody><tr><td>数量限制</td><td>一个组件只能有一个（Vue 2）</td><td>无限制，可绑定多个属性</td></tr><tr><td>语义</td><td>偏向「用户输入驱动的双向绑定」</td><td>偏向「子组件修改父组件属性的同步」</td></tr><tr><td>适用场景</td><td>输入框、单选框、复选框等表单组件</td><td>分页组件、弹窗组件等多属性同步场景</td></tr></tbody></table><p>正是因为分页组件需要「同步多个属性」且「语义偏向属性修改」，所以不用 <code>v-model</code>，而是选择 <code>.sync</code>。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>不同的插入语句</title>
      <link href="/2025/09/17/%E4%B8%8D%E5%90%8C%E7%9A%84%E6%8F%92%E5%85%A5%E8%AF%AD%E5%8F%A5/"/>
      <url>/2025/09/17/%E4%B8%8D%E5%90%8C%E7%9A%84%E6%8F%92%E5%85%A5%E8%AF%AD%E5%8F%A5/</url>
      
        <content type="html"><![CDATA[<h2 id="不同的插入语句"><a href="#不同的插入语句" class="headerlink" title="不同的插入语句"></a>不同的插入语句</h2><p>insert into  ignore   on duplicate update  replace 三种写法都会导致主键计数器的自增 </p><p>insert into  ignore ：可能当唯一键或者主键一致的时候，会忽略其他字段的变更 </p><p>replace ： 先插入，冲突，再删除后继续插入，会导致原主键变更，第一次冲突有可能被升级系统检测为错误</p><p> on duplicate update：会保证预期的字段都更新 </p><p>因此上述三种写法中，只能使用  on duplicate update</p><table><thead><tr><th>语句</th><th>冲突处理逻辑</th><th>主键是否变更</th><th>适用场景</th></tr></thead><tbody><tr><td>INSERT IGNORE</td><td>忽略插入</td><td>不变（但自增计数器仍增长）</td><td>仅需「存在则跳过」，无需更新</td></tr><tr><td>REPLACE</td><td>删除旧记录 + 插入新记录</td><td>变更（生成新主键）</td><td>需完全替换旧记录，且允许主键变更</td></tr><tr><td>INSERT … ON DUPLICATE KEY UPDATE</td><td>仅更新指定字段</td><td>不变</td><td>需保留旧记录，仅更新部分字段（推荐）</td></tr></tbody></table>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>@Async</title>
      <link href="/2025/09/17/@Async/"/>
      <url>/2025/09/17/@Async/</url>
      
        <content type="html"><![CDATA[<p><code>@EnableAsync</code> 是 Spring 框架中的一个注解，用于开启异步方法的支持。当在配置类上添加该注解后，Spring 会自动扫描并识别带有 <code>@Async</code> 注解的方法，使其能够异步执行（即不在调用线程中同步执行，而是交给其他线程处理）。</p><h3 id="基本使用步骤："><a href="#基本使用步骤：" class="headerlink" title="基本使用步骤："></a>基本使用步骤：</h3><ol><li><p><strong>在配置类上添加 <code>@EnableAsync</code></strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.EnableAsync;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAsync</span> <span class="comment">// 开启异步支持</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncConfig</span> &#123;</span><br><span class="line">    <span class="comment">// 可以在这里配置线程池等</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>在需要异步执行的方法上添加 <code>@Async</code></strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.scheduling.annotation.Async;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncService</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 该方法会异步执行</span></span><br><span class="line">    <span class="meta">@Async</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">asyncMethod</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 耗时操作，如网络请求、文件处理等</span></span><br><span class="line">        System.out.println(<span class="string">&quot;异步方法执行中，线程名：&quot;</span> + Thread.currentThread().getName());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>调用异步方法</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.GetMapping;</span><br><span class="line"><span class="keyword">import</span> org.springframework.web.bind.annotation.RestController;</span><br><span class="line"></span><br><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">TestController</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Autowired</span></span><br><span class="line">    <span class="keyword">private</span> AsyncService asyncService;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@GetMapping(&quot;/test&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> String <span class="title function_">test</span><span class="params">()</span> &#123;</span><br><span class="line">        asyncService.asyncMethod(); <span class="comment">// 调用后立即返回，不会等待方法执行完成</span></span><br><span class="line">        <span class="keyword">return</span> <span class="string">&quot;请求已受理&quot;</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><h3 id="注意事项："><a href="#注意事项：" class="headerlink" title="注意事项："></a>注意事项：</h3><ul><li><code>@Async</code> 注解的方法必须是 <strong>public</strong> 的，且不能在同一个类中调用（否则异步不生效）。</li><li>默认情况下，Spring 使用 <code>SimpleAsyncTaskExecutor</code> 作为线程池，建议自定义线程池以更好地控制线程资源：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncConfig</span> &#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> Executor <span class="title function_">taskExecutor</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">ThreadPoolTaskExecutor</span> <span class="variable">executor</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">ThreadPoolTaskExecutor</span>();</span><br><span class="line">        executor.setCorePoolSize(<span class="number">5</span>); <span class="comment">// 核心线程数</span></span><br><span class="line">        executor.setMaxPoolSize(<span class="number">10</span>); <span class="comment">// 最大线程数</span></span><br><span class="line">        executor.setQueueCapacity(<span class="number">25</span>); <span class="comment">// 队列容量</span></span><br><span class="line">        executor.setThreadNamePrefix(<span class="string">&quot;Async-&quot;</span>); <span class="comment">// 线程名前缀</span></span><br><span class="line">        executor.initialize();</span><br><span class="line">        <span class="keyword">return</span> executor;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li>如果需要获取异步方法的返回值，可以让方法返回 <code>Future&lt;T&gt;</code> 或 <code>CompletableFuture&lt;T&gt;</code> 类型。</li></ul><p>通过 <code>@EnableAsync</code> 和 <code>@Async</code> 的配合，可以很方便地实现方法的异步执行，适用于处理耗时操作而不阻塞主线程的场景。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>多个table标签</title>
      <link href="/2025/09/16/%E5%A4%9A%E4%B8%AAtable%E6%A0%87%E7%AD%BE/"/>
      <url>/2025/09/16/%E5%A4%9A%E4%B8%AAtable%E6%A0%87%E7%AD%BE/</url>
      
        <content type="html"><![CDATA[<h2 id="多个标签"><a href="#多个标签" class="headerlink" title="多个&lt;table&gt;标签"></a>多个<code>&lt;table&gt;</code>标签</h2><p>多个<code>&lt;table&gt;</code>标签并非冗余，而是基于 <strong>“模板分离、状态切换、功能分工”</strong> 的设计思路，分别承担“数据渲染模板”“新增行模板”“视图模式适配”三大核心角色。每个表格的作用和关联逻辑如下，可按“模板类型”分类理解：</p><h3 id="一、核心功能表格：-count-apply-detail（主数据展示表格）"><a href="#一、核心功能表格：-count-apply-detail（主数据展示表格）" class="headerlink" title="一、核心功能表格：#count_apply_detail（主数据展示表格）"></a>一、核心功能表格：<code>#count_apply_detail</code>（主数据展示表格）</h3><p>这是 <strong>唯一用于“实时展示和编辑计数单明细数据”的核心表格</strong>，也是用户直接交互的表格（如查看货品信息、修改采购量）。</p><h4 id="关键属性与作用："><a href="#关键属性与作用：" class="headerlink" title="关键属性与作用："></a>关键属性与作用：</h4><ul><li>**<code>row_template=&quot;#count_apply_detail_temp &gt; tr&quot;</code>**：指定“已存在数据行”的渲染模板（关联<code>#count_apply_detail_temp</code>表格的<code>&lt;tr&gt;</code>），即加载历史数据时，用这个模板生成每行内容。</li><li>**<code>relative_row_template=&quot;#count_apply_detail_new &gt; tr&quot;</code>**：指定“新增数据行”的渲染模板（关联<code>#count_apply_detail_new</code>表格的<code>&lt;tr&gt;</code>），即点击“添加单品”时，用这个模板生成新的可编辑行。</li><li>**<code>.listview</code>&#x2F;<code>fixedrows=&quot;1&quot;</code>&#x2F;<code>fixedlayout</code>**：框架样式&#x2F;功能类，确保表格是“列表视图”、固定表头（第一行为表头不滚动）、固定列宽布局，提升交互体验。</li><li>**<code>&lt;tr .header&gt;</code>**：定义表头（商家编码、货品编号等），仅用于展示列名，不参与数据渲染。</li></ul><h4 id="总结："><a href="#总结：" class="headerlink" title="总结："></a>总结：</h4><p>它是“数据容器”，本身不存储模板结构，而是通过<code>row_template</code>和<code>relative_row_template</code>关联其他模板表格，动态生成数据行。</p><h3 id="二、模板表格1：-count-apply-detail-temp（已存在数据行模板）"><a href="#二、模板表格1：-count-apply-detail-temp（已存在数据行模板）" class="headerlink" title="二、模板表格1：#count_apply_detail_temp（已存在数据行模板）"></a>二、模板表格1：<code>#count_apply_detail_temp</code>（已存在数据行模板）</h3><p>这是 <strong>“加载历史数据时的行模板”</strong>，用于渲染从数据库查询到的已有明细（如编辑已有计数单时，展示已添加的货品）。</p><h4 id="关键设计与作用："><a href="#关键设计与作用：" class="headerlink" title="关键设计与作用："></a>关键设计与作用：</h4><ul><li><strong><code>.temp</code>类</strong>：框架约定的“模板类”，通常在CSS中配置为<code>display: none</code>，仅作为模板被引用，不直接在页面显示。</li><li><strong><code>&lt;tr&gt;</code>内的<code>&lt;td&gt;</code>配置</strong>：<ul><li><code>field=&quot;spec_no&quot;</code>&#x2F;<code>field=&quot;goods_no&quot;</code>：绑定数据字段，即渲染时从后台数据中取<code>spec_no</code>（商家编码）、<code>goods_no</code>（货品编号）等字段值，填充到对应单元格。</li><li><code>.readonly</code>：标记该单元格“只读不可编辑”（如商家编码、货品编号是基础信息，不允许修改）。</li><li><code>not_insert</code>：（框架自定义属性）可能表示“新增行时不显示该字段”或“该字段不参与新增数据的提交”，仅用于展示已有数据。</li><li><code>field=&quot;num&quot;</code>+<code>filter=&quot;.0~9&quot;</code>+<code>v_require</code>：采购量字段，允许编辑（无<code>.readonly</code>），限制输入为数字，且为必填项（<code>v_require</code>）。</li></ul></li></ul><h4 id="总结：-1"><a href="#总结：-1" class="headerlink" title="总结："></a>总结：</h4><p>针对“已有数据”的渲染需求，强调“部分字段只读、采购量可编辑”，适配“编辑历史明细”的场景。</p><h3 id="三、模板表格2：-count-apply-detail-new（新增数据行模板）"><a href="#三、模板表格2：-count-apply-detail-new（新增数据行模板）" class="headerlink" title="三、模板表格2：#count_apply_detail_new（新增数据行模板）"></a>三、模板表格2：<code>#count_apply_detail_new</code>（新增数据行模板）</h3><p>这是 <strong>“点击‘添加单品’时生成新行的模板”</strong>，用于用户新增货品明细（如创建新计数单时，添加需要计数的货品）。</p><h4 id="与-count-apply-detail-temp的核心差异："><a href="#与-count-apply-detail-temp的核心差异：" class="headerlink" title="与#count_apply_detail_temp的核心差异："></a>与<code>#count_apply_detail_temp</code>的核心差异：</h4><ol><li>**<code>&lt;tr edit changed&gt;</code>**：<ul><li><code>edit</code>：标记这是“可编辑行”，框架可能会为该行添加编辑态样式（如边框高亮）。</li><li><code>changed</code>：（框架自定义属性）标记该行“已修改”，用于后续提交时识别哪些新增行需要保存到数据库。</li></ul></li><li><strong><code>not_bind</code>属性</strong>：<ul><li>如<code>field=&quot;num&quot; not_bind</code>&#x2F;<code>edit field=&quot;remark&quot; not_bind</code>：表示“新增时该字段不与后台初始数据绑定”（因为新增行本身没有后台数据），仅作为用户输入的临时容器，提交时再将值传给后台。</li></ul></li><li><strong>字段权限一致</strong>：<ul><li>与<code>#count_apply_detail_temp</code>一样，基础信息（商家编码、货品编号等）仍为<code>.readonly</code>（新增时可能通过“选择单品”自动填充，不允许手动输入），仅采购量和备注可编辑。</li></ul></li></ol><h4 id="总结：-2"><a href="#总结：-2" class="headerlink" title="总结："></a>总结：</h4><p>针对“新增数据”的交互需求，强调“行编辑态标记、临时输入不绑定初始数据”，适配“添加新货品”的场景。</p><h3 id="四、视图切换表格：-count-apply-detail-view-count-apply-detail-temp-view（只读视图模板）"><a href="#四、视图切换表格：-count-apply-detail-view-count-apply-detail-temp-view（只读视图模板）" class="headerlink" title="四、视图切换表格：#count_apply_detail_view + #count_apply_detail_temp_view（只读视图模板）"></a>四、视图切换表格：<code>#count_apply_detail_view</code> + <code>#count_apply_detail_temp_view</code>（只读视图模板）</h3><p>这两组表格是 <strong>“只读视图”的专用模板</strong>，用于“非编辑场景”（如查看已提交&#x2F;已审核的计数单，不允许修改数据），通过<code>body[show_mode]</code>状态控制显示&#x2F;隐藏。</p><h4 id="关键逻辑："><a href="#关键逻辑：" class="headerlink" title="关键逻辑："></a>关键逻辑：</h4><ol><li><p><strong>状态控制（CSS部分）</strong>：</p><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 当body的show_mode为&quot;view&quot;（只读视图）时：</span></span><br><span class="line"><span class="comment">   - 隐藏可编辑的#count_apply_detail表格</span></span><br><span class="line"><span class="comment">   - 显示只读的#count_apply_detail_view表格 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;view&quot;</span>]</span> <span class="selector-id">#count_apply_detail</span> &#123; <span class="attribute">display</span>: none; &#125;</span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;view&quot;</span>]</span> <span class="selector-id">#count_apply_detail_view</span> &#123; <span class="attribute">display</span>: block; &#125;</span><br></pre></td></tr></table></figure><ul><li>其他状态（如<code>edit_view</code>编辑态、<code>submit_view</code>提交后视图）则隐藏<code>#count_apply_detail_view</code>，显示可编辑的<code>#count_apply_detail</code>。</li></ul></li><li><p><strong><code>#count_apply_detail_view</code>与<code>#count_apply_detail</code>的差异</strong>：</p><ul><li>无<code>relative_row_template</code>：只读视图不需要“新增行”，因此不关联新增模板。</li><li>初始为<code>.temp</code>（隐藏）：仅在<code>show_mode=&quot;view&quot;</code>时才显示。</li></ul></li><li><p><strong><code>#count_apply_detail_temp_view</code>（只读行模板）</strong>：</p><ul><li>无<code>.readonly</code>标记，但因为整个表格在只读视图下显示，且无<code>edit</code>态配置，实际效果为“全行列只读”。</li><li>无<code>not_insert</code>&#x2F;<code>not_bind</code>等交互属性：仅用于纯展示，不涉及编辑或提交。</li></ul></li></ol><h4 id="总结：-3"><a href="#总结：-3" class="headerlink" title="总结："></a>总结：</h4><p>针对“只读场景”的展示需求，通过状态切换隐藏编辑功能，仅保留数据展示，适配“查看已完成计数单”的场景。</p><h3 id="五、所有表格的关联逻辑（一句话总结）"><a href="#五、所有表格的关联逻辑（一句话总结）" class="headerlink" title="五、所有表格的关联逻辑（一句话总结）"></a>五、所有表格的关联逻辑（一句话总结）</h3><table><thead><tr><th>表格ID</th><th>类型</th><th>核心作用</th><th>显示时机</th></tr></thead><tbody><tr><td><code>#count_apply_detail</code></td><td>核心数据容器</td><td>展示&#x2F;编辑明细数据，关联其他模板</td><td>编辑态（<code>edit_view</code>）、提交后态</td></tr><tr><td><code>#count_apply_detail_temp</code></td><td>已有数据模板</td><td>渲染历史明细行，部分字段只读</td><td>加载已有数据时（通过<code>row_template</code>）</td></tr><tr><td><code>#count_apply_detail_new</code></td><td>新增数据模板</td><td>生成新增明细行，标记编辑态和临时输入</td><td>点击“添加单品”时（通过<code>relative_row_template</code>）</td></tr><tr><td><code>#count_apply_detail_view</code></td><td>只读数据容器</td><td>展示只读明细数据，无编辑功能</td><td>只读态（<code>show_mode=&quot;view&quot;</code>）</td></tr><tr><td><code>#count_apply_detail_temp_view</code></td><td>只读行模板</td><td>渲染只读明细行，全行列不可编辑</td><td>只读态（通过<code>row_template</code>）</td></tr></tbody></table><h3 id="六、设计思路的优势"><a href="#六、设计思路的优势" class="headerlink" title="六、设计思路的优势"></a>六、设计思路的优势</h3><ol><li><strong>职责分离</strong>：将“容器”“已有数据模板”“新增数据模板”“只读模板”分开，避免单个表格内逻辑混乱，便于维护（如修改新增行样式只需改<code>#count_apply_detail_new</code>）。</li><li><strong>状态适配</strong>：通过<code>show_mode</code>和模板切换，实现“编辑&#x2F;新增&#x2F;只读”多场景的无缝适配，无需动态修改单个表格的属性（如添加&#x2F;移除<code>.readonly</code>）。</li><li><strong>框架兼容</strong>：这类模板分离的设计，通常是适配Htmlayout等桌面端HTML框架的最佳实践，框架可通过<code>row_template</code>等属性高效渲染数据，减少DOM操作开销。</li></ol><p>简单来说：<strong>不是“多个表格重复”，而是“一个核心容器+多个场景化模板”，共同实现计数单明细的完整交互流程</strong>。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Proxy.newProxyInstance</title>
      <link href="/2025/09/16/Proxy.newProxyInstance/"/>
      <url>/2025/09/16/Proxy.newProxyInstance/</url>
      
        <content type="html"><![CDATA[<h2 id="Proxy-newProxyInstance"><a href="#Proxy-newProxyInstance" class="headerlink" title="Proxy.newProxyInstance"></a>Proxy.newProxyInstance</h2><p><code>Proxy.newProxyInstance</code> 是 Java 反射机制中 <strong>动态生成代理对象</strong> 的核心方法，它能在运行时为目标接口创建一个代理类实例，实现对目标对象的方法增强（如添加日志、事务等）。这个方法是 JDK 动态代理的“入口”，底层完全基于反射实现。</p><h3 id="一、方法定义与参数解析"><a href="#一、方法定义与参数解析" class="headerlink" title="一、方法定义与参数解析"></a>一、方法定义与参数解析</h3><p><code>Proxy.newProxyInstance</code> 是 <code>java.lang.reflect.Proxy</code> 类的静态方法，签名如下：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> Object <span class="title function_">newProxyInstance</span><span class="params">(</span></span><br><span class="line"><span class="params">    ClassLoader loader,       // 类加载器</span></span><br><span class="line"><span class="params">    Class&lt;?&gt;[] interfaces,    // 目标对象实现的接口数组</span></span><br><span class="line"><span class="params">    InvocationHandler h       // 调用处理器（代理逻辑）</span></span><br><span class="line"><span class="params">)</span> <span class="keyword">throws</span> IllegalArgumentException</span><br></pre></td></tr></table></figure><h4 id="三个参数的作用："><a href="#三个参数的作用：" class="headerlink" title="三个参数的作用："></a>三个参数的作用：</h4><ol><li><p><strong><code>ClassLoader loader</code></strong>  </p><ul><li>用于加载动态生成的代理类的类加载器。  </li><li>通常传入 <strong>目标对象的类加载器</strong>（如 <code>target.getClass().getClassLoader()</code>），确保代理类与目标类在同一类加载器环境中，避免类加载冲突。</li></ul></li><li><p><strong><code>Class&lt;?&gt;[] interfaces</code></strong>  </p><ul><li>目标对象实现的所有接口的 <code>Class</code> 数组（JDK 动态代理 <strong>只能代理接口</strong>，代理类会实现这些接口）。  </li><li>例如：若目标对象实现了 <code>UserService</code> 接口，则传入 <code>new Class[]{UserService.class}</code>。  </li><li>代理对象会拥有这些接口的所有方法，调用代理对象的方法时，会转发到 <code>InvocationHandler</code> 处理。</li></ul></li><li><p><strong><code>InvocationHandler h</code></strong>  </p><ul><li>代理逻辑的核心处理器，所有对代理对象的方法调用都会被转发到其 <code>invoke</code> 方法。  </li><li>开发者需自定义 <code>InvocationHandler</code> 实现类，在 <code>invoke</code> 中编写增强逻辑（如日志、权限校验）和对目标方法的调用。</li></ul></li></ol><h3 id="二、方法返回值"><a href="#二、方法返回值" class="headerlink" title="二、方法返回值"></a>二、方法返回值</h3><p>返回一个 <strong>动态生成的代理对象</strong>，该对象实现了 <code>interfaces</code> 参数指定的所有接口，因此可以强制转换为目标接口类型（如 <code>UserService proxy = (UserService) newProxyInstance(...)</code>）。</p><h3 id="三、使用流程与示例"><a href="#三、使用流程与示例" class="headerlink" title="三、使用流程与示例"></a>三、使用流程与示例</h3><p>以“为 <code>UserService</code> 接口创建代理，添加日志功能”为例，完整演示 <code>newProxyInstance</code> 的用法：</p><h4 id="1-定义目标接口和实现类"><a href="#1-定义目标接口和实现类" class="headerlink" title="1. 定义目标接口和实现类"></a>1. 定义目标接口和实现类</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 目标接口</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">interface</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(String username)</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 接口实现类（目标对象）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserServiceImpl</span> <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;实际执行：添加用户 &quot;</span> + username);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="2-实现-InvocationHandler（代理逻辑）"><a href="#2-实现-InvocationHandler（代理逻辑）" class="headerlink" title="2. 实现 InvocationHandler（代理逻辑）"></a>2. 实现 <code>InvocationHandler</code>（代理逻辑）</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.InvocationHandler;</span><br><span class="line"><span class="keyword">import</span> java.lang.reflect.Method;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LogHandler</span> <span class="keyword">implements</span> <span class="title class_">InvocationHandler</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Object target; <span class="comment">// 目标对象（被代理的对象）</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">LogHandler</span><span class="params">(Object target)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.target = target;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 所有代理对象的方法调用都会触发此方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">invoke</span><span class="params">(Object proxy, Method method, Object[] args)</span> <span class="keyword">throws</span> Throwable &#123;</span><br><span class="line">        <span class="comment">// 增强逻辑：方法调用前打印日志</span></span><br><span class="line">        System.out.println(<span class="string">&quot;日志：准备调用 &quot;</span> + method.getName() + <span class="string">&quot; 方法，参数：&quot;</span> + args[<span class="number">0</span>]);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 反射调用目标对象的原始方法</span></span><br><span class="line">        <span class="type">Object</span> <span class="variable">result</span> <span class="operator">=</span> method.invoke(target, args);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 增强逻辑：方法调用后打印日志</span></span><br><span class="line">        System.out.println(<span class="string">&quot;日志：&quot;</span> + method.getName() + <span class="string">&quot; 方法调用完成&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> result;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="3-使用-newProxyInstance-生成代理对象并调用"><a href="#3-使用-newProxyInstance-生成代理对象并调用" class="headerlink" title="3. 使用 newProxyInstance 生成代理对象并调用"></a>3. 使用 <code>newProxyInstance</code> 生成代理对象并调用</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.lang.reflect.Proxy;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Main</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        <span class="comment">// 1. 创建目标对象</span></span><br><span class="line">        <span class="type">UserService</span> <span class="variable">target</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">UserServiceImpl</span>();</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 2. 创建调用处理器（传入目标对象）</span></span><br><span class="line">        <span class="type">LogHandler</span> <span class="variable">handler</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">LogHandler</span>(target);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 3. 动态生成代理对象（核心步骤）</span></span><br><span class="line">        <span class="type">UserService</span> <span class="variable">proxy</span> <span class="operator">=</span> (UserService) Proxy.newProxyInstance(</span><br><span class="line">            target.getClass().getClassLoader(), <span class="comment">// 目标对象的类加载器</span></span><br><span class="line">            target.getClass().getInterfaces(),  <span class="comment">// 目标对象实现的接口</span></span><br><span class="line">            handler                             <span class="comment">// 代理逻辑处理器</span></span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 4. 调用代理对象的方法（实际执行 handler.invoke()）</span></span><br><span class="line">        proxy.add(<span class="string">&quot;张三&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-运行结果"><a href="#4-运行结果" class="headerlink" title="4. 运行结果"></a>4. 运行结果</h4><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">日志：准备调用 add 方法，参数：张三</span><br><span class="line">实际执行：添加用户 张三</span><br><span class="line">日志：add 方法调用完成</span><br></pre></td></tr></table></figure><h3 id="四、底层原理：动态生成代理类"><a href="#四、底层原理：动态生成代理类" class="headerlink" title="四、底层原理：动态生成代理类"></a>四、底层原理：动态生成代理类</h3><p><code>newProxyInstance</code> 的核心能力是 <strong>在运行时动态生成代理类的字节码</strong>，并加载为 Class 对象，最终创建实例。整个过程可拆解为 3 步：</p><ol><li><p><strong>生成代理类的字节码</strong><br>根据 <code>interfaces</code> 参数，动态生成一个实现了所有接口的代理类（类名通常为 <code>$ProxyN</code>，如 <code>$Proxy0</code>），该类继承自 <code>Proxy</code> 类。<br>代理类的每个方法都会调用 <code>InvocationHandler</code> 的 <code>invoke</code> 方法，例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 动态生成的代理类伪代码</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">$Proxy0</span> <span class="keyword">extends</span> <span class="title class_">Proxy</span> <span class="keyword">implements</span> <span class="title class_">UserService</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> InvocationHandler h;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> $Proxy0(InvocationHandler h) &#123;</span><br><span class="line">        <span class="built_in">super</span>(h);</span><br><span class="line">        <span class="built_in">this</span>.h = h;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 实现 UserService 的 add 方法</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">add</span><span class="params">(String username)</span> &#123;</span><br><span class="line">        <span class="keyword">try</span> &#123;</span><br><span class="line">            <span class="comment">// 调用 InvocationHandler 的 invoke 方法</span></span><br><span class="line">            <span class="type">Method</span> <span class="variable">method</span> <span class="operator">=</span> UserService.class.getMethod(<span class="string">&quot;add&quot;</span>, String.class);</span><br><span class="line">            h.invoke(<span class="built_in">this</span>, method, <span class="keyword">new</span> <span class="title class_">Object</span>[]&#123;username&#125;);</span><br><span class="line">        &#125; <span class="keyword">catch</span> (Throwable e) &#123;</span><br><span class="line">            <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">RuntimeException</span>(e);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>通过类加载器加载代理类</strong><br>使用 <code>loader</code> 参数指定的类加载器，将动态生成的代理类字节码加载到 JVM 中，生成 <code>Class</code> 对象。</p></li><li><p><strong>创建代理实例</strong><br>调用代理类的构造器（传入 <code>InvocationHandler</code> 实例），创建代理对象并返回。</p></li></ol><h3 id="五、常见问题与注意事项"><a href="#五、常见问题与注意事项" class="headerlink" title="五、常见问题与注意事项"></a>五、常见问题与注意事项</h3><ol><li><p><strong>只能代理接口</strong><br>JDK 动态代理基于接口实现，若目标对象没有实现任何接口，调用 <code>newProxyInstance</code> 会抛出异常。此时需使用 CGLIB 代理（基于继承类实现）。</p></li><li><p><strong>代理对象的类型</strong><br>代理对象是 <code>$ProxyN</code> 类的实例，<strong>不是目标对象的类型</strong>（如 <code>UserServiceImpl</code>），但它实现了目标接口，因此只能强转为接口类型（如 <code>UserService</code>），不能强转为实现类类型。</p></li><li><p><strong>性能开销</strong><br>动态代理通过反射调用方法，性能略低于直接调用（约慢 10-100 倍），但在非性能敏感场景（如框架开发）可忽略。</p></li><li><p><strong><code>invoke</code> 方法中的 <code>proxy</code> 参数</strong><br>该参数是代理对象本身，<strong>不要在 <code>invoke</code> 中调用代理对象的方法</strong>，否则会导致无限递归（如 <code>proxy.add(...)</code> 会再次触发 <code>invoke</code>）。</p></li></ol><h3 id="六、典型应用场景"><a href="#六、典型应用场景" class="headerlink" title="六、典型应用场景"></a>六、典型应用场景</h3><ul><li><strong>Spring AOP</strong>：通过动态代理实现切面逻辑（如事务管理、日志记录）。  </li><li><strong>RPC 框架</strong>：如 Dubbo，通过代理对象屏蔽网络通信细节，让远程调用像本地调用一样简单。  </li><li><strong>权限控制</strong>：在代理中校验用户权限，无权限则拒绝调用目标方法。</li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>Proxy.newProxyInstance</code> 是 JDK 动态代理的核心方法，通过传入类加载器、接口数组和调用处理器，在运行时动态生成代理对象，实现对目标接口方法的增强。其底层基于反射动态生成代理类字节码，是 Java 中“动态编程”和“面向切面编程”的重要基础。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>泛型擦除</title>
      <link href="/2025/09/16/%E6%B3%9B%E5%9E%8B%E6%93%A6%E9%99%A4/"/>
      <url>/2025/09/16/%E6%B3%9B%E5%9E%8B%E6%93%A6%E9%99%A4/</url>
      
        <content type="html"><![CDATA[<h2 id="泛型擦除"><a href="#泛型擦除" class="headerlink" title="泛型擦除"></a>泛型擦除</h2><p>在Java中，<strong>泛型擦除（Type Erasure）</strong> 是编译器实现泛型的核心机制——<strong>在编译阶段将泛型类型参数的信息“擦除”，仅保留原生类型（Raw Type）</strong>，使生成的字节码与“无泛型的JDK 1.5之前的代码”兼容。这意味着：泛型仅在编译时提供类型检查，运行时JVM完全不知道泛型类型参数的存在。</p><h3 id="一、泛型擦除的核心规则"><a href="#一、泛型擦除的核心规则" class="headerlink" title="一、泛型擦除的核心规则"></a>一、泛型擦除的核心规则</h3><p>编译器对泛型的擦除遵循两大规则，直接决定了泛型的运行时行为：</p><h4 id="1-规则1：无约束的泛型参数-→-擦除为-Object"><a href="#1-规则1：无约束的泛型参数-→-擦除为-Object" class="headerlink" title="1. 规则1：无约束的泛型参数 → 擦除为 Object"></a>1. 规则1：无约束的泛型参数 → 擦除为 <code>Object</code></h4><p>若泛型类型参数没有通过 <code>extends</code> 指定上限（如 <code>&lt;T&gt;</code>、<code>&lt;K,V&gt;</code>），编译后会直接替换为 <code>Object</code>。</p><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 泛型类定义</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T value;</span><br><span class="line">    <span class="keyword">public</span> T <span class="title function_">getValue</span><span class="params">()</span> &#123; <span class="keyword">return</span> value; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setValue</span><span class="params">(T value)</span> &#123; <span class="built_in">this</span>.value = value; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>编译后擦除结果</strong>（等效于JDK 1.5前的代码）：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 泛型参数 T 被擦除为 Object</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Object value;</span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">getValue</span><span class="params">()</span> &#123; <span class="keyword">return</span> value; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setValue</span><span class="params">(Object value)</span> &#123; <span class="built_in">this</span>.value = value; &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>编译时：<code>Box&lt;String&gt; box = new Box&lt;&gt;();</code> 会检查 <code>setValue</code> 的参数必须是 <code>String</code>，若传入 <code>Integer</code> 则编译报错；</li><li>运行时：JVM看到的只是 <code>Box</code> 类，<code>value</code> 的类型是 <code>Object</code>，无法区分 <code>Box&lt;String&gt;</code> 和 <code>Box&lt;Integer&gt;</code>。</li></ul><h4 id="2-规则2：有约束的泛型参数-→-擦除为“约束类型的上限”"><a href="#2-规则2：有约束的泛型参数-→-擦除为“约束类型的上限”" class="headerlink" title="2. 规则2：有约束的泛型参数 → 擦除为“约束类型的上限”"></a>2. 规则2：有约束的泛型参数 → 擦除为“约束类型的上限”</h4><p>若泛型参数通过 <code>extends</code> 指定了上限（如 <code>&lt;T extends Number&gt;</code>、<code>&lt;E extends List&gt;</code>），编译后会替换为这个“上限类型”（而非 <code>Object</code>）。</p><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 泛型类定义（T 必须是 Number 或其子类）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NumberBox</span>&lt;T <span class="keyword">extends</span> <span class="title class_">Number</span>&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T number;</span><br><span class="line">    <span class="keyword">public</span> T <span class="title function_">getNumber</span><span class="params">()</span> &#123; <span class="keyword">return</span> number; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setNumber</span><span class="params">(T number)</span> &#123; <span class="built_in">this</span>.number = number; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getDoubleValue</span><span class="params">()</span> &#123; <span class="keyword">return</span> number.doubleValue(); &#125; <span class="comment">// 调用 Number 的方法</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>编译后擦除结果</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 泛型参数 T 被擦除为上限类型 Number</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">NumberBox</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Number number;</span><br><span class="line">    <span class="keyword">public</span> Number <span class="title function_">getNumber</span><span class="params">()</span> &#123; <span class="keyword">return</span> number; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setNumber</span><span class="params">(Number number)</span> &#123; <span class="built_in">this</span>.number = number; &#125;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">double</span> <span class="title function_">getDoubleValue</span><span class="params">()</span> &#123; <span class="keyword">return</span> number.doubleValue(); &#125; <span class="comment">// 无需改变（Number 本身有该方法）</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>关键优势：擦除为上限类型后，可直接调用上限类型的方法（如 <code>doubleValue()</code>），无需强制转型；</li><li>编译时检查：<code>NumberBox&lt;Integer&gt; box = new NumberBox&lt;&gt;();</code> 允许，<code>NumberBox&lt;String&gt; box = new NumberBox&lt;&gt;();</code> 编译报错（<code>String</code> 不是 <code>Number</code> 子类）。</li></ul><h3 id="二、泛型擦除的“副作用”：3个典型问题"><a href="#二、泛型擦除的“副作用”：3个典型问题" class="headerlink" title="二、泛型擦除的“副作用”：3个典型问题"></a>二、泛型擦除的“副作用”：3个典型问题</h3><p>由于擦除后泛型信息丢失，运行时会出现一些“反直觉”的问题，需通过特殊机制规避：</p><h4 id="1-问题1：运行时无法区分泛型类型（如-List-和-List）"><a href="#1-问题1：运行时无法区分泛型类型（如-List-和-List）" class="headerlink" title="1. 问题1：运行时无法区分泛型类型（如 List&lt;String&gt; 和 List&lt;Integer&gt;）"></a>1. 问题1：运行时无法区分泛型类型（如 <code>List&lt;String&gt;</code> 和 <code>List&lt;Integer&gt;</code>）</h4><p>擦除后，所有泛型实例的类型都退化为原生类型（如 <code>List</code>），JVM无法通过 <code>instanceof</code> 判断其泛型参数。</p><p><strong>示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">List&lt;String&gt; strList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">List&lt;Integer&gt; intList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 编译报错：无法通过 instanceof 检查泛型类型（运行时两者都是 List）</span></span><br><span class="line"><span class="keyword">if</span> (strList <span class="keyword">instanceof</span> List&lt;String&gt;) &#123; ... &#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 允许：只能检查原生类型 List</span></span><br><span class="line"><span class="keyword">if</span> (strList <span class="keyword">instanceof</span> List) &#123; ... &#125;</span><br></pre></td></tr></table></figure><ul><li>原因：擦除后 <code>strList</code> 和 <code>intList</code> 的运行时类型都是 <code>ArrayList</code>（原生类型），泛型参数 <code>&lt;String&gt;</code> 和 <code>&lt;Integer&gt;</code> 已不存在；</li><li>结论：运行时无法判断一个集合的泛型具体类型。</li></ul><h4 id="2-问题2：不能创建泛型类型的实例（new-T-报错）"><a href="#2-问题2：不能创建泛型类型的实例（new-T-报错）" class="headerlink" title="2. 问题2：不能创建泛型类型的实例（new T() 报错）"></a>2. 问题2：不能创建泛型类型的实例（<code>new T()</code> 报错）</h4><p>由于擦除后 <code>T</code> 被替换为 <code>Object</code> 或上限类型，编译器无法确定 <code>T</code> 的具体构造方法，因此禁止直接创建泛型参数的实例。</p><p><strong>错误示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Box</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 编译报错：Cannot instantiate the type T</span></span><br><span class="line">        <span class="built_in">this</span>.value = <span class="keyword">new</span> <span class="title class_">T</span>(); </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>解决方案</strong>：通过“类型令牌（Type Token）”或“工厂方法”间接创建实例：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方案1：传入 Class 对象（类型令牌）</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">private</span> T value;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">Box</span><span class="params">(Class&lt;T&gt; clazz)</span> <span class="keyword">throws</span> InstantiationException, IllegalAccessException &#123;</span><br><span class="line">        <span class="built_in">this</span>.value = clazz.newInstance(); <span class="comment">// 通过 Class 对象创建实例</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用：传入具体类型的 Class</span></span><br><span class="line">Box&lt;String&gt; strBox = <span class="keyword">new</span> <span class="title class_">Box</span>&lt;&gt;(String.class);</span><br></pre></td></tr></table></figure><h4 id="3-问题3：泛型数组的创建受限（new-T-报错）"><a href="#3-问题3：泛型数组的创建受限（new-T-报错）" class="headerlink" title="3. 问题3：泛型数组的创建受限（new T[] 报错）"></a>3. 问题3：泛型数组的创建受限（<code>new T[]</code> 报错）</h4><p>与实例创建类似，擦除后无法确定 <code>T</code> 的具体类型，因此禁止直接创建泛型数组（但允许声明泛型数组引用）。</p><p><strong>错误示例</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">createArray</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="comment">// 编译报错：Cannot create a generic array of T</span></span><br><span class="line">        T[] array = <span class="keyword">new</span> <span class="title class_">T</span>[<span class="number">10</span>]; </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>解决方案</strong>：</p><ul><li>方案1：创建 <code>Object[]</code> 数组，强制转型为 <code>T[]</code>（需抑制unchecked警告）；</li><li>方案2：传入数组的 <code>Class</code> 对象，通过 <code>Array.newInstance()</code> 创建。</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 方案2：通过 Array.newInstance() 创建泛型数组</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Box</span>&lt;T&gt; &#123;</span><br><span class="line">    <span class="keyword">public</span> T[] createArray(Class&lt;T&gt; clazz, <span class="type">int</span> size) &#123;</span><br><span class="line">        <span class="comment">// 创建 clazz 类型的数组，长度为 size</span></span><br><span class="line">        <span class="keyword">return</span> (T[]) Array.newInstance(clazz, size);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 使用</span></span><br><span class="line">Box&lt;String&gt; box = <span class="keyword">new</span> <span class="title class_">Box</span>&lt;&gt;();</span><br><span class="line">String[] strArray = box.createArray(String.class, <span class="number">5</span>);</span><br></pre></td></tr></table></figure><h3 id="三、为什么Java要设计“泛型擦除”？"><a href="#三、为什么Java要设计“泛型擦除”？" class="headerlink" title="三、为什么Java要设计“泛型擦除”？"></a>三、为什么Java要设计“泛型擦除”？</h3><p>泛型是JDK 1.5引入的新特性，而擦除机制的核心目的是 <strong>“向前兼容”</strong>——确保JDK 1.5及以后的泛型代码，能与JDK 1.5之前的非泛型代码（如 <code>List</code> 而非 <code>List&lt;String&gt;</code>）无缝协作。</p><ul><li>若采用“运行时保留泛型信息”的设计（如C#的泛型），则JVM需要重构，导致旧代码无法兼容；</li><li>擦除机制让泛型仅在编译时生效，运行时仍使用原生类型，完美兼容旧代码，但也带来了上述“副作用”。</li></ul><h3 id="四、总结"><a href="#四、总结" class="headerlink" title="四、总结"></a>四、总结</h3><p>泛型擦除是Java泛型的“底层实现逻辑”，核心要点可概括为：</p><ol><li><strong>擦除时机</strong>：编译阶段，将泛型参数替换为 <code>Object</code>（无约束）或上限类型（有约束）；</li><li><strong>核心目的</strong>：向前兼容JDK 1.5之前的非泛型代码；</li><li><strong>关键影响</strong>：运行时丢失泛型信息，导致无法区分泛型类型、不能直接创建 <code>T</code> 实例和 <code>T[]</code> 数组；</li><li><strong>规避方案</strong>：通过“类型令牌（Class<T>）”“工厂方法”等机制，间接实现泛型实例&#x2F;数组的创建。</li></ol><p>理解泛型擦除，是解决Java泛型相关问题（如unchecked警告、类型转换异常）的关键。</p><h3 id="与通配符（-）的区别"><a href="#与通配符（-）的区别" class="headerlink" title="与通配符（?）的区别"></a>与通配符（<code>?</code>）的区别</h3><p>方法签名中，<code>&lt;T&gt;</code>（类型参数）和<code>?</code>（通配符）都用于泛型，但用途不同：</p><ul><li>**<code>&lt;T&gt;</code>**：用于声明 “可重用的类型变量”，方法内部可多次使用该类型（如作为返回值、参数、局部变量类型）。</li><li>**<code>?</code>**：用于 “不确定的单个类型”，通常作为参数类型，但方法内部无法直接使用该类型（只能调用<code>Object</code>的方法）。</li></ul><h4 id="示例-4：对比类型参数和通配符"><a href="#示例-4：对比类型参数和通配符" class="headerlink" title="示例 4：对比类型参数和通配符"></a>示例 4：对比类型参数和通配符</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 使用类型参数 &lt;T&gt;：方法内部可返回 T 类型</span></span><br><span class="line"><span class="keyword">public</span> &lt;T&gt; T <span class="title function_">getFirstElement</span><span class="params">(List&lt;T&gt; list)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> list.get(<span class="number">0</span>); <span class="comment">// 正确：返回 T 类型</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 使用通配符 ?：方法内部无法返回具体类型（只能返回 Object）</span></span><br><span class="line"><span class="keyword">public</span> Object <span class="title function_">getFirstElementWithWildcard</span><span class="params">(List&lt;?&gt; list)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> list.get(<span class="number">0</span>); <span class="comment">// 只能返回 Object</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>C#中的ToQueryParam</title>
      <link href="/2025/09/16/C#%E4%B8%AD%E7%9A%84ToQueryParam/"/>
      <url>/2025/09/16/C#%E4%B8%AD%E7%9A%84ToQueryParam/</url>
      
        <content type="html"><![CDATA[<h2 id="C-中的ToQueryParam"><a href="#C-中的ToQueryParam" class="headerlink" title="C#中的ToQueryParam"></a>C#中的ToQueryParam</h2><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">public</span> <span class="keyword">static</span> Dictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt; <span class="title">ToQueryParam</span>(<span class="params"><span class="keyword">this</span> Element self, <span class="built_in">string</span> qpselector = <span class="string">&quot;[qp]&quot;</span></span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    Dictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt; qps = <span class="keyword">new</span> Dictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt;();</span><br><span class="line">    self.ForEach(<span class="built_in">delegate</span>(Element x)</span><br><span class="line">        &#123;</span><br><span class="line">            </span><br><span class="line">            <span class="keyword">var</span> key = x.Attr(<span class="string">&quot;qp&quot;</span>);</span><br><span class="line">            <span class="keyword">if</span> (String.IsNullOrEmpty(key))</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            <span class="keyword">if</span> (x.AttrExists(<span class="string">&quot;local&quot;</span>))</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">            <span class="comment">//默认值不打包</span></span><br><span class="line">            <span class="built_in">string</span> defVal = x.Attr(<span class="string">&quot;defval&quot;</span>);</span><br><span class="line">            <span class="keyword">if</span> (defVal == <span class="literal">null</span>)</span><br><span class="line">            &#123;  </span><br><span class="line">                <span class="keyword">switch</span> (x.CtrlType())</span><br><span class="line">                &#123;</span><br><span class="line">                    <span class="keyword">case</span> CtlType.CTL_DATE:</span><br><span class="line">                        defVal = <span class="string">&quot;0000-00-00&quot;</span>;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    <span class="keyword">case</span> CtlType.CTL_TIME:</span><br><span class="line">                        defVal = <span class="string">&quot;00:00:00&quot;</span>;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                    <span class="literal">default</span>:</span><br><span class="line">                        defVal = <span class="string">&quot;&quot;</span>;</span><br><span class="line">                        <span class="keyword">break</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="built_in">object</span> val = x.Value;</span><br><span class="line">            <span class="built_in">string</span> strVal;</span><br><span class="line">            <span class="keyword">if</span> (val == <span class="literal">null</span>)</span><br><span class="line">                strVal = <span class="string">&quot;&quot;</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">string</span>)</span><br><span class="line">                strVal = (<span class="built_in">string</span>)val;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">bool</span>)</span><br><span class="line">                strVal = (<span class="built_in">bool</span>)val ? <span class="string">&quot;1&quot;</span> : <span class="string">&quot;0&quot;</span>;</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">int</span>[])</span><br><span class="line">                strVal = <span class="built_in">string</span>.Join(<span class="string">&quot;,&quot;</span>, (<span class="built_in">int</span>[])val);</span><br><span class="line">            <span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">object</span>[])</span><br><span class="line">                strVal = <span class="built_in">string</span>.Join(<span class="string">&quot;,&quot;</span>, (<span class="built_in">object</span>[])val);</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">                strVal = Convert.ToString(val);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (strVal == defVal &amp;&amp; !x.AttrExists(<span class="string">&quot;send_qp&quot;</span>))</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">            qps[key] = strVal; </span><br><span class="line"></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">        &#125;, qpselector);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> qps;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="一、方法整体作用与使用场景"><a href="#一、方法整体作用与使用场景" class="headerlink" title="一、方法整体作用与使用场景"></a>一、方法整体作用与使用场景</h3><p>先明确几个关键背景，帮助理解代码设计意图：</p><ul><li><strong>扩展方法</strong>：通过 <code>this Element self</code> 定义，意味着任何 <code>Element</code> 类型的对象（如搜索表单容器、整个页面）都可以直接调用 <code>ToQueryParam()</code>（例：<code>Q(&quot;#search_form&quot;).ToQueryParam()</code>）。</li><li>**<code>qpselector = &quot;[qp]&quot;</code>**：默认筛选“带有 <code>qp</code> 属性的UI组件”——<code>qp</code> 是“Query Parameter”的缩写，在HTML中用于标记“该组件的值需要作为查询参数”（如搜索框 <code>&lt;edit qp=&quot;count_apply_no&quot;&gt;</code>，表示其值对应“申请单号”查询参数）。</li><li>**返回值 <code>Dictionary&lt;string, object&gt;</code>**：键是 <code>qp</code> 属性的值（如 <code>count_apply_no</code>），值是组件的实际输入值（如用户输入的“AP202405001”），可直接传递给数据库查询接口。</li></ul><h3 id="二、逐段解析代码逻辑"><a href="#二、逐段解析代码逻辑" class="headerlink" title="二、逐段解析代码逻辑"></a>二、逐段解析代码逻辑</h3><p>代码按“遍历组件→提取参数名→处理默认值→获取实际值→过滤无效值→添加到字典”的流程执行，每一步都有明确的业务规则：</p><h4 id="1-初始化与遍历组件"><a href="#1-初始化与遍历组件" class="headerlink" title="1. 初始化与遍历组件"></a>1. 初始化与遍历组件</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">Dictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt; qps = <span class="keyword">new</span> Dictionary&lt;<span class="built_in">string</span>, <span class="built_in">object</span>&gt;();</span><br><span class="line">self.ForEach(<span class="built_in">delegate</span>(Element x)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="comment">// 核心逻辑：对每个符合 qpselector 的组件（x）执行操作</span></span><br><span class="line">&#125;, qpselector);</span><br><span class="line"><span class="keyword">return</span> qps;</span><br></pre></td></tr></table></figure><ul><li>**<code>self.ForEach(...)</code>**：遍历当前元素（<code>self</code>，如搜索表单容器）下所有符合 <code>qpselector</code>（默认 <code>[qp]</code>）的子组件（<code>x</code>，如带有 <code>qp</code> 属性的输入框、下拉框）。</li><li>**委托返回 <code>false</code>**：<code>ForEach</code> 是框架遍历方法，返回 <code>false</code> 表示“继续遍历下一个组件”（若返回 <code>true</code> 则终止遍历），这里确保提取所有符合条件的参数。</li></ul><h4 id="2-步骤1：提取参数名（qp-属性）并过滤无效组件"><a href="#2-步骤1：提取参数名（qp-属性）并过滤无效组件" class="headerlink" title="2. 步骤1：提取参数名（qp 属性）并过滤无效组件"></a>2. 步骤1：提取参数名（<code>qp</code> 属性）并过滤无效组件</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> key = x.Attr(<span class="string">&quot;qp&quot;</span>); <span class="comment">// 获取组件的 qp 属性值（如 &quot;count_apply_no&quot;）</span></span><br><span class="line"><span class="keyword">if</span> (String.IsNullOrEmpty(key))</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// 无 qp 属性，跳过该组件</span></span><br><span class="line"><span class="keyword">if</span> (x.AttrExists(<span class="string">&quot;local&quot;</span>))</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// 带有 local 属性的组件，标记为“本地参数”，不参与查询，跳过</span></span><br></pre></td></tr></table></figure><ul><li>**<code>x.Attr(&quot;qp&quot;)</code>**：读取UI组件的 <code>qp</code> 属性，作为最终查询参数的“键”（如HTML中 <code>&lt;edit qp=&quot;goods_no&quot;&gt;</code>，<code>key</code> 就是 <code>goods_no</code>）。</li><li><strong><code>local</code> 属性过滤</strong>：业务中可能存在“仅前端使用、不传给后端”的参数（如前端分页临时变量），通过 <code>local</code> 属性标记，避免误提交。</li></ul><h4 id="3-步骤2：处理“默认值”——决定是否跳过该参数"><a href="#3-步骤2：处理“默认值”——决定是否跳过该参数" class="headerlink" title="3. 步骤2：处理“默认值”——决定是否跳过该参数"></a>3. 步骤2：处理“默认值”——决定是否跳过该参数</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 1. 先从组件属性中获取自定义默认值（如 &lt;widget qp=&quot;status&quot; defval=&quot;-1&quot;&gt;）</span></span><br><span class="line"><span class="built_in">string</span> defVal = x.Attr(<span class="string">&quot;defval&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 2. 若未设置自定义默认值，按组件类型设置框架默认值</span></span><br><span class="line"><span class="keyword">if</span> (defVal == <span class="literal">null</span>)</span><br><span class="line">&#123;  </span><br><span class="line">    <span class="keyword">switch</span> (x.CtrlType()) <span class="comment">// 获取组件类型（日期、时间、普通输入框等）</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">case</span> CtlType.CTL_DATE: <span class="comment">// 日期选择器</span></span><br><span class="line">            defVal = <span class="string">&quot;0000-00-00&quot;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> CtlType.CTL_TIME: <span class="comment">// 时间选择器</span></span><br><span class="line">            defVal = <span class="string">&quot;00:00:00&quot;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="literal">default</span>: <span class="comment">// 其他组件（输入框、下拉框等）</span></span><br><span class="line">            defVal = <span class="string">&quot;&quot;</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><strong>核心目的</strong>：判断“用户是否输入了有效值”——若组件值等于默认值，说明用户未修改该参数（如“申请单状态”默认选“全部”，值为 <code>-1</code>），后续会跳过该参数，避免传递无效的默认值给后端。</li><li><strong>示例</strong>：你之前的搜索表单中，状态下拉框 <code>&lt;widget qp=&quot;status&quot; defval=&quot;-1&quot;&gt;</code>，若用户未选择状态，<code>defVal</code> 就是 <code>-1</code>，后续会跳过该参数，后端查询时不筛选状态。</li></ul><h4 id="4-步骤3：获取组件的“实际输入值”并统一格式"><a href="#4-步骤3：获取组件的“实际输入值”并统一格式" class="headerlink" title="4. 步骤3：获取组件的“实际输入值”并统一格式"></a>4. 步骤3：获取组件的“实际输入值”并统一格式</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">object</span> val = x.Value; <span class="comment">// 获取组件当前值（如用户输入的文本、选择的下拉框值）</span></span><br><span class="line"><span class="built_in">string</span> strVal;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 按值的类型，统一转换为字符串格式（后端查询通常需要字符串参数）</span></span><br><span class="line"><span class="keyword">if</span> (val == <span class="literal">null</span>)</span><br><span class="line">    strVal = <span class="string">&quot;&quot;</span>;</span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">string</span>)</span><br><span class="line">    strVal = (<span class="built_in">string</span>)val; <span class="comment">// 字符串直接使用</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">bool</span>)</span><br><span class="line">    strVal = (<span class="built_in">bool</span>)val ? <span class="string">&quot;1&quot;</span> : <span class="string">&quot;0&quot;</span>; <span class="comment">// 布尔值转“1/0”（如复选框选中为1，未选为0）</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">int</span>[])</span><br><span class="line">    strVal = <span class="built_in">string</span>.Join(<span class="string">&quot;,&quot;</span>, (<span class="built_in">int</span>[])val); <span class="comment">// 整数数组转“逗号分隔符”（如多选下拉框选中多个ID：1,2,3）</span></span><br><span class="line"><span class="keyword">else</span> <span class="keyword">if</span> (val <span class="keyword">is</span> <span class="built_in">object</span>[])</span><br><span class="line">    strVal = <span class="built_in">string</span>.Join(<span class="string">&quot;,&quot;</span>, (<span class="built_in">object</span>[])val); <span class="comment">// 其他数组类型同理</span></span><br><span class="line"><span class="keyword">else</span></span><br><span class="line">    strVal = Convert.ToString(val); <span class="comment">// 其他类型（如数字）直接转字符串</span></span><br></pre></td></tr></table></figure><ul><li><strong>关键作用</strong>：统一参数格式——无论组件是日期、多选框还是普通输入框，最终都转为字符串，避免后端处理不同类型参数的麻烦。</li><li><strong>示例</strong>：多选仓库的下拉框，用户选中ID为 <code>1</code> 和 <code>3</code> 的仓库，<code>val</code> 是 <code>int[] {1,3}</code>，转换后 <code>strVal</code> 为 <code>&quot;1,3&quot;</code>，后端可通过 <code>IN (1,3)</code> 筛选仓库。</li></ul><h4 id="5-步骤4：过滤“默认值参数”并添加到字典"><a href="#5-步骤4：过滤“默认值参数”并添加到字典" class="headerlink" title="5. 步骤4：过滤“默认值参数”并添加到字典"></a>5. 步骤4：过滤“默认值参数”并添加到字典</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 若组件值等于默认值，且无 send_qp 属性，跳过该参数（不添加到字典）</span></span><br><span class="line"><span class="keyword">if</span> (strVal == defVal &amp;&amp; !x.AttrExists(<span class="string">&quot;send_qp&quot;</span>))</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 若通过过滤，将参数添加到字典（key是qp属性值，value是处理后的实际值）</span></span><br><span class="line">qps[key] = strVal; </span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> <span class="literal">false</span>; <span class="comment">// 继续遍历下一个组件</span></span><br></pre></td></tr></table></figure><ul><li><strong>核心过滤规则</strong>：仅传递“用户修改过的参数”——若用户未修改（值等于默认值），且组件无 <code>send_qp</code> 属性，就不添加到字典，减少后端查询的无效参数。</li><li><strong><code>send_qp</code> 属性例外</strong>：若组件需要“即使是默认值也要传递”（如特殊业务场景），可添加 <code>send_qp</code> 属性（如 <code>&lt;edit qp=&quot;keyword&quot; defval=&quot;&quot; send_qp&gt;</code>），强制保留该参数。</li></ul><h3 id="三、实际使用示例（结合你之前的搜索功能）"><a href="#三、实际使用示例（结合你之前的搜索功能）" class="headerlink" title="三、实际使用示例（结合你之前的搜索功能）"></a>三、实际使用示例（结合你之前的搜索功能）</h3><p>以你之前计数申请单的搜索表单为例，HTML结构如下：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">.flat_bar</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 状态下拉框：qp=&quot;status&quot;，defval=&quot;-1&quot;（默认“全部”） --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">widget</span> <span class="attr">qp</span>=<span class="string">&quot;status&quot;</span> <span class="attr">defval</span>=<span class="string">&quot;-1&quot;</span>&gt;</span>...<span class="tag">&lt;/<span class="name">widget</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 申请单号输入框：qp=&quot;count_apply_no&quot;，defval=&quot;&quot; --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">edit</span> <span class="attr">qp</span>=<span class="string">&quot;count_apply_no&quot;</span> <span class="attr">defval</span>=<span class="string">&quot;&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">edit</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- 建单日期：qp=&quot;create_begin&quot;，日期类型，默认“0000-00-00” --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">widget</span> <span class="attr">qp</span>=<span class="string">&quot;create_begin&quot;</span> <span class="attr">type</span>=<span class="string">&quot;date&quot;</span>&gt;</span><span class="tag">&lt;/<span class="name">widget</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure><p>当用户：</p><ol><li>未修改“状态”（值为 <code>-1</code>，等于 <code>defval=&quot;-1&quot;</code>）；</li><li>输入申请单号“AP202405001”（值不等于 <code>defval=&quot;&quot;</code>）；</li><li>未选择“建单日期”（值为 <code>0000-00-00</code>，等于默认值）。</li></ol><p>调用 <code>Q(&quot;.flat_bar&quot;).ToQueryParam()</code> 后，返回的字典为：</p><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;count_apply_no&quot;</span>: <span class="string">&quot;AP202405001&quot;</span> <span class="comment">// 仅用户输入的参数被保留</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>该字典可直接传递给后端查询接口（如 <code>count.CountApply.query</code>），后端仅按“申请单号”筛选数据，符合用户实际搜索意图。</p><h3 id="四、核心设计亮点"><a href="#四、核心设计亮点" class="headerlink" title="四、核心设计亮点"></a>四、核心设计亮点</h3><ol><li><strong>自动化提取</strong>：无需手动逐个获取组件值，通过 <code>qp</code> 属性标记即可自动遍历，减少重复代码；</li><li><strong>默认值过滤</strong>：仅传递有效参数，减轻后端查询压力，避免“全量默认值”导致的性能问题；</li><li><strong>格式统一</strong>：自动处理布尔值、数组等特殊类型，确保后端接收的参数格式一致；</li><li><strong>灵活性</strong>：通过 <code>local</code>（本地参数不传递）、<code>send_qp</code>（强制传递默认值）属性，适配不同业务场景。</li></ol>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>CSS属性选择器实现界面模式切换 学习笔记</title>
      <link href="/2025/09/12/CSS%E5%B1%9E%E6%80%A7%E9%80%89%E6%8B%A9%E5%99%A8%E5%AE%9E%E7%8E%B0%E7%95%8C%E9%9D%A2%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/"/>
      <url>/2025/09/12/CSS%E5%B1%9E%E6%80%A7%E9%80%89%E6%8B%A9%E5%99%A8%E5%AE%9E%E7%8E%B0%E7%95%8C%E9%9D%A2%E6%A8%A1%E5%BC%8F%E5%88%87%E6%8D%A2-%E5%AD%A6%E4%B9%A0%E7%AC%94%E8%AE%B0/</url>
      
        <content type="html"><![CDATA[<h2 id="CSS属性选择器实现界面模式切换-学习笔记"><a href="#CSS属性选择器实现界面模式切换-学习笔记" class="headerlink" title="CSS属性选择器实现界面模式切换 学习笔记"></a>CSS属性选择器实现界面模式切换 学习笔记</h2><h3 id="1-基本概念"><a href="#1-基本概念" class="headerlink" title="1. 基本概念"></a>1. 基本概念</h3><p>CSS属性选择器是一种通过HTML元素的属性和属性值来选择元素的方法。在界面模式切换中，通过改变元素的属性值来触发不同的样式规则。</p><h3 id="2-实现原理"><a href="#2-实现原理" class="headerlink" title="2. 实现原理"></a>2. 实现原理</h3><h4 id="2-1-设置属性"><a href="#2-1-设置属性" class="headerlink" title="2.1 设置属性"></a>2.1 设置属性</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;edit_view&quot;</span>);</span><br></pre></td></tr></table></figure><h4 id="2-2-CSS规则匹配"><a href="#2-2-CSS规则匹配" class="headerlink" title="2.2 CSS规则匹配"></a>2.2 CSS规则匹配</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;edit_view&quot;</span>]</span> <span class="selector-class">.select</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>当body元素的show_mode属性值为”edit_view”时，所有带有”select”类的子元素都会被隐藏。</p><h3 id="3-在项目中的应用模式"><a href="#3-在项目中的应用模式" class="headerlink" title="3. 在项目中的应用模式"></a>3. 在项目中的应用模式</h3><h4 id="3-1-多种界面模式"><a href="#3-1-多种界面模式" class="headerlink" title="3.1 多种界面模式"></a>3.1 多种界面模式</h4><p>项目中定义了三种模式：</p><ul><li><code>edit_view</code>: 编辑模式</li><li><code>submit_view</code>: 提交模式</li><li><a href="file://D:\study\dev\EShopMaster\csharp\WdtERP.Control\goods\GoodsArchiveWindow.cs#L124-L124">view</a>: 查看模式</li></ul><h4 id="3-2-不同模式下的UI控制"><a href="#3-2-不同模式下的UI控制" class="headerlink" title="3.2 不同模式下的UI控制"></a>3.2 不同模式下的UI控制</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 编辑模式</span></span><br><span class="line">Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;edit_view&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 提交模式</span></span><br><span class="line">Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;submit_view&quot;</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// 查看模式</span></span><br><span class="line">Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;view&quot;</span>);</span><br></pre></td></tr></table></figure><h3 id="4-典型应用场景"><a href="#4-典型应用场景" class="headerlink" title="4. 典型应用场景"></a>4. 典型应用场景</h3><h4 id="4-1-按钮显示控制"><a href="#4-1-按钮显示控制" class="headerlink" title="4.1 按钮显示控制"></a>4.1 按钮显示控制</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 编辑模式下隐藏选择按钮 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;edit_view&quot;</span>]</span> <span class="selector-class">.select</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 提交模式下禁用某些输入框 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;submit_view&quot;</span>]</span> <span class="selector-tag">input</span> &#123;</span><br><span class="line">    <span class="attribute">pointer-events</span>: none;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-2-表格行状态控制"><a href="#4-2-表格行状态控制" class="headerlink" title="4.2 表格行状态控制"></a>4.2 表格行状态控制</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 查看模式下禁用所有表格元素 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;view&quot;</span>]</span> <span class="selector-id">#count_apply_detail</span> &gt; <span class="selector-tag">tr</span> &gt; <span class="selector-tag">td</span> &gt; widget &#123;</span><br><span class="line">    <span class="attribute">pointer-events</span>: none;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="5-优势与特点"><a href="#5-优势与特点" class="headerlink" title="5. 优势与特点"></a>5. 优势与特点</h3><h4 id="5-1-优势"><a href="#5-1-优势" class="headerlink" title="5.1 优势"></a>5.1 优势</h4><ul><li><strong>集中控制</strong>: 通过一个属性控制整个界面的显示状态</li><li><strong>易于维护</strong>: 样式规则集中定义，便于管理</li><li><strong>灵活性强</strong>: 可以轻松添加新的模式和对应的样式规则</li><li><strong>性能良好</strong>: 避免了大量的DOM操作</li></ul><h4 id="5-2-实现要点"><a href="#5-2-实现要点" class="headerlink" title="5.2 实现要点"></a>5.2 实现要点</h4><ol><li>在代码中适时设置模式属性</li><li>编写对应的CSS规则</li><li>确保各种模式间的切换逻辑清晰</li></ol><h3 id="6-实际代码示例"><a href="#6-实际代码示例" class="headerlink" title="6. 实际代码示例"></a>6. 实际代码示例</h3><h4 id="6-1-C-代码设置模式"><a href="#6-1-C-代码设置模式" class="headerlink" title="6.1 C#代码设置模式"></a>6.1 C#代码设置模式</h4><figure class="highlight csharp"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 根据不同条件设置不同的显示模式</span></span><br><span class="line"><span class="keyword">if</span>(status == <span class="number">10</span>) &#123;</span><br><span class="line">    Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;edit_view&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> <span class="keyword">if</span>(status == <span class="number">20</span>) &#123;</span><br><span class="line">    Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;submit_view&quot;</span>);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    Q(<span class="string">&quot;body&quot;</span>).SetAttr(<span class="string">&quot;show_mode&quot;</span>, <span class="string">&quot;view&quot;</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="6-2-对应CSS规则"><a href="#6-2-对应CSS规则" class="headerlink" title="6.2 对应CSS规则"></a>6.2 对应CSS规则</h4><figure class="highlight css"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">/* 编辑模式样式 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;edit_view&quot;</span>]</span> <span class="selector-class">.readonly-field</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 提交模式样式 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;submit_view&quot;</span>]</span> <span class="selector-class">.edit-button</span> &#123;</span><br><span class="line">    <span class="attribute">display</span>: none;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 查看模式样式 */</span></span><br><span class="line"><span class="selector-tag">body</span><span class="selector-attr">[show_mode=<span class="string">&quot;view&quot;</span>]</span> <span class="selector-class">.input-field</span> &#123;</span><br><span class="line">    <span class="attribute">border</span>: none;</span><br><span class="line">    <span class="attribute">background</span>: transparent;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="7-最佳实践建议"><a href="#7-最佳实践建议" class="headerlink" title="7. 最佳实践建议"></a>7. 最佳实践建议</h3><ol><li><strong>统一命名</strong>: 使用一致的属性名和值命名规范</li><li><strong>文档注释</strong>: 在CSS中添加注释说明每种模式的用途</li><li><strong>模式管理</strong>: 在代码中集中管理所有可能的模式值</li><li><strong>状态同步</strong>: 确保界面状态与数据状态保持一致</li></ol><p>这种方法是现代Web应用中实现界面状态管理的常用模式，通过属性选择器可以实现简洁而强大的界面控制逻辑。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>demo1表设计</title>
      <link href="/2025/09/02/demo1%E8%A1%A8%E8%AE%BE%E8%AE%A1/"/>
      <url>/2025/09/02/demo1%E8%A1%A8%E8%AE%BE%E8%AE%A1/</url>
      
        <content type="html"><![CDATA[<figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> `purchase_apply`;</span><br><span class="line"><span class="keyword">CREATE TABLE</span> `purchase_apply` (</span><br><span class="line">  `rec_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> AUTO_INCREMENT,</span><br><span class="line">  `purchase_apply_no` <span class="type">varchar</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;采购申请单编号&#x27;</span>,</span><br><span class="line">  `status` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;10&#x27;</span> COMMENT <span class="string">&#x27;5 已取消 10 编辑中  20 已提交  25部分引用  30 已引用&#x27;</span>,</span><br><span class="line">  `creator_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">  `warehouse_id` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span>,</span><br><span class="line">  `expected_time` datetime <span class="keyword">DEFAULT</span> <span class="keyword">NULL</span> COMMENT <span class="string">&#x27;期望到货时间&#x27;</span>,</span><br><span class="line">  `remark` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  `version_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;版本号&#x27;</span>,</span><br><span class="line">  `modified` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  `created` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`rec_id`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;采购申请单&#x27;</span>;</span><br></pre></td></tr></table></figure><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line">DROP TABLE IF EXISTS `purchase_apply_detail`;</span><br><span class="line">CREATE TABLE `purchase_apply_detail` (</span><br><span class="line">  `rec_id` int(11) NOT NULL AUTO_INCREMENT,</span><br><span class="line">  `apply_id` int(11) NOT NULL,</span><br><span class="line">  `spec_id` int(11) NOT NULL,</span><br><span class="line">  `num` decimal(19,4) NOT NULL,</span><br><span class="line">  `recommand_num` decimal(19,4) NOT NULL DEFAULT &#x27;0.0000&#x27; COMMENT &#x27;理论数量、推荐数量&#x27;,</span><br><span class="line">  `real_num` decimal(19,4) NOT NULL DEFAULT &#x27;0.0000&#x27; COMMENT &#x27;实际数量&#x27;,</span><br><span class="line">  `ref_num` decimal(19,4) NOT NULL DEFAULT &#x27;0.0000&#x27; COMMENT &#x27;已经引用的的数量&#x27;,</span><br><span class="line">  `unit` int(11) NOT NULL DEFAULT &#x27;0&#x27;,</span><br><span class="line">  `remark` varchar(255) NOT NULL DEFAULT &#x27;&#x27;,</span><br><span class="line">  `modified` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,</span><br><span class="line">  `created` timestamp NOT NULL DEFAULT CURRENT_TIMESTAMP,</span><br><span class="line">  PRIMARY KEY (`rec_id`),</span><br><span class="line">  UNIQUE KEY `U_PURCHASE_APPLY_DETAIL_APID_SPID` (`apply_id`,`spec_id`),</span><br><span class="line">  KEY `FK_purchase_apply_detail_apply_id` (`apply_id`),</span><br><span class="line">  CONSTRAINT `FK_purchase_apply_detail_apply_id` FOREIGN KEY (`apply_id`) REFERENCES `purchase_apply` (`rec_id`)</span><br><span class="line">) ENGINE=InnoDB DEFAULT CHARSET=utf8 COMMENT=&#x27;采购申请单&#x27;;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> `goods_spec`;</span><br><span class="line"><span class="keyword">CREATE TABLE</span> `goods_spec` (</span><br><span class="line">  `spec_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> AUTO_INCREMENT,</span><br><span class="line">  `goods_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">  `spec_no` <span class="type">varchar</span>(<span class="number">40</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;商家编码，可以将货品的goods_no和spec_code拼在一起,由程序自动管理&#x27;</span>,</span><br><span class="line">  `spec_code` <span class="type">varchar</span>(<span class="number">40</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;规格码&#x27;</span>,</span><br><span class="line">  `provider_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;主供应商，注意：修改的时候要和purchase_provider_goods表连动&#x27;</span>,</span><br><span class="line">  `barcode` <span class="type">varchar</span>(<span class="number">50</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;主条码，注意：修改的时候要和goods_barcode表连动&#x27;</span>,</span><br><span class="line">  `spec_name` <span class="type">varchar</span>(<span class="number">100</span>) <span class="keyword">NOT NULL</span>,</span><br><span class="line">  `wms_process_mask` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;仓库流程控制掩码:1.允许负库存出库 2.无需验货 4.使用同批 8.需要质检 16.无需拣货&#x27;</span>,</span><br><span class="line">  `is_not_need_examine` tinyint(<span class="number">1</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;不需要验货&#x27;</span>,</span><br><span class="line">  `goods_label` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;货品标签 mask 自定义属性6个,1,航空禁运 2,陆路禁运 4,自定义3 8,自定义4 16,自定义5 32,自定义6&#x27;</span>,</span><br><span class="line">  `sn_type` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;0不启用序列号 1强序列号 2弱序列号&#x27;</span>,</span><br><span class="line">  `is_single_batch` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;0,不启用同一批次，1,使用同一批次&#x27;</span>,</span><br><span class="line">  `lowest_price` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;最低&#x27;</span>,</span><br><span class="line">  `retail_price` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;零售价&#x27;</span>,</span><br><span class="line">  `wholesale_price` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;批发价&#x27;</span>,</span><br><span class="line">  `member_price` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;会员价&#x27;</span>,</span><br><span class="line">  `market_price` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;市场价&#x27;</span>,</span><br><span class="line">  `custom_price1` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;自定义价格1&#x27;</span>,</span><br><span class="line">  `custom_price2` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;自定义价格2&#x27;</span>,</span><br><span class="line">  `validity_days` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;保质期，天数&#x27;</span>,</span><br><span class="line">  `sales_days` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;最佳销售天数&#x27;</span>,</span><br><span class="line">  `receive_days` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;最佳收货天数&#x27;</span>,</span><br><span class="line">  `weight` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;重量&#x27;</span>,</span><br><span class="line">  `length` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span>,</span><br><span class="line">  `width` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span>,</span><br><span class="line">  `height` <span class="type">decimal</span>(<span class="number">19</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span>,</span><br><span class="line">  `washing_label` <span class="type">varchar</span>(<span class="number">40</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;水洗标&#x27;</span>,</span><br><span class="line">  `tax_rate` <span class="type">decimal</span>(<span class="number">8</span>,<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0.0000&#x27;</span> COMMENT <span class="string">&#x27;税率&#x27;</span>,</span><br><span class="line">  `tax_code_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;税务编码id&#x27;</span>,</span><br><span class="line">  `large_type` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;0非大件1普通大件2独立大件（不可和小件一起发）-1非单发件&#x27;</span>,</span><br><span class="line">  `unit` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;基本单位&#x27;</span>,</span><br><span class="line">  `aux_unit` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;辅助单位&#x27;</span>,</span><br><span class="line">  `prop1` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性1&#x27;</span>,</span><br><span class="line">  `prop2` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性2&#x27;</span>,</span><br><span class="line">  `prop3` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性3&#x27;</span>,</span><br><span class="line">  `prop4` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性4&#x27;</span>,</span><br><span class="line">  `prop5` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性5&#x27;</span>,</span><br><span class="line">  `prop6` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性6&#x27;</span>,</span><br><span class="line">  `flag_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;标记&#x27;</span>,</span><br><span class="line">  `is_lower_cost` tinyint(<span class="number">1</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;是否允许低于成本价&#x27;</span>,</span><br><span class="line">  `img_url` <span class="type">varchar</span>(<span class="number">1024</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;图片URL&#x27;</span>,</span><br><span class="line">  `img_key` <span class="type">varchar</span>(<span class="number">100</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;看图片在外部空间的key  比如说 云盘的一个 外链&#x27;</span>,</span><br><span class="line">  `barcode_count` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;条码个数&#x27;</span>,</span><br><span class="line">  `plat_spec_count` <span class="type">smallint</span>(<span class="number">6</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;平台货品数量(不包含删除的)&#x27;</span>,</span><br><span class="line">  `is_need_inspect` tinyint(<span class="number">1</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;需要质检&#x27;</span>,</span><br><span class="line">  `remark` <span class="type">varchar</span>(<span class="number">512</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;备注&#x27;</span>,</span><br><span class="line">  `deleted` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;删除时间戳&#x27;</span>,</span><br><span class="line">  `modified` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  `created` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`spec_id`),</span><br><span class="line">  <span class="keyword">UNIQUE</span> KEY `UX_goods_spec_no` (`spec_no`,`deleted`),</span><br><span class="line">  KEY `FK_goods_spec_goods_id` (`goods_id`),</span><br><span class="line">  KEY `IX_goods_spec_barcode` (`barcode`),</span><br><span class="line">  KEY `IX_goods_spec_deleted` (`deleted`),</span><br><span class="line">  KEY `IX_goods_spec_modified` (`modified`),</span><br><span class="line">  <span class="keyword">CONSTRAINT</span> `FK_goods_spec_goods_id` <span class="keyword">FOREIGN KEY</span> (`goods_id`) <span class="keyword">REFERENCES</span> `goods_goods` (`goods_id`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;货品规格&#x27;</span>;</span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">DROP</span> <span class="keyword">TABLE</span> IF <span class="keyword">EXISTS</span> `goods_goods`;</span><br><span class="line"><span class="keyword">CREATE TABLE</span> `goods_goods` (</span><br><span class="line">  `goods_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> AUTO_INCREMENT,</span><br><span class="line">  `goods_type` tinyint(<span class="number">4</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;1销售商品 2原材料 3包装 4周转材料5虚拟商品6固定资产 0其它&#x27;</span>,</span><br><span class="line">  `goods_no` <span class="type">varchar</span>(<span class="number">40</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;货品编码，必须唯一，可以系统生成&#x27;</span>,</span><br><span class="line">  `goods_name` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> COMMENT <span class="string">&#x27;货品名称&#x27;</span>,</span><br><span class="line">  `short_name` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;简称&#x27;</span>,</span><br><span class="line">  `alias` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;别名&#x27;</span>,</span><br><span class="line">  `spec_count` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;多规格个数&#x27;</span>,</span><br><span class="line">  `class_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;分类id,0表示无分类&#x27;</span>,</span><br><span class="line">  `brand_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;品牌ID&#x27;</span>,</span><br><span class="line">  `unit` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;基本单位&#x27;</span>,</span><br><span class="line">  `aux_unit` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;辅助单位&#x27;</span>,</span><br><span class="line">  `pinyin` <span class="type">varchar</span>(<span class="number">40</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  `origin` <span class="type">varchar</span>(<span class="number">64</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;产地&#x27;</span>,</span><br><span class="line">  `remark` <span class="type">varchar</span>(<span class="number">512</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  `flag_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span>,</span><br><span class="line">  `properties` <span class="type">varchar</span>(<span class="number">1024</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0,0,0,0,0,0&#x27;</span>,</span><br><span class="line">  `washing_label` <span class="type">varchar</span>(<span class="number">20</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span>,</span><br><span class="line">  `category_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;系统商品类目id&#x27;</span>,</span><br><span class="line">  `prop1` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性1&#x27;</span>,</span><br><span class="line">  `prop2` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性2&#x27;</span>,</span><br><span class="line">  `prop3` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性3&#x27;</span>,</span><br><span class="line">  `prop4` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性4&#x27;</span>,</span><br><span class="line">  `prop5` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性5&#x27;</span>,</span><br><span class="line">  `prop6` <span class="type">varchar</span>(<span class="number">255</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;&#x27;</span> COMMENT <span class="string">&#x27;自定义属性6&#x27;</span>,</span><br><span class="line">  `deleted` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;删除时间&#x27;</span>,</span><br><span class="line">  `version_id` <span class="type">int</span>(<span class="number">11</span>) <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="string">&#x27;0&#x27;</span> COMMENT <span class="string">&#x27;版本号，用来检查同时修改的&#x27;</span>,</span><br><span class="line">  `modified` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span> <span class="keyword">ON</span> <span class="keyword">UPDATE</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  `created` <span class="type">timestamp</span> <span class="keyword">NOT NULL</span> <span class="keyword">DEFAULT</span> <span class="built_in">CURRENT_TIMESTAMP</span>,</span><br><span class="line">  <span class="keyword">PRIMARY KEY</span> (`goods_id`),</span><br><span class="line">  <span class="keyword">UNIQUE</span> KEY `UK_goods_goods_no` (`goods_no`,`deleted`),</span><br><span class="line">  KEY `IX_goods_goods_deleted` (`deleted`),</span><br><span class="line">  KEY `FK_goods_goods_class_id` (`class_id`),</span><br><span class="line">  KEY `FK_goods_goods_brand_id` (`brand_id`),</span><br><span class="line">  KEY `IX_goods_goods_type` (`goods_type`),</span><br><span class="line">  KEY `IX_goods_goods_modified` (`modified`),</span><br><span class="line">  KEY `IX_goods_goods_catid`(`category_id`),</span><br><span class="line">  <span class="keyword">CONSTRAINT</span> `FK_goods_goods_brand_id` <span class="keyword">FOREIGN KEY</span> (`brand_id`) <span class="keyword">REFERENCES</span> `goods_brand` (`brand_id`),</span><br><span class="line">  <span class="keyword">CONSTRAINT</span> `FK_goods_goods_class_id` <span class="keyword">FOREIGN KEY</span> (`class_id`) <span class="keyword">REFERENCES</span> `goods_class` (`class_id`)</span><br><span class="line">) ENGINE<span class="operator">=</span>InnoDB <span class="keyword">DEFAULT</span> CHARSET<span class="operator">=</span>utf8 COMMENT<span class="operator">=</span><span class="string">&#x27;货品表&#x27;</span>;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>C#与java不同的地方</title>
      <link href="/2025/09/02/C#%E4%B8%8Ejava%E4%B8%8D%E5%90%8C%E7%9A%84%E5%9C%B0%E6%96%B9/"/>
      <url>/2025/09/02/C#%E4%B8%8Ejava%E4%B8%8D%E5%90%8C%E7%9A%84%E5%9C%B0%E6%96%B9/</url>
      
        <content type="html"><![CDATA[<h1 id="C-与java不同的地方"><a href="#C-与java不同的地方" class="headerlink" title="C#与java不同的地方"></a>C#与java不同的地方</h1><h4 id="ref参数"><a href="#ref参数" class="headerlink" title="ref参数"></a>ref参数</h4><p>迫使值参数作为引用参数进行函数参数传递，变量前需要加ref关键词，函数对使用ref修饰的变量修改其值后，会影响你原始值。<br>注意：区函数形参传入的是值参数和引用参数的影响，如果函数形参传入的是值参数，形参会在栈上开辟空间；如果传入的是引用参数，则不会在栈上开辟空间，直接引用到实参地址；</p><p>还要区别引用类型作为值参数和引用参数：如果引用类型作为值参数传入函数，则会在方法内部开辟新对象去引用这个传入的引用类型变量；</p><p>注意：使用ref时，变量需要先初始化。</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">jiaohuan</span>(<span class="params"><span class="keyword">ref</span> <span class="built_in">int</span> a, <span class="keyword">ref</span> <span class="built_in">int</span> b</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">int</span> tempt;</span><br><span class="line">        tempt = a;</span><br><span class="line">        a = b;</span><br><span class="line">        b = tempt;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line"> </span><br><span class="line">        <span class="keyword">try</span></span><br><span class="line">        &#123;</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;输入你要试验的数字：&quot;</span>);</span><br><span class="line">            <span class="built_in">int</span> x = Convert.ToInt32(Console.ReadLine());</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;请输入第二个数字&quot;</span>);</span><br><span class="line">            <span class="built_in">int</span> y = Convert.ToInt32(Console.ReadLine());</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;x的数值是 &quot;</span>+x);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;y的数值是 &quot;</span>+y);</span><br><span class="line">            jiaohuan(<span class="keyword">ref</span> x,<span class="keyword">ref</span> y);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;/////////////////&quot;</span>);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;x的数值是 &quot;</span>+x);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;y的数值是 &quot;</span>+y);</span><br><span class="line">            Console.ReadKey();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span></span><br><span class="line">        &#123;</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;输入的内容不能够转成数字，请重新输入&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="out参数"><a href="#out参数" class="headerlink" title="out参数"></a>out参数</h4><p><strong>out使用时需注意，使用out修饰的变量，需要在函数内部对其进行初始化</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line">    <span class="function"><span class="keyword">static</span> <span class="built_in">double</span> <span class="title">average</span>(<span class="params"><span class="built_in">int</span> [] x, <span class="keyword">out</span> <span class="built_in">int</span> maxval,<span class="keyword">out</span> <span class="built_in">int</span> minval</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        maxval = x[<span class="number">0</span>];</span><br><span class="line">        minval = x[<span class="number">0</span>];</span><br><span class="line">        <span class="built_in">int</span> sum = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; x.Length; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (x[i]&gt;maxval)</span><br><span class="line">            &#123;</span><br><span class="line">                maxval = x[i];</span><br><span class="line">            &#125;</span><br><span class="line"> </span><br><span class="line">            <span class="keyword">if</span>(x[i]&lt;minval)</span><br><span class="line">            &#123;</span><br><span class="line">                minval = x[i];</span><br><span class="line">            &#125;</span><br><span class="line">            sum += x[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum * <span class="number">1.0</span> / x.Length;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">try</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="built_in">int</span>[] x = &#123;<span class="number">1</span>,<span class="number">1</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>&#125;;</span><br><span class="line">            <span class="built_in">int</span> maxval;</span><br><span class="line">            <span class="built_in">int</span> minval;</span><br><span class="line">            <span class="built_in">double</span> y = average(x, <span class="keyword">out</span> maxval, <span class="keyword">out</span> minval);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;maxval的数值是 &quot;</span>+maxval);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;minval的数值是 &quot;</span>+minval);</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;数组的平均值是 &#123;0:0.00&#125;&quot;</span>,y);</span><br><span class="line">            Console.ReadKey();</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">catch</span></span><br><span class="line">        &#123;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="部分类"><a href="#部分类" class="headerlink" title="部分类"></a>部分类</h4><p><em><strong>关键词是partial</strong></em></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">partial</span> <span class="keyword">class</span> <span class="title">calculate</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">maxvalue</span>(<span class="params"><span class="built_in">int</span> [] x</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">int</span> maxvale = x[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt;x.Length; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (x[i]&gt;maxvale)</span><br><span class="line">            &#123;</span><br><span class="line">                maxvale = x[i];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> maxvale;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">partial</span> <span class="keyword">class</span> <span class="title">calculate</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">int</span> sum = <span class="number">0</span>;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">average</span>(<span class="params"><span class="built_in">int</span> [] x</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; x.Length; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            sum += x[i];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> sum * <span class="number">1.0</span> / x.Length;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> customer</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">int</span> age;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">string</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">int</span>[] x = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>&#125;;</span><br><span class="line">        calculate y = <span class="keyword">new</span> calculate();</span><br><span class="line">        <span class="built_in">int</span> max1 = y.maxvalue(x);</span><br><span class="line">        <span class="built_in">double</span> averagev = y.average(x);</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;数组最大值是 &quot;</span>+max1+<span class="string">&quot; 数组平均值是 &quot;</span>+averagev);</span><br><span class="line">        Console.ReadKey();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="结构"><a href="#结构" class="headerlink" title="结构"></a>结构</h4><p>结构是值类型，</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> customer</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">int</span> age;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">string</span> name;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        customer x = <span class="keyword">new</span> customer();</span><br><span class="line">        x.age = <span class="number">15</span>;</span><br><span class="line">        x.name = <span class="string">&quot;jerry&quot;</span>;</span><br><span class="line">        Console.WriteLine(x.name + x.age);</span><br><span class="line">        Console.ReadKey();</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="静态类"><a href="#静态类" class="headerlink" title="静态类"></a><strong>静态类</strong></h4><p><strong>使用static对类进行修饰的，称之为静态类，其不能够被实例化。</strong></p><p><strong>静态类只包含静态属性和静态方法，不包含实例成员，且不能够进行实例化。</strong></p><h3 id="实现继承和接口继承"><a href="#实现继承和接口继承" class="headerlink" title="实现继承和接口继承"></a><strong>实现继承和接口继承</strong></h3><p><strong>实现继承：</strong>该类继承于某一个类，可以调用继承的父类的所有字段、属性和方法。</p><p><strong>接口继承：</strong>这种方式只是简单继承了接口函数名称，但是并没有实际继承程序代码。</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">struct</span> customer</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">int</span> age;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">string</span> name;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title">teacher</span>: <span class="title">people</span></span><br><span class="line">&#123; </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">work</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;老师的工作是教学&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">class</span> <span class="title">people</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">int</span> age;</span><br><span class="line">    <span class="keyword">public</span> <span class="built_in">string</span> name;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">sleep</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;人需要睡觉&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    teacher x = <span class="keyword">new</span> teacher();</span><br><span class="line">    x.sleep();</span><br><span class="line">    x.age = <span class="number">12</span>;</span><br><span class="line">    x.name = <span class="string">&quot;jerry&quot;</span>;</span><br><span class="line">    x.work();</span><br><span class="line">    Console.ReadKey();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>注意事项：</p><p>① 当基类的派生对象存在和基类相同的成员名称时，基类的派生对象内部可以使用关键词“new”对基类中具有相同名称的成员进行覆盖。目前可以实现对字段、属性、方法的覆盖。</p><p>② 在使用new方法对基类中同名字的成员进行覆盖后，如果想派生类中访问基类中同名的成员时，可以使用base关键词进行选择需要访问的成员。</p><p>③ 派生类强制转换成基类后，派生类调用成员时都是调用的基类的成员。</p><p>using System;<br>using System.Collections.Generic;<br>using System.Linq;<br>using System.Text;<br>using System.Threading.Tasks;</p><p>namespace ConsoleApplication2<br>{<br>    class baseclass<br>    {<br>        public string name&#x3D;”这个是基类的名字”;<br>        public string Name<br>        {<br>            set<br>            {<br>                name &#x3D; value;<br>            }<br>            get<br>            {<br>                return name;<br>            }<br>        }<br>        public int add(int a,int b)<br>        {<br>            return a + b;<br>        }</p><pre><code>} class paishnegclass : baseclass{    public new string name = &quot;这是派生类的名字&quot;;    public new string Name    {        set        {            name = value;        }        get        {            return name;        }    }    public new int add(int a, int b)    {        return a + b + 10;    }    public void show()    {        Console.WriteLine(base.Name);    } }</code></pre><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        baseclass x = <span class="keyword">new</span> baseclass();</span><br><span class="line">        Console.WriteLine(x.Name);</span><br><span class="line">        <span class="built_in">int</span> a = x.<span class="keyword">add</span>(<span class="number">4</span>,<span class="number">5</span>);</span><br><span class="line">        paishnegclass y = <span class="keyword">new</span> paishnegclass();</span><br><span class="line">        <span class="built_in">int</span> b = y.<span class="keyword">add</span>(<span class="number">4</span>,<span class="number">5</span>);</span><br><span class="line">        Console.WriteLine(y.Name);</span><br><span class="line">        Console.ReadKey();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="虚方法"><a href="#虚方法" class="headerlink" title="虚方法"></a>虚方法</h4><p><strong>使用虚方法重写父类的函数，父类函数声明时需要加上virtual&#x2F;abstract关键词，子类重写父类的函数时，需要在函数前加上override关键词。</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">animal</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">work</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;我是动物&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">monkey</span> : <span class="title">animal</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">override</span> <span class="keyword">void</span> <span class="title">work</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;我是猴子&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">struct</span> customer</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">int</span> age;</span><br><span class="line">        <span class="keyword">public</span> <span class="built_in">string</span> name;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        animal x = <span class="keyword">new</span> animal();</span><br><span class="line">        x.work();  <span class="comment">//输出我是动物</span></span><br><span class="line">        monkey y = <span class="keyword">new</span> monkey();</span><br><span class="line">        y.work();  <span class="comment">//输出我是猴子</span></span><br><span class="line">        animal z = <span class="keyword">new</span> monkey();</span><br><span class="line">        z.work();  <span class="comment">//输出我是猴子</span></span><br><span class="line">        Console.ReadKey();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="注"><a href="#注" class="headerlink" title="注"></a>注</h3><p>通过虚方法和new方法对子类函数进行重写对比结论：<br>①new    .new声明的方法，当使用子类的类型来调用的时候，它会运行子类中的函数，当类型是基类的话 ，则使用基类的函数来执行</p><p>②override  当使用子类的类型来调用的时候，它会运行子类中的函数，当类型是基类的话 ，则使用子类的函数来执行</p><p>简单说：<code>override</code> 是 “取而代之”（多态），<code>new</code> 是 “另起炉灶”（隐藏）</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// new </span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">BaseClass</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">Show</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;BaseClass.Show()&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title">SubClass</span> : <span class="title">BaseClass</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">new</span> <span class="keyword">void</span> <span class="title">Show</span>() <span class="comment">// 隐藏基类的Show()</span></span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;SubClass.Show()&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 调用</span></span><br><span class="line">BaseClass obj1 = <span class="keyword">new</span> SubClass();</span><br><span class="line">obj1.Show(); <span class="comment">// 输出：BaseClass.Show()（基类引用调用基类方法）</span></span><br><span class="line"></span><br><span class="line">SubClass obj2 = <span class="keyword">new</span> SubClass();</span><br><span class="line">obj2.Show(); <span class="comment">// 输出：SubClass.Show()（子类引用调用子类新方法）</span></span><br></pre></td></tr></table></figure><h4 id="调用基类的函数方式base-基类函数名"><a href="#调用基类的函数方式base-基类函数名" class="headerlink" title="调用基类的函数方式base.基类函数名"></a>调用基类的函数方式base.基类函数名</h4><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">zhekou</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">calculate</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">15</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">zhekou1</span> : <span class="title">zhekou</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">calculate1</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">base</span>.calculate() * <span class="number">0.9</span>;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        zhekou1 x = <span class="keyword">new</span> zhekou1();</span><br><span class="line">        <span class="built_in">double</span> y = x.calculate1();</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;折扣是&quot;</span>+y);</span><br><span class="line">        Console.ReadKey();</span><br><span class="line"> </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="密封类"><a href="#密封类" class="headerlink" title="密封类"></a>密封类</h4><p><strong>使用关键词sealed</strong> 类似于java中final修饰的class</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">sealed</span> <span class="keyword">class</span> <span class="title">number1</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">static</span> <span class="built_in">int</span> age;</span><br><span class="line">    <span class="keyword">public</span>  <span class="keyword">static</span> <span class="built_in">string</span> name;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        number1.age = <span class="number">15</span>;</span><br><span class="line">        number1.name = <span class="string">&quot;jerry&quot;</span>;</span><br><span class="line">        Console.ReadKey();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="修饰符"><a href="#修饰符" class="headerlink" title="修饰符"></a>修饰符</h3><p>① protected 只有派生类可以访问该项</p><p>② public      任何代码都可以访问该项</p><p>③ internal    只有包含该类的程序集中访问该项</p><p>④ private     只有在它所属的类型中进行访问该项</p><p>⑤ protected internal  只有在包含它的程序集和派生类中的任何代码中访问该项</p><h3 id="接口"><a href="#接口" class="headerlink" title="接口"></a>接口</h3><p>和java的接口类似</p><p>接口声明和抽象类比较相似，接口只能包含方法、属性、索引器和事件的声明。</p><p>注意:接口不能够实例化操作；不能声明为虚方法或者静态类</p><p>以银行账号为例，创建接口，用于用户进行存钱、取钱和查询余额。</p><pre><code>public interface IDbank{    void Payin(decimal amount);    void withdraw(decimal amount);    decimal Balance { get; }    void check();} public class bankB : IDbank{    private decimal balance;    public decimal Balance    {        get        {            return balance;        }    }     public void Payin(decimal amount)    {        balance += amount;    }     public void withdraw(decimal amount)    {        if (balance&gt; amount)        {            balance -= amount;        }    }    public void check()    {        Console.WriteLine(&quot;账户余额剩余{0:0.00}&quot;,this.balance);    }}</code></pre><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        IDbank x = <span class="keyword">new</span> bankB();</span><br><span class="line">        x.Payin(<span class="number">100</span>);</span><br><span class="line">        x.withdraw(<span class="number">50</span>);</span><br><span class="line">        x.check();</span><br><span class="line">        Console.ReadKey();    </span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="装箱和拆箱"><a href="#装箱和拆箱" class="headerlink" title="装箱和拆箱"></a>装箱和拆箱</h3><p>装箱：将值类型转成引用类型</p><p>拆箱：将引用类型转成值类型</p><p><strong>注意：装箱和拆箱操作容易使用，但是性能损伤比较大，遍历许多项时尤其如其。</strong></p><p><strong>例如:将object类型转成数组类型</strong></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">var</span> x = <span class="keyword">new</span> ArrayList();</span><br><span class="line">    x.Add(<span class="number">4</span>);</span><br><span class="line">    x.Add(<span class="string">&quot;你是&quot;</span>);</span><br><span class="line">    x.Add(<span class="keyword">new</span> <span class="built_in">int</span> []&#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>&#125;);</span><br><span class="line">    <span class="keyword">foreach</span> (<span class="keyword">var</span> item <span class="keyword">in</span> x)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (item <span class="keyword">is</span> <span class="built_in">int</span>[])</span><br><span class="line">        &#123;</span><br><span class="line">            Console.WriteLine(<span class="string">&quot;我是数组&quot;</span>);</span><br><span class="line">            <span class="built_in">int</span>[] y = (<span class="built_in">int</span>[])item;</span><br><span class="line">            <span class="keyword">for</span> (<span class="built_in">int</span> i = <span class="number">0</span>; i &lt; y.Length; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                Console.Write(y[i]+<span class="string">&quot; &quot;</span>);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            Console.WriteLine(item);</span><br><span class="line">     </span><br><span class="line">    &#125;</span><br><span class="line">    Console.ReadKey();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="list-泛型"><a href="#list-泛型" class="headerlink" title="list 泛型"></a>list 泛型</h3><p>由于arraylist可以存储任意类型的元素，使用list可以创建存储指定类型的泛型。</p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">var</span> x = <span class="keyword">new</span> List&lt;<span class="built_in">int</span>&gt;();</span><br><span class="line">x.Add(<span class="number">4</span>);</span><br><span class="line">x.Add(<span class="number">40</span>);</span><br><span class="line"><span class="keyword">foreach</span> (<span class="built_in">int</span> item <span class="keyword">in</span> x)</span><br><span class="line">&#123;</span><br><span class="line">    Console.WriteLine(item);</span><br><span class="line">&#125;</span><br><span class="line">Console.ReadKey();</span><br></pre></td></tr></table></figure><h2 id="数组"><a href="#数组" class="headerlink" title="数组"></a>数组</h2><p><em><strong>*数组：一种数据结构，可以存储包含同一种类型的多个元素*</strong></em></p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">int</span>[] x = <span class="keyword">new</span> <span class="built_in">int</span>[<span class="number">4</span>] &#123; <span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span> &#125;;  <span class="comment">// 和C类似</span></span><br><span class="line"><span class="built_in">int</span>[] y = &#123;<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>&#125;;</span><br></pre></td></tr></table></figure><h4 id="委托"><a href="#委托" class="headerlink" title="委托"></a>委托</h4><ol><li><p><code>Action</code> 和 <code>Func</code> </p><figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title">calculate</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title">show</span>()</span></span><br><span class="line">    &#123;</span><br><span class="line">        Console.WriteLine(<span class="string">&quot;我是show函数&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">add</span>(<span class="params"><span class="built_in">int</span> a,<span class="built_in">int</span> b</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> a + b;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">int</span> <span class="title">sub</span>(<span class="params"><span class="built_in">int</span> a, <span class="built_in">int</span> b</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> a-b;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">Program</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">Main</span>(<span class="params"><span class="built_in">string</span>[] <span class="keyword">args</span></span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        calculate w = <span class="keyword">new</span> calculate();</span><br><span class="line">        <span class="comment">//利用action关键词对应到show函数</span></span><br><span class="line">        Action x = <span class="keyword">new</span> Action(w.show);</span><br><span class="line">        x.Invoke();   <span class="comment">//执行show函数</span></span><br><span class="line">        x();      <span class="comment">//执行show函数</span></span><br><span class="line"> </span><br><span class="line">        Func&lt;<span class="built_in">int</span>, <span class="built_in">int</span>, <span class="built_in">int</span>&gt; y = <span class="keyword">new</span> Func&lt;<span class="built_in">int</span>, <span class="built_in">int</span>, <span class="built_in">int</span>&gt;(w.<span class="keyword">add</span>);</span><br><span class="line">        <span class="built_in">int</span> g = y.Invoke(<span class="number">5</span>,<span class="number">6</span>);</span><br><span class="line">        <span class="built_in">int</span> h = y(<span class="number">5</span>,<span class="number">6</span>);</span><br><span class="line">        Console.WriteLine(g);</span><br><span class="line">        Console.WriteLine(h);</span><br><span class="line">        Console.ReadKey();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>自定义委托</p></li></ol>   <figure class="highlight c#"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;summary&gt;</span></span></span><br><span class="line"><span class="comment"><span class="doctag">///</span> 声明自定义委托</span></span><br><span class="line"><span class="comment"><span class="doctag">///</span> <span class="doctag">&lt;/summary&gt;</span></span></span><br><span class="line"><span class="comment"><span class="doctag">///</span> </span></span><br><span class="line"><span class="function"><span class="keyword">public</span> <span class="built_in">delegate</span> <span class="built_in">double</span> <span class="title">cal</span>(<span class="params"><span class="built_in">double</span> x, <span class="built_in">double</span> y</span>)</span>;</span><br><span class="line"> </span><br><span class="line"><span class="keyword">class</span> <span class="title">calulator</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">add</span>(<span class="params"><span class="built_in">double</span> x, <span class="built_in">double</span> y</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> x + y;</span><br><span class="line">    &#125;</span><br><span class="line"> </span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">sub</span>(<span class="params"><span class="built_in">double</span> x, <span class="built_in">double</span> y</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> x - y;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">mul</span>(<span class="params"><span class="built_in">double</span> x, <span class="built_in">double</span> y</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> x*y;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="function"><span class="keyword">public</span> <span class="built_in">double</span> <span class="title">div</span>(<span class="params"><span class="built_in">double</span> x, <span class="built_in">double</span> y</span>)</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> x/y;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"> </span><br><span class="line">        calulator p = <span class="keyword">new</span> calulator();</span><br><span class="line">        cal h = <span class="keyword">new</span> cal(p.div);</span><br><span class="line"> </span><br><span class="line">        <span class="built_in">double</span> c = h.Invoke(<span class="number">3.0</span>,<span class="number">5.0</span>);</span><br><span class="line">        <span class="built_in">double</span> d = h(<span class="number">3.0</span>,<span class="number">5.0</span>);</span><br><span class="line"> </span><br><span class="line">        Console.WriteLine(c);</span><br><span class="line">        Console.WriteLine(d);</span><br><span class="line">        Console.ReadKey();</span><br></pre></td></tr></table></figure><p>​委托的声明：委托与所封装的方法必须“类型兼容”</p><p><img src="https://i-blog.csdnimg.cn/direct/fc1b3ed43878463f989f94d61eccfc3f.png" alt="img"></p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>CountDownLatch 和CompletableFuture</title>
      <link href="/2025/08/31/CountDownLatch-%E5%92%8CCompletableFuture-2/"/>
      <url>/2025/08/31/CountDownLatch-%E5%92%8CCompletableFuture-2/</url>
      
        <content type="html"><![CDATA[<h2 id="CountDownLatch-和CompletableFuture"><a href="#CountDownLatch-和CompletableFuture" class="headerlink" title="CountDownLatch 和CompletableFuture"></a>CountDownLatch 和CompletableFuture</h2><p>在Java并发编程中，<code>CountDownLatch</code>和<code>CompletableFuture</code>都是处理多线程协作的重要工具，但它们的设计目标和应用场景有所不同。下面是两者的详细说明及代码示例：</p><h3 id="一、CountDownLatch"><a href="#一、CountDownLatch" class="headerlink" title="一、CountDownLatch"></a>一、CountDownLatch</h3><p><code>CountDownLatch</code>是基于AQS（抽象队列同步器）的同步工具，用于<strong>等待一组线程完成后再执行主线程</strong>。它通过一个计数器实现：</p><ul><li>初始化时设置计数器值（线程数量）</li><li>每个线程完成任务后调用<code>countDown()</code>递减计数器</li><li>主线程调用<code>await()</code>阻塞等待，直到计数器变为0</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.CountDownLatch;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CountDownLatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 初始化计数器为3（需要等待3个线程完成）</span></span><br><span class="line">        <span class="type">CountDownLatch</span> <span class="variable">latch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CountDownLatch</span>(<span class="number">3</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 启动3个工作线程</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt;= <span class="number">3</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">int</span> <span class="variable">taskId</span> <span class="operator">=</span> i;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(<span class="string">&quot;任务&quot;</span> + taskId + <span class="string">&quot;开始执行&quot;</span>);</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span> * taskId); <span class="comment">// 模拟不同耗时的任务</span></span><br><span class="line">                    System.out.println(<span class="string">&quot;任务&quot;</span> + taskId + <span class="string">&quot;执行完成&quot;</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    latch.countDown(); <span class="comment">// 任务完成，计数器减1</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        System.out.println(<span class="string">&quot;主线程等待所有任务完成...&quot;</span>);</span><br><span class="line">        latch.await(); <span class="comment">// 阻塞等待计数器变为0</span></span><br><span class="line">        System.out.println(<span class="string">&quot;所有任务已完成，主线程继续执行&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>应用场景</strong>：</p><ul><li>主线程等待多个子线程初始化完成后再启动</li><li>批量任务执行完毕后汇总结果</li><li>模拟并发测试（让所有线程准备就绪后同时执行）</li></ul><p><strong>特点</strong>：</p><ul><li>计数器只能使用一次，不能重置</li><li>是非阻塞的线程间通信方式</li><li>适合”一对多”的等待场景</li></ul><h3 id="二、CompletableFuture"><a href="#二、CompletableFuture" class="headerlink" title="二、CompletableFuture"></a>二、CompletableFuture</h3><p><code>CompletableFuture</code>是Java 8引入的异步编程工具，基于Future模式扩展，支持<strong>异步任务的链式执行、结果处理和异常处理</strong>。它可以：</p><ul><li>异步执行任务并获取结果</li><li>多个异步任务串行或并行组合</li><li>自动处理任务完成后的回调逻辑</li></ul><p><strong>常用API</strong>：</p><ul><li><code>runAsync()</code>：异步执行Runnable任务（无返回值）</li><li><code>supplyAsync()</code>：异步执行Supplier任务（有返回值）</li><li><code>thenApply()</code>：任务完成后处理结果（有返回值）</li><li><code>thenAccept()</code>：任务完成后消费结果（无返回值）</li><li><code>thenCompose()</code>：串联两个异步任务</li><li><code>allOf()</code>：等待所有任务完成</li><li><code>anyOf()</code>：等待任一任务完成</li></ul><p>​</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutionException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CompletableFutureDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 1. 异步执行任务（无返回值）</span></span><br><span class="line">        CompletableFuture&lt;Void&gt; future1 = CompletableFuture.runAsync(() -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;异步任务1执行: &quot;</span> + Thread.currentThread().getName());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 2. 异步执行任务（有返回值）</span></span><br><span class="line">        CompletableFuture&lt;String&gt; future2 = CompletableFuture.supplyAsync(() -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;异步任务2执行: &quot;</span> + Thread.currentThread().getName());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;任务2结果&quot;</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 3. 任务2完成后执行回调（链式操作）</span></span><br><span class="line">        CompletableFuture&lt;String&gt; future3 = future2.thenApply(result -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;接收任务2结果: &quot;</span> + result);</span><br><span class="line">            <span class="keyword">return</span> result + <span class="string">&quot; -&gt; 处理后&quot;</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 4. 等待所有任务完成</span></span><br><span class="line">        CompletableFuture.allOf(future1, future3).join();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取最终结果</span></span><br><span class="line">        System.out.println(<span class="string">&quot;最终结果: &quot;</span> + future3.get());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>应用场景</strong>：</p><ul><li>异步IO操作（如网络请求、文件读写）</li><li>复杂的异步任务编排（串行、并行、分支）</li><li>非阻塞的结果处理（避免主线程阻塞）</li><li>函数式编程风格的并发代码</li></ul><p><strong>特点</strong>：</p><ul><li>支持任务间的依赖关系和流水线操作</li><li>内置异常处理机制（<code>exceptionally()</code>）</li><li>可与线程池结合使用，控制并发资源</li></ul><h3 id="三、两者对比与选择"><a href="#三、两者对比与选择" class="headerlink" title="三、两者对比与选择"></a>三、两者对比与选择</h3><table><thead><tr><th>特性</th><th>CountDownLatch</th><th>CompletableFuture</th></tr></thead><tbody><tr><td>核心功能</td><td>等待多个线程完成</td><td>异步任务执行与结果处理</td></tr><tr><td>灵活性</td><td>功能单一（仅等待）</td><td>支持复杂的任务组合和链式操作</td></tr><tr><td>结果处理</td><td>不直接支持结果传递</td><td>原生支持结果处理和转换</td></tr><tr><td>重用性</td><td>计数器不可重置，一次性使用</td><td>可多次组合使用</td></tr><tr><td>适用场景</td><td>简单的等待多线程完成场景</td><td>复杂的异步任务编排和结果处理</td></tr></tbody></table><p><strong>选择建议</strong>：</p><ul><li>若只需等待一组线程完成后继续执行，用<code>CountDownLatch</code></li><li>若需要处理异步任务的结果、组合多个任务，用<code>CompletableFuture</code></li><li>并发编程中，<code>CompletableFuture</code>更符合现代异步编程模式，应用更广泛</li></ul><p>在实际开发中，两者也可结合使用，例如用<code>CompletableFuture</code>处理异步任务，用<code>CountDownLatch</code>控制整体等待逻辑。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>阿里巴巴学习手册抄录</title>
      <link href="/2025/08/29/%E9%98%BF%E9%87%8C%E5%B7%B4%E5%B7%B4%E5%AD%A6%E4%B9%A0%E6%89%8B%E5%86%8C%E6%8A%84%E5%BD%95/"/>
      <url>/2025/08/29/%E9%98%BF%E9%87%8C%E5%B7%B4%E5%B7%B4%E5%AD%A6%E4%B9%A0%E6%89%8B%E5%86%8C%E6%8A%84%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="阿里巴巴学习手册抄录"><a href="#阿里巴巴学习手册抄录" class="headerlink" title="阿里巴巴学习手册抄录"></a>阿里巴巴学习手册抄录</h1><h3 id="代码要求"><a href="#代码要求" class="headerlink" title="代码要求"></a>代码要求</h3><p>【参考】各层命名规约：   A) Service&#x2F;DAO 层方法命名规约  1） 获取单个对象的方法用get做前缀。  2） 获取多个对象的方法用list做前缀，复数形式结尾如：listObjects。  3） 获取统计值的方法用count做前缀。  4） 插入的方法用save&#x2F;insert做前缀。  5） 删除的方法用remove&#x2F;delete做前缀。  6） 修改的方法用update做前缀。  B) 领域模型命名规约  1） 数据对象：xxxDO，xxx即为数据表名。  2） 数据传输对象：xxxDTO，xxx为业务领域相关的名称。  3） 展示对象：xxxVO，xxx一般为网页名称。  4） POJO是DO&#x2F;DTO&#x2F;BO&#x2F;VO的统称，禁止命名成xxxPOJO。</p><p>【强制】不能使用过时的类或方法。  说明：java.net.URLDecoder 中的方法 decode(String encodeStr) 这个方法已经过时，应该使用双参数 decode(String source, String encode)。接口提供方既然明确是过时接口，那么有义务同时提供新的接 口；作为调用方来说，有义务去考证过时方法的新实现是什么。</p><p>【强制】Object的equals方法容易抛空指针异常，应使用常量或确定有值的对象来调用 equals。  正例：”test”.equals(object);  反例：object.equals(“test”);  说明：推荐使用java.util.Objects#equals（JDK7 引入的工具类）。</p><p>【强制】浮点数之间的等值判断，基本数据类型不能用&#x3D;&#x3D;来比较，包装数据类型不能用 equals 来判断。  说明：浮点数采用“尾数+阶码”的编码方式，类似于科学计数法的“有效数字+指数”的表示方式。二进 7&#x2F;44  Java 开发手册  制无法精确表示大部分的十进制小数，具体原理参考《码出高效》。  反例：  float a &#x3D; 1.0f - 0.9f;  float b &#x3D; 0.9f - 0.8f;  if (a &#x3D;&#x3D; b) {  &#x2F;&#x2F; 预期进入此代码快，执行其它业务逻辑  &#x2F;&#x2F; 但事实上a&#x3D;&#x3D;b的结果为false  }  Float x &#x3D; Float.valueOf(a);  Float y &#x3D; Float.valueOf(b);  if (x.equals(y)) {  &#x2F;&#x2F; 预期进入此代码快，执行其它业务逻辑  &#x2F;&#x2F; 但事实上equals的结果为false  }    正例：  (1) 指定一个误差范围，两个浮点数的差值在此范围之内，则认为是相等的。  float a &#x3D; 1.0f - 0.9f;  float b &#x3D; 0.9f - 0.8f;  float diff &#x3D; 1e-6f;  if (Math.abs(a - b) &lt; diff) {  System.out.println(“true”);  }  (2) 使用BigDecimal 来定义值，再进行浮点数的运算操作。  BigDecimal a &#x3D; new BigDecimal(“1.0”);  BigDecimal b &#x3D; new BigDecimal(“0.9”);  BigDecimal c &#x3D; new BigDecimal(“0.8”);  BigDecimal x &#x3D; a.subtract(b);  BigDecimal y &#x3D; b.subtract(c);  if (x.equals(y)) {  System.out.println(“true”);  } </p><p>【强制】为了防止精度损失，禁止使用构造方法BigDecimal(double)的方式把double值转 化为BigDecimal 对象。  说明：BigDecimal(double)存在精度损失风险，在精确计算或值比较的场景中可能会导致业务逻辑异常。 如：BigDecimal g &#x3D; new BigDecimal(0.1f); 实际的存储值为：0.10000000149  正例：优先推荐入参为String的构造方法，或使用BigDecimal的valueOf方法，此方法内部其实执行了 Double 的toString，而 Double 的toString 按 double 的实际能表达的精度对尾数进行了截断。  8&#x2F;44  Java 开发手册  BigDecimal recommend1 &#x3D; new BigDecimal(“0.1”);  BigDecimal recommend2 &#x3D; BigDecimal.valueOf(0.1); </p><p>【推荐】使用索引访问用String的split方法得到的数组时，需做最后一个分隔符后有无内 容的检查，否则会有抛IndexOutOfBoundsException的风险。  说明：  String str &#x3D; “a,b,c,,”;   String[] ary &#x3D; str.split(“,”);   &#x2F;&#x2F; 预期大于3，结果是3  System.out.println(ary.length);  </p><h4 id="集合处理"><a href="#集合处理" class="headerlink" title="集合处理"></a>集合处理</h4><p>【强制】关于hashCode和equals的处理，遵循如下规则：  1） 只要覆写equals，就必须覆写hashCode。  2） 因为Set存储的是不重复的对象，依据hashCode和equals进行判断，所以Set存储的对象必须覆 写这两个方法。  3） 如果自定义对象作为Map的键，那么必须覆写hashCode和equals。  说明：String 已覆写hashCode和equals方法，所以我们可以愉快地使用String对象作为key来使用。</p><p>【强制】ArrayList 的subList 结果不可强转成ArrayList，否则会抛出 ClassCastException异 常，即java.util.RandomAccessSubList cannot be cast to java.util.ArrayList。  说明：subList 返回的是ArrayList 的内部类SubList，并不是 ArrayList 而是ArrayList 的一个视图，对 于SubList 子列表的所有操作最终会反映到原列表上。 </p><p>【强制】使用Map的方法keySet()&#x2F;values()&#x2F;entrySet()返回集合对象时，不可以对其进行添 加元素操作，否则会抛出UnsupportedOperationException 异常。 </p><p>【强制】Collections 类返回的对象，如：emptyList()&#x2F;singletonList()等都是 immutable  list，不可对其进行添加或者删除元素的操作。  反例：如果查询无结果，返回Collections.emptyList()空集合对象，调用方一旦进行了添加元素的操作，就 会触发UnsupportedOperationException 异常。</p><p>【强制】在subList场景中，高度注意对原集合元素的增加或删除，均会导致子列表的遍 历、增加、删除产生ConcurrentModificationException 异常。</p><p>【强制】使用集合转数组的方法，必须使用集合的toArray(T[] array)，传入的是类型完全一 致、长度为0的空数组。  反例：直接使用toArray无参方法存在问题，此方法返回值只能是Object[]类，若强转其它类型数组将出 现ClassCastException 错误。  正例：  List list &#x3D; new ArrayList&lt;&gt;(2);  list.add(“guan”);  list.add(“bao”);  String[] array &#x3D; list.toArray(new String[0]);</p><p>说明：使用toArray带参方法，数组空间大小的length：  1） 等于0，动态创建与size相同的数组，性能最好。  2） 大于0但小于size，重新创建大小等于size的数组，增加GC负担。  11&#x2F;44  Java 开发手册  3） 等于size，在高并发情况下，数组创建完成之后，size正在变大的情况下，负面影响与上相同。  4） 大于size，空间浪费，且在size处插入null值，存在NPE隐患。</p><p>【强制】在使用Collection接口任何实现类的addAll()方法时，都要对输入的集合参数进行 NPE判断。  说明：在ArrayList#addAll 方法的第一行代码即Object[] a &#x3D; c.toArray(); 其中 c为输入集合参数，如果 为null，则直接抛出异常。 </p><p>【强制】使用工具类Arrays.asList()把数组转换成集合时，不能使用其修改集合相关的方 法，它的add&#x2F;remove&#x2F;clear方法会抛出UnsupportedOperationException 异常。  说明：asList 的返回对象是一个Arrays内部类，并没有实现集合的修改方法。Arrays.asList体现的是适 配器模式，只是转换接口，后台的数据仍是数组。  </p><p>String[] str &#x3D; new String[] { “yang”, “hao” };  List list &#x3D; Arrays.asList(str);  </p><p>第一种情况：list.add(“yangguanbao”); 运行时异常。 </p><p>第二种情况：str[0] &#x3D; “changed”; 也会随之修改，反之亦然。 </p><p>【强制】不要在foreach循环里进行元素的remove&#x2F;add操作。remove元素请使用 Iterator 方式，如果并发操作，需要对Iterator对象加锁。  正例：  List list &#x3D; new ArrayList&lt;&gt;();       list.add(“1”);       list.add(“2”);     Iterator iterator &#x3D; list.iterator();   while (iterator.hasNext()) {               String item &#x3D; iterator.next();                        if (删除元素的条件) {                               12&#x2F;44  Java 开发手册  iterator.remove();                  }       }  反例：  for (String item : list) {           if (“1”.equals(item)) {               list.remove(item);           }       }   说明：以上代码的执行结果肯定会出乎大家的意料，那么试一下把“1”换成“2”，会是同样的结果吗？</p><p>在 Java 中，foreach 循环底层是通过迭代器（Iterator）实现的，但它隐藏了迭代器的细节。当在 foreach 循环中直接调用 <code>list.remove()</code> 方法时，会导致迭代器内部的修改计数与集合的实际修改计数不一致，从而抛出 <code>ConcurrentModificationException</code> 异常。</p><p>我们来分析一下反例中两种情况的不同结果：</p><ol><li>当删除元素是 “1” 时：<ul><li>循环第一次迭代获取到元素 “1”，执行 <code>list.remove(&quot;1&quot;)</code></li><li>此时集合修改计数发生变化，但迭代器的修改计数未更新</li><li>进行下一次迭代时，迭代器发现两者不一致，抛出 <code>ConcurrentModificationException</code></li></ul></li><li>当删除元素是 “2” 时：<ul><li>循环第一次迭代获取到元素 “1”，不满足删除条件</li><li>第二次迭代获取到元素 “2”，执行 <code>list.remove(&quot;2&quot;)</code></li><li>此时集合大小变为 1，迭代器的指针已经到达末尾</li><li>循环会正常结束，不会抛出异常（这是一种巧合情况）</li></ul></li></ol><p>这种差异的原因是删除最后一个元素时，迭代器不会再尝试访问下一个元素，所以不会检测到并发修改。但这只是特殊情况，不能依赖这种行为，因为它可能导致不可预测的结果。</p><p>正确的做法始终是使用迭代器的 <code>remove()</code> 方法，如正例所示，这样可以保证迭代器和集合的状态同步。如果涉及并发操作，还需要对迭代器对象进行加锁，以避免多线程冲突。</p><p> 【强制】在JDK7版本及以上，Comparator实现类要满足如下三个条件，不然Arrays.sort， Collections.sort 会抛 IllegalArgumentException 异常。  说明：三个条件如下  1） x，y的比较结果和y，x的比较结果相反。  2） x&gt;y，y&gt;z，则x&gt;z。  3） x&#x3D;y，则x，z比较结果和y，z比较结果相同。  反例：下例中没有处理相等的情况，交换两个对象判断结果并不互反，不符合第一个条件，在实际使用中 可能会出现异常。  new Comparator() {            @Override           public int compare(Student o1, Student o2) {               return o1.getId() &gt; o2.getId() ? 1 : -1;           }       };</p><p>【推荐】集合初始化时，指定集合初始值大小。  说明：HashMap使用HashMap(int initialCapacity) 初始化。  正例：initialCapacity &#x3D; (需要存储的元素个数 &#x2F; 负载因子) + 1。注意负载因子（即loader factor）默认 为0.75，如果暂时无法确定初始值大小，请设置为16（即默认值）。  反例：HashMap需要放置1024个元素，由于没有设置容量初始大小，随着元素不断增加，容量7次被 迫扩大，resize 需要重建hash表，严重影响性能。  15. 【推荐】使用entrySet遍历Map类集合KV，而不是keySet方式进行遍历。  说明：keySet其实是遍历了2次，一次是转为Iterator对象，另一次是从hashMap中取出key所对应 的value。而entrySet 只是遍历了一次就把key和value都放到了entry中，效率更高。如果是JDK8， 使用Map.forEach 方法。</p><p>【推荐】高度注意Map类集合K&#x2F;V能不能存储null值的情况，如下表格：  </p><p><img src="C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250829163330284.png" alt="image-20250829163330284"></p><p>反例：由于HashMap的干扰，很多人认为ConcurrentHashMap是可以置入null值，而事实上，存储 null 值时会抛出NPE异常。</p><p>【参考】合理利用好集合的有序性(sort)和稳定性(order)，避免集合的无序性(unsort)和不稳 定性(unorder)带来的负面影响。  说明：有序性是指遍历的结果是按某种比较规则依次排列的。稳定性指集合每次遍历的元素次序是一定 的。如：ArrayList 是order&#x2F;unsort；HashMap 是 unorder&#x2F;unsort；TreeSet 是 order&#x2F;sort。  </p><h3 id="并发处理"><a href="#并发处理" class="headerlink" title="并发处理"></a>并发处理</h3><p>【强制】线程池不允许使用Executors去创建，而是通过ThreadPoolExecutor的方式，这 样的处理方式让写的同学更加明确线程池的运行规则，规避资源耗尽的风险。  说明：Executors 返回的线程池对象的弊端如下： </p><p> 1） FixedThreadPool 和 SingleThreadPool：  允许的请求队列长度为Integer.MAX_VALUE，可能会堆积大量的请求，从而导致OOM。</p><p>  2） CachedThreadPool：  允许的创建线程数量为Integer.MAX_VALUE，可能会创建大量的线程，从而导致OOM。 </p><p>【强制】SimpleDateFormat 是线程不安全的类，一般不要定义为static变量，如果定义为 static，必须加锁，或者使用DateUtils工具类。  正例：注意线程安全，使用DateUtils。亦推荐如下处理：  private static final ThreadLocal df &#x3D; new ThreadLocal() {        @Override         protected DateFormat initialValue() {             return new SimpleDateFormat(“yyyy-MM-dd”);         }     };</p><p>​     说明：如果是JDK8的应用，可以使用Instant代替Date，LocalDateTime代替Calendar， DateTimeFormatter 代替 SimpleDateFormat，官方给出的解释：simple beautiful strong immutable  thread-safe。</p><p><strong>强调</strong></p><p><img src="C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250829171011512.png" alt="image-20250829171011512"></p><p><img src="C:\Users\admin\AppData\Roaming\Typora\typora-user-images\image-20250829171235641.png" alt="image-20250829171235641"></p><p>【强制】并发修改同一记录时，避免更新丢失，需要加锁。要么在应用层加锁，要么在缓存 加锁，要么在数据库层使用乐观锁，使用version作为更新依据。  说明：如果每次访问冲突概率小于20%，推荐使用乐观锁，否则使用悲观锁。乐观锁的重试次数不得小于 3 次。  </p><p>【强制】多线程并行处理定时任务时，Timer运行多个TimeTask时，只要其中之一没有捕获 抛出的异常，其它任务便会自动终止运行，如果在处理定时任务时使用 ScheduledExecutorService 则没有这个问题。    </p><p>【推荐】资金相关的金融敏感信息，使用悲观锁策略。  说明：乐观锁在获得锁的同时已经完成了更新操作，校验逻辑容易出现漏洞，另外，乐观锁对冲突的解决 策略有较复杂的要求，处理不当容易造成系统压力或数据异常，所以资金相关的金融敏感信息不建议使用 乐观锁更新。 </p><p>【推荐】使用CountDownLatch进行异步转同步操作，每个线程退出前必须调用countDown 方法，线程执行代码注意catch异常，确保countDown方法被执行到，避免主线程无法执行 至await 方法，直到超时才返回结果。  说明：注意，子线程抛出异常堆栈，不能在主线程try-catch到。</p><p><strong>【推荐】避免Random实例被多线程使用，虽然共享该实例是线程安全的，但会因竞争同一 seed 导致的性能下降。  说明：Random实例包括java.util.Random 的实例或者 Math.random()的方式。  正例：在JDK7之后，可以直接使用API ThreadLocalRandom，而在 JDK7之前，需要编码保证每个线 程持有一个实例。</strong></p><p>【推荐】在并发场景下，通过双重检查锁（double-checked locking）实现延迟初始化的优化 问题隐患(可参考 The “Double-Checked Locking is Broken” Declaration)，推荐解决方案中较为 简单一种（适用于JDK5及以上版本），将目标属性声明为 volatile型。  </p><p>反例：  public class LazyInitDemo {    private Helper helper &#x3D; null;   public Helper getHelper() {   if (helper &#x3D;&#x3D; null) synchronized(this) {       if (helper &#x3D;&#x3D; null)          helper &#x3D; new Helper();     }       return helper;   }   &#x2F;&#x2F; other methods and fields…   }  </p><p>【参考】volatile 解决多线程内存不可见问题。对于一写多读，是可以解决变量同步问题，但 是如果多写，同样无法解决线程安全问题。  说明：如果是count++操作，使用如下类实现：AtomicInteger count &#x3D; new AtomicInteger();  count.addAndGet(1); 如果是JDK8，推荐使用**LongAdder **对象，比AtomicLong性能更好（减少乐观 锁的重试次数）。  </p><p>【参考】HashMap在容量不够进行resize时由于高并发可能出现死链（链表成环），导致CPU飙升，在 开发过程中可以使用其它数据结构或加锁来规避此风险。  </p><p>【参考】ThreadLocal 对象使用static修饰，ThreadLocal 无法解决共享对象的更新问题。  说明：这个变量是针对一个线程内所有操作共享的，所以设置为静态变量，所有此类实例共享此静态变 量，也就是说在类第一次被使用时装载，只分配一块存储空间，所有此类的对象(只要是这个线程内定义 的)都可以操控这个变量。</p><p>【强制】当switch括号内的变量类型为String并且此变量为外部参数时，必须先进行null 判断。</p><p>  反例：猜猜下面的代码输出是什么？  </p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SwitchString</span> &#123; </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123; </span><br><span class="line">method(<span class="literal">null</span>); </span><br><span class="line">&#125; </span><br><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">method</span><span class="params">(String param)</span> &#123; </span><br><span class="line"><span class="keyword">switch</span> (param) &#123; </span><br><span class="line"><span class="comment">// 肯定不是进入这里 </span></span><br><span class="line"><span class="keyword">case</span> <span class="string">&quot;sth&quot;</span>: </span><br><span class="line">System.out.println(<span class="string">&quot;it&#x27;s sth&quot;</span>); </span><br><span class="line"><span class="keyword">break</span>; </span><br><span class="line"><span class="comment">// 也不是进入这里 </span></span><br><span class="line"><span class="keyword">case</span> <span class="string">&quot;null&quot;</span>: </span><br><span class="line">System.out.println(<span class="string">&quot;it&#x27;s null&quot;</span>); </span><br><span class="line"><span class="keyword">break</span>; </span><br><span class="line"><span class="comment">// 也不是进入这里 </span></span><br><span class="line"><span class="keyword">default</span>: </span><br><span class="line">System.out.println(<span class="string">&quot;default&quot;</span>); </span><br><span class="line">&#125; </span><br><span class="line">&#125; </span><br><span class="line">&#125; </span><br></pre></td></tr></table></figure><p>当<code>switch</code>语句的表达式是<code>String</code>类型且值为<code>null</code>时，会直接抛出<code>NullPointerException</code>，因为<code>switch</code>对字符串的处理依赖于<code>equals()</code>方法和<code>hashCode()</code>方法，而<code>null</code>调用这些方法会导致空指针异常。</p><h3 id="异常处理"><a href="#异常处理" class="headerlink" title="异常处理"></a>异常处理</h3><p>【推荐】防止NPE，是程序员的基本修养，注意NPE产生的场景：  </p><p>1） 返回类型为基本数据类型，return包装数据类型的对象时，自动拆箱有可能产生NPE。  反例：public int f() { return Integer 对象}， 如果为 null，自动解箱抛 NPE。</p><p>  2） 数据库的查询结果可能为null。</p><p>  3） 集合里的元素即使isNotEmpty，取出的数据元素也可能为null。</p><p>  4） 远程调用返回对象时，一律要求进行空指针判断，防止NPE。</p><p>  5） 对于Session中获取的数据，建议进行NPE检查，避免空指针。</p><p>  6） 级联调用obj.getA().getB().getC()；一连串调用，易产生NPE。  正例：使用JDK8的Optional类来防止NPE问题。 </p><h3 id="Mysql数据库"><a href="#Mysql数据库" class="headerlink" title="Mysql数据库"></a>Mysql数据库</h3><p>【强制】表名、字段名必须使用小写字母或数字，禁止出现数字开头，禁止两个下划线中间 只出现数字。数据库字段名的修改代价很大，因为无法进行预发布，所以字段名称需要慎重 考虑。  说明：MySQL在Windows下不区分大小写，但在Linux下默认是区分大小写。因此，数据库名、表 名、字段名，都不允许出现任何大写字母，避免节外生枝。 </p><p>【强制】主键索引名为pk_字段名；唯一索引名为uk_字段名；普通索引名则为idx_字段名。  说明：pk_ 即primary key；uk_ 即 unique key；idx_ 即index的简称。 </p><p>【强制】小数类型为decimal，禁止使用float和double。  说明：在存储的时候，float 和 double 都存在精度损失的问题，很可能在比较值的时候，得到不正确的 结果。如果存储的数据范围超过 decimal 的范围，建议将数据拆成整数和小数并分开存储。</p><p>【强制】表必备三字段：id, create_time, update_time。  说明：其中id必为主键，类型为bigint unsigned、单表时自增、步长为1。create_time, update_time 的类型均为datetime类型。 </p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>学习《Java开发手册（华山版）》总结</title>
      <link href="/2025/08/29/%E5%AD%A6%E4%B9%A0%E3%80%8AJava%E5%BC%80%E5%8F%91%E6%89%8B%E5%86%8C%EF%BC%88%E5%8D%8E%E5%B1%B1%E7%89%88%EF%BC%89%E3%80%8B%E6%80%BB%E7%BB%93/"/>
      <url>/2025/08/29/%E5%AD%A6%E4%B9%A0%E3%80%8AJava%E5%BC%80%E5%8F%91%E6%89%8B%E5%86%8C%EF%BC%88%E5%8D%8E%E5%B1%B1%E7%89%88%EF%BC%89%E3%80%8B%E6%80%BB%E7%BB%93/</url>
      
        <content type="html"><![CDATA[<h1 id="学习《Java开发手册（华山版）》总结"><a href="#学习《Java开发手册（华山版）》总结" class="headerlink" title="学习《Java开发手册（华山版）》总结"></a>学习《Java开发手册（华山版）》总结</h1><p>《Java开发手册（华山版）》作为阿里巴巴技术团队与社区开发者共同打磨的实践指南，涵盖编程、异常日志、单元测试、安全、MySQL数据库、工程结构、设计七大核心维度，以“强制”“推荐”“参考”三级约束力明确规范，兼具理论指导性与实战落地性。</p><h2 id="一、编程规约：奠定代码质量基石"><a href="#一、编程规约：奠定代码质量基石" class="headerlink" title="一、编程规约：奠定代码质量基石"></a>一、编程规约：奠定代码质量基石</h2><p>编程规约是手册的核心，从命名、常量、格式到OOP、集合、并发等，全方位规范代码编写，旨在解决“代码可读性差、协作效率低、潜在风险多”的问题。</p><h3 id="（一）命名与格式"><a href="#（一）命名与格式" class="headerlink" title="（一）命名与格式"></a>（一）命名与格式</h3><p>命名风格上，“强制”要求类名用UpperCamelCase（DO&#x2F;BO等特殊类除外）、方法&#x2F;变量用lowerCamelCase、常量全大写且下划线分隔，严禁拼音与英文混合（如“DaZhePromotion”）或直接用中文。这一规则看似基础，却能避免团队协作中“望文不知义”的困境——例如将“getPingfenByName()”改为“getScoreByName()”，可直接降低代码理解成本。</p><p>代码格式方面，4个空格缩进、单行不超过120字符、运算符前后加空格、注释双斜线后留1个空格等细节，看似“繁琐”，实则是统一编码风格的关键。尤其是“左大括号前不换行、后换行，右大括号前换行”的规则，能让代码结构更清晰，减少因格式混乱导致的逻辑误读。</p><h3 id="（二）OOP与集合"><a href="#（二）OOP与集合" class="headerlink" title="（二）OOP与集合"></a>（二）OOP与集合</h3><p>OOP规约中，多个“强制”条款直指开发痛点：例如覆写方法必须加@Override，避免因方法签名差异（如“getObject()”与“get0bject()”）导致的覆写失败；POJO类属性用包装数据类型（如Integer而非int），防止RPC调用或数据库查询时因自动拆箱抛出NPE；浮点数比较禁用“&#x3D;&#x3D;”或equals，需通过误差范围（如1e-6）或BigDecimal（用String构造方法）实现精确判断。</p><p>集合处理则重点强调“避免并发风险与类型异常”：foreach循环中禁止增删元素，需用Iterator；ArrayList的subList是视图而非独立集合，修改原集合会导致子列表抛出ConcurrentModificationException；Map遍历优先用entrySet（避免keySet二次查询value），且需注意不同Map对null的支持差异（如ConcurrentHashMap不允许key&#x2F;value为null）。</p><h3 id="（三）并发处理"><a href="#（三）并发处理" class="headerlink" title="（三）并发处理"></a>（三）并发处理</h3><p>并发部分是高并发场景的“避坑指南”：线程池必须通过ThreadPoolExecutor手动创建，禁用Executors（避免FixedThreadPool队列溢出或CachedThreadPool线程过多导致OOM）；SimpleDateFormat线程不安全，需用ThreadLocal封装或JDK8的DateTimeFormatter替代；ThreadLocal使用后必须在finally中remove，防止线程池复用导致内存泄露。</p><p>此外，“锁的粒度控制”与“死锁规避”尤为关键——能锁代码块就不锁方法、能用对象锁就不用类锁；多资源加锁需保持顺序一致（如先锁表A再锁表B），避免交叉锁导致死锁。这些规则直接关联系统稳定性，例如某项目因未控制锁粒度，在高并发下频繁出现锁等待，优化后TPS提升30%。</p><h2 id="二、异常日志与单元测试"><a href="#二、异常日志与单元测试" class="headerlink" title="二、异常日志与单元测试"></a>二、异常日志与单元测试</h2><p>异常日志与单元测试是系统“排障”与“质量把关”的核心环节，手册对其规范细致且具有极强的可操作性。</p><h3 id="（一）异常处理：不只是“catch”那么简单"><a href="#（一）异常处理：不只是“catch”那么简单" class="headerlink" title="（一）异常处理：不只是“catch”那么简单"></a>（一）异常处理：不只是“catch”那么简单</h3><p>异常处理的核心原则是“精准捕获、合理传递、清晰记录”：严禁用异常做流程控制（如用NullPointerException判断空值）；catch异常后必须处理（如记录日志或抛给上层），禁止“捕获后不处理”；RPC&#x2F;反射调用需捕获Throwable，避免因NoSuchMethodError等Error导致程序崩溃。</p><p>同时，防止NPE是“基础修养”：级联调用（如obj.getA().getB()）需拆解判断或用JDK8的Optional；POJO类不设默认值，避免更新时误改未赋值字段（如createTime默认new Date()导致数据混乱）。这些规则能大幅减少线上“莫名其妙”的NPE故障。</p><h3 id="（二）日志规约：规范记录，便于排障"><a href="#（二）日志规约：规范记录，便于排障" class="headerlink" title="（二）日志规约：规范记录，便于排障"></a>（二）日志规约：规范记录，便于排障</h3><p>日志规范围绕“性能”与“可读性”展开：必须依赖SLF4J门面，禁用Log4j&#x2F;Logback直接API，便于日志框架切换；日志输出用占位符（如“id: {}”）而非字符串拼接，减少StringBuilder开销；info&#x2F;trace&#x2F;debug日志需先判断级别（如logger.isDebugEnabled()），避免无用参数计算。</p><p>此外，日志保存周期与命名也有明确要求：普通日志保存15天，安全&#x2F;敏感操作日志保存6个月；日志文件名需含应用名、类型、描述（如“force_web_timeZoneConvert.log”），便于快速定位日志来源。某项目曾因日志命名混乱，排查线上问题时花费2小时才找到对应日志，规范后可在5分钟内定位。</p><h3 id="（三）单元测试：不是“测试同学的事”"><a href="#（三）单元测试：不是“测试同学的事”" class="headerlink" title="（三）单元测试：不是“测试同学的事”"></a>（三）单元测试：不是“测试同学的事”</h3><p>单元测试的AIR原则（Automatic自动化、Independent独立性、Repeatable可重复性）是核心：测试用例需全自动执行，禁用System.out验证，必须用assert断言；用例之间禁止依赖（如method2不依赖method1的执行结果）；禁止依赖外部环境（如数据库数据），需用Mock或内存数据模拟。</p><p>手册还明确“测试粒度”与“覆盖率要求”：单测粒度至方法级，核心模块语句覆盖率需达100%；编写用例需覆盖边界值（如循环边界、特殊时间点）、错误输入（如非法参数）。例如某DAO层方法因未测试“查询结果为null”的场景，线上出现NPE，补充用例后问题彻底解决。</p><h2 id="三、安全与MySQL"><a href="#三、安全与MySQL" class="headerlink" title="三、安全与MySQL"></a>三、安全与MySQL</h2><p>安全规约聚焦“防注入、防泄露、防滥用”，MySQL规约则围绕“建表、索引、SQL”优化，两者均直接关联业务安全与系统性能。</p><h3 id="（一）安全规约：堵住“漏洞”入口"><a href="#（一）安全规约：堵住“漏洞”入口" class="headerlink" title="（一）安全规约：堵住“漏洞”入口"></a>（一）安全规约：堵住“漏洞”入口</h3><p>安全是“底线”，手册的“强制”条款直指高频风险点：用户输入必须校验（如pageSize限制大小、防止SQL注入）；SQL参数绑定用#{}而非${}，禁止字符串拼接SQL；表单&#x2F;AJAX提交需做CSRF校验，防止跨站请求伪造；敏感数据展示需脱敏（如手机号显示“137****0969”）。</p><p>此外，“防重放机制”不可忽视——短信&#x2F;支付等接口需加验证码、频率限制（如1分钟内最多发3条短信），避免资源滥用。某项目曾因未做短信防刷，被恶意攻击导致单日短信费用超10万元，优化后成本降至正常水平。</p><h3 id="（二）MySQL规约：从“能用”到“好用”"><a href="#（二）MySQL规约：从“能用”到“好用”" class="headerlink" title="（二）MySQL规约：从“能用”到“好用”"></a>（二）MySQL规约：从“能用”到“好用”</h3><p>MySQL规约覆盖“建表、索引、SQL”全流程，每一条都对应性能或数据安全问题：</p><ul><li><strong>建表</strong>：必含id（bigint unsigned）、create_time、update_time；小数用decimal禁用float&#x2F;double；varchar长度超5000时拆分为text表，避免影响索引效率。</li><li><strong>索引</strong>：唯一特性字段必建唯一索引（如用户手机号）；varchar索引需指定长度（如idx_name(20)），平衡区分度与性能；禁止左模糊查询（如like “%name”），避免索引失效。</li><li><strong>SQL</strong>：禁用select <em>，只查必要字段；count行数用count(</em>)而非count(列名)；禁止外键与级联，外键逻辑在应用层实现（避免分布式场景下死锁）。</li></ul><p>某电商项目曾因未建唯一索引，导致用户重复下单；优化后建唯一索引（user_id+order_no），彻底解决该问题。此外，“分库分表阈值”（单表500万行或2GB）也为架构演进提供了明确依据，避免过早分库分表增加复杂度。</p><h2 id="四、工程结构与设计"><a href="#四、工程结构与设计" class="headerlink" title="四、工程结构与设计"></a>四、工程结构与设计</h2><p>工程结构与设计规约是“从代码到架构”的升华，确保系统“可维护、可扩展”。</p><h3 id="（一）工程结构：分层清晰，依赖有序"><a href="#（一）工程结构：分层清晰，依赖有序" class="headerlink" title="（一）工程结构：分层清晰，依赖有序"></a>（一）工程结构：分层清晰，依赖有序</h3><p>应用分层遵循“上层依赖下层，禁止跨层依赖”：开放接口层（RPC&#x2F;HTTP）→Web层（参数校验、转发）→Service层（业务逻辑）→Manager层（第三方封装、缓存处理）→DAO层（数据访问）。每层职责明确，例如Web层不处理复杂业务，Service层不直接调用DAO层（需通过Manager层复用）。</p><p>二方库依赖管理也尤为关键：GAV命名需规范（如GroupID为“com.taobao.jstorm”）；版本号遵循“主版本.次版本.修订号”（如1.3.4）；线上禁用SNAPSHOT版本，避免依赖不稳定包。某项目曾因二方库版本冲突，导致线上类加载异常，规范依赖后未再出现类似问题。</p><h3 id="（二）设计规约：从“能用”到“优雅”"><a href="#（二）设计规约：从“能用”到“优雅”" class="headerlink" title="（二）设计规约：从“能用”到“优雅”"></a>（二）设计规约：从“能用”到“优雅”</h3><p>设计规约强调“结构化设计、隔离变化”：需求分析阶段，User Case超5个需用例图、状态超3个需状态图、调用链路超3个需时序图，确保需求与设计清晰可追溯；系统设计需遵循单一职责（一个类只做一件事）、依赖倒置（依赖抽象而非具体实现）、开闭原则（对扩展开放、对修改关闭）。</p><p>此外，“避免过度设计”与“重视异常流程”同样重要——不盲目引入设计模式，需结合业务复杂度；付款、订单等核心流程，必须考虑异常场景（如银行扣款成功但订单未更新）。某项目因未考虑“支付回调超时”场景，导致多笔订单状态不一致，优化后通过“重试+补偿”机制解决。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>CountDownLatch 和CompletableFuture</title>
      <link href="/2025/08/29/CountDownLatch-%E5%92%8CCompletableFuture/"/>
      <url>/2025/08/29/CountDownLatch-%E5%92%8CCompletableFuture/</url>
      
        <content type="html"><![CDATA[<h2 id="CountDownLatch-和CompletableFuture"><a href="#CountDownLatch-和CompletableFuture" class="headerlink" title="CountDownLatch 和CompletableFuture"></a>CountDownLatch 和CompletableFuture</h2><p>在Java并发编程中，<code>CountDownLatch</code>和<code>CompletableFuture</code>都是处理多线程协作的重要工具，但它们的设计目标和应用场景有所不同。下面是两者的详细说明及代码示例：</p><h3 id="一、CountDownLatch"><a href="#一、CountDownLatch" class="headerlink" title="一、CountDownLatch"></a>一、CountDownLatch</h3><p><code>CountDownLatch</code>是基于AQS（抽象队列同步器）的同步工具，用于<strong>等待一组线程完成后再执行主线程</strong>。它通过一个计数器实现：</p><ul><li>初始化时设置计数器值（线程数量）</li><li>每个线程完成任务后调用<code>countDown()</code>递减计数器</li><li>主线程调用<code>await()</code>阻塞等待，直到计数器变为0</li></ul><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.CountDownLatch;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CountDownLatchDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 初始化计数器为3（需要等待3个线程完成）</span></span><br><span class="line">        <span class="type">CountDownLatch</span> <span class="variable">latch</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">CountDownLatch</span>(<span class="number">3</span>);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 启动3个工作线程</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">1</span>; i &lt;= <span class="number">3</span>; i++) &#123;</span><br><span class="line">            <span class="keyword">final</span> <span class="type">int</span> <span class="variable">taskId</span> <span class="operator">=</span> i;</span><br><span class="line">            <span class="keyword">new</span> <span class="title class_">Thread</span>(() -&gt; &#123;</span><br><span class="line">                <span class="keyword">try</span> &#123;</span><br><span class="line">                    System.out.println(<span class="string">&quot;任务&quot;</span> + taskId + <span class="string">&quot;开始执行&quot;</span>);</span><br><span class="line">                    Thread.sleep(<span class="number">1000</span> * taskId); <span class="comment">// 模拟不同耗时的任务</span></span><br><span class="line">                    System.out.println(<span class="string">&quot;任务&quot;</span> + taskId + <span class="string">&quot;执行完成&quot;</span>);</span><br><span class="line">                &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                    e.printStackTrace();</span><br><span class="line">                &#125; <span class="keyword">finally</span> &#123;</span><br><span class="line">                    latch.countDown(); <span class="comment">// 任务完成，计数器减1</span></span><br><span class="line">                &#125;</span><br><span class="line">            &#125;).start();</span><br><span class="line">        &#125;</span><br><span class="line">        </span><br><span class="line">        System.out.println(<span class="string">&quot;主线程等待所有任务完成...&quot;</span>);</span><br><span class="line">        latch.await(); <span class="comment">// 阻塞等待计数器变为0</span></span><br><span class="line">        System.out.println(<span class="string">&quot;所有任务已完成，主线程继续执行&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>应用场景</strong>：</p><ul><li>主线程等待多个子线程初始化完成后再启动</li><li>批量任务执行完毕后汇总结果</li><li>模拟并发测试（让所有线程准备就绪后同时执行）</li></ul><p><strong>特点</strong>：</p><ul><li>计数器只能使用一次，不能重置</li><li>是非阻塞的线程间通信方式</li><li>适合”一对多”的等待场景</li></ul><h3 id="二、CompletableFuture"><a href="#二、CompletableFuture" class="headerlink" title="二、CompletableFuture"></a>二、CompletableFuture</h3><p><code>CompletableFuture</code>是Java 8引入的异步编程工具，基于Future模式扩展，支持<strong>异步任务的链式执行、结果处理和异常处理</strong>。它可以：</p><ul><li>异步执行任务并获取结果</li><li>多个异步任务串行或并行组合</li><li>自动处理任务完成后的回调逻辑</li></ul><p><strong>常用API</strong>：</p><ul><li><code>runAsync()</code>：异步执行Runnable任务（无返回值）</li><li><code>supplyAsync()</code>：异步执行Supplier任务（有返回值）</li><li><code>thenApply()</code>：任务完成后处理结果（有返回值）</li><li><code>thenAccept()</code>：任务完成后消费结果（无返回值）</li><li><code>thenCompose()</code>：串联两个异步任务</li><li><code>allOf()</code>：等待所有任务完成</li><li><code>anyOf()</code>：等待任一任务完成</li></ul><p>​</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.util.concurrent.CompletableFuture;</span><br><span class="line"><span class="keyword">import</span> java.util.concurrent.ExecutionException;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CompletableFutureDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> <span class="keyword">throws</span> ExecutionException, InterruptedException &#123;</span><br><span class="line">        <span class="comment">// 1. 异步执行任务（无返回值）</span></span><br><span class="line">        CompletableFuture&lt;Void&gt; future1 = CompletableFuture.runAsync(() -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;异步任务1执行: &quot;</span> + Thread.currentThread().getName());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">1000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 2. 异步执行任务（有返回值）</span></span><br><span class="line">        CompletableFuture&lt;String&gt; future2 = CompletableFuture.supplyAsync(() -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;异步任务2执行: &quot;</span> + Thread.currentThread().getName());</span><br><span class="line">            <span class="keyword">try</span> &#123;</span><br><span class="line">                Thread.sleep(<span class="number">2000</span>);</span><br><span class="line">            &#125; <span class="keyword">catch</span> (InterruptedException e) &#123;</span><br><span class="line">                e.printStackTrace();</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">return</span> <span class="string">&quot;任务2结果&quot;</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 3. 任务2完成后执行回调（链式操作）</span></span><br><span class="line">        CompletableFuture&lt;String&gt; future3 = future2.thenApply(result -&gt; &#123;</span><br><span class="line">            System.out.println(<span class="string">&quot;接收任务2结果: &quot;</span> + result);</span><br><span class="line">            <span class="keyword">return</span> result + <span class="string">&quot; -&gt; 处理后&quot;</span>;</span><br><span class="line">        &#125;);</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 4. 等待所有任务完成</span></span><br><span class="line">        CompletableFuture.allOf(future1, future3).join();</span><br><span class="line">        </span><br><span class="line">        <span class="comment">// 获取最终结果</span></span><br><span class="line">        System.out.println(<span class="string">&quot;最终结果: &quot;</span> + future3.get());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>应用场景</strong>：</p><ul><li>异步IO操作（如网络请求、文件读写）</li><li>复杂的异步任务编排（串行、并行、分支）</li><li>非阻塞的结果处理（避免主线程阻塞）</li><li>函数式编程风格的并发代码</li></ul><p><strong>特点</strong>：</p><ul><li>支持任务间的依赖关系和流水线操作</li><li>内置异常处理机制（<code>exceptionally()</code>）</li><li>可与线程池结合使用，控制并发资源</li></ul><h3 id="三、两者对比与选择"><a href="#三、两者对比与选择" class="headerlink" title="三、两者对比与选择"></a>三、两者对比与选择</h3><table><thead><tr><th>特性</th><th>CountDownLatch</th><th>CompletableFuture</th></tr></thead><tbody><tr><td>核心功能</td><td>等待多个线程完成</td><td>异步任务执行与结果处理</td></tr><tr><td>灵活性</td><td>功能单一（仅等待）</td><td>支持复杂的任务组合和链式操作</td></tr><tr><td>结果处理</td><td>不直接支持结果传递</td><td>原生支持结果处理和转换</td></tr><tr><td>重用性</td><td>计数器不可重置，一次性使用</td><td>可多次组合使用</td></tr><tr><td>适用场景</td><td>简单的等待多线程完成场景</td><td>复杂的异步任务编排和结果处理</td></tr></tbody></table><p><strong>选择建议</strong>：</p><ul><li>若只需等待一组线程完成后继续执行，用<code>CountDownLatch</code></li><li>若需要处理异步任务的结果、组合多个任务，用<code>CompletableFuture</code></li><li>并发编程中，<code>CompletableFuture</code>更符合现代异步编程模式，应用更广泛</li></ul><p>在实际开发中，两者也可结合使用，例如用<code>CompletableFuture</code>处理异步任务，用<code>CountDownLatch</code>控制整体等待逻辑。</p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>es学习总结</title>
      <link href="/2025/08/29/es%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/"/>
      <url>/2025/08/29/es%E5%AD%A6%E4%B9%A0%E6%80%BB%E7%BB%93/</url>
      
        <content type="html"><![CDATA[<h1 id="elasticsearch"><a href="#elasticsearch" class="headerlink" title="elasticsearch"></a>elasticsearch</h1><ul><li>一个开源的分布式搜索引擎，可以用来实现搜索、日志统计、分析、系统监控等功能</li></ul><p></p><h4 id="1-创建索引库和映射"><a href="#1-创建索引库和映射" class="headerlink" title="1.  创建索引库和映射"></a>1.  创建索引库和映射</h4><p><strong>基本语法：</strong></p><ul><li>请求方式：PUT</li><li>请求路径：&#x2F;索引库名，可以自定义<ul><li>请求参数：mapping映射</li></ul></li></ul><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line">PUT /索引库名称</span><br><span class="line">&#123;</span><br><span class="line">  &quot;mappings&quot;: &#123;</span><br><span class="line">    &quot;properties&quot;: &#123;</span><br><span class="line">      &quot;字段名&quot;:&#123;</span><br><span class="line">        &quot;type&quot;: &quot;text&quot;,</span><br><span class="line">        &quot;analyzer&quot;: &quot;ik_smart&quot;</span><br><span class="line">      &#125;,</span><br><span class="line">      &quot;字段名2&quot;:&#123;</span><br><span class="line">        &quot;type&quot;: &quot;keyword&quot;,</span><br><span class="line">        &quot;index&quot;: &quot;false&quot;</span><br><span class="line">      &#125;,</span><br><span class="line">      &quot;字段名3&quot;:&#123;</span><br><span class="line">        &quot;properties&quot;: &#123;</span><br><span class="line">          &quot;子字段&quot;: &#123;</span><br><span class="line">            &quot;type&quot;: &quot;keyword&quot;</span><br><span class="line">          &#125;</span><br><span class="line">        &#125;</span><br><span class="line">      &#125;,</span><br><span class="line">      // ...略</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="2-查询索引库"><a href="#2-查询索引库" class="headerlink" title="2.  查询索引库"></a>2.  查询索引库</h4><p><strong>基本语法</strong>：</p><ul><li>请求方式：GET</li><li>请求路径：&#x2F;索引库名</li><li>请求参数：无</li></ul><p><code>GET /索引库名</code></p><h4 id="3-修改索引库"><a href="#3-修改索引库" class="headerlink" title="3. 修改索引库"></a>3. 修改索引库</h4><p>这里的修改是只能增加新的字段到mapping中</p><p>倒排索引结构虽然不复杂，但是一旦数据结构改变（比如改变了分词器），就需要重新创建倒排索引，这简直是灾难。因此索引库一旦创建，无法修改mapping。</p><p>虽然无法修改mapping中已有的字段，但是却允许添加新的字段到mapping中，因为不会对倒排索引产生影响。</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">PUT /索引库名/_mapping</span><br><span class="line">&#123;</span><br><span class="line">  &quot;properties&quot;: &#123;</span><br><span class="line">    &quot;新字段名&quot;:&#123;</span><br><span class="line">      &quot;type&quot;: &quot;integer&quot;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-删除索引库"><a href="#4-删除索引库" class="headerlink" title="4. 删除索引库"></a>4. 删除索引库</h4><p><strong>语法：</strong></p><ul><li>请求方式：DELETE</li><li>请求路径：&#x2F;索引库名</li><li>请求参数：无</li></ul><p><code>DELETE /索引库名</code></p>]]></content>
      
      
      <categories>
          
          <category> 学习总结 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>TCP三次握手，四次拒绝</title>
      <link href="/2025/08/08/TCP%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B%EF%BC%8C%E5%9B%9B%E6%AC%A1%E6%8B%92%E7%BB%9D/"/>
      <url>/2025/08/08/TCP%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B%EF%BC%8C%E5%9B%9B%E6%AC%A1%E6%8B%92%E7%BB%9D/</url>
      
        <content type="html"><![CDATA[<h2 id="TCP三次握手，四次拒绝"><a href="#TCP三次握手，四次拒绝" class="headerlink" title="TCP三次握手，四次拒绝"></a>TCP三次握手，四次拒绝</h2><p><img src="/img_posts/image-20250808013842100.png" alt="image-20250808013842100"></p><p><img src="/img_posts/image-20250808013951725.png" alt="image-20250808013951725"></p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>可能的AI面试清单</title>
      <link href="/2025/07/09/%E5%8F%AF%E8%83%BD%E7%9A%84AI%E9%9D%A2%E8%AF%95%E6%B8%85%E5%8D%95/"/>
      <url>/2025/07/09/%E5%8F%AF%E8%83%BD%E7%9A%84AI%E9%9D%A2%E8%AF%95%E6%B8%85%E5%8D%95/</url>
      
        <content type="html"><![CDATA[<h1 id="可能的AI面试清单"><a href="#可能的AI面试清单" class="headerlink" title="可能的AI面试清单"></a>可能的AI面试清单</h1><p>以下是为你准备的<strong>AI Agent 工程师岗位面试清单</strong>，包含重点问题分类、具体问题及参考答案（含技术点、表达逻辑和加分内容）：</p><hr><h2 id="🧠-一、技术能力类问题"><a href="#🧠-一、技术能力类问题" class="headerlink" title="🧠 一、技术能力类问题"></a>🧠 一、技术能力类问题</h2><h3 id="✅-1-什么是-RAG？你怎么优化它的效果？"><a href="#✅-1-什么是-RAG？你怎么优化它的效果？" class="headerlink" title="✅ 1. 什么是 RAG？你怎么优化它的效果？"></a>✅ 1. 什么是 RAG？你怎么优化它的效果？</h3><p><strong>参考答案：</strong></p><blockquote><p>RAG（Retrieval-Augmented Generation）是一种结合检索与生成的技术流程，模型在生成回答前会先从知识库中检索相关内容。<br> 我在项目中使用了 <code>LangChain + FAISS + OpenAI</code> 搭建了 RAG 流程，优化主要通过以下方式：</p></blockquote><ul><li>向量数据库中使用 <code>text-embedding-ada-002</code> 提升语义相关性；</li><li>提高 chunk 粒度控制：根据文档内容用 semantic chunk 而非固定字数；</li><li>在生成阶段引入 prompt 模板加强引用内容与问句的一致性；</li><li>加入 query expansion 和 rerank 模块进一步提高召回精度。</li></ul><p><strong>加分点：</strong></p><ul><li>是否接入企业知识库（如 Notion、Confluence）；</li><li>是否做过检索链路评估（如 Recall@K、BLEU）。</li></ul><hr><h3 id="✅-2-请设计一个-AI-Agent-系统，有多个-Agent-协同工作。"><a href="#✅-2-请设计一个-AI-Agent-系统，有多个-Agent-协同工作。" class="headerlink" title="✅ 2. 请设计一个 AI Agent 系统，有多个 Agent 协同工作。"></a>✅ 2. 请设计一个 AI Agent 系统，有多个 Agent 协同工作。</h3><p><strong>参考答案：</strong></p><blockquote><p>例如构建一个“智能文档处理助手”，可拆分为：</p></blockquote><ul><li><strong>Reader Agent</strong>：提取文档内容，调用 OCR&#x2F;分段；</li><li><strong>Summarizer Agent</strong>：调用大模型生成摘要；</li><li><strong>Reviewer Agent</strong>：负责审查输出是否存在风险；</li><li><strong>Writer Agent</strong>：根据需求生成文档或回答；<br> 各 Agent 通过消息队列通信（如 Kafka），使用状态机管理生命周期。使用 Dify 搭建界面，Langflow 进行流程编排。</li></ul><p><strong>加分点：</strong></p><ul><li>使用 Memory 保持 Agent 状态；</li><li>具备计划（Planning）与执行（Tool-Calling）能力。</li></ul><hr><h3 id="✅-3-你对-langflow、flowise、dify-的使用经验？"><a href="#✅-3-你对-langflow、flowise、dify-的使用经验？" class="headerlink" title="✅ 3. 你对 langflow、flowise、dify 的使用经验？"></a>✅ 3. 你对 langflow、flowise、dify 的使用经验？</h3><p><strong>参考答案：</strong></p><blockquote><p>我分别使用过：</p></blockquote><ul><li><strong>Langflow</strong>：更适合开发调试阶段，支持模块化开发；</li><li><strong>Flowise</strong>：适合前端嵌入和快速部署，节点式配置便捷；</li><li><strong>Dify</strong>：产品化程度高，支持团队协作、权限系统和消息记录；<br> 项目中我用 Dify 构建了内部知识问答系统，支持 API 接入并打通用户工单系统，极大提升响应效率。</li></ul><hr><h2 id="👨‍💻-二、编程-工具类问题"><a href="#👨‍💻-二、编程-工具类问题" class="headerlink" title="👨‍💻 二、编程 &amp; 工具类问题"></a>👨‍💻 二、编程 &amp; 工具类问题</h2><h3 id="✅-4-你用-Python-写过哪些-AI-工具或自动化脚本？"><a href="#✅-4-你用-Python-写过哪些-AI-工具或自动化脚本？" class="headerlink" title="✅ 4. 你用 Python 写过哪些 AI 工具或自动化脚本？"></a>✅ 4. 你用 Python 写过哪些 AI 工具或自动化脚本？</h3><p><strong>参考答案：</strong></p><blockquote><ul><li>使用 <code>requests + OpenAI API</code> 写过 prompt 批量测试工具；</li></ul></blockquote><ul><li>用 <code>LangChain</code> 构建过 agent-chaining 模型；</li><li>用 <code>BeautifulSoup</code> 爬取目标网页知识并自动上传向量库；</li><li>Shell 脚本用于定时清洗日志、监控 API 调用频率等。</li></ul><hr><h3 id="✅-5-你怎么用-Python-实现一个简单的-RAG？"><a href="#✅-5-你怎么用-Python-实现一个简单的-RAG？" class="headerlink" title="✅ 5. 你怎么用 Python 实现一个简单的 RAG？"></a>✅ 5. 你怎么用 Python 实现一个简单的 RAG？</h3><p><strong>参考答案：</strong></p><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">from</span> langchain.embeddings.openai <span class="keyword">import</span> OpenAIEmbeddings</span><br><span class="line"><span class="keyword">from</span> langchain.vectorstores <span class="keyword">import</span> FAISS</span><br><span class="line"><span class="keyword">from</span> langchain.chains <span class="keyword">import</span> RetrievalQA</span><br><span class="line"><span class="keyword">from</span> langchain.llms <span class="keyword">import</span> OpenAI</span><br><span class="line"></span><br><span class="line"><span class="comment"># 加载知识</span></span><br><span class="line">documents = load_my_docs()</span><br><span class="line">db = FAISS.from_documents(documents, OpenAIEmbeddings())</span><br><span class="line"></span><br><span class="line"><span class="comment"># 构建 RAG</span></span><br><span class="line">qa = RetrievalQA.from_chain_type(</span><br><span class="line">    llm=OpenAI(),</span><br><span class="line">    retriever=db.as_retriever()</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(qa.run(<span class="string">&quot;请总结一下该知识点&quot;</span>))</span><br></pre></td></tr></table></figure><hr><h2 id="📊-三、产品分析与用户导向"><a href="#📊-三、产品分析与用户导向" class="headerlink" title="📊 三、产品分析与用户导向"></a>📊 三、产品分析与用户导向</h2><h3 id="✅-6-如何参与-AI-产品的需求分析与用户体验改进？"><a href="#✅-6-如何参与-AI-产品的需求分析与用户体验改进？" class="headerlink" title="✅ 6. 如何参与 AI 产品的需求分析与用户体验改进？"></a>✅ 6. 如何参与 AI 产品的需求分析与用户体验改进？</h3><p><strong>参考答案：</strong></p><blockquote><p>我常用“用户旅程图”分析用户痛点。例如在 AI 问答产品中，用户对回答可信度有疑虑，于是我提出增加来源引用机制；<br> 技术选型方面，我推动使用 <code>RAG</code> 替代单一调用 GPT 模型，从而降低成本并提升可控性；<br> 上线后根据用户点击率和会话长度优化了 prompt 策略。</p></blockquote><hr><h2 id="🧩-四、学习能力-问题解决类"><a href="#🧩-四、学习能力-问题解决类" class="headerlink" title="🧩 四、学习能力 &amp; 问题解决类"></a>🧩 四、学习能力 &amp; 问题解决类</h2><h3 id="✅-7-遇到-Google-找不到答案的问题你怎么解决？"><a href="#✅-7-遇到-Google-找不到答案的问题你怎么解决？" class="headerlink" title="✅ 7. 遇到 Google 找不到答案的问题你怎么解决？"></a>✅ 7. 遇到 Google 找不到答案的问题你怎么解决？</h3><p><strong>参考答案：</strong></p><blockquote><p>我遇到 langchain 中 AgentExecutor 无法处理工具超时的问题，Google 找不到直接答案。我通过阅读其源码定位到 <code>run_tools</code> 的逻辑，通过调整 <code>tool_caller.run()</code> 的异步设置并使用 retry 包实现了稳定调用。<br> 我习惯查官方文档 + GitHub Issue + Discord 社群，并不断实验。</p></blockquote><hr><h2 id="🔍-五、趋势理解类"><a href="#🔍-五、趋势理解类" class="headerlink" title="🔍 五、趋势理解类"></a>🔍 五、趋势理解类</h2><h3 id="✅-8-你如何看待-AI-Agent-的发展趋势？"><a href="#✅-8-你如何看待-AI-Agent-的发展趋势？" class="headerlink" title="✅ 8. 你如何看待 AI Agent 的发展趋势？"></a>✅ 8. 你如何看待 AI Agent 的发展趋势？</h3><p><strong>参考答案：</strong></p><blockquote><p>AI Agent 未来一定朝两个方向演进：</p></blockquote><ol><li><strong>更强的规划与协作能力</strong>：从 LLM + 工具调用走向多模型协同；</li><li><strong>产品化平台化</strong>：如 Dify、Cognosys 等把 Agent 构建和部署变得更简单；<br> 我认为 AI Agent 会成为企业内部流程自动化的重要推动力，而非单一问答应用。</li></ol><hr><h2 id="🎁-六、行为类问题"><a href="#🎁-六、行为类问题" class="headerlink" title="🎁 六、行为类问题"></a>🎁 六、行为类问题</h2><h3 id="✅-9-你曾在项目中如何推进一个技术落地？"><a href="#✅-9-你曾在项目中如何推进一个技术落地？" class="headerlink" title="✅ 9. 你曾在项目中如何推进一个技术落地？"></a>✅ 9. 你曾在项目中如何推进一个技术落地？</h3><p><strong>参考答案：</strong></p><blockquote><p>在公司知识库项目中，我主导了 AI Agent 的引入，从调研到落地共 3 周，过程包括：</p></blockquote><ul><li>与业务沟通痛点；</li><li>快速原型验证（RAG + prompt 优化）；</li><li>向上汇报 ROI，推动资源支持；<br> 最终上线后用户满意度达 92%，节省客服人力 40%。</li></ul><hr><h2 id="📋-面试清单总结"><a href="#📋-面试清单总结" class="headerlink" title="📋 面试清单总结"></a>📋 面试清单总结</h2><table><thead><tr><th>模块</th><th>关键点</th><th>建议准备</th></tr></thead><tbody><tr><td>Prompt &amp; RAG</td><td>技术理解 + 优化实践</td><td>实操项目、常见问题</td></tr><tr><td>Multi-Agent 构建</td><td>Agent 分工 + 调度机制</td><td>Langflow&#x2F;Dify&#x2F;Flowise</td></tr><tr><td>编程能力</td><td>Python + Shell + API</td><td>编写小项目</td></tr><tr><td>平台使用</td><td>Dify&#x2F;Coze</td><td>UI 功能 + 应用场景</td></tr><tr><td>产品意识</td><td>用户视角 + 技术选型</td><td>项目复盘、痛点洞察</td></tr><tr><td>快速学习</td><td>无文档解决问题能力</td><td>GitHub&#x2F;Discord 查证</td></tr><tr><td>趋势理解</td><td>关注 LLM + 工具协同</td><td>OpenAI&#x2F;Mistral&#x2F;GPTs</td></tr><tr><td>行为能力</td><td>项目推动 + 沟通协调</td><td>STAR 法则回答经验题</td></tr></tbody></table><hr><p>如需我帮你把这些问题做成一个面试模拟文档（PDF 或 Word），或针对某个问题给出更详细的 STAR 法则答案，也可以告诉我。是否需要我生成一套 Word 模拟面试文档？</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Ai应用开发面试岗位面经</title>
      <link href="/2025/07/09/Ai%E5%BA%94%E7%94%A8%E5%BC%80%E5%8F%91%E9%9D%A2%E8%AF%95%E5%B2%97%E4%BD%8D%E9%9D%A2%E7%BB%8F/"/>
      <url>/2025/07/09/Ai%E5%BA%94%E7%94%A8%E5%BC%80%E5%8F%91%E9%9D%A2%E8%AF%95%E5%B2%97%E4%BD%8D%E9%9D%A2%E7%BB%8F/</url>
      
        <content type="html"><![CDATA[<h1 id="Ai应用开发面试岗位面经"><a href="#Ai应用开发面试岗位面经" class="headerlink" title="Ai应用开发面试岗位面经"></a>Ai应用开发面试岗位面经</h1><p>同步or异步方式传输数据、用户是怎么保持认证状态的用的token还是JWT</p><p>Mybatis的一级缓存和二级缓存。</p><p>Mybatis是怎么封装sql查询的结果集</p><p>多态<br>static关键字<br>集合相关实现类，arraylist底层<br>java8新增哪些特性<br>定义线程的方法<br>为什么不用executor定义的方法？<br>synchronized关键字<br>如何避免死锁<br>spring常用注解<br>spring中Bean有哪些特点<br>Mybatis如何讲一个sql结果集封装成一个java对象返回<br>mybatis一级缓存和二级缓存<br>什么是事务<br>mysql默认隔离级别<br>优化sql的方案<br>sql的递归查询<br>redis数据类型<br>如果使用git拉取代码下来发现冲突了怎么解决<br>前端js定义一个对象<br>为什么使用分布式，有什么优点<br>如何实现分布式id</p><p>作者：如果清醒是种罪<br>链接：<a href="https://www.nowcoder.com/feed/main/detail/32a66eafe43c47d981131ad2b7157e41?sourceSSR=users">https://www.nowcoder.com/feed/main/detail/32a66eafe43c47d981131ad2b7157e41?sourceSSR=users</a><br>来源：牛客网</p><p>x这是一份后端开发常见的面试问题清单，涵盖了 Java 核心、并发、集合、Spring、MyBatis、数据库、分布式、Redis、Git 及前端基础等方面。以下是每个问题的简洁答案（适合面试回答或复习准备）：</p><hr><h2 id="✅-Java-基础-面向对象"><a href="#✅-Java-基础-面向对象" class="headerlink" title="✅ Java 基础 &amp; 面向对象"></a>✅ <strong>Java 基础 &amp; 面向对象</strong></h2><h3 id="1-多态"><a href="#1-多态" class="headerlink" title="1. 多态"></a>1. 多态</h3><ul><li><strong>定义</strong>：父类引用指向子类对象，调用方法时根据实际对象类型决定执行哪个方法（运行时多态）。</li><li><strong>前提</strong>：继承 + 方法重写 + 父类引用指向子类对象。</li><li><strong>好处</strong>：提高代码扩展性，解耦。</li></ul><hr><h3 id="2-static-关键字"><a href="#2-static-关键字" class="headerlink" title="2. static 关键字"></a>2. static 关键字</h3><ul><li><strong>作用</strong>：表示静态成员，属于类而非对象。</li><li><strong>常见用法</strong>：<ul><li>静态变量：所有对象共享。</li><li>静态方法：不能访问非静态成员。</li><li>静态代码块：类加载时执行一次。</li><li>静态内部类：不依赖外部类对象。</li></ul></li></ul><hr><h2 id="✅-集合-Java-底层"><a href="#✅-集合-Java-底层" class="headerlink" title="✅ 集合 &amp; Java 底层"></a>✅ <strong>集合 &amp; Java 底层</strong></h2><h3 id="3-ArrayList-底层原理"><a href="#3-ArrayList-底层原理" class="headerlink" title="3. ArrayList 底层原理"></a>3. ArrayList 底层原理</h3><ul><li>底层是 <strong>动态数组</strong>，默认初始容量为 10。</li><li>增长策略：容量不够时扩容为原来的 <strong>1.5 倍</strong>。</li><li><strong>线程不安全</strong>，可用 <code>Collections.synchronizedList</code> 或 <code>CopyOnWriteArrayList</code> 实现线程安全。</li></ul><hr><h2 id="✅-Java-8-新特性"><a href="#✅-Java-8-新特性" class="headerlink" title="✅ Java 8 新特性"></a>✅ <strong>Java 8 新特性</strong></h2><h3 id="4-Java8-新增特性"><a href="#4-Java8-新增特性" class="headerlink" title="4. Java8 新增特性"></a>4. Java8 新增特性</h3><ul><li>Lambda 表达式</li><li>函数式接口（@FunctionalInterface）</li><li>Stream 流式处理</li><li>Optional 避免 NullPointerException</li><li>新的时间日期 API（java.time.*）</li><li>接口默认方法（default）、静态方法</li><li>CompletableFuture（异步编程）</li></ul><hr><h2 id="✅-并发编程"><a href="#✅-并发编程" class="headerlink" title="✅ 并发编程"></a>✅ <strong>并发编程</strong></h2><h3 id="5-定义线程的方法"><a href="#5-定义线程的方法" class="headerlink" title="5. 定义线程的方法"></a>5. 定义线程的方法</h3><ul><li>实现 <code>Runnable</code> 接口</li><li>继承 <code>Thread</code> 类</li><li>实现 <code>Callable</code> 接口（配合 Future）</li></ul><hr><h3 id="6-为什么不用-executor-定义的方法？"><a href="#6-为什么不用-executor-定义的方法？" class="headerlink" title="6. 为什么不用 executor 定义的方法？"></a>6. 为什么不用 executor 定义的方法？</h3><ul><li>实际开发推荐使用 Executor 框架（线程池），<strong>避免频繁创建销毁线程，提升性能和资源利用率</strong>。</li><li>不推荐手动 <code>new Thread()</code>，难以管理和扩展。</li></ul><hr><h3 id="7-synchronized-关键字"><a href="#7-synchronized-关键字" class="headerlink" title="7. synchronized 关键字"></a>7. synchronized 关键字</h3><ul><li>实现线程同步的关键字，修饰：<ul><li>实例方法（锁当前对象）</li><li>静态方法（锁类对象）</li><li>代码块（自定义锁对象）</li></ul></li><li>实现原理：JVM 指令加锁，依赖对象监视器（Monitor）</li></ul><hr><h3 id="8-如何避免死锁"><a href="#8-如何避免死锁" class="headerlink" title="8. 如何避免死锁"></a>8. 如何避免死锁</h3><ul><li>避免嵌套锁&#x2F;嵌套 synchronized</li><li><strong>统一加锁顺序</strong></li><li>设置加锁超时时间（如 <code>tryLock</code>）</li><li>尽量使用并发工具类如 <code>Lock</code>, <code>Semaphore</code>, <code>BlockingQueue</code></li></ul><hr><h2 id="✅-Spring-框架"><a href="#✅-Spring-框架" class="headerlink" title="✅ Spring 框架"></a>✅ <strong>Spring 框架</strong></h2><h3 id="9-Spring-常用注解"><a href="#9-Spring-常用注解" class="headerlink" title="9. Spring 常用注解"></a>9. Spring 常用注解</h3><ul><li><code>@Component</code> &#x2F; <code>@Service</code> &#x2F; <code>@Repository</code> &#x2F; <code>@Controller</code></li><li><code>@Autowired</code> &#x2F; <code>@Resource</code> &#x2F; <code>@Qualifier</code></li><li><code>@Configuration</code> &#x2F; <code>@Bean</code></li><li><code>@Value</code> &#x2F; <code>@PropertySource</code></li><li><code>@Transactional</code></li><li><code>@RequestMapping</code>, <code>@GetMapping</code> 等</li></ul><hr><h3 id="10-Spring-中-Bean-有哪些特点"><a href="#10-Spring-中-Bean-有哪些特点" class="headerlink" title="10. Spring 中 Bean 有哪些特点"></a>10. Spring 中 Bean 有哪些特点</h3><ul><li>单例（默认）</li><li>延迟加载（默认）</li><li>生命周期受容器管理</li><li>可通过注解或 XML 进行配置</li></ul><hr><h2 id="✅-MyBatis"><a href="#✅-MyBatis" class="headerlink" title="✅ MyBatis"></a>✅ <strong>MyBatis</strong></h2><h3 id="11-MyBatis-如何将-SQL-结果集封装成-Java-对象"><a href="#11-MyBatis-如何将-SQL-结果集封装成-Java-对象" class="headerlink" title="11. MyBatis 如何将 SQL 结果集封装成 Java 对象"></a>11. MyBatis 如何将 SQL 结果集封装成 Java 对象</h3><ul><li>通过 <code>&lt;resultMap&gt;</code> 或使用 <code>resultType</code></li><li>字段名和 Java 属性不一致时使用 <code>column</code> 映射</li><li>支持嵌套映射（多对一、一对多）</li></ul><hr><h3 id="12-MyBatis-一级缓存和二级缓存"><a href="#12-MyBatis-一级缓存和二级缓存" class="headerlink" title="12. MyBatis 一级缓存和二级缓存"></a>12. MyBatis 一级缓存和二级缓存</h3><ul><li><strong>一级缓存</strong>：SqlSession 级别，默认开启，同一个 session 有缓存。</li><li><strong>二级缓存</strong>：mapper 级别，需要配置开启；跨 session 有效，通常使用第三方缓存实现。</li></ul><hr><h2 id="✅-数据库-SQL"><a href="#✅-数据库-SQL" class="headerlink" title="✅ 数据库 &amp; SQL"></a>✅ <strong>数据库 &amp; SQL</strong></h2><h3 id="13-什么是事务"><a href="#13-什么是事务" class="headerlink" title="13. 什么是事务"></a>13. 什么是事务</h3><ul><li>数据库操作的最小单位，具备 ACID：<ul><li>原子性、一致性、隔离性、持久性</li></ul></li></ul><hr><h3 id="14-MySQL-默认隔离级别"><a href="#14-MySQL-默认隔离级别" class="headerlink" title="14. MySQL 默认隔离级别"></a>14. MySQL 默认隔离级别</h3><ul><li><strong>Repeatable Read</strong>（可重复读）</li></ul><hr><h3 id="15-优化-SQL-的方案"><a href="#15-优化-SQL-的方案" class="headerlink" title="15. 优化 SQL 的方案"></a>15. 优化 SQL 的方案</h3><ul><li>建索引（尤其是 where、join、order by）</li><li>避免 select *</li><li>使用分页查询</li><li>避免函数操作字段</li><li>分表分库、读写分离</li><li>使用执行计划 <code>EXPLAIN</code></li></ul><hr><h3 id="16-SQL-的递归查询"><a href="#16-SQL-的递归查询" class="headerlink" title="16. SQL 的递归查询"></a>16. SQL 的递归查询</h3><ul><li>使用 <code>WITH RECURSIVE</code> 实现层级关系查询（如组织树）</li><li>示例：</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">WITH</span> <span class="keyword">RECURSIVE</span> temp <span class="keyword">AS</span> (</span><br><span class="line">  <span class="keyword">SELECT</span> id, name, parent_id <span class="keyword">FROM</span> category <span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">1</span></span><br><span class="line">  <span class="keyword">UNION</span> <span class="keyword">ALL</span></span><br><span class="line">  <span class="keyword">SELECT</span> c.id, c.name, c.parent_id <span class="keyword">FROM</span> category c</span><br><span class="line">  <span class="keyword">JOIN</span> temp t <span class="keyword">ON</span> c.parent_id <span class="operator">=</span> t.id</span><br><span class="line">)</span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> temp;</span><br></pre></td></tr></table></figure><hr><h2 id="✅-Redis"><a href="#✅-Redis" class="headerlink" title="✅ Redis"></a>✅ <strong>Redis</strong></h2><h3 id="17-Redis-数据类型"><a href="#17-Redis-数据类型" class="headerlink" title="17. Redis 数据类型"></a>17. Redis 数据类型</h3><ul><li>String（字符串）</li><li>List（列表）</li><li>Set（集合）</li><li>Hash（哈希）</li><li>ZSet（有序集合）</li><li>Bitmap、HyperLogLog、Geo 等</li></ul><hr><h2 id="✅-Git"><a href="#✅-Git" class="headerlink" title="✅ Git"></a>✅ <strong>Git</strong></h2><h3 id="18-使用-Git-拉取代码发现冲突怎么解决"><a href="#18-使用-Git-拉取代码发现冲突怎么解决" class="headerlink" title="18. 使用 Git 拉取代码发现冲突怎么解决"></a>18. 使用 Git 拉取代码发现冲突怎么解决</h3><ol><li>拉取代码时报冲突：<ul><li><code>git pull</code> 时提示冲突，需手动解决。</li></ul></li><li>步骤：<ul><li>修改冲突文件，清除冲突标记。</li><li><code>git add 文件名</code></li><li><code>git commit -m &quot;fix conflict&quot;</code></li><li>可选择继续 rebase 或合并。</li></ul></li></ol><hr><h2 id="✅-前端基础"><a href="#✅-前端基础" class="headerlink" title="✅ 前端基础"></a>✅ <strong>前端基础</strong></h2><h3 id="19-前端-JS-定义一个对象"><a href="#19-前端-JS-定义一个对象" class="headerlink" title="19. 前端 JS 定义一个对象"></a>19. 前端 JS 定义一个对象</h3><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> person = &#123;</span><br><span class="line">  <span class="attr">name</span>: <span class="string">&quot;Tom&quot;</span>,</span><br><span class="line">  <span class="attr">age</span>: <span class="number">20</span>,</span><br><span class="line">  <span class="attr">sayHi</span>: <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&quot;Hi&quot;</span>);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><hr><h2 id="✅-分布式系统"><a href="#✅-分布式系统" class="headerlink" title="✅ 分布式系统"></a>✅ <strong>分布式系统</strong></h2><h3 id="20-为什么使用分布式，有什么优点"><a href="#20-为什么使用分布式，有什么优点" class="headerlink" title="20. 为什么使用分布式，有什么优点"></a>20. 为什么使用分布式，有什么优点</h3><ul><li>提升系统的 <strong>高可用性</strong></li><li>提高 <strong>并发能力</strong> 和 <strong>性能</strong></li><li>实现 <strong>服务拆分，灵活扩展</strong></li><li>解决 <strong>单点故障</strong></li><li>支持 <strong>异地多活</strong></li></ul><hr><h3 id="21-如何实现分布式-ID"><a href="#21-如何实现分布式-ID" class="headerlink" title="21. 如何实现分布式 ID"></a>21. 如何实现分布式 ID</h3><ul><li>常见方式：<ul><li>UUID（唯一但不有序）</li><li>数据库自增（单点瓶颈）</li><li>Redis 自增</li><li>雪花算法（Snowflake，Twitter 出品，64位长整型）</li><li>百度 UidGenerator、Leaf 等</li></ul></li></ul><hr><p>如需我根据岗位（如“AI Agent 工程师”或“后端开发”）定制结构化面经文档、模拟面试题本、视频讲解、或配合项目经验整理，欢迎继续提问。</p><p><img src="/img_posts/image-20250708185518219.png" alt="image-20250708185518219"></p><p>如何让AI输出的更准确更可信真实（RAG，WorkFlow。</p><p>如何提高RAG召回率、工具调用和MCP区别等问题。</p><p><img src="/img_posts/VGaQ3NDpvpf8ASAM.webp" alt="img"></p><p>SSE 重连机制？如何实现重连续传？</p><p>SSE（Server-Sent Events）是一种允许服务器主动向客户端推送数据的技术，其重连机制是其重要特性之一。以下是关于SSE重连机制及其实现的详细说明：</p><h3 id="SSE重连机制"><a href="#SSE重连机制" class="headerlink" title="SSE重连机制"></a>SSE重连机制</h3><ol><li><strong>自动重连特性</strong>：SSE的重连机制是由浏览器自动实现的。当连接因网络问题或其他原因中断时，浏览器会自动尝试重新连接。</li><li><strong>重连时间间隔</strong>：重连的时间间隔可以通过服务端发送的<code>retry</code>字段来指定。例如，服务端可以发送<code>retry: 5000</code>，表示在连接中断后5秒尝试重新连接。如果服务端未指定<code>retry</code>字段，浏览器会使用默认值，通常是3秒。</li><li><strong>Last-Event-ID</strong>：客户端在每次接收到消息时，会将消息的<code>id</code>字段作为内部属性<code>Last-Event-ID</code>存储起来。在重连成功后，客户端会将<code>Last-Event-ID</code>作为请求头发送给服务器，以便服务器可以根据这个ID继续发送后续消息。</li></ol><h3 id="实现重连的两种方式"><a href="#实现重连的两种方式" class="headerlink" title="实现重连的两种方式"></a>实现重连的两种方式</h3><ol><li><p><strong>浏览器自动重连</strong>：</p><ul><li><strong>监听error事件</strong>：当连接中断时，<code>EventSource</code>的<code>error</code>事件会被触发。浏览器会自动尝试重新连接，无需手动干预。</li><li><strong>示例代码</strong>：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> eventSource = <span class="keyword">new</span> <span class="title class_">EventSource</span>(<span class="string">&#x27;your_event_source_url&#x27;</span>);</span><br><span class="line">eventSource.<span class="title function_">addEventListener</span>(<span class="string">&#x27;error&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Connection lost. Browser will try to reconnect.&#x27;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>手动重连</strong>：</p><ul><li><strong>手动控制重连</strong>：如果需要手动控制重连，可以在<code>error</code>事件处理函数中关闭当前连接，然后重新创建一个新的<code>EventSource</code>实例。</li><li><strong>限制重连次数</strong>：可以通过设置重连次数来避免无限重连。</li><li><strong>示例代码</strong>：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> eventSource = <span class="keyword">new</span> <span class="title class_">EventSource</span>(<span class="string">&#x27;your_event_source_url&#x27;</span>);</span><br><span class="line"><span class="keyword">let</span> reconnectAttempts = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">const</span> maxReconnectAttempts = <span class="number">3</span>;</span><br><span class="line"></span><br><span class="line">eventSource.<span class="title function_">addEventListener</span>(<span class="string">&#x27;error&#x27;</span>, <span class="keyword">function</span>(<span class="params"></span>) &#123;</span><br><span class="line">    reconnectAttempts++;</span><br><span class="line">    <span class="keyword">if</span> (reconnectAttempts &lt;= maxReconnectAttempts) &#123;</span><br><span class="line">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">`Reconnect attempt <span class="subst">$&#123;reconnectAttempts&#125;</span>`</span>);</span><br><span class="line">        eventSource.<span class="title function_">close</span>();</span><br><span class="line">        eventSource = <span class="keyword">new</span> <span class="title class_">EventSource</span>(<span class="string">&#x27;your_event_source_url&#x27;</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Max reconnect attempts reached. Aborting.&#x27;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li></ul></li></ol><h3 id="重连续传的实现"><a href="#重连续传的实现" class="headerlink" title="重连续传的实现"></a>重连续传的实现</h3><ol><li><strong>服务端发送<code>id</code>字段</strong>：为了保证数据的完整性，服务端在发送消息时需要带上<code>id</code>字段。这样客户端在重连后可以通过<code>Last-Event-ID</code>请求后续数据。</li><li>**客户端处理<code>Last-Event-ID</code>**：客户端在重连成功后，会将<code>Last-Event-ID</code>作为请求头发送给服务器。服务器可以根据这个ID继续发送后续消息。</li><li><strong>示例代码</strong>：<ul><li><strong>服务端</strong>：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">res.<span class="title function_">write</span>(<span class="string">&#x27;id: 12345\n&#x27;</span>);</span><br><span class="line">res.<span class="title function_">write</span>(<span class="string">&#x27;data: Hello, client!\n\n&#x27;</span>);</span><br></pre></td></tr></table></figure></li><li><strong>客户端</strong>：<figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">let</span> eventSource = <span class="keyword">new</span> <span class="title class_">EventSource</span>(<span class="string">&#x27;your_event_source_url&#x27;</span>);</span><br><span class="line">eventSource.<span class="title function_">addEventListener</span>(<span class="string">&#x27;message&#x27;</span>, <span class="keyword">function</span>(<span class="params">event</span>) &#123;</span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Received message:&#x27;</span>, event.<span class="property">data</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure></li></ul></li></ol><p>通过以上机制和实现方式，SSE可以有效地实现自动重连和数据的连续传输，确保在连接中断后能够恢复数据传输。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>布隆过滤器（Bloom Filter）</title>
      <link href="/2025/07/09/%E5%B8%83%E9%9A%86%E8%BF%87%E6%BB%A4%E5%99%A8%EF%BC%88Bloom-Filter%EF%BC%89/"/>
      <url>/2025/07/09/%E5%B8%83%E9%9A%86%E8%BF%87%E6%BB%A4%E5%99%A8%EF%BC%88Bloom-Filter%EF%BC%89/</url>
      
        <content type="html"><![CDATA[<h1 id="布隆过滤器（Bloom-Filter）"><a href="#布隆过滤器（Bloom-Filter）" class="headerlink" title="布隆过滤器（Bloom Filter）"></a>布隆过滤器（<strong>Bloom Filter</strong>）</h1><p>布隆过滤器（<strong>Bloom Filter</strong>）是一个<strong>空间效率非常高的概率型数据结构</strong>，用于判断一个元素是否在一个集合中。它的特点是：</p><ul><li><strong>可能误判为存在（false positive）</strong>，但<strong>绝不会漏判（false negative）</strong></li><li>空间占用远小于哈希表</li><li>查询效率极高（适合大数据、高并发场景）</li></ul><hr><h2 id="✅-一、原理简述"><a href="#✅-一、原理简述" class="headerlink" title="✅ 一、原理简述"></a>✅ 一、原理简述</h2><p>布隆过滤器的核心原理是：</p><ol><li>使用一个<strong>位数组（bit array）</strong>，初始时所有位为 0；</li><li>需要 k 个不同的<strong>哈希函数</strong>；</li><li><strong>添加元素时</strong>：<ul><li>将元素通过 k 个哈希函数计算出 k 个位置；</li><li>把这些位置上的位都置为 1；</li></ul></li><li><strong>查询元素是否存在</strong>：<ul><li>同样通过 k 个哈希函数计算位置；</li><li>若所有这些位置上的位都是 1，<strong>可能存在</strong>；</li><li>若其中有一个为 0，<strong>一定不存在</strong>。</li></ul></li></ol><hr><h2 id="✅-二、图示示意"><a href="#✅-二、图示示意" class="headerlink" title="✅ 二、图示示意"></a>✅ 二、图示示意</h2><p>假设位数组长度为 10，哈希函数数量为 3：</p><p>添加元素 <code>x</code>：</p><figure class="highlight text"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">哈希1(x) = 2</span><br><span class="line">哈希2(x) = 4</span><br><span class="line">哈希3(x) = 7</span><br></pre></td></tr></table></figure><p>则设置 bit[2]、bit[4]、bit[7] 为 1。</p><hr><h2 id="✅-三、优缺点"><a href="#✅-三、优缺点" class="headerlink" title="✅ 三、优缺点"></a>✅ 三、优缺点</h2><table><thead><tr><th>特点</th><th>描述</th></tr></thead><tbody><tr><td>✅ 优点</td><td>高效、节省内存、查询速度快、适合大规模数据去重</td></tr><tr><td>❌ 缺点</td><td>可能存在误判，不能删除元素（除非使用计数型布隆过滤器），不支持获取原始数据</td></tr></tbody></table><hr><h2 id="✅-四、典型应用场景"><a href="#✅-四、典型应用场景" class="headerlink" title="✅ 四、典型应用场景"></a>✅ 四、典型应用场景</h2><ul><li>缓存穿透（Redis + 布隆过滤器防止大量非法 key 查询数据库）</li><li>黑名单、敏感词过滤</li><li>垃圾邮件识别</li><li>搜索引擎 URL 去重</li><li>区块链地址验证</li><li>广告系统曝光记录去重</li></ul><hr><h2 id="✅-五、Java-实现示例"><a href="#✅-五、Java-实现示例" class="headerlink" title="✅ 五、Java 实现示例"></a>✅ 五、Java 实现示例</h2><p>使用 Google Guava 提供的布隆过滤器：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> com.google.common.hash.BloomFilter;</span><br><span class="line"><span class="keyword">import</span> com.google.common.hash.Funnels;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">BloomFilterDemo</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        BloomFilter&lt;Integer&gt; bloomFilter = BloomFilter.create(</span><br><span class="line">                Funnels.integerFunnel(),</span><br><span class="line">                <span class="number">10000</span>, <span class="comment">// 预期插入数量</span></span><br><span class="line">                <span class="number">0.01</span>   <span class="comment">// 可接受的误判率（1%）</span></span><br><span class="line">        );</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 添加元素</span></span><br><span class="line">        bloomFilter.put(<span class="number">123</span>);</span><br><span class="line">        bloomFilter.put(<span class="number">456</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 查询元素</span></span><br><span class="line">        System.out.println(bloomFilter.mightContain(<span class="number">123</span>)); <span class="comment">// true</span></span><br><span class="line">        System.out.println(bloomFilter.mightContain(<span class="number">999</span>)); <span class="comment">// false（大概率）</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="✅-六、扩展类型"><a href="#✅-六、扩展类型" class="headerlink" title="✅ 六、扩展类型"></a>✅ 六、扩展类型</h2><table><thead><tr><th>类型</th><th>描述</th></tr></thead><tbody><tr><td>标准布隆过滤器</td><td>普通的，不能删除元素</td></tr><tr><td>计数型布隆过滤器（Counting Bloom Filter）</td><td>可以实现删除操作（每个位置是计数器而非 0&#x2F;1）</td></tr><tr><td>分布式布隆过滤器</td><td>Redis、Kafka 中均支持</td></tr><tr><td>RoaringBitmap</td><td>更适合稀疏数据集合（性能优于布隆过滤器）</td></tr></tbody></table><hr><h2 id="✅-七、Redis-中的布隆过滤器"><a href="#✅-七、Redis-中的布隆过滤器" class="headerlink" title="✅ 七、Redis 中的布隆过滤器"></a>✅ 七、Redis 中的布隆过滤器</h2><p>Redis 官方模块 <strong>RedisBloom</strong> 支持布隆过滤器：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">BF.ADD user:filter <span class="string">&quot;user123&quot;</span></span><br><span class="line">BF.EXISTS user:filter <span class="string">&quot;user123&quot;</span>  <span class="comment"># 1（可能存在）</span></span><br><span class="line">BF.EXISTS user:filter <span class="string">&quot;user456&quot;</span>  <span class="comment"># 0（一定不存在）</span></span><br></pre></td></tr></table></figure><hr><p>如你正在做高并发系统、分布式缓存或推荐系统项目，布隆过滤器是一个非常值得加入的组件。如果你需要结合 Spring Boot 或 Redis 实现布隆过滤器的完整样例代码，我也可以帮你生成。需要吗？</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>JVM 读后感</title>
      <link href="/2025/07/04/JVM-%E8%AF%BB%E5%90%8E%E6%84%9F/"/>
      <url>/2025/07/04/JVM-%E8%AF%BB%E5%90%8E%E6%84%9F/</url>
      
        <content type="html"><![CDATA[<h1 id="JVM-读后感"><a href="#JVM-读后感" class="headerlink" title="JVM 读后感"></a>JVM 读后感</h1><h2 id="内存区域"><a href="#内存区域" class="headerlink" title="内存区域"></a>内存区域</h2><p>程序计数器是唯一一个不会出现 <code>OutOfMemoryError</code> 的内存区域，它的生命周期随着线程的创建而创建，随着线程的结束而死亡。</p><p>栈由一个个栈帧组成，而每个栈帧中都拥有：局部变量表、操作数栈、动态链接、方法返回地址。和数据结构上的栈类似，两者都是先进后出的数据结构，只支持出栈和入栈两种操作。</p><h2 id="桟帧"><a href="#桟帧" class="headerlink" title="桟帧"></a>桟帧</h2><p><strong>局部变量表</strong> 主要存放了编译期可知的各种数据类型（boolean、byte、char、short、int、float、long、double）、对象引用（reference 类型，它不同于对象本身，可能是一个指向对象起始地址的引用指针，也可能是指向一个代表对象的句柄或其他与此对象相关的位置）。<br><strong>操作数栈</strong> 主要作为方法调用的中转站使用，用于存放方法执行过程中产生的中间计算结果。另外，计算过程中产生的临时变量也会放在操作数栈中。</p><hr><p>著作权归JavaGuide(javaguide.cn)所有 基于MIT协议 原文链接：<a href="https://javaguide.cn/java/jvm/memory-area.html">https://javaguide.cn/java/jvm/memory-area.html</a></p><p><strong>动态链接</strong> 主要服务一个方法需要调用其他方法的场景。Class 文件的常量池里保存有大量的符号引用比如方法引用的符号引用。当一个方法要调用其他方法，需要将常量池中指向方法的符号引用转化为其在内存地址中的直接引用。动态链接的作用就是为了将符号引用转换为调用方法的直接引用，这个过程也被称为 <strong>动态连接</strong> 。</p><hr><p>著作权归JavaGuide(javaguide.cn)所有 基于MIT协议 原文链接：<a href="https://javaguide.cn/java/jvm/memory-area.html">https://javaguide.cn/java/jvm/memory-area.html</a></p><p>！！！！！！</p><p>Java 方法有两种返回方式，一种是 return 语句正常返回，一种是抛出异常。不管哪种返回方式，都会导致栈帧被弹出。也就是说， <strong>栈帧随着方法调用而创建，随着方法结束而销毁。无论方法正常完成还是异常完成都算作方法结束。</strong></p><p>除了 <code>StackOverFlowError</code> 错误之外，栈还可能会出现<code>OutOfMemoryError</code>错误，这是因为如果栈的内存大小可以动态扩展， 那么当虚拟机在动态扩展栈时无法申请到足够的内存空间，则抛出<code>OutOfMemoryError</code>异常。</p><p>简单总结一下程序运行中栈可能会出现两种错误：</p><ul><li><strong><code>StackOverFlowError</code>：</strong> 如果栈的内存大小不允许动态扩展，那么当线程请求栈的深度超过当前 Java 虚拟机栈的最大深度的时候，就抛出 <code>StackOverFlowError</code> 错误。</li><li><strong><code>OutOfMemoryError</code>：</strong> 如果栈的内存大小可以动态扩展， 那么当虚拟机在动态扩展栈时无法申请到足够的内存空间，则抛出<code>OutOfMemoryError</code>异常。</li></ul><hr><p>著作权归JavaGuide(javaguide.cn)所有 基于MIT协议 原文链接：<a href="https://javaguide.cn/java/jvm/memory-area.html">https://javaguide.cn/java/jvm/memory-area.html</a></p><h3 id="本地方法栈"><a href="#本地方法栈" class="headerlink" title="本地方法栈"></a><a href="#%E6%9C%AC%E5%9C%B0%E6%96%B9%E6%B3%95%E6%A0%88">本地方法栈</a></h3><p>和虚拟机栈所发挥的作用非常相似，区别是：<strong>虚拟机栈为虚拟机执行 Java 方法 （也就是字节码）服务，而本地方法栈则为虚拟机使用到的 Native 方法服务。</strong> 在 HotSpot 虚拟机中和 Java 虚拟机栈合二为一。</p><p>本地方法被执行的时候，在本地方法栈也会创建一个栈帧，用于存放该本地方法的局部变量表、操作数栈、动态链接、出口信息。</p><p>方法执行完毕后相应的栈帧也会出栈并释放内存空间，也会出现 <code>StackOverFlowError</code> 和 <code>OutOfMemoryError</code> 两种错误。</p><hr><p>著作权归JavaGuide(javaguide.cn)所有 基于MIT协议 原文链接：<a href="https://javaguide.cn/java/jvm/memory-area.html">https://javaguide.cn/java/jvm/memory-area.html</a></p><p>Java 堆是垃圾收集器管理的主要区域，因此也被称作 <strong>GC 堆（Garbage Collected Heap）</strong>。从垃圾回收的角度，由于现在收集器基本都采用分代垃圾收集算法，所以 Java 堆还可以细分为：新生代和老年代；再细致一点有：Eden、Survivor、Old 等空间。进一步划分的目的是更好地回收内存，或者更快地分配内存。</p><p>在 JDK 7 版本及 JDK 7 版本之前，堆内存被通常分为下面三部分：</p><ol><li>新生代内存(Young Generation)</li><li>老生代(Old Generation)</li><li>永久代(Permanent Generation)</li></ol><hr><p>著作权归JavaGuide(javaguide.cn)所有 基于MIT协议 原文链接：<a href="https://javaguide.cn/java/jvm/memory-area.html">https://javaguide.cn/java/jvm/memory-area.html</a></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>AI访问地址</title>
      <link href="/2025/07/03/AI%E8%AE%BF%E9%97%AE%E5%9C%B0%E5%9D%80/"/>
      <url>/2025/07/03/AI%E8%AE%BF%E9%97%AE%E5%9C%B0%E5%9D%80/</url>
      
        <content type="html"><![CDATA[<p>AI访问地址</p><p><a href="https://zxl-ai-agent-fronted-171035-8-1367327977.sh.run.tcloudbase.com/">https://zxl-ai-agent-fronted-171035-8-1367327977.sh.run.tcloudbase.com</a></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Spring在进行注入的时候会先调用构造器再进行依赖注入</title>
      <link href="/2025/07/01/Spring%E5%9C%A8%E8%BF%9B%E8%A1%8C%E6%B3%A8%E5%85%A5%E7%9A%84%E6%97%B6%E5%80%99%E4%BC%9A%E5%85%88%E8%B0%83%E7%94%A8%E6%9E%84%E9%80%A0%E5%99%A8%E5%86%8D%E8%BF%9B%E8%A1%8C%E4%BE%9D%E8%B5%96%E6%B3%A8%E5%85%A5/"/>
      <url>/2025/07/01/Spring%E5%9C%A8%E8%BF%9B%E8%A1%8C%E6%B3%A8%E5%85%A5%E7%9A%84%E6%97%B6%E5%80%99%E4%BC%9A%E5%85%88%E8%B0%83%E7%94%A8%E6%9E%84%E9%80%A0%E5%99%A8%E5%86%8D%E8%BF%9B%E8%A1%8C%E4%BE%9D%E8%B5%96%E6%B3%A8%E5%85%A5/</url>
      
        <content type="html"><![CDATA[<h1 id="Spring在进行注入的时候会先调用构造器再进行依赖注入"><a href="#Spring在进行注入的时候会先调用构造器再进行依赖注入" class="headerlink" title="Spring在进行注入的时候会先调用构造器再进行依赖注入"></a>Spring在进行注入的时候会先调用构造器再进行依赖注入</h1><p>从代码和之前的异常信息来看，问题的根本原因在于 <code>fileChatBasedMemory</code> 在构造器中被使用时为 <code>null</code>。这是因为 Spring 在调用构造器时，尚未完成依赖注入。以下是具体的分析和解决方法：</p><h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><ol><li><p><strong>构造器中直接使用依赖</strong>：<br>在 <code>LoveApp</code> 的构造器中，直接调用了 <code>fileChatBasedMemory.setBaseDir(baseDir)</code>。然而，此时 <code>fileChatBasedMemory</code> 还没有被 Spring 完全注入，因此为 <code>null</code>，导致了 <code>NullPointerException</code>。</p></li><li><p><strong>依赖注入时机</strong>：<br>!!!!!!!!!<strong><span style="color: red">Spring 在创建 Bean 时，会先调用构造器，然后才进行依赖注入。因此，在构造器中直接使用依赖项是不安全的。</span></strong></p></li></ol><h3 id="解决方法"><a href="#解决方法" class="headerlink" title="解决方法"></a>解决方法</h3><p>为了解决这个问题，可以将构造器中的逻辑移到一个初始化方法中，确保依赖注入完成后再执行相关逻辑。Spring 提供了 <code>@PostConstruct</code> 注解，用于定义初始化方法。</p><h4 id="修改代码"><a href="#修改代码" class="headerlink" title="修改代码"></a>修改代码</h4><p>将 <code>fileChatBasedMemory.setBaseDir(baseDir)</code> 移到一个 <code>@PostConstruct</code> 方法中：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="meta">@Slf4j</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoveApp</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> SimpleVectorStore loveAppVectorStore;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> Advisor loveAppRagCloudAdvisor;</span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * chatClient 使用实例</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">final</span> ChatClient chatClient;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> FileChatBasedMemory fileChatBasedMemory;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">String</span> <span class="variable">SYSTEM_PROMPT</span> <span class="operator">=</span> <span class="string">&quot;你扮演深耕恋爱心理领域的专家。开场向用户表明身份，告知用户可倾诉恋爱难题。&quot;</span> +</span><br><span class="line">            <span class="string">&quot;围绕单身、恋爱、已婚三种状态提问：单身状态询问社交圈拓展及追求心仪对象的困扰；&quot;</span> +</span><br><span class="line">            <span class="string">&quot;恋爱状态询问沟通、习惯差异引发的矛盾；已婚状态询问家庭责任与亲属关系处理的问题。&quot;</span> +</span><br><span class="line">            <span class="string">&quot;引导用户详述事情经过、对方反应及自身想法，以便给出专属解决方案。&quot;</span> +</span><br><span class="line">            <span class="string">&quot;每一次对话你只需要在开头自我简绍，并记住自己的名字&quot;</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">LoveApp</span><span class="params">(ChatModel dashscopeChatModel)</span> &#123;</span><br><span class="line">        chatClient = ChatClient.builder(dashscopeChatModel)</span><br><span class="line">                .defaultSystem(SYSTEM_PROMPT) <span class="comment">//设置系统提示词</span></span><br><span class="line">                .defaultAdvisors(</span><br><span class="line">                        <span class="keyword">new</span> <span class="title class_">MessageChatMemoryAdvisor</span>(fileChatBasedMemory), <span class="comment">// 基于文件保存对话</span></span><br><span class="line">                        <span class="keyword">new</span> <span class="title class_">MyLoggerAdviser</span>()</span><br><span class="line">                )</span><br><span class="line">                .build();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostConstruct</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">baseDir</span> <span class="operator">=</span> System.getProperty(<span class="string">&quot;user.dir&quot;</span>) + <span class="string">&quot;/chat-memory&quot;</span>;</span><br><span class="line">        fileChatBasedMemory.setBaseDir(baseDir);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 其他方法保持不变</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="说明"><a href="#说明" class="headerlink" title="说明"></a>说明</h3><ul><li>**<code>@PostConstruct</code>**：这个注解用于定义一个初始化方法，该方法在依赖注入完成后执行。这样可以确保在调用 <code>fileChatBasedMemory.setBaseDir(baseDir)</code> 时，<code>fileChatBasedMemory</code> 已经被正确注入。</li><li><strong>构造器中的依赖</strong>：在构造器中，<code>fileChatBasedMemory</code> 还没有被注入，因此不能直接使用。将相关逻辑移到 <code>@PostConstruct</code> 方法中可以避免这个问题。</li></ul><h3 id="其他注意事项"><a href="#其他注意事项" class="headerlink" title="其他注意事项"></a>其他注意事项</h3><ol><li><p><strong>确保 <code>FileChatBasedMemory</code> 是一个 Spring Bean</strong>：<br>确保 <code>FileChatBasedMemory</code> 类上有 <code>@Component</code> 或其他相关的 Spring 注解，以便 Spring 能够管理它。</p></li><li><p><strong>检查 <code>ChatModel</code> 的注入</strong>：<br>确保 <code>ChatModel</code> 也是一个 Spring 管理的 Bean，否则构造器中的 <code>dashscopeChatModel</code> 也可能会为 <code>null</code>。</p></li><li><p><strong>日志和调试</strong>：<br>如果问题仍然存在，可以添加更多的日志信息来帮助诊断问题。例如，在构造器和 <code>@PostConstruct</code> 方法中添加日志，检查依赖项是否正确注入。</p></li></ol><p>通过上述修改，应该可以解决 <code>NullPointerException</code> 的问题，并确保 <code>LoveApp</code> 类能够正确初始化和运行。</p><h1 id="Bean注入流程"><a href="#Bean注入流程" class="headerlink" title="Bean注入流程"></a>Bean注入流程</h1><p>Spring 注入一个 Bean 的流程是一个复杂的过程，涉及到多个步骤和组件。以下是 Spring 创建和注入一个 Bean 的主要流程：</p><h3 id="1-加载配置"><a href="#1-加载配置" class="headerlink" title="1. 加载配置"></a>1. <strong>加载配置</strong></h3><p>Spring 首先需要加载配置信息，这些配置可以是 XML 文件、注解或 Java 配置类。配置信息告诉 Spring 需要创建哪些 Bean，以及这些 Bean 的依赖关系。</p><ul><li><p><strong>XML 配置</strong>：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">bean</span> <span class="attr">id</span>=<span class="string">&quot;myBean&quot;</span> <span class="attr">class</span>=<span class="string">&quot;com.example.MyBean&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>注解配置</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>Java 配置</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AppConfig</span> &#123;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> MyBean <span class="title function_">myBean</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">MyBean</span>();</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="2-解析配置"><a href="#2-解析配置" class="headerlink" title="2. 解析配置"></a>2. <strong>解析配置</strong></h3><p>Spring 解析配置信息，生成 Bean 定义（<code>BeanDefinition</code>）。每个 <code>BeanDefinition</code> 包含了 Bean 的类信息、作用域、依赖关系等。</p><h3 id="3-初始化-BeanFactory"><a href="#3-初始化-BeanFactory" class="headerlink" title="3. 初始化 BeanFactory"></a>3. <strong>初始化 BeanFactory</strong></h3><p>Spring 创建一个 <code>BeanFactory</code>，这是一个工厂类，负责管理 Bean 的生命周期和依赖注入。<code>BeanFactory</code> 会根据 <code>BeanDefinition</code> 创建 Bean。</p><h3 id="4-实例化-Bean"><a href="#4-实例化-Bean" class="headerlink" title="4. 实例化 Bean"></a>4. <strong>实例化 Bean</strong></h3><p>Spring 根据 <code>BeanDefinition</code> 中的类信息，通过反射创建 Bean 的实例。如果 Bean 的构造器需要参数，Spring 会尝试通过依赖注入来提供这些参数。</p><ul><li><p><strong>无参构造器</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyBean</span><span class="params">()</span> &#123;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>有参构造器</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Dependency dependency;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="title function_">MyBean</span><span class="params">(Dependency dependency)</span> &#123;</span><br><span class="line">        <span class="built_in">this</span>.dependency = dependency;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="5-依赖注入"><a href="#5-依赖注入" class="headerlink" title="5. 依赖注入"></a>5. <strong>依赖注入</strong></h3><p>Spring 通过依赖注入（DI）将依赖项注入到 Bean 中。依赖注入可以通过构造器注入、字段注入或 setter 方法注入。</p><ul><li><p><strong>构造器注入</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">MyBean</span><span class="params">(Dependency dependency)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.dependency = dependency;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>字段注入</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">private</span> Dependency dependency;</span><br></pre></td></tr></table></figure></li><li><p><strong>Setter 方法注入</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Autowired</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">setDependency</span><span class="params">(Dependency dependency)</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.dependency = dependency;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="6-初始化后处理"><a href="#6-初始化后处理" class="headerlink" title="6. 初始化后处理"></a>6. <strong>初始化后处理</strong></h3><p>Spring 调用 Bean 的初始化方法。如果 Bean 实现了 <code>InitializingBean</code> 接口或定义了 <code>@PostConstruct</code> 注解的方法，Spring 会调用这些方法。</p><ul><li><p><strong><code>InitializingBean</code> 接口</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> <span class="keyword">implements</span> <span class="title class_">InitializingBean</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">afterPropertiesSet</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 初始化逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong><code>@PostConstruct</code> 注解</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@PostConstruct</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">init</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 初始化逻辑</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="7-Bean-后处理器"><a href="#7-Bean-后处理器" class="headerlink" title="7. Bean 后处理器"></a>7. <strong>Bean 后处理器</strong></h3><p>Spring 调用 <code>BeanPostProcessor</code> 的 <code>postProcessBeforeInitialization</code> 和 <code>postProcessAfterInitialization</code> 方法。这些方法可以对 Bean 进行进一步的处理。</p><ul><li>**<code>BeanPostProcessor</code>**：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBeanPostProcessor</span> <span class="keyword">implements</span> <span class="title class_">BeanPostProcessor</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessBeforeInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="comment">// 在初始化之前处理</span></span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> Object <span class="title function_">postProcessAfterInitialization</span><span class="params">(Object bean, String beanName)</span> <span class="keyword">throws</span> BeansException &#123;</span><br><span class="line">        <span class="comment">// 在初始化之后处理</span></span><br><span class="line">        <span class="keyword">return</span> bean;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="8-Bean-准备就绪"><a href="#8-Bean-准备就绪" class="headerlink" title="8. Bean 准备就绪"></a>8. <strong>Bean 准备就绪</strong></h3><p>Bean 完全初始化后，Spring 将其放入 Bean 容器中，供其他 Bean 使用。</p><h3 id="9-销毁-Bean"><a href="#9-销毁-Bean" class="headerlink" title="9. 销毁 Bean"></a>9. <strong>销毁 Bean</strong></h3><p>当 Spring 容器关闭时，Spring 会调用 Bean 的销毁方法。如果 Bean 实现了 <code>DisposableBean</code> 接口或定义了 <code>@PreDestroy</code> 注解的方法，Spring 会调用这些方法。</p><ul><li><p><strong><code>DisposableBean</code> 接口</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">MyBean</span> <span class="keyword">implements</span> <span class="title class_">DisposableBean</span> &#123;</span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">destroy</span><span class="params">()</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        <span class="comment">// 销毁逻辑</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong><code>@PreDestroy</code> 注解</strong>：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@PreDestroy</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">destroy</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="comment">// 销毁逻辑</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p>Spring 创建和注入一个 Bean 的流程包括加载配置、解析配置、初始化 <code>BeanFactory</code>、实例化 Bean、依赖注入、初始化后处理、Bean 后处理器处理、Bean 准备就绪和销毁 Bean。这个过程确保了 Bean 的生命周期被正确管理，并且依赖关系被正确注入。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>git</title>
      <link href="/2025/06/17/git/"/>
      <url>/2025/06/17/git/</url>
      
        <content type="html"><![CDATA[<h1 id="git学习记录"><a href="#git学习记录" class="headerlink" title="git学习记录"></a>git学习记录</h1><p>初始化仓库的两个方法</p><p>1<code>git init</code> 需要指定push的远程仓库:<code>git remote add origin  URL</code> </p><p>2.<code>git clone </code>需要指定push的远程仓库:<code>git remote add origin  URL</code></p><p>从远程仓库拉取代码和推送到远程仓库代码</p><p>1.<code>git pull (origin) (分支名) </code></p><p>2.<code>git push (origin) (分支名)</code></p><p>3.<code>git fetch (origin) (分支名)</code></p><p>ssh公钥方式连接远程仓库</p><p>设置用户签名</p><p><code>git config --global user.name</code> “yorname”设置用户签名<br><code>git config --global user.email your_email@youremail.com </code>    设置用户签名</p><p>生成密钥(SSH key)</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;your_email@youremail.com     &quot;-t&quot; 代表公钥的类型，&quot;-C&quot;为注释 </span><br></pre></td></tr></table></figure><p>生成的公钥文件在你C盘用户目录下.ssh文件下，可以看到id_rsa.pub的文件，赋值上面的内容到代码托管平台上添加ssh密钥的位置即可</p><h2 id="一-修改提交信息"><a href="#一-修改提交信息" class="headerlink" title="一.修改提交信息"></a>一.修改提交信息</h2><h3 id="1-修改提交信息"><a href="#1-修改提交信息" class="headerlink" title="1. 修改提交信息"></a>1. 修改提交信息</h3><p>如果你刚刚提交了更改，但想要修改提交信息，可以使用：</p><p>bash</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit --amend -m <span class="string">&quot;新的提交信息&quot;</span></span><br></pre></td></tr></table></figure><p>这将打开一个编辑器，让你修改提交信息。</p><h3 id="2-添加更多更改到上次提交"><a href="#2-添加更多更改到上次提交" class="headerlink" title="2. 添加更多更改到上次提交"></a>2. 添加更多更改到上次提交</h3><p>如果你在提交后进行了更多的更改，并且想要将这些更改添加到上次提交中，而不是创建一个新的提交，可以这样做：</p><p>bash</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git add &lt;更改的文件&gt;</span><br><span class="line">git commit --amend --no-edit</span><br></pre></td></tr></table></figure><p>这里 <code>--no-edit</code> 选项表示不更改提交信息，直接使用上次的提交信息</p><p>###3.<strong>要修改已推送到远程仓库的提交</strong></p><ol><li><p><strong>修改本地提交</strong>：使用 <code>git commit --amend</code> 命令来修改最近一次提交。这会打开一个编辑器，允许你修改提交信息。</p></li><li><p><strong>强制推送到远程仓库</strong>：由于你已经修改了最近一次提交，需要使用 <code>git push --force</code> 命令将修改后的提交推送到远程仓库。注意，这里使用了 <code>--force</code> 选项来覆盖远程仓库中的提交记录。</p></li><li><p><strong>注意事项</strong>：使用 <code>--force</code> 选项会强制更新远程分支，使其与你的本地分支状态完全一致。这意味着如果你或他人在此期间有其他提交到远程，它们将被你的这次推送覆盖。因此，除非你确信没有其他人在此期间推送到该分支，否则应谨慎使用 <code>--force</code>。</p></li><li><p>**推荐使用 <code>--force-with-lease</code>**：为了更安全地强制推送，可以使用 <code>git push --force-with-lease</code>。它会在推送前检查远程分支的状态是否与你预期的一致。如果远程分支在你上次拉取后有其他人的新提交，推送会失败，从而避免意外覆盖他人的工作。</p></li></ol><h3 id="4-提交删除的文件"><a href="#4-提交删除的文件" class="headerlink" title="4.提交删除的文件"></a>4.提交删除的文件</h3><p>1.<code>git rm &#39;path/file&#39; </code><br>2.<code>git commit -m &#39;提交信息&#39; </code><br>3.<code>git push</code></p><h2 id="二、修改指定文件的提交信息（已推送）"><a href="#二、修改指定文件的提交信息（已推送）" class="headerlink" title="二、修改指定文件的提交信息（已推送）"></a>二、修改指定文件的提交信息（已推送）</h2><p>这里的方法是通过<strong>手动拆分</strong>和<strong>重新提交</strong>，与指定文件夹相关的提交信息重新组织历史。以下是详细步骤：</p><hr><h3 id="具体场景说明"><a href="#具体场景说明" class="headerlink" title="具体场景说明"></a>具体场景说明</h3><p>假设有一个文件夹 <code>src</code>，你希望修改它的相关提交信息，其他文件夹的提交信息保持不变。</p><hr><h3 id="详细步骤"><a href="#详细步骤" class="headerlink" title="详细步骤"></a>详细步骤</h3><h4 id="1-备份当前仓库"><a href="#1-备份当前仓库" class="headerlink" title="1. 备份当前仓库"></a>1. <strong>备份当前仓库</strong></h4><p>在进行历史修改之前，创建一个安全备份分支：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch backup</span><br></pre></td></tr></table></figure><p>这样可以随时恢复到原始状态。</p><hr><h4 id="2-查找与指定文件夹相关的提交"><a href="#2-查找与指定文件夹相关的提交" class="headerlink" title="2. 查找与指定文件夹相关的提交"></a>2. <strong>查找与指定文件夹相关的提交</strong></h4><p>运行以下命令以筛选出与目标文件夹相关的提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline -- &lt;folder_path&gt;</span><br></pre></td></tr></table></figure><p>例如：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git <span class="built_in">log</span> --oneline -- src</span><br></pre></td></tr></table></figure><p>这将输出类似以下内容：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">abc1234 Fix: Bug in src module</span><br><span class="line">def5678 Refactor: Updated src structure</span><br><span class="line">ghi9012 Initial commit for src</span><br></pre></td></tr></table></figure><p>记下相关提交的哈希值。</p><hr><h4 id="3-交互式-rebase-定位提交"><a href="#3-交互式-rebase-定位提交" class="headerlink" title="3. 交互式 rebase 定位提交"></a>3. <strong>交互式 rebase 定位提交</strong></h4><p>使用 <code>git rebase</code> 将历史分解为可编辑状态：</p><p>运行：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase -i &lt;starting_commit_hash&gt;^</span><br></pre></td></tr></table></figure><p>如果你只需要修改最近 3 次提交：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase -i HEAD~3</span><br></pre></td></tr></table></figure><p>进入编辑器后，你会看到提交历史，例如：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pick abc1234 Fix: Bug in src module</span><br><span class="line">pick def5678 Refactor: Updated src structure</span><br><span class="line">pick ghi9012 Initial commit for src</span><br></pre></td></tr></table></figure><p>将涉及 <code>src</code> 文件夹的提交标记为 <code>edit</code>：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">pick abc1234 Fix: Bug in src module</span><br><span class="line">edit def5678 Refactor: Updated src structure</span><br><span class="line">pick ghi9012 Initial commit for src</span><br></pre></td></tr></table></figure><p>保存并退出。</p><hr><h4 id="4-拆分与目标文件夹无关的修改"><a href="#4-拆分与目标文件夹无关的修改" class="headerlink" title="4. 拆分与目标文件夹无关的修改"></a>4. <strong>拆分与目标文件夹无关的修改</strong></h4><p>Git 会暂停在标记为 <code>edit</code> 的提交上，你可以拆分内容。</p><ol><li><p><strong>检查提交内容：</strong> 查看该提交改动：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git show</span><br></pre></td></tr></table></figure></li><li><p><strong>重置提交到暂存区：</strong> 将当前提交的内容重置为未提交状态：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset HEAD~</span><br></pre></td></tr></table></figure></li><li><p><strong>暂存指定文件夹的更改：</strong> 只将 <code>src</code> 文件夹相关的改动添加到暂存区：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git add src</span><br></pre></td></tr></table></figure></li><li><p><strong>创建新的提交：</strong> 提交新的信息：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git commit -m <span class="string">&quot;New message for src folder&quot;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>提交其他更改：</strong> 将剩余未暂存的文件提交回去：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit -m <span class="string">&quot;Other unrelated changes&quot;</span></span><br></pre></td></tr></table></figure></li><li><p><strong>继续 rebase：</strong> 完成拆分后，继续 rebase：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git rebase --<span class="built_in">continue</span></span><br></pre></td></tr></table></figure></li></ol><hr><h4 id="5-重复操作"><a href="#5-重复操作" class="headerlink" title="5. 重复操作"></a>5. <strong>重复操作</strong></h4><p>对于所有标记为 <code>edit</code> 的提交，重复上述步骤，直到完成。</p><hr><h4 id="6-强制推送更改"><a href="#6-强制推送更改" class="headerlink" title="6. 强制推送更改"></a>6. <strong>强制推送更改</strong></h4><p>完成所有修改后，需要强制推送到远程仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git push --force</span><br></pre></td></tr></table></figure><hr><h3 id="示例操作流程"><a href="#示例操作流程" class="headerlink" title="示例操作流程"></a>示例操作流程</h3><p>假设 <code>src</code> 文件夹的某个提交信息需要修改：</p><ol><li>当前提交内容：<ul><li>修改了 <code>src/main.c</code>。</li><li>修改了 <code>README.md</code>。</li></ul></li><li>通过 <code>git reset</code> 和 <code>git add</code> 拆分：<ul><li><code>git add src</code> -&gt; 提交 <code>src</code> 文件的改动，修改提交信息。</li><li><code>git add README.md</code> -&gt; 提交其他文件的改动。</li></ul></li></ol><hr><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol><li><strong>操作环境</strong>：确保团队成员没有并行修改提交历史，避免冲突。</li><li><strong>备份</strong>：始终在操作前备份分支，避免数据丢失。</li><li><strong>强制推送</strong>：所有历史变更都需要 <code>git push --force</code>（这是不安全的），务必与团队沟通。</li></ol><h2 id="三、要连接到其他-Git-仓库，您可以使用以下步骤："><a href="#三、要连接到其他-Git-仓库，您可以使用以下步骤：" class="headerlink" title="三、要连接到其他 Git 仓库，您可以使用以下步骤："></a>三、要连接到其他 Git 仓库，您可以使用以下步骤：</h2><ol><li><p><strong>查找或创建新的远程仓库</strong>：</p><ul><li>如果您想要连接到一个已经存在的仓库，您需要知道该仓库的 URL。</li><li>如果您想要创建一个新的远程仓库，您可以在 GitHub、GitLab、Bitbucket 或其他 Git 托管服务上创建一个新的仓库，并获取该仓库的 URL。</li></ul></li><li><p><strong>使用 <code>git remote add</code> 命令添加远程仓库</strong>：</p><ul><li><p>打开终端或命令行界面。</p></li><li><p>切换到您的本地 Git 仓库目录。</p></li><li><p>执行以下命令来添加一个新的远程仓库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote add &lt;remote-name&gt; &lt;repository-url&gt;</span><br></pre></td></tr></table></figure></li><li><p>其中 <code>&lt;remote-name&gt;</code> 是您为远程仓库起的别名（通常是 <code>origin</code>，但也可以是任何其他名称），而 <code>&lt;repository-url&gt;</code> 是远程仓库的 URL。</p></li></ul><p>例如，如果您想要将远程仓库别名设置为 <code>upstream</code> 并连接到一个名为 <code>https://github.com/other-user/other-repo.git</code> 的仓库，您可以执行：</p>  <figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote add upstream https://github.com/other-user/other-repo.git</span><br></pre></td></tr></table></figure></li><li><p><strong>验证远程仓库</strong>：</p><ul><li>使用 <code>git remote -v</code> 命令来查看所有远程仓库的 URL，确保新的远程仓库已经被正确添加。</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure></li><li><p><strong>从远程仓库拉取代码</strong>：</p><ul><li>如果您想要从新添加的远程仓库拉取代码，可以使用 <code>git fetch</code> 命令：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git fetch &lt;remote-name&gt;</span><br></pre></td></tr></table></figure><ul><li>这会将远程仓库的代码拉取到您的本地仓库，但不会自动合并到您的当前分支。</li></ul></li><li><p><strong>合并代码</strong>：</p><ul><li>如果您想要将远程仓库的代码合并到您的本地分支，可以使用 <code>git merge</code> 命令：</li></ul><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git merge &lt;remote-name&gt;/branch-name</span><br></pre></td></tr></table></figure><ul><li>替换 <code>branch-name</code> 为远程仓库中您想要合并的分支名称。</li></ul></li><li><p><strong>git删除远程仓库连接</strong>：</p></li></ol><ul><li><p>使用<code>git remote rm &lt;仓库名&gt;</code>命令，例如<code>git remote rm origin</code>可以删除名为origin的远程仓库。</p></li><li><p>使用<code>git remote remove &lt;仓库名&gt;</code>命令，例如<code>git remote remove origin</code>也可以删除名为origin的远程仓库。</p></li><li><p>使用<code>git remote prune &lt;仓库名&gt;</code>命令，例如<code>git remote prune origin</code>可以删除名为origin的远程仓库。</p></li><li><p>使用<code>git remote rm --all</code>命令可以删除所有远程仓库。</p></li></ul><ol start="6"><li><p><strong>git修改连接的远程仓库的别名</strong>：<br>  使用<code>git remote rename &lt;old_name&gt; &lt;new_name&gt;</code>命令将指定的旧别名修改为新别名</p></li><li><p><strong>提交到远程不同仓库</strong></p><p><code>git push 自定义仓库名 分支名</code></p></li><li><p><strong>git查看关联的远程库</strong></p><p>要查看当前 Git 仓库关联的远程仓库，可以使用以下命令：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote -v</span><br></pre></td></tr></table></figure><p>这将列出所有远程仓库及其URL。</p><p>如果你只想查看远程仓库的简要信息，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git remote</span><br></pre></td></tr></table></figure><p>这将列出所有远程仓库的名称。</p></li></ol><h2 id="四、git撤销记录"><a href="#四、git撤销记录" class="headerlink" title="四、git撤销记录"></a>四、git撤销记录</h2><p>###git 如何撤销commit记录</p><p>要撤销最后一次commit但保留更改，可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset --soft HEAD^</span><br></pre></td></tr></table></figure><p>如果要撤销commit并且也撤销更改（慎用，这会丢失代码更改），可以使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset --hard HEAD^</span><br></pre></td></tr></table></figure><p>如果需要撤销特定的一个commit（不是最后一个），你需要先找到那个commit的哈希值，然后使用：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git reset --soft &lt;commit_hash&gt;^</span><br><span class="line"><span class="comment"># 或者</span></span><br><span class="line">git reset --hard &lt;commit_hash&gt;^</span><br></pre></td></tr></table></figure><p>替换<code>&lt;commit_hash&gt;</code>为你想要撤销的commit的哈希值。</p><p>注意：使用<code>--hard</code>选项会丢失所有未提交的更改，请谨慎操作。如果需要保留更改而不是撤销commit，请使用<code>--soft</code>选项。</p><hr><p>###git 如何撤销add记录</p><p>要撤销<code>git add</code>添加到暂存区的文件，可以使用<code>git reset</code>命令。具体的命令取决于你要撤销添加的文件是单个文件还是多个文件。</p><p>撤销单个文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset &lt;file&gt;</span><br></pre></td></tr></table></figure><p>撤销多个文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset &lt;file1&gt; &lt;file2&gt; ...</span><br></pre></td></tr></table></figure><p>撤销所有文件：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git reset</span><br></pre></td></tr></table></figure><p>##五、git本地master分支推送到远程main分支<br>如果你在本地使用的是 master 分支，而远程仓库使用的是 main 分支，那么需要进行一些设置才能将本地的 master 分支推送到远程的 main 分支。</p><p>一种简单的方法是，在本地使用 git branch -m 命令将本地的 master 分支重命名为 main 分支，然后再将其推送到远程仓库。具体步骤如下：</p><ol><li><p><strong>检查本地分支：</strong><br><code>git branch</code><br>你应该能看到 master 分支。</p></li><li><p><strong>将本地的 master 分支重命名为 main 分支：</strong><br><code>git branch -m master main</code></p></li><li><p><strong>推送本地 main 分支到远程仓库：</strong><br><code>git push -u origin main</code><br>上述命令会将本地的 main 分支推送到远程的 main 分支，并且在远程仓库中创建一个新的 main 分支（如果该分支不存在）。</p></li><li><p><strong>如果你想删除在远程仓库中的 master 分支，可以运行以下命令：</strong></p></li></ol><p><code>git push origin :master</code><br>上述命令会将一个空分支推送到远程仓库的 master 分支，从而删除该分支。</p><p>注意：在重命名本地分支和推送到远程仓库之前，请确保没有其他人正在共享该仓库，并且你的代码没有在其他地方被引用或依赖。</p><h2 id="六、常见报错"><a href="#六、常见报错" class="headerlink" title="六、常见报错"></a>六、常见报错</h2><ol><li><strong>报错：fatal: refusing to merge unrelated histories</strong></li></ol><p>在执行 <code>git pull</code> 时出现 <code>fatal: refusing to merge unrelated histories</code> 错误，这是因为本地仓库和远程仓库的提交历史（history）不相关，Git 默认拒绝合并不相关的历史。</p><p><strong>解决方法</strong></p><p>可以通过添加 <code>--allow-unrelated-histories</code> 参数强制合并不同历史：</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git pull movie_app main --allow-unrelated-histories </span><br></pre></td></tr></table></figure><ol start="2"><li><strong>报错：fatal: The current branch master has no upstream branch.</strong><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">     fatal: The current branch master has no upstream branch.</span><br><span class="line">     To push the current branch and set the remote as upstream, use</span><br><span class="line"></span><br><span class="line">       git push --set-upstream origin master</span><br><span class="line"></span><br><span class="line">   To have this happen automatically for branches without a tracking</span><br><span class="line">   upstream, see &#x27;push.autoSetupRemote&#x27; in &#x27;git help config&#x27;.</span><br><span class="line">`````</span><br><span class="line"></span><br><span class="line">这段提示是 Git 在执行某些操作时发出的，意思是当前分支 `master` 没有设置上游分支（即远程分支）。以下是它的含义和解决方法：</span><br><span class="line"></span><br><span class="line">2.1  **问题解释**</span><br><span class="line"></span><br><span class="line">1. **上游分支（Upstream Branch）**：  </span><br><span class="line">   在 Git 中，本地分支可以与远程分支关联，这种关联关系称为“上游分支”。当你从远程仓库克隆代码时，Git 会自动设置本地分支与远程分支的关联关系。例如，`master` 分支通常会与远程仓库的 `origin/master` 关联。</span><br><span class="line"></span><br><span class="line">2. **没有上游分支的原因**：  </span><br><span class="line">   - 可能是本地分支是新创建的，还没有推送到远程仓库。</span><br><span class="line">   - 或者本地分支是从其他分支创建的，但没有设置与远程分支的关联。</span><br><span class="line"></span><br><span class="line">3. **提示内容的含义**：  </span><br><span class="line">   - Git 建议你使用命令 `git push --set-upstream origin master`，将本地的 `master` 分支推送到远程仓库，并设置远程分支 `origin/master` 作为上游分支。</span><br><span class="line">   - 它还提到可以通过配置 `push.autoSetupRemote` 来自动设置这种关联关系。</span><br><span class="line"></span><br><span class="line">2.2  **解决方法**</span><br><span class="line"></span><br><span class="line">**方法 1：手动设置上游分支**</span><br><span class="line">运行以下命令，将本地分支推送到远程仓库，并设置上游分支：</span><br><span class="line"></span><br><span class="line">```bash</span><br><span class="line">git push --set-upstream origin master</span><br></pre></td></tr></table></figure>这个命令会：</li><li>将本地的 <code>master</code> 分支推送到远程仓库的 <code>origin/master</code>。</li><li>设置本地分支与远程分支的关联关系。</li></ol><p> <strong>方法 2：自动设置上游分支</strong><br>如果你希望 Git 在推送时自动设置上游分支，可以配置 <code>push.autoSetupRemote</code>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git config --global push.autoSetupRemote origin</span><br></pre></td></tr></table></figure><p>这样，当你下次推送分支时，Git 会自动设置上游分支。</p><p><strong>方法 3：检查当前分支状态</strong><br>如果你不确定当前分支的状态，可以运行以下命令查看：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">git branch -vv</span><br></pre></td></tr></table></figure><p>这会显示本地分支及其关联的远程分支信息。如果某个分支没有关联远程分支，你会看到类似 <code>[]</code> 的空值。</p><p><strong>注意事项</strong></p><ol><li>如果你使用的是 <code>main</code> 分支（而不是 <code>master</code>），请将命令中的 <code>master</code> 替换为 <code>main</code>。</li><li>如果你不确定远程仓库的名称（默认是 <code>origin</code>），可以用 <code>git remote -v</code> 查看远程仓库的名称和地址。</li></ol><h2 id="七、常用命令"><a href="#七、常用命令" class="headerlink" title="七、常用命令"></a>七、常用命令</h2><ol><li><p>查看当前文件状态<br><code>git status</code><br><code>git status -s| --short</code>  简介的展示文件状态,例如</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br><span class="line">82</span><br><span class="line">83</span><br><span class="line">84</span><br><span class="line">85</span><br><span class="line">86</span><br><span class="line">87</span><br><span class="line">88</span><br><span class="line">89</span><br><span class="line">90</span><br><span class="line">91</span><br><span class="line">92</span><br><span class="line">93</span><br><span class="line">94</span><br><span class="line">95</span><br><span class="line">96</span><br><span class="line">97</span><br><span class="line">98</span><br><span class="line">99</span><br><span class="line">100</span><br><span class="line">101</span><br><span class="line">102</span><br><span class="line">103</span><br><span class="line">104</span><br><span class="line">105</span><br><span class="line">106</span><br><span class="line">107</span><br><span class="line">108</span><br><span class="line">109</span><br><span class="line">110</span><br><span class="line">111</span><br><span class="line">112</span><br><span class="line">113</span><br><span class="line">114</span><br><span class="line">115</span><br><span class="line">116</span><br><span class="line">117</span><br><span class="line">118</span><br><span class="line">119</span><br><span class="line">120</span><br><span class="line">121</span><br><span class="line">122</span><br><span class="line">123</span><br><span class="line">124</span><br><span class="line">125</span><br><span class="line">126</span><br><span class="line">127</span><br><span class="line">128</span><br><span class="line">129</span><br><span class="line">130</span><br><span class="line">131</span><br><span class="line">132</span><br><span class="line">133</span><br><span class="line">134</span><br><span class="line">135</span><br><span class="line">136</span><br><span class="line">137</span><br><span class="line">138</span><br><span class="line">139</span><br><span class="line">140</span><br><span class="line">141</span><br><span class="line">142</span><br><span class="line">143</span><br><span class="line">144</span><br><span class="line">145</span><br><span class="line">146</span><br><span class="line">147</span><br><span class="line">148</span><br><span class="line">149</span><br><span class="line">150</span><br><span class="line">151</span><br><span class="line">152</span><br><span class="line">153</span><br><span class="line">154</span><br><span class="line">155</span><br><span class="line">156</span><br><span class="line">157</span><br><span class="line">158</span><br><span class="line">159</span><br></pre></td><td class="code"><pre><span class="line">$ git status -s</span><br><span class="line"> M README</span><br><span class="line">MM Rakefile</span><br><span class="line">A  lib/git.rb</span><br><span class="line">M  lib/simplegit.rb</span><br><span class="line">?? LICENSE.txt</span><br><span class="line">`````</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">​输出结果：左栏有两列，第一列指明了**暂存区的状态**，第二列指明了**工作区的状态**（新添加的未跟踪文件前面有 `??` 标记，新添加到暂存区中的文件前面有 `A` 标记，修改过的文件前面有 `M` 标记)，右栏指相应文件。</span><br><span class="line"></span><br><span class="line">**常见输出**</span><br><span class="line"></span><br><span class="line">`Untracked files` : 未跟踪的文件</span><br><span class="line"></span><br><span class="line">`Changes to be committed`： 已暂存状态</span><br><span class="line"></span><br><span class="line">`Changes not staged for commit`(未暂存清单) : 说明已跟踪文件的内容发生了变化，但还没有放到暂存区。 要暂存这次更新,需要运行 `git add` 命令.</span><br><span class="line"></span><br><span class="line">---</span><br><span class="line"></span><br><span class="line">2. 查看已暂存和未暂存的修改</span><br><span class="line"></span><br><span class="line">​`git diff`  此命令比较的是工作目录中当前文件和暂存区域快照之间的差异。 也就是修改之后还没有暂存起来的变化内容</span><br><span class="line"></span><br><span class="line">​        `git diff -staged` 查看已暂存的将要添加到下次提交里的内容</span><br><span class="line"></span><br><span class="line">​`git diff --cached` 查看已经暂存起来的变化</span><br><span class="line"></span><br><span class="line">-----</span><br><span class="line"></span><br><span class="line">3. 提交记录</span><br><span class="line"></span><br><span class="line">   `git commit`</span><br><span class="line"></span><br><span class="line">   `git commit -a` :给 `git commit` 加上 `-a` 选项，Git 就会自动把所有已经跟踪过的文件暂存起来一并提交，从而跳过 `git add` 步骤</span><br><span class="line"></span><br><span class="line">-----</span><br><span class="line"></span><br><span class="line">4. 移除文件（从暂存区域移除）</span><br><span class="line"></span><br><span class="line">   `git rm`   一定要提交此次删除才能从暂存区域移除文件(同时删除工作目录的文件)</span><br><span class="line"></span><br><span class="line">   `git rm --cached`  把文件从 Git 仓库中删除（亦即从暂存区域移除），但仍然希望保留在当前工作目录中（不会删除工作目录上的文件）</span><br><span class="line"></span><br><span class="line">   ----</span><br><span class="line"></span><br><span class="line">5. 查看提交日志</span><br><span class="line"></span><br><span class="line">    **`git log`**</span><br><span class="line"></span><br><span class="line">    作用:查看提交记录</span><br><span class="line"></span><br><span class="line">    命令形式：`git log [option]`</span><br><span class="line"></span><br><span class="line">    `options:`</span><br><span class="line"></span><br><span class="line">    - --all 显示所有分支</span><br><span class="line"></span><br><span class="line">    - --pretty=oneline 将提交信息显示为一行</span><br><span class="line"></span><br><span class="line">    - --abbrev-commit 使得输出的commitId更简短</span><br><span class="line"></span><br><span class="line">    - --graph 以图的形式显示</span><br><span class="line"></span><br><span class="line">    - --pretty=format 定制记录的显示格式</span><br><span class="line"></span><br><span class="line">    常用命令</span><br><span class="line"></span><br><span class="line">    `git log --online` </span><br><span class="line"></span><br><span class="line">    `git log --graph`</span><br><span class="line">    </span><br><span class="line">    `git log -p | -patch  -N` 查看最近N次提交，附带显示每次提交的差异</span><br><span class="line">    </span><br><span class="line">    `git log -stat` 在每次提交的下面列出所有被修改过的文件、有多少文件被修改了以及被修改过的文件的哪些行被移除或是添加了。 在每次提交的最后还有一个总结.</span><br><span class="line"></span><br><span class="line">----</span><br><span class="line"></span><br><span class="line">2. ​撤销操作</span><br><span class="line"></span><br><span class="line">   `git commit -amend` 修改最近一次提交的提交信息,本质上是**新的提交** 替换旧的提交</span><br><span class="line"></span><br><span class="line">   `git reset HEAD &lt;file&gt;`  指定取消暂存区的某个文件</span><br><span class="line"></span><br><span class="line">   ` git checkout — &lt;file&gt;`  ：**撤消对文件的修改** -》 对那个文件在本地的任何修改都会消失——Git 会用最近提交的版本覆盖掉它</span><br><span class="line"></span><br><span class="line">   ----</span><br><span class="line"></span><br><span class="line">2.  远程仓库</span><br><span class="line"></span><br><span class="line">   `git remote ` 只列举远程仓库名</span><br><span class="line"></span><br><span class="line">   `git remote -v` 列举远程仓库名,同时显示与其对应的URL</span><br><span class="line"></span><br><span class="line">   `git remote add &lt;shortname&gt; &lt;url&gt;`  添加远程仓库</span><br><span class="line"></span><br><span class="line">   **从远程仓库中抓取与拉取**  </span><br><span class="line"></span><br><span class="line">      - `git  fetch &lt;remote&gt; ` 只会将数据下载到你的本地仓库——它并不会自动合并或修改你当前的工作</span><br><span class="line">      - `git pull` 自动抓取后合并该远程分支到当前分支,前提是你的当前分支设置了跟踪远程分支</span><br><span class="line"></span><br><span class="line">   `git push &lt;remote&gt; &lt;branch&gt;`  推送到远程仓库</span><br><span class="line"></span><br><span class="line">​`git remote show &lt;remote&gt;`  查看某一个远程仓库的更多信息</span><br><span class="line"></span><br><span class="line">&gt; **它同样会列出远程仓库的 URL 与跟踪分支的信息。 这些信息非常有用，它告诉你正处于 `master` 分支，并且如果运行 `git pull`， 就会抓取所有的远程引用，然后将远程 `master` 分支合并到本地 `master` 分支。 它也会列出拉取到的所有远程引用。**</span><br><span class="line"></span><br><span class="line">​`git remote rename &lt;oldName&gt; &lt;newName&gt;`   远程仓库的重命名</span><br><span class="line"></span><br><span class="line">​`git remote remove | rm &lt;remoteName&gt;` 远程仓库的移除</span><br><span class="line"></span><br><span class="line">-----</span><br><span class="line"></span><br><span class="line">4. 打标签（人们会使用这个功能来标记发布结点）</span><br><span class="line"></span><br><span class="line">   `git tag [-l | --list ]`   列出已有的标签</span><br><span class="line"></span><br><span class="line">   **创建标签** </span><br><span class="line"></span><br><span class="line">   - 轻量标签  `git tag &lt;tagName&gt;` </span><br><span class="line">   - 附属标签 `git tag -a &lt;tagName&gt;-m &quot;&lt;message&gt;&quot;` </span><br><span class="line"></span><br><span class="line">    后期打标签  `git tag -a &lt;tagName&gt; &lt;散列值&gt;`  对过去的提交打标签，散列值可以使用`git log`查看</span><br><span class="line"></span><br><span class="line">   &gt; 共享标签 :`git push` 命令并不会传送标签到远程仓库服务器上。 在创建完标签后你必须显式地推送标签到共享服务器上。 这个过程就像共享远程分支一样——你可以运行 `git push origin &lt;tagname&gt;`如果想要一次性推送很多标签，也可以使用带有 `--tags` 选项的 `git push` 命令。 这将会把所有不在远程仓库服务器上的标签全部传送到那里</span><br><span class="line"></span><br><span class="line">   **删除标签**</span><br><span class="line"></span><br><span class="line">   `git tag -d &lt;tagname&gt;`  删除掉本地仓库上的标签</span><br><span class="line">   </span><br><span class="line">    删除远程仓库标签:  </span><br><span class="line">   </span><br><span class="line">    - `git push &lt;remote&gt; :refs/tags/&lt;tagname&gt;`</span><br><span class="line">   </span><br><span class="line">    - `git push origin --delete &lt;tagName&gt;`</span><br><span class="line"></span><br><span class="line">&gt;  ​**检出标签**</span><br><span class="line">&gt;</span><br><span class="line">&gt; 如果你想查看某个标签所指向的文件版本，可以使用 `git checkout` 命令， 虽然这会使你的仓库处于“分离头指针（detached HEAD）”的状态——这个状态有些不好的副作用：</span><br><span class="line">&gt;</span><br><span class="line">&gt; ```console</span><br><span class="line">&gt; $ git checkout 2.0.0</span><br><span class="line">&gt; Note: checking out &#x27;2.0.0&#x27;.</span><br><span class="line">&gt; </span><br><span class="line">&gt; You are in &#x27;detached HEAD&#x27; state. You can look around, make experimental</span><br><span class="line">&gt; changes and commit them, and you can discard any commits you make in this</span><br><span class="line">&gt; state without impacting any branches by performing another checkout.</span><br><span class="line">&gt; </span><br><span class="line">&gt; If you want to create a new branch to retain commits you create, you may</span><br><span class="line">&gt; do so (now or later) by using -b with the checkout command again. Example:</span><br><span class="line">&gt; </span><br><span class="line">&gt;   git checkout -b &lt;new-branch&gt;</span><br><span class="line">&gt; </span><br><span class="line">&gt; HEAD is now at 99ada87... Merge pull request #89 from schacon/appendix-final</span><br><span class="line">&gt; </span><br><span class="line">&gt; $ git checkout 2.0-beta-0.1</span><br><span class="line">&gt; Previous HEAD position was 99ada87... Merge pull request #89 from schacon/appendix-final</span><br><span class="line">&gt; HEAD is now at df3f601... add atlas.json and cover image</span><br></pre></td></tr></table></figure></li></ol><blockquote><p>在“分离头指针”状态下，如果你做了某些更改然后提交它们，标签不会发生变化， 但你的新提交将不属于任何分支，并且将无法访问，除非通过确切的提交哈希才能访问。 因此，如果你需要进行更改，比如你要修复旧版本中的错误，那么通常需要创建一个新分支：</p><figure class="highlight console"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">git checkout -b version2 v2.0.0</span></span><br><span class="line">Switched to a new branch &#x27;version2&#x27;</span><br></pre></td></tr></table></figure><p>如果在这之后又进行了一次提交，<code>version2</code> 分支就会因为这个改动向前移动， 此时它就会和 <code>v2.0.0</code> 标签稍微有些不同，这时就要当心了。</p></blockquote><hr><ol start="5"><li>Git 别名</li></ol><p>   <code>git config --global alias.&lt;别名&gt; &#39;&lt;git子命令&#39;&gt;</code>  </p><p>   <code>git config --global alias.&lt;别名&gt; &#39;!命令&#39;</code></p><hr><ol start="6"><li><strong>版本回退(版本穿梭)</strong></li></ol><p>(1) <code>git reset</code></p><ul><li><p>命令形式：git reset –hard commitID</p><p>commitID 可以使用 git-log 或 git log 指令查看</p></li><li><p>如何查看已经删除的记录？</p></li></ul><p>​       <code>git reflog</code><strong>这个指令可以看到已经删除的提交记录</strong></p><p>(2) <code>git reset --hard(--soft或--mixed)</code></p><ul><li>–hard : 本地仓库、暂存区、工作区都回退版本</li><li>– soft: 只有本地仓库回退，暂存区和工作区不会回退</li><li>–mixed:这是默认操作，本地仓库和暂存区回退，工作区不会改变</li></ul><hr><ol start="7"><li><strong><span style="color: red">分支</span></strong></li></ol><p>​<code>git branch  &lt;brachName&gt;</code> 新建分支</p><p>​<code>git checkout &lt;brachName&gt;</code>   切换分支</p><p>​<code>git checkout -b &lt;branchName&gt;</code> 新建分支并切换到该分支</p><p>​<code>git branch -d &lt;branchName&gt;</code> 删除分支</p><p>​<code>git merge &lt;branchName&gt;</code> 合并其他分支到当前分支</p><p>​<code>git branch</code> 得到当前所有分支的一个列表</p><p>​<code>git branch -v</code> 查看每一个分支的最后一次提交</p><p>​<code>git branch --merged</code> 查看哪些分支已经合并到当前分支</p><p>​<code>git branch --no-merged</code>   查看所有包含未合并工作的分支</p><hr><blockquote><pre><code>  **遇到冲突时的分支合并**  在合并冲突后的任意时刻使用 `git status` 命令来查看那些因包含合并冲突而处于未合并（unmerged）状态的文件：  <figure class="highlight console"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">git status</span></span><br><span class="line">On branch master</span><br><span class="line">You have unmerged paths.</span><br><span class="line">  (fix conflicts and run &quot;git commit&quot;)</span><br><span class="line"></span><br><span class="line">Unmerged paths:</span><br><span class="line">  (use &quot;git add &lt;file&gt;...&quot; to mark resolution)</span><br><span class="line"></span><br><span class="line">    both modified:      index.html</span><br><span class="line"></span><br><span class="line">no changes added to commit (use &quot;git add&quot; and/or &quot;git commit -a&quot;)</span><br></pre></td></tr></table></figure>  任何因包含合并冲突而有待解决的文件，都会以未合并状态标识出来。 Git 会在有冲突的文件中加入标准的冲突解决标记，这样你可以打开这些包含冲突的文件然后手动解决冲突。 出现冲突的文件会包含一些特殊区段，看起来像下面这个样子：  <figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD:index.html</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;footer&quot;</span>&gt;</span>contact : email.support@github.com<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">=======</span><br><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;footer&quot;</span>&gt;</span></span><br><span class="line">please contact us at support@github.com</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; iss53:index.html</span><br></pre></td></tr></table></figure>  这表示 `HEAD` 所指示的版本（也就是你的 `master` 分支所在的位置，因为你在运行 merge 命令的时候已经检出到了这个分支）在这个区段的上半部分（`=======` 的上半部分），而 `iss53` 分支所指示的版本在 `=======` 的下半部分。 为了解决冲突，你必须选择使用由 `=======` 分割的两部分中的一个，或者你也可以自行合并这些内容。 例如，你可以通过把这段内容换成下面的样子来解决冲突：  <figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">&quot;footer&quot;</span>&gt;</span></span><br><span class="line">please contact us at email.support@github.com</span><br><span class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br></pre></td></tr></table></figure>  上述的冲突解决方案仅保留了其中一个分支的修改，并且 `&lt;&lt;&lt;&lt;&lt;&lt;&lt;` , `=======` , 和 `&gt;&gt;&gt;&gt;&gt;&gt;&gt;` 这些行被完全删除了。 在你解决了所有文件里的冲突之后，对每个文件使用 `git add` 命令来将其标记为冲突已解决。 一旦暂存这些原本有冲突的文件，Git 就会将它们标记为冲突已解决。</code></pre></blockquote><hr><ol start="8"><li><p>变基</p><p><code>git resbase &lt;branceName&gt;</code> </p><p><strong>原理：</strong></p><p>在这个例子中，你可以检出 <code>experiment</code> 分支，然后将它变基到 <code>master</code> 分支上：</p><figure class="highlight console"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">git checkout experiment</span></span><br><span class="line"><span class="meta prompt_">$ </span><span class="language-bash">git rebase master</span></span><br><span class="line">First, rewinding head to replay your work on top of it...</span><br><span class="line">Applying: added staged command</span><br></pre></td></tr></table></figure><p>它的原理是首先找到这两个分支（即当前分支 <code>experiment</code>、变基操作的目标基底分支 <code>master</code>） 的最近共同祖先 <code>C2</code>，然后对比当前分支相对于该祖先的历次提交，提取相应的修改并存为临时文件， 然后将当前分支指向目标基底 <code>C3</code>, 最后以此将之前另存为临时文件的修改依序应用。 （译注：写明了 commit id，以便理解，下同）</p><p><img src="/img_posts/basic-rebase-3.png" alt="将 `C4` 中的修改变基到 `C3` 上。"></p></li></ol><hr><ol start="9"><li>存储</li></ol><p>git stash命令的语法如下表所示。</p><table><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td>git stath</td><td>将当前工作空间的状态保存</td></tr><tr><td>git stash list</td><td>查看当前Git中存储的所有状态</td></tr><tr><td>git stash apply {stashName}</td><td>根据存储名称读取Git存储</td></tr><tr><td>git stash drop {stashName}</td><td>根据存储名称删除Git存储</td></tr><tr><td>git stash save “日志信息”</td><td>将当前工作空间的状态保存并指定一个日志信息</td></tr><tr><td>git stash pop</td><td>读取stash堆栈中的第一个存储，并将该存储从stash堆栈中移除</td></tr><tr><td>git stash show [-p] {stashName}</td><td>查看指定存储与未建立存储时的差异 -p：显示详细差异</td></tr><tr><td>git stash branch {branchName} [stashName]</td><td>创建并切换到一个新分支来读取指定的存储  stashName：存储的名称，默认情况下读取stash堆栈中栈顶的存储</td></tr></tbody></table><h2 id="八、不常用命令"><a href="#八、不常用命令" class="headerlink" title="八、不常用命令"></a>八、不常用命令</h2><p><code>git rev-parse --abbrev-ref @{u}</code>  查看当前分支的上游分支</p><h2 id="九、多人协同开发"><a href="#九、多人协同开发" class="headerlink" title="九、多人协同开发"></a>九、多人协同开发</h2><ol><li>托管平台的仓库上的成员管理添加成员</li><li><code>git clone </code> 仓库</li><li>新建分支</li><li>推送到远程仓库新的分支<code> git push &lt;仓库名&gt; 分支名：远程仓库分支名（远程没有会新建）</code></li></ol><h2 id="推送本地分支到远程指定分支"><a href="#推送本地分支到远程指定分支" class="headerlink" title="推送本地分支到远程指定分支"></a>推送本地分支到远程指定分支</h2><p>git checkout -b <bracname></p><p>git push origin <bracname>&#x2F;&lt;&gt;</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Nginx 入门教程</title>
      <link href="/2025/06/10/Nginx-%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8B/"/>
      <url>/2025/06/10/Nginx-%E5%85%A5%E9%97%A8%E6%95%99%E7%A8%8B/</url>
      
        <content type="html"><![CDATA[<h2 id="1-什么是-Nginx"><a href="#1-什么是-Nginx" class="headerlink" title="1. 什么是 Nginx?"></a>1. 什么是 Nginx?</h2><p>Nginx 是一个高性能的 HTTP 服务器和反向代理服务器。它最初由 Igor Sysoev 开发，旨在解决 C10K 问题，即在单台服务器上处理 10,000 个并发连接的问题。如今，NGINX 还可以作为负载均衡器、邮件代理服务器和通用 TCP&#x2F;UDP 代理服务器。</p><p><img src="https://cdn.nlark.com/yuque/0/2024/png/26411187/1725116572685-5d6963b3-0934-484a-9178-7d04d43ebbda.png"></p><h2 id="2-Nginx-的基本概念"><a href="#2-Nginx-的基本概念" class="headerlink" title="2. Nginx 的基本概念"></a>2. Nginx 的基本概念</h2><ul><li><strong>Web 服务器</strong>: Nginx 可用作静态内容（如 HTML 页面、图片和视频）的 Web 服务器。它处理客户端请求并返回相应的内容。</li><li><strong>反向代理</strong>: <font style="color:rgb(31, 35, 40);">是指以代理服务器来接受 internet 上的连接请求，然后将请求转发给内部网络上的服务器，并将从服务器上得到的结果返回给 internet 上请求连接的客户端，此时代理服务器对外就表现为一个反向代理服务器。</font></li><li><strong>负载均衡</strong>: Nginx 可以将流量分发到多个后端服务器，以提高系统的性能和可靠性。</li></ul><h2 id="3-安装-Nginx"><a href="#3-安装-Nginx" class="headerlink" title="3. 安装 Nginx"></a>3. 安装 Nginx</h2><h3 id="3-1-在windows上安装"><a href="#3-1-在windows上安装" class="headerlink" title="3.1 在windows上安装"></a>3.1 在windows上安装</h3><ol><li><strong>下载Nginx</strong>:<ul><li>访问Nginx的官方网站：<a href="https://nginx.org/en/download.html">https://nginx.org/en/download.html</a></li><li>选择适合Windows的版本下载。通常是一个<code>.zip</code>文件。</li></ul></li><li><strong>解压Nginx</strong>:<ul><li>将下载的<code>.zip</code>文件解压到你选择的目录。例如，你可以将其解压到<code>C:\nginx</code>。</li></ul></li><li><strong>启动Nginx</strong>:<ul><li>打开命令提示符（CMD）或PowerShell。</li><li>切换到Nginx的目录，例如，如果你将其解压到<code>C:\nginx</code>，你可以输入：</li></ul></li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">cd C:\nginx</span><br></pre></td></tr></table></figure><pre><code>- 运行Nginx：</code></pre><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">start nginx</span><br></pre></td></tr></table></figure><p>或者直接双击<code>nginx.exe</code>文件。</p><ol start="4"><li><strong>验证Nginx是否运行</strong>:<ul><li>在浏览器中输入<code>http://localhost:8080</code>或<code>http://127.0.0.1:8080</code>，如果Nginx正在运行，你应该能看到Nginx的欢迎页面。</li></ul></li><li><strong>配置Nginx</strong>:<ul><li>Nginx的配置文件通常位于Nginx目录下的<code>conf</code>文件夹中，名为<code>nginx.conf</code>。</li><li>你可以编辑这个文件来配置服务器，例如设置端口、添加虚拟主机等。</li></ul></li><li><strong>重启Nginx以应用更改</strong>:<ul><li>如果你修改了配置文件，需要重启Nginx以使更改生效。在命令行中运行：</li></ul></li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nginx -s reload</span><br></pre></td></tr></table></figure><ol start="7"><li><strong>关闭Nginx</strong>:<ul><li>如果需要停止Nginx，可以在命令行中运行：</li></ul></li></ol><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">nginx -s stop</span><br></pre></td></tr></table></figure><p>请注意，如果你的系统防火墙开启，可能需要在防火墙设置中允许Nginx使用的端口（默认是80和443）。</p><p>如果你需要更详细的指导或遇到问题，可以查看Nginx的官方文档或搜索相关的教程。</p><h3 id="3-2-在Mac上安装"><a href="#3-2-在Mac上安装" class="headerlink" title="3.2 在Mac上安装"></a>3.2 在Mac上安装</h3><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">// 安装</span><br><span class="line">brew install nginx</span><br><span class="line"></span><br><span class="line">// 启动nginx</span><br><span class="line">brew services start nginx</span><br><span class="line"></span><br><span class="line">// 重启nginx</span><br><span class="line">brew services restart nginx</span><br></pre></td></tr></table></figure><h2 id="4-Nginx基本命令"><a href="#4-Nginx基本命令" class="headerlink" title="4.Nginx基本命令"></a>4.Nginx基本命令</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">nginx -s stop       快速关闭Nginx，可能不保存相关信息，并迅速终止web服务。</span><br><span class="line">nginx -s quit       平稳关闭Nginx，保存相关信息，有安排的结束web服务。</span><br><span class="line">nginx -s reload     因改变了Nginx相关配置，需要重新加载配置而重载。</span><br><span class="line">nginx -s reopen     重新打开日志文件。</span><br><span class="line">nginx -c filename   为 Nginx 指定一个配置文件，来代替缺省的。</span><br><span class="line">nginx -t            不运行，仅仅测试配置文件。nginx 将检查配置文件的语法的正确性，并尝试打开配置文件中所引用到的文件。</span><br><span class="line">nginx -v            显示 nginx 的版本。</span><br><span class="line">nginx -V            显示 nginx 的版本，编译器版本和配置参数。</span><br></pre></td></tr></table></figure><h2 id="5-Nginx配置文件介绍"><a href="#5-Nginx配置文件介绍" class="headerlink" title="5. Nginx配置文件介绍"></a>5. Nginx配置文件介绍</h2><p>Nginx 的配置文件通常由一系列的块组成，这些块按照层级顺序组织，每个块定义了不同的配置上下文。主要的配置块包括 <code>http</code>、<code>server</code> 和 <code>location</code>，每个块都有其特定的作用和配置指令。以下是对这些配置块的详细介绍：</p><h3 id="1-http-块"><a href="#1-http-块" class="headerlink" title="1. http 块"></a>1. <code>http</code> 块</h3><p><code>http</code> 块是配置文件中最高级别的块之一（另一个是 <code>events</code>），它定义了 HTTP 服务器的全局设置。在 <code>http</code> 块中，你可以设置各种参数，如日志路径、缓存路径、默认的 <code>server</code> 和 <code>location</code> 行为等。<code>http</code> 块可以包含多个 <code>server</code> 块。</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">http</span> &#123;</span><br><span class="line">    <span class="attribute">include</span>       mime.types;</span><br><span class="line">    <span class="attribute">default_type</span>  application/octet-stream;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 日志文件定义</span></span><br><span class="line">    <span class="attribute">access_log</span>  /var/log/nginx/access.log;</span><br><span class="line">    <span class="attribute">error_log</span>   /var/log/nginx/<span class="literal">error</span>.log;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 其他全局设置...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="2-server-块"><a href="#2-server-块" class="headerlink" title="2. server 块"></a>2. <code>server</code> 块</h3><p><code>server</code> 块定义了虚拟主机的配置，每个 <code>server</code> 块代表一个虚拟主机，可以监听一个或多个 IP 地址和端口。在 <code>server</code> 块中，你可以设置域名、端口、SSL 证书、日志文件、代理设置等。</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">server</span> &#123;</span><br><span class="line">    <span class="attribute">listen</span>       <span class="number">80</span>;</span><br><span class="line">    <span class="attribute">server_name</span>  example.com www.example.com;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 代理设置、静态文件处理、重定向等...</span></span><br><span class="line">    <span class="section">location</span> / &#123;</span><br><span class="line">        <span class="attribute">root</span>   /usr/share/nginx/html;</span><br><span class="line">        <span class="attribute">index</span>  index.html index.htm;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 其他 location 块...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="3-location-块"><a href="#3-location-块" class="headerlink" title="3. location 块"></a>3. <code>location</code> 块</h3><p><code>location</code> 块定义了请求的处理方式，它可以根据请求的 URI、命名管道或正则表达式来匹配和处理请求。<code>location</code> 块可以存在于 <code>server</code> 块中，也可以嵌套在其他 <code>location</code> 块中。在 <code>location</code> 块中，你可以设置代理、重写规则、静态文件服务、权限控制等。</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">location</span> / &#123;</span><br><span class="line">    <span class="comment"># 处理对 / 的请求</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> = / &#123;</span><br><span class="line">    <span class="comment"># 精确匹配 /</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> <span class="regexp">~* \.(jpg|jpeg|png)$</span> &#123;</span><br><span class="line">    <span class="comment"># 处理所有 jpg、jpeg 和 png 图片请求</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="section">location</span> <span class="variable">@fallback</span> &#123;</span><br><span class="line">    <span class="comment"># 命名 location，用作错误页面处理</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="其他重要指令"><a href="#其他重要指令" class="headerlink" title="其他重要指令"></a>其他重要指令</h3><ul><li><code>listen</code>：指定 <code>server</code> 块监听的端口和 IP 地址。</li><li><code>server_name</code>：定义 <code>server</code> 块的域名。</li><li><code>root</code>：定义服务器的根目录，用于静态文件服务。</li><li><code>index</code>：定义默认的首页文件。</li><li><code>proxy_pass</code>：将请求代理到另一个服务器。</li><li><code>try_files</code>：尝试按顺序查找并返回文件，直到找到为止。</li><li><code>error_page</code>：定义错误页面的重定向。</li><li><code>rewrite</code>：重写 URL 规则。</li></ul><h3 id="示例配置"><a href="#示例配置" class="headerlink" title="示例配置"></a>示例配置</h3><p>下面是一个更完整的示例配置，展示了 <code>http</code>、<code>server</code> 和 <code>location</code> 块的组合使用：</p><figure class="highlight nginx"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br></pre></td><td class="code"><pre><span class="line"><span class="section">http</span> &#123;</span><br><span class="line">    <span class="attribute">include</span>       mime.types;</span><br><span class="line">    <span class="attribute">default_type</span>  application/octet-stream;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 定义日志文件</span></span><br><span class="line">    <span class="attribute">access_log</span>  /var/log/nginx/access.log  main;</span><br><span class="line">    <span class="attribute">error_log</span>   /var/log/nginx/<span class="literal">error</span>.log;</span><br><span class="line"></span><br><span class="line">    <span class="comment"># 定义负载均衡服务器组</span></span><br><span class="line">    <span class="section">upstream</span> myapp &#123;</span><br><span class="line">        <span class="attribute">server</span> app1.example.com;</span><br><span class="line">        <span class="attribute">server</span> app2.example.com;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="section">server</span> &#123;</span><br><span class="line">        <span class="attribute">listen</span>       <span class="number">80</span>;</span><br><span class="line">        <span class="attribute">server_name</span>  example.com www.example.com;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理根目录请求</span></span><br><span class="line">        <span class="section">location</span> / &#123;</span><br><span class="line">            <span class="attribute">root</span>   /usr/share/nginx/html;</span><br><span class="line">            <span class="attribute">index</span>  index.html index.htm;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理图片文件</span></span><br><span class="line">        <span class="section">location</span> <span class="regexp">~* \.(jpg|jpeg|png)$</span> &#123;</span><br><span class="line">            <span class="attribute">root</span>   /usr/share/nginx/images;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 代理到后端应用服务器</span></span><br><span class="line">        <span class="section">location</span> /api &#123;</span><br><span class="line">            <span class="attribute">proxy_pass</span> http://myapp;</span><br><span class="line">            <span class="attribute">proxy_set_header</span> Host <span class="variable">$host</span>;</span><br><span class="line">            <span class="attribute">proxy_set_header</span> X-Real-IP <span class="variable">$remote_addr</span>;</span><br><span class="line">            <span class="attribute">proxy_set_header</span> X-Forwarded-For <span class="variable">$proxy_add_x_forwarded_for</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 静态文件服务</span></span><br><span class="line">        <span class="section">location</span> /static &#123;</span><br><span class="line">            <span class="attribute">alias</span> /var/www/static/;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 处理 404 错误</span></span><br><span class="line">        <span class="attribute">error_page</span> <span class="number">404</span> /<span class="number">404</span>.html;</span><br><span class="line">        <span class="section">location</span> = /<span class="number">404</span>.html &#123;</span><br><span class="line">            <span class="attribute">root</span> /usr/share/nginx/html;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个示例中，<code>http</code> 块定义了全局设置，<code>server</code> 块定义了虚拟主机的配置，而 <code>location</code> 块则根据不同的请求路径定义了具体的处理规则。这种结构使得 Nginx 的配置非常灵活和强大。</p><h2 id="6-HTTP反向代理"><a href="#6-HTTP反向代理" class="headerlink" title="6.HTTP反向代理"></a>6.HTTP反向代理</h2><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line">server &#123;</span><br><span class="line">    listen       80;</span><br><span class="line">    server_name  fengzhu.com;</span><br><span class="line"></span><br><span class="line">    location / &#123;</span><br><span class="line">        proxy_pass http://my_server1;</span><br><span class="line">        proxy_set_header Host $host;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    error_log /tmp/error.log warn;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">upstream my_server1 &#123;</span><br><span class="line">    server localhost:8080;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>这样，访问fengzhu.com的请求都会被转发到后端8080端口上</p><h2 id="7-负载均衡"><a href="#7-负载均衡" class="headerlink" title="7.负载均衡"></a>7.负载均衡</h2><p><img src="https://cdn.nlark.com/yuque/0/2024/png/26411187/1725116525658-b4cb41ed-807c-4164-913e-3118ba146ea9.png"></p><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">upstream my_server1 &#123;</span><br><span class="line">        server localhost:8080;</span><br><span class="line">        server localhost:8081;</span><br><span class="line">        server localhost:8082;</span><br><span class="line">    &#125;</span><br></pre></td></tr></table></figure><h3 id="负载均衡策略"><a href="#负载均衡策略" class="headerlink" title="负载均衡策略"></a>负载均衡策略</h3><p>Nginx 支持多种负载均衡策略，可以在 <code>upstream</code> 块中配置以实现不同的请求分发方式。以下是 Nginx 支持的一些常见负载均衡策略：</p><ol><li><strong>轮询（round-robin）</strong>：默认的负载均衡方法，将请求依次分配给每个服务器，不考虑服务器的当前负载情况。</li><li><strong>权重（weight）</strong>：在轮询的基础上，可以根据服务器的性能或角色分配不同的权重，权重高的服务器将处理更多的请求。</li><li><strong>最少连接（least_conn）</strong>：将请求分配给当前活跃连接数最少的服务器，适合处理时间长短不一的请求。</li><li><strong>IP哈希（ip_hash）</strong>：通过客户端IP地址进行哈希，确保来自同一IP的请求总是被分配到同一个服务器上，适用于需要保持会话持久性的应用。</li><li><strong>公平（fair）</strong>：（需要第三方模块支持）根据服务器的响应时间来分配请求，响应时间越短的服务器优先分配请求。</li><li><strong>URL哈希（url_hash）</strong>：（需要第三方模块支持）根据请求的URL进行哈希，使得相同的URL总是被分配到同一个服务器上，适合缓存服务器的场景。</li></ol><p>除了上述策略，还可以结合使用 <code>max_fails</code>、<code>fail_timeout</code> 和 <code>backup</code> 等参数来增强负载均衡的健壮性。例如，<code>max_fails</code> 可以设置尝试连接后端服务器失败的次数，<code>fail_timeout</code> 用于设置在一定时间内失败达到 <code>max_fails</code> 次数后，服务器被认为是不可用的，而 <code>backup</code> 用于标记某个服务器作为备份服务器，在所有主服务器都不可用时才会被使用。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Spring Security</title>
      <link href="/2025/05/09/Spring-Security/"/>
      <url>/2025/05/09/Spring-Security/</url>
      
        <content type="html"><![CDATA[<h1 id="Spring-Security"><a href="#Spring-Security" class="headerlink" title="Spring Security"></a>Spring Security</h1><h2 id="一、用户认证流程（Authentication）"><a href="#一、用户认证流程（Authentication）" class="headerlink" title="一、用户认证流程（Authentication）"></a>一、用户认证流程（Authentication）</h2><h3 id="流程步骤："><a href="#流程步骤：" class="headerlink" title="流程步骤："></a>流程步骤：</h3><ol><li><strong>用户提交登录请求</strong><br> 用户通过表单提交用户名和密码，发起登录请求。</li><li><strong>请求交给 <code>UsernamePasswordAuthenticationFilter</code> 过滤器处理</strong><br> Spring Security 默认会通过 <code>UsernamePasswordAuthenticationFilter</code> 来处理基于用户名和密码的认证。</li><li><strong>获取用户名和密码，生成 <code>AuthenticationToken</code></strong><br> <code>UsernamePasswordAuthenticationFilter</code> 从请求中提取用户名和密码，创建一个 <code>UsernamePasswordAuthenticationToken</code> 对象。</li><li><strong>交给 <code>AuthenticationManager</code> 进行认证</strong><br> 创建的 <code>AuthenticationToken</code> 会被传递给 <code>AuthenticationManager</code>，由其进一步处理认证过程。</li><li><strong>通过 <code>UserDetailsService</code> 获取用户信息，校验密码</strong><br> <code>AuthenticationManager</code> 会使用 <code>UserDetailsService</code> 来加载用户的详细信息，然后使用 <code>PasswordEncoder</code> 对比输入的密码与存储的密码是否匹配。</li><li><strong>认证成功或失败处理</strong><ul><li>如果密码正确，<code>AuthenticationManager</code> 会生成一个认证成功的 <code>Authentication</code> 对象。</li><li>如果密码不正确，<code>AuthenticationManager</code> 会抛出 <code>AuthenticationException</code> 异常。</li></ul></li><li><strong>成功认证处理</strong><br> <code>UsernamePasswordAuthenticationFilter</code> 将认证成功的 <code>Authentication</code> 对象交给 <code>SecurityContextHolder</code> 进行管理。接着，<code>AuthenticationSuccessHandler</code> 会被调用来处理认证成功的后续操作。</li><li><strong>失败认证处理</strong><br> 如果认证失败，<code>UsernamePasswordAuthenticationFilter</code> 会调用 <code>AuthenticationFailureHandler</code> 来处理认证失败的情况，例如返回错误消息或重定向到登录页面。</li></ol><h3 id="流程图"><a href="#流程图" class="headerlink" title="流程图"></a>流程图</h3><p><img src="/img_posts/image-20250509223706686.png" alt="image-20250509223706686"></p><h3 id="相关类和接口："><a href="#相关类和接口：" class="headerlink" title="相关类和接口："></a>相关类和接口：</h3><table><thead><tr><th>角色</th><th>类 &#x2F; 接口</th><th>你项目中的实现</th></tr></thead><tbody><tr><td>用户信息服务</td><td><code>UserDetailsService</code></td><td><code>UserDetailsServiceImpl</code></td></tr><tr><td>用户信息模型</td><td><code>UserDetails</code></td><td>Spring 提供的 <code>User</code></td></tr><tr><td>身份验证管理器</td><td><code>AuthenticationManager</code></td><td>自定义 <code>@Bean</code> 配置</td></tr><tr><td>提供者</td><td><code>DaoAuthenticationProvider</code></td><td>在 <code>SecurityConfig</code> 中配置</td></tr></tbody></table><p>##相关配置</p><p><strong>SecurityConfig配置</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">package</span> com.ch.personmis.security;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.apache.tomcat.util.security.MD5Encoder;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Bean;</span><br><span class="line"><span class="keyword">import</span> org.springframework.context.annotation.Configuration;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.authentication.AuthenticationManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.authentication.ProviderManager;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.authentication.dao.DaoAuthenticationProvider;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.Customizer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.builders.HttpSecurity;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.config.annotation.web.configurers.HeadersConfigurer;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.security.core.userdetails.UserDetailsService;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.SecurityFilterChain;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.annotation.Resource;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">SecurityConfig</span> &#123;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> UserDetailsService userDetailsServiceImpl;</span><br><span class="line">    <span class="meta">@Resource</span></span><br><span class="line">    <span class="keyword">private</span> MyAuthenticationSuccessHandler myAuthenticationSuccessHandler;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> SecurityFilterChain <span class="title function_">securityFilterChain</span><span class="params">(HttpSecurity httpSecurity)</span> <span class="keyword">throws</span> Exception &#123;</span><br><span class="line">        httpSecurity</span><br><span class="line">                .csrf().disable() <span class="comment">// 关闭csrf防护</span></span><br><span class="line">                .authorizeHttpRequests(authorize -&gt; authorize</span><br><span class="line">                        .antMatchers(<span class="string">&quot;/login&quot;</span>).permitAll()  <span class="comment">// 放行所有访问者</span></span><br><span class="line">                        .antMatchers(<span class="string">&quot;/getDepartment/**&quot;</span>).hasRole(<span class="string">&quot;user&quot;</span>) <span class="comment">//指定权限，security会在user前加上‘ROLE_&#x27;</span></span><br><span class="line">                        .anyRequest().authenticated() </span><br><span class="line">                )</span><br><span class="line">                .cors(Customizer.withDefaults()) <span class="comment">// 启用默认 CORS 配置 //主要放行option预检请求，否则会报跨域错误</span></span><br><span class="line">                .headers(HeadersConfigurer::cacheControl)</span><br><span class="line">                .formLogin()  <span class="comment">// 不指定loginpage</span></span><br><span class="line">                .loginProcessingUrl(<span class="string">&quot;/login&quot;</span>)<span class="comment">// 指定前端post登录请求提交地址，不接受json的数据，默认接受表单格式数据</span></span><br><span class="line">                .usernameParameter(<span class="string">&quot;uname&quot;</span>)  <span class="comment">// 对应 input 的 name</span></span><br><span class="line">                .passwordParameter(<span class="string">&quot;upwd&quot;</span>)</span><br><span class="line">                <span class="comment">// defaultSuccessUrl() 不能返回 JSON，只会触发一次 HTTP 302 重定向，不会调用你自定义的控制器接口返回数据 ,也就是让游览器location重定向路径</span></span><br><span class="line">                <span class="comment">//.defaultSuccessUrl(&quot;/sucLogin&quot;); //登录成功后跳转的页面;</span></span><br><span class="line">                .successHandler(myAuthenticationSuccessHandler); <span class="comment">//登录成功处理</span></span><br><span class="line">        <span class="keyword">return</span> httpSecurity.build();</span><br><span class="line">    &#125;</span><br><span class="line">      <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> AuthenticationManager <span class="title function_">authenticationManager</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="type">DaoAuthenticationProvider</span> <span class="variable">authenticationProvider</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">DaoAuthenticationProvider</span>();</span><br><span class="line">        authenticationProvider.setUserDetailsService(userDetailsServiceImpl);</span><br><span class="line">        authenticationProvider.setPasswordEncoder(passwordEncoder());</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">ProviderManager</span>(authenticationProvider);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="meta">@Bean</span></span><br><span class="line">    <span class="keyword">public</span> PasswordEncoder <span class="title function_">passwordEncoder</span><span class="params">()</span> &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> <span class="title class_">BCryptPasswordEncoder</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>前端请求配置</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">this.$axios.post(&#x27;/login&#x27;, qs.stringify(&#123;</span><br><span class="line">  uname: loginForm.uname,</span><br><span class="line">  upwd: loginForm.upwd</span><br><span class="line">&#125;), &#123;</span><br><span class="line">  headers: &#123;</span><br><span class="line">    &#x27;Content-Type&#x27;: &#x27;application/x-www-form-urlencoded&#x27;, // 指定数据为表单格式</span><br><span class="line">  &#125;</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure><p><strong>UserDetailsServiceImpl实现UserDetailsService，重写loadUserByUsername()方法</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line">package com.ch.personmis.security;</span><br><span class="line"></span><br><span class="line">import com.ch.personmis.entity.UserEntity;</span><br><span class="line">import com.ch.personmis.repository.AdminRepository;</span><br><span class="line">import org.springframework.beans.factory.annotation.Autowired;</span><br><span class="line">import org.springframework.context.annotation.Bean;</span><br><span class="line">import org.springframework.security.core.GrantedAuthority;</span><br><span class="line">import org.springframework.security.core.authority.SimpleGrantedAuthority;</span><br><span class="line">import org.springframework.security.core.userdetails.User;</span><br><span class="line">import org.springframework.security.core.userdetails.UserDetails;</span><br><span class="line">import org.springframework.security.core.userdetails.UserDetailsService;</span><br><span class="line">import org.springframework.security.core.userdetails.UsernameNotFoundException;</span><br><span class="line">import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;</span><br><span class="line">import org.springframework.security.crypto.password.PasswordEncoder;</span><br><span class="line">import org.springframework.stereotype.Service;</span><br><span class="line"></span><br><span class="line">import java.util.ArrayList;</span><br><span class="line">import java.util.List;</span><br><span class="line"></span><br><span class="line">@Service</span><br><span class="line">public class UserDetailsServiceImpl implements UserDetailsService &#123;</span><br><span class="line"></span><br><span class="line">    @Autowired</span><br><span class="line">    private AdminRepository adminMapper;</span><br><span class="line">    @Override</span><br><span class="line">    public UserDetails loadUserByUsername(String username) throws UsernameNotFoundException &#123;</span><br><span class="line">        System.out.println(username);</span><br><span class="line">        // 从数据库中获取用户信息</span><br><span class="line">        UserEntity us=adminMapper.selectUserByName(username);</span><br><span class="line">        String[] roles=us.getRoles().split(&quot;;&quot;);</span><br><span class="line">        List&lt;GrantedAuthority&gt; authorizes = new ArrayList&lt;&gt;();</span><br><span class="line">        for(String role:roles)&#123;</span><br><span class="line">            System.out.println(role);</span><br><span class="line">            authorizes.add(new SimpleGrantedAuthority(&quot;ROLE_&quot;+role));</span><br><span class="line">        &#125;</span><br><span class="line">       // 明文加密</span><br><span class="line">        //User user=new User(us.getUname(),&quot;&#123;noop&#125;&quot;+us.getUpwd(),authorizes);</span><br><span class="line">        //密文加密</span><br><span class="line">        User user=new User(us.getUname(),us.getUpwd(),authorizes);</span><br><span class="line">        return user;</span><br><span class="line">   &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>登录成功处理</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line">package com.ch.personmis.security;</span><br><span class="line"></span><br><span class="line">import org.springframework.security.core.Authentication;</span><br><span class="line">import org.springframework.security.web.authentication.AuthenticationSuccessHandler;</span><br><span class="line">import org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line">import javax.servlet.http.HttpServletRequest;</span><br><span class="line">import javax.servlet.http.HttpServletResponse;</span><br><span class="line">import java.io.IOException;</span><br><span class="line"></span><br><span class="line">/**</span><br><span class="line"> * 登录成功处理</span><br><span class="line"> */</span><br><span class="line">@Component</span><br><span class="line">public class MyAuthenticationSuccessHandler implements AuthenticationSuccessHandler &#123;</span><br><span class="line">    @Override</span><br><span class="line">    public void onAuthenticationSuccess(HttpServletRequest request,</span><br><span class="line">                                        HttpServletResponse response,</span><br><span class="line">                                        Authentication authentication) throws IOException &#123;</span><br><span class="line">        response.setContentType(&quot;application/json;charset=UTF-8&quot;);</span><br><span class="line">        response.getWriter().write(&quot;\&quot;ok\&quot;&quot;);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>登录失败处理</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.springframework.security.core.AuthenticationException;</span><br><span class="line"><span class="keyword">import</span> org.springframework.security.web.authentication.AuthenticationFailureHandler;</span><br><span class="line"><span class="keyword">import</span> org.springframework.stereotype.Component;</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> javax.servlet.ServletException;</span><br><span class="line"><span class="keyword">import</span> java.io.IOException;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletRequest;</span><br><span class="line"><span class="keyword">import</span> javax.servlet.http.HttpServletResponse;</span><br><span class="line"></span><br><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CustomAuthenticationFailureHandler</span> <span class="keyword">implements</span> <span class="title class_">AuthenticationFailureHandler</span> &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">onAuthenticationFailure</span><span class="params">(HttpServletRequest request, HttpServletResponse response,</span></span><br><span class="line"><span class="params">                                        AuthenticationException exception)</span> <span class="keyword">throws</span> IOException, ServletException &#123;</span><br><span class="line">        <span class="comment">// 例如：设置错误消息到请求属性</span></span><br><span class="line">        request.setAttribute(<span class="string">&quot;error&quot;</span>, <span class="string">&quot;Invalid username or password&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 重定向到登录页面并带上错误参数</span></span><br><span class="line">        response.sendRedirect(<span class="string">&quot;/login?error&quot;</span>);</span><br><span class="line">        ......................</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>设置请求携带cookie</strong></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> axios = <span class="built_in">require</span>(<span class="string">&#x27;axios&#x27;</span>)<span class="comment">//使用 axios 来完成 ajax 请求。</span></span><br><span class="line"><span class="comment">// 设置全局请求默认携带 cookie</span></span><br><span class="line">axios.<span class="property">defaults</span>.<span class="property">withCredentials</span> = <span class="literal">true</span></span><br></pre></td></tr></table></figure><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">CorsConfig</span> <span class="keyword">implements</span> <span class="title class_">WebMvcConfigurer</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/**</span></span><br><span class="line"><span class="comment">     * 跨域处理</span></span><br><span class="line"><span class="comment">     * <span class="doctag">@param</span> registry</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    <span class="meta">@Override</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">addCorsMappings</span><span class="params">(CorsRegistry registry)</span> &#123;</span><br><span class="line">        <span class="comment">// 覆盖所有请求</span></span><br><span class="line">        registry.addMapping(<span class="string">&quot;/**&quot;</span>)</span><br><span class="line">                <span class="comment">// 允许发送 Cookie  </span></span><br><span class="line">                .allowCredentials(<span class="literal">true</span>)</span><br><span class="line">            ...............</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>security认证用户默认工作原理：</strong></p><ol><li>用户登录时，Spring Security 会创建一个 <code>SecurityContext</code> 对象，其中保存了用户认证信息（例如用户名、角色等）。</li><li><code>SecurityContext</code> 会存储在 <strong>HTTP Session</strong> 中。</li><li>Spring Security 会在每次请求时检查 Session 中的 <code>SecurityContext</code>，如果存在且有效，用户就被认为已认证。<ol><li><strong>security默认保存用户信息</strong></li><li>Spring Security 会生成一个 Session ID 并在浏览器中存储一个 <strong>Session Cookie</strong>（例如 <code>JSESSIONID</code>）。</li><li>在每个请求中，客户端都会携带这个 <code>JSESSIONID</code> Cookie，Spring Security 根据此 ID 从服务器的 Session 存储中取出 <code>SecurityContext</code> 来验证用户身份。</li></ol></li></ol>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Cookie，session,token</title>
      <link href="/2025/04/28/Cookie%EF%BC%8Csession,token/"/>
      <url>/2025/04/28/Cookie%EF%BC%8Csession,token/</url>
      
        <content type="html"><![CDATA[<h1 id="Cookie，session-token"><a href="#Cookie，session-token" class="headerlink" title="Cookie，session,token"></a>Cookie，session,token</h1>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>@PostConstruct</title>
      <link href="/2025/04/24/@PostConstruct/"/>
      <url>/2025/04/24/@PostConstruct/</url>
      
        <content type="html"><![CDATA[<h1 id="PostConstruct"><a href="#PostConstruct" class="headerlink" title="@PostConstruct"></a>@PostConstruct</h1><p>‌**<a href="https://www.baidu.com/s?rsv_dl=re_dqa_generate&sa=re_dqa_generate&wd=@PostConstruct&rsv_pq=dc2d29b2000091f7&oq=@PostConstruct&rsv_t=caa5lr1wyXnoc8tZJ+QlJSVyX1k8U6fzZjtPs5EgUF+Uu277TYSiZQbx3t2HePgT1U3eSg&tn=15007414_9_dg&ie=utf-8">@PostConstruct</a>注解**‌是Java EE 5规范中引入的一个注解，用于标记在依赖注入完成后需要执行的方法。它位于<code>javax.annotation</code>包下，主要用于在对象创建和依赖注入后执行初始化操作。</p><h3 id="基本概念和用途"><a href="#基本概念和用途" class="headerlink" title="基本概念和用途"></a>基本概念和用途</h3><p>@PostConstruct注解用于标记一个方法，该方法会在Bean实例化后、依赖注入完成后自动调用。其主要用途是在对象创建和依赖注入后执行一些初始化操作，例如加载配置文件、建立数据库连接、初始化缓存等‌12。</p><h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a>使用场景</h3><p>@PostConstruct注解通常用于以下场景：</p><ul><li>‌<strong>初始化资源</strong>‌：例如打开数据库连接、初始化缓存、加载配置文件等。</li><li>‌<strong>执行一些必须在依赖注入完成后才能进行的操作</strong>‌：例如校验依赖是否正确注入、设置一些默认值等‌2。</li></ul><h3 id="执行时机"><a href="#执行时机" class="headerlink" title="执行时机"></a>执行时机</h3><p>@PostConstruct注解的方法会在以下几个阶段之后执行：</p><ol><li>‌<strong>Bean实例化</strong>‌：Spring容器创建Bean的实例。</li><li>‌<strong>依赖注入</strong>‌：Spring容器完成对Bean的依赖注入（如@Autowired、@Value等）。</li><li>‌**@PostConstruct方法调用**‌：Spring容器调用标注了@PostConstruct的方法‌。</li></ol><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ul><li>‌<strong>方法签名</strong>‌：@PostConstruct注解的方法必须是public或protected，且不能有任何参数。方法的返回类型通常是void，但也可以是其他类型（尽管返回值通常会被忽略）‌。</li><li>‌<strong>执行次数</strong>‌：一个Bean中可以有多个@PostConstruct注解的方法，但它们都会在依赖注入完成后依次执行‌4</li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>JVM</title>
      <link href="/2025/04/23/JVM/"/>
      <url>/2025/04/23/JVM/</url>
      
        <content type="html"><![CDATA[<h1 id="JVM"><a href="#JVM" class="headerlink" title="JVM"></a>JVM</h1><ul><li><p>Java 方法有两种返回方式，一种是 return 语句正常返回，一种是抛出异常。不管哪种返回方式，都会导致栈帧被弹出。也就是说， <strong>栈帧随着方法调用而创建，随着方法结束而销毁。无论方法正常完成还是异常完成都算作方法结束。</strong></p><p>除了 <code>StackOverFlowError</code> 错误之外，栈还可能会出现<code>OutOfMemoryError</code>错误，这是因为如果栈的内存大小可以动态扩展， 那么当虚拟机在动态扩展栈时无法申请到足够的内存空间，则抛出<code>OutOfMemoryError</code>异常。</p><p>简单总结一下程序运行中栈可能会出现两种错误：</p><ul><li><strong><code>StackOverFlowError</code>：</strong> 如果栈的内存大小不允许动态扩展，那么当线程请求栈的深度超过当前 Java 虚拟机栈的最大深度的时候，就抛出 <code>StackOverFlowError</code> 错误。</li><li><strong><code>OutOfMemoryError</code>：</strong> 如果栈的内存大小可以动态扩展， 那么当虚拟机在动态扩展栈时无法申请到足够的内存空间，则抛出<code>OutOfMemoryError</code>异常。</li></ul></li><li><p>从 JDK 1.7 开始已经默认开启逃逸分析，如果某些方法中的对象引用没有被返回或者未被外面使用（也就是未逃逸出去），那么对象可以直接在栈上分配内存。</p></li><li><p>大部分情况，对象都会首先在 Eden 区域分配，在一次新生代垃圾回收后，如果对象还存活，则会进入 S0 或者 S1，并且对象的年龄还会加 1(Eden 区-&gt;Survivor 区后对象的初始年龄变为 1)，当它的年龄增加到一定程度（默认为 15 岁），就会被晋升到老年代中。对象晋升到老年代的年龄阈值，可以通过参数 <code>-XX:MaxTenuringThreshold</code> 来设置。不过，设置的值应该在 0-15，否则会爆出以下错误：<code>MaxTenuringThreshold of 20 is invalid; must be between 0 and 15</code></p></li></ul><p>​</p><p><img src="/img_posts/image-20250423200310534.png" alt="image-20250423200310534"></p><p>可以，因为数组存放的是对象的引用，</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>定时任务</title>
      <link href="/2025/04/22/%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1/"/>
      <url>/2025/04/22/%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1/</url>
      
        <content type="html"><![CDATA[<h1 id="定时任务"><a href="#定时任务" class="headerlink" title="定时任务"></a>定时任务</h1><h2 id="定时任务，需要专门写一个方法，启动？"><a href="#定时任务，需要专门写一个方法，启动？" class="headerlink" title="定时任务，需要专门写一个方法，启动？"></a>定时任务，需要专门写一个方法，启动？</h2><hr><h2 id="✅-是否需要“专门写一个方法来启动定时任务”？"><a href="#✅-是否需要“专门写一个方法来启动定时任务”？" class="headerlink" title="✅ 是否需要“专门写一个方法来启动定时任务”？"></a>✅ 是否需要“专门写一个方法来启动定时任务”？</h2><h3 id="答案是：视你使用的方式而定，分两种情况："><a href="#答案是：视你使用的方式而定，分两种情况：" class="headerlink" title="答案是：视你使用的方式而定，分两种情况："></a>答案是：<strong>视你使用的方式而定</strong>，分两种情况：</h3><hr><h3 id="✅-方式一：使用-Spring-的-Scheduled-注解（最简单）"><a href="#✅-方式一：使用-Spring-的-Scheduled-注解（最简单）" class="headerlink" title="✅ 方式一：使用 Spring 的 @Scheduled 注解（最简单）"></a>✅ 方式一：使用 Spring 的 <code>@Scheduled</code> 注解（最简单）</h3><h4 id="✅-不需要专门启动，Spring-会自动识别执行"><a href="#✅-不需要专门启动，Spring-会自动识别执行" class="headerlink" title="✅ 不需要专门启动，Spring 会自动识别执行"></a>✅ 不需要专门启动，Spring 会自动识别执行</h4><p>你只要在方法上加 <code>@Scheduled</code>，Spring Boot 会自动帮你定时运行，不需要手动启动：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Component</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">EmailTask</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@Scheduled(cron = &quot;0 0 8 ? * SAT&quot;)</span>  <span class="comment">// 每周六早上8点执行</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">sendWeeklyEmail</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;每周六早上8点，发送邮件！&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>只要你在配置类上加了：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@EnableScheduling</span></span><br></pre></td></tr></table></figure><p>🔔 就自动生效了，不用你手动调用。</p><hr><h3 id="✅-方式二：使用-Quartz（更强大，但复杂）"><a href="#✅-方式二：使用-Quartz（更强大，但复杂）" class="headerlink" title="✅ 方式二：使用 Quartz（更强大，但复杂）"></a>✅ 方式二：使用 Quartz（更强大，但复杂）</h3><h4 id="✅-你需要“手动启动”Scheduler，并注册-Job-Trigger"><a href="#✅-你需要“手动启动”Scheduler，并注册-Job-Trigger" class="headerlink" title="✅ 你需要“手动启动”Scheduler，并注册 Job + Trigger"></a>✅ 你需要“手动启动”Scheduler，并注册 Job + Trigger</h4><p>Quartz 是一个完整的调度框架，它不像 <code>@Scheduled</code> 那样自动帮你调度。你需要：</p><ul><li>创建一个 <code>Job</code> 类（干具体的事）</li><li>创建一个 <code>Trigger</code>（什么时候干）</li><li>创建并启动 <code>Scheduler</code></li></ul><p>🌟 所以这里你就<strong>确实需要写一个 <code>startScheduler()</code> 方法</strong>，比如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">startScheduler</span><span class="params">()</span> <span class="keyword">throws</span> SchedulerException &#123;</span><br><span class="line">    <span class="type">Scheduler</span> <span class="variable">scheduler</span> <span class="operator">=</span> StdSchedulerFactory.getDefaultScheduler();</span><br><span class="line"></span><br><span class="line">    <span class="type">JobDetail</span> <span class="variable">job</span> <span class="operator">=</span> JobBuilder.newJob(EmailJob.class)</span><br><span class="line">            .withIdentity(<span class="string">&quot;emailJob&quot;</span>)</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">    <span class="type">Trigger</span> <span class="variable">trigger</span> <span class="operator">=</span> TriggerBuilder.newTrigger()</span><br><span class="line">            .withIdentity(<span class="string">&quot;emailTrigger&quot;</span>)</span><br><span class="line">            .withSchedule(CronScheduleBuilder.cronSchedule(<span class="string">&quot;0 0 8 ? * SAT&quot;</span>))</span><br><span class="line">            .build();</span><br><span class="line"></span><br><span class="line">    scheduler.scheduleJob(job, trigger);</span><br><span class="line">    scheduler.start(); <span class="comment">// ✅ 你必须手动调用</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>然后你可以在 <code>CommandLineRunner</code> 中调用这个方法启动它：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Bean</span></span><br><span class="line"><span class="keyword">public</span> CommandLineRunner <span class="title function_">run</span><span class="params">(WeeklyEmailScheduler scheduler)</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> args -&gt; scheduler.startScheduler();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="✨-总结一句话"><a href="#✨-总结一句话" class="headerlink" title="✨ 总结一句话"></a>✨ 总结一句话</h2><table><thead><tr><th>方式</th><th>是否需要写启动方法</th><th>自动启动支持</th><th>用途</th></tr></thead><tbody><tr><td><code>@Scheduled</code></td><td>❌ 不需要</td><td>✅ 自动调度</td><td>简单定时任务，轻量、够用</td></tr><tr><td>Quartz</td><td>✅ 需要自己写启动逻辑</td><td>❌ 需手动调用</td><td>强大、灵活、支持动态添加任务等</td></tr></tbody></table><hr><p>如果你不打算搞太复杂的调度管理，直接用 <code>@Scheduled</code> 就很舒服。如果你需要动态增删任务、分布式调度、持久化任务，那就建议用 Quartz。</p><p>如果你不确定自己适合哪种方式，也可以告诉我你的使用场景，我帮你推荐~</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>python</title>
      <link href="/2025/04/07/python/"/>
      <url>/2025/04/07/python/</url>
      
        <content type="html"><![CDATA[<h1 id="python初识"><a href="#python初识" class="headerlink" title="python初识"></a>python初识</h1><p><img src="/img_posts/image-20250403092810593.png" alt="image-20250403092810593"></p><p><img src="/img_posts/image-20250403093051470.png" alt="image-20250403093051470"></p><ul><li>单行注释#</li><li>多行注释”””</li></ul><p><img src="/img_posts/image-20250403093541332.png" alt="image-20250403093541332"></p><ul><li>python 中 <code>/</code> 返回一个浮点数</li><li>python中<code>//</code> 整数除法，返回一个向下取整的整数</li></ul><p><img src="/img_posts/image-20250407010603714.png" alt="image-20250407010603714"></p><p><img src="/img_posts/image-20250407010945223.png" alt="image-20250407010751637"></p><p><img src="/img_posts/image-20250407011315102.png" alt="image-20250407011315102"></p><p><img src="/img_posts/image-20250407011415001.png" alt="image-20250407011415001"></p><p><img src="/img_posts/image-20250407011516792.png" alt="image-20250407011516792"></p><p><img src="/img_posts/image-20250407011853218.png" alt="image-20250407011853218"></p><p><img src="/img_posts/image-20250407012209209.png" alt="image-20250407012209209"></p><p><img src="/img_posts/image-20250407012439698.png" alt="image-20250407012439698"></p><p><img src="/img_posts/image-20250407201614311.png"></p><p>##函数</p><p><img src="/img_posts/image-20250407202535614.png" alt="image-20250407202535614"></p><p><img src="/img_posts/image-20250407202636301.png" alt="image-20250407202636301"></p><p><img src="/img_posts/image-20250407202759452.png" alt="image-20250407202759452"></p><p><img src="/img_posts/image-20250407232312017.png" alt="image-20250407232312017"></p><p><img src="/img_posts/image-20250407232706667.png" alt="image-20250407232706667"></p><p><img src="/img_posts/image-20250407233057747.png" alt="image-20250407233057747"></p><h4 id="lamda表达式，传入自定义函数作为其他函数实参"><a href="#lamda表达式，传入自定义函数作为其他函数实参" class="headerlink" title="lamda表达式，传入自定义函数作为其他函数实参"></a>lamda表达式，传入自定义函数作为其他函数实参</h4><p><img src="/img_posts/image-20250407233257863.png" alt="image-20250407233257863"></p><ul><li>python函数支持多返回值、可变，缺省，不定长参数</li><li>pass ,起占位的作用，什么也不做</li></ul><h2 id="容器"><a href="#容器" class="headerlink" title="容器"></a>容器</h2><h3 id="列表-list-可修改"><a href="#列表-list-可修改" class="headerlink" title="列表(list)(可修改)"></a>列表(list)(可修改)</h3><p><img src="/img_posts/image-20250407221631173.png" alt="image-20250407221631173"></p><p><img src="/img_posts/image-20250407222022051.png" alt="image-20250407222022051"></p><h4 id="list操作"><a href="#list操作" class="headerlink" title="list操作"></a>list操作</h4><p><img src="/img_posts/image-20250407224016326.png" alt="image-20250407224016326"></p><p><img src="/img_posts/image-20250407224049146.png" alt="image-20250407224049146"></p><p><img src="/img_posts/image-20250407224313124.png" alt="image-20250407224313124"></p><h3 id="元组-tuple-不可修改"><a href="#元组-tuple-不可修改" class="headerlink" title="元组(tuple)(不可修改)"></a>元组(tuple)(不可修改)</h3><p><img src="/img_posts/image-20250407225028408.png" alt="image-20250407225028408"></p><h3 id="字符串-str-（不可修改）本质上就是一个特殊的元组"><a href="#字符串-str-（不可修改）本质上就是一个特殊的元组" class="headerlink" title="字符串(str)（不可修改）本质上就是一个特殊的元组"></a>字符串(str)（不可修改）本质上就是一个特殊的元组</h3><p><img src="/img_posts/image-20250407225417718.png" alt="image-20250407225417718"></p><h3 id="序列"><a href="#序列" class="headerlink" title="序列"></a>序列</h3><p><img src="/img_posts/image-20250407230006726.png" alt="image-20250407230006726"></p><h3 id="集合（set）（不可重复元素，无序）"><a href="#集合（set）（不可重复元素，无序）" class="headerlink" title="集合（set）（不可重复元素，无序）"></a>集合（set）（不可重复元素，无序）</h3><p><img src="/img_posts/image-20250407230824291.png" alt="image-20250407230824291"></p><h3 id="字典（dict）-不可重复，无序"><a href="#字典（dict）-不可重复，无序" class="headerlink" title="字典（dict）(不可重复，无序)"></a>字典（dict）(不可重复，无序)</h3><p><img src="/img_posts/image-20250407231134551.png" alt="image-20250407231134551"></p><p><img src="/img_posts/image-20250407231332705.png" alt="image-20250407231332705"></p><h3 id="容器总结"><a href="#容器总结" class="headerlink" title="容器总结"></a>容器总结</h3><p><img src="/img_posts/image-20250407231511285.png" alt="image-20250407231511285"></p><p><strong>通用操作</strong></p><p><img src="/img_posts/image-20250407231641397.png" alt="image-20250407231641397"></p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Arrays.sort()</title>
      <link href="/2025/04/03/Arrays.sort()/"/>
      <url>/2025/04/03/Arrays.sort()/</url>
      
        <content type="html"><![CDATA[<p>#<strong>各种lambda表达式的应用场景</strong></p><h1 id="Arrays-sort"><a href="#Arrays-sort" class="headerlink" title="Arrays.sort()"></a>Arrays.sort()</h1><p>你提到的这行代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Arrays.sort(intervals, (a, b) -&gt; a[<span class="number">0</span>] - b[<span class="number">0</span>]); <span class="comment">//记住</span></span><br></pre></td></tr></table></figure><p>是正确的，它用于对二维数组 <code>intervals</code> 按照每个区间的起始位置（<code>a[0]</code> 和 <code>b[0]</code>）进行升序排序。这种写法是基于 Java 的 <code>Arrays.sort</code> 方法，结合 Lambda 表达式实现自定义排序逻辑。</p><h3 id="关于这行代码的说明"><a href="#关于这行代码的说明" class="headerlink" title="关于这行代码的说明"></a>关于这行代码的说明</h3><ol><li><p><strong><code>Arrays.sort</code> 方法</strong>：</p><ul><li><code>Arrays.sort</code> 是 Java 中用于对数组进行排序的方法。</li><li>它可以接受一个自定义的比较器（Comparator），用于定义排序规则。</li></ul></li><li><p>**Lambda 表达式 <code>(a, b) -&gt; a[0] - b[0]</code>**：</p><ul><li>这是一个比较器，用于比较两个区间 <code>a</code> 和 <code>b</code> 的起始位置。</li><li><code>a[0] - b[0]</code> 的结果决定了排序顺序：<ul><li>如果结果为负数，<code>a</code> 排在 <code>b</code> 前面。</li><li>如果结果为正数，<code>b</code> 排在 <code>a</code> 前面。</li><li>如果结果为零，表示 <code>a</code> 和 <code>b</code> 的起始位置相同。</li></ul></li></ul></li><li><p><strong>排序规则</strong>：</p><ul><li>这行代码将 <code>intervals</code> 按照每个区间的起始位置进行升序排序。</li><li>例如，对于输入 <code>[[1, 3], [2, 6], [8, 10], [15, 18]]</code>，排序后仍然是 <code>[[1, 3], [2, 6], [8, 10], [15, 18]]</code>，因为它们已经按起始位置排序。</li></ul></li></ol><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol><li><p><strong>整数溢出问题</strong>：</p><ul><li>如果 <code>a[0]</code> 和 <code>b[0]</code> 的值非常大，<code>a[0] - b[0]</code> 可能会导致整数溢出。</li><li>为了避免这个问题，可以使用 <code>Integer.compare(a[0], b[0])</code>，这是一个更安全的比较方式：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Arrays.sort(intervals, (a, b) -&gt; Integer.compare(a[<span class="number">0</span>], b[<span class="number">0</span>]));</span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>排序的必要性</strong>：</p><ul><li>在区间合并问题中，排序是必要的，因为合并操作依赖于区间的顺序。</li><li>排序后，可以方便地判断两个相邻区间是否重叠，并进行合并。</li></ul></li></ol><h3 id="完整的区间合并代码"><a href="#完整的区间合并代码" class="headerlink" title="完整的区间合并代码"></a>完整的区间合并代码</h3><p>结合排序逻辑，以下是完整的区间合并代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> <span class="type">int</span>[][] merge(<span class="type">int</span>[][] intervals) &#123;</span><br><span class="line">        <span class="keyword">if</span> (intervals == <span class="literal">null</span> || intervals.length &lt;= <span class="number">1</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> intervals;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 按照区间的起始位置排序（安全的比较方式）</span></span><br><span class="line">        Arrays.sort(intervals, (a, b) -&gt; Integer.compare(a[<span class="number">0</span>], b[<span class="number">0</span>]));</span><br><span class="line"></span><br><span class="line">        List&lt;<span class="type">int</span>[]&gt; merged = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;();</span><br><span class="line">        <span class="type">int</span>[] currentInterval = intervals[<span class="number">0</span>]; <span class="comment">// 当前处理的区间</span></span><br><span class="line">        merged.add(currentInterval);</span><br><span class="line"></span><br><span class="line">        <span class="keyword">for</span> (<span class="type">int</span>[] interval : intervals) &#123;</span><br><span class="line">            <span class="type">int</span> <span class="variable">currentEnd</span> <span class="operator">=</span> currentInterval[<span class="number">1</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">nextStart</span> <span class="operator">=</span> interval[<span class="number">0</span>];</span><br><span class="line">            <span class="type">int</span> <span class="variable">nextEnd</span> <span class="operator">=</span> interval[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">            <span class="keyword">if</span> (currentEnd &gt;= nextStart) &#123; <span class="comment">// 有重叠</span></span><br><span class="line">                currentInterval[<span class="number">1</span>] = Math.max(currentEnd, nextEnd); <span class="comment">// 合并区间</span></span><br><span class="line">            &#125; <span class="keyword">else</span> &#123; <span class="comment">// 没有重叠</span></span><br><span class="line">                currentInterval = interval; <span class="comment">// 更新当前区间</span></span><br><span class="line">                merged.add(currentInterval);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> merged.toArray(<span class="keyword">new</span> <span class="title class_">int</span>[merged.size()][]);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Arrays-sort-对Double类型的数组排序"><a href="#Arrays-sort-对Double类型的数组排序" class="headerlink" title="Arrays.sort()对Double类型的数组排序"></a>Arrays.sort()对Double类型的数组排序</h2><p>三种排序方式</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 使用 Double.compare 进行倒序排序</span></span><br><span class="line">Arrays.sort(arr, <span class="keyword">new</span> <span class="title class_">Comparator</span>&lt;Double&gt;() &#123;</span><br><span class="line">        <span class="meta">@Override</span></span><br><span class="line">        <span class="keyword">public</span> <span class="type">int</span> <span class="title function_">compare</span><span class="params">(Double o1, Double o2)</span> &#123;</span><br><span class="line">                <span class="comment">// 使用 Double.compare 方法避免浮点数精度问题</span></span><br><span class="line">                <span class="keyword">return</span> Double.compare(o2, o1); <span class="comment">//倒叙</span></span><br><span class="line">        &#125;</span><br><span class="line">&#125;);</span><br><span class="line">Arrays.sort(arr, (o1, o2) -&gt; Double.compare(o2, o1)); <span class="comment">//倒叙</span></span><br><span class="line">Arrays.sort(arr, Double::compare);  <span class="comment">//正序，使用的Lambda表达式</span></span><br></pre></td></tr></table></figure><p>#Collections.sort(l.stream().distinct().toList());</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">Collections.sort(l.stream().distinct().toList());</span><br></pre></td></tr></table></figure><p>这里，<code>l.stream().distinct().toList()</code> 创建了一个<strong>不可变列表</strong>，而你试图对它调用 <code>Collections.sort()</code>，这导致了 <code>java.lang.UnsupportedOperationException</code>。</p><h3 id="问题分析"><a href="#问题分析" class="headerlink" title="问题分析"></a>问题分析</h3><ol><li><strong><code>toList()</code> 的特性</strong>：<ul><li>在 Java 16 及更高版本中，<code>Stream.toList()</code> 会返回一个不可变列表（<code>List.of()</code> 的类似实现）。对不可变列表进行修改操作（如排序）会抛出异常。</li><li>如果你使用的是 Java 16 或更高版本，这就是问题的根源。</li></ul></li><li><strong><code>Collections.sort()</code> 的要求</strong>：<ul><li><code>Collections.sort()</code> 需要一个可变的 <code>List</code>，因为它会直接修改列表的内容</li></ul></li></ol><h3 id="正确用法"><a href="#正确用法" class="headerlink" title="正确用法"></a>正确用法</h3><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; l = new ArrayList&lt;&gt;();</span><br><span class="line">List&lt;Integer&gt; sortedList = new ArrayList&lt;&gt;(l.stream().distinct().toList());//对list数组去重加排序</span><br><span class="line">Collections.sort(sortedList);</span><br></pre></td></tr></table></figure><p>#<strong>注意</strong><code>List.of()</code> 创建的集合是不可修改的</p><p>报错</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">java.lang.UnsupportedOperationException</span><br><span class="line">  at line 142, java.base/java.util.ImmutableCollections.uoe</span><br><span class="line">  at line 263, java.base/java.util.ImmutableCollections$AbstractImmutableList.sort</span><br><span class="line">  at line 145, java.base/java.util.Collections.sort</span><br><span class="line">  at line 9, Solution.merge</span><br><span class="line">  at line 56, __DriverSolution__.__helper__</span><br><span class="line">  at line 86, __Driver__.main</span><br></pre></td></tr></table></figure><p>从你提供的堆栈跟踪信息来看，问题出在尝试对一个不可变集合（Immutable Collection）进行排序操作，这导致了 <code>java.lang.UnsupportedOperationException</code> 异常。</p><p>以下是问题的详细分析和可能的解决方案：</p><h3 id="问题分析-1"><a href="#问题分析-1" class="headerlink" title="问题分析"></a>问题分析</h3><ol><li><p><strong>异常类型</strong>：<code>java.lang.UnsupportedOperationException</code> 是一个运行时异常，通常表示某个操作不被支持。</p></li><li><p><strong>关键信息</strong>：</p><ul><li>异常发生在 <code>java.util.ImmutableCollections$AbstractImmutableList.sort</code>，表明你尝试对一个不可变列表调用了 <code>sort()</code> 方法。</li><li>不可变集合（如 <code>List.of()</code> 创建的集合）是不可修改的，因此任何试图修改集合的操作（如排序、添加、删除等）都会抛出此异常。</li></ul></li><li><p><strong>代码路径</strong>：</p><ul><li><code>Collections.sort</code> 调用了 <code>ImmutableCollections$AbstractImmutableList.sort</code>。</li><li>最终问题发生在 <code>Solution.merge</code> 方法中。</li></ul></li></ol><h3 id="可能的原因"><a href="#可能的原因" class="headerlink" title="可能的原因"></a>可能的原因</h3><ol><li><strong>使用了不可变集合</strong>：你可能使用了 <code>List.of()</code> 或其他方式创建了一个不可变集合，然后尝试对其进行排序。</li><li><strong>错误的集合类型</strong>：你可能误以为这个集合是可以修改的，或者在代码中不小心传递了不可变集合。</li></ol><h3 id="解决方案"><a href="#解决方案" class="headerlink" title="解决方案"></a>解决方案</h3><h4 id="方法-1：转换为可变集合"><a href="#方法-1：转换为可变集合" class="headerlink" title="方法 1：转换为可变集合"></a>方法 1：转换为可变集合</h4><p>在对集合进行排序之前，将其转换为可变集合（如 <code>ArrayList</code>）。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; immutableList = List.of(<span class="number">3</span>, <span class="number">1</span>, <span class="number">2</span>); <span class="comment">// 不可变集合</span></span><br><span class="line">List&lt;Integer&gt; mutableList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(immutableList); <span class="comment">// 转换为可变集合</span></span><br><span class="line">Collections.sort(mutableList); <span class="comment">// 现在可以排序了</span></span><br></pre></td></tr></table></figure><h4 id="方法-2：使用其他方式创建可变集合"><a href="#方法-2：使用其他方式创建可变集合" class="headerlink" title="方法 2：使用其他方式创建可变集合"></a>方法 2：使用其他方式创建可变集合</h4><p>如果你需要一个可变集合，可以直接使用 <code>ArrayList</code> 或其他可变集合类型：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; mutableList = <span class="keyword">new</span> <span class="title class_">ArrayList</span>&lt;&gt;(Arrays.asList(<span class="number">3</span>, <span class="number">1</span>, <span class="number">2</span>));</span><br><span class="line">Collections.sort(mutableList);</span><br></pre></td></tr></table></figure><h4 id="方法-3：检查代码逻辑"><a href="#方法-3：检查代码逻辑" class="headerlink" title="方法 3：检查代码逻辑"></a>方法 3：检查代码逻辑</h4><p>检查 <code>Solution.merge</code> 方法中是否有错误地传递了不可变集合。如果确实需要传递可变集合，确保在创建集合时使用可变集合类型。</p><p>##<strong>方法 4：使用 Stream API</strong>(记住)<br>如果你使用的是 Java 8 或更高版本，可以使用 Stream API 对集合进行排序，而无需直接修改集合：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">List&lt;Integer&gt; sortedList = List.of(<span class="number">3</span>, <span class="number">1</span>, <span class="number">2</span>).stream()</span><br><span class="line">                                .sorted()</span><br><span class="line">                                .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><h1 id="List-转为二维数组"><a href="#List-转为二维数组" class="headerlink" title="List&lt;int[]&gt; 转为二维数组"></a>List<code>&lt;int[]&gt;</code> 转为二维数组</h1><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"> List&lt;int[]&gt; merged = new ArrayList&lt;&gt;();</span><br><span class="line"></span><br><span class="line">merged.toArray(new int[merged.size()][]);  // 指定行数</span><br></pre></td></tr></table></figure><h1 id="小知识点（java二维数组类比“按行优先”）"><a href="#小知识点（java二维数组类比“按行优先”）" class="headerlink" title="小知识点（java二维数组类比“按行优先”）"></a>小知识点（java二维数组类比“按行优先”）</h1><p>在 Java 中，二维数组（例如 <code>int[][]</code>）本质上是一个<strong>数组的数组</strong>，而不是一个连续的二维矩阵。因此，Java 的二维数组存储方式既不是典型的“按行优先”，也不是“按列优先”，而是基于<strong>数组的数组</strong>的结构。不过，从逻辑上和访问习惯上，我们可以类比为“按行优先”。</p><h3 id="Java-二维数组的结构"><a href="#Java-二维数组的结构" class="headerlink" title="Java 二维数组的结构"></a>Java 二维数组的结构</h3><ol><li><p><strong>数组的数组</strong>：</p><ul><li>二维数组 <code>int[][]</code> 是一个一维数组，其中的每个元素又是一个一维数组（即“行”）。</li><li>例如：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>[][] matrix = &#123;</span><br><span class="line">    &#123;<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>&#125;,  <span class="comment">// 第一行</span></span><br><span class="line">    &#123;<span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>&#125;,  <span class="comment">// 第二行</span></span><br><span class="line">    &#123;<span class="number">7</span>, <span class="number">8</span>, <span class="number">9</span>&#125;   <span class="comment">// 第三行</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure></li><li>在这个例子中，<code>matrix</code> 是一个一维数组，包含三个元素，每个元素是一个一维数组（行）。</li></ul></li><li><p><strong>访问方式</strong>：</p><ul><li>通过 <code>matrix[i][j]</code> 访问元素时，<code>i</code> 表示行索引，<code>j</code> 表示列索引。</li><li>这种访问方式类似于“按行优先”，因为首先定位到第 <code>i</code> 行，然后在该行中访问第 <code>j</code> 列的元素。</li></ul></li></ol><h3 id="Java-二维数组的存储方式"><a href="#Java-二维数组的存储方式" class="headerlink" title="Java 二维数组的存储方式"></a>Java 二维数组的存储方式</h3><ul><li>Java 的二维数组并不是一个连续的内存块，而是由多个独立的一维数组组成。</li><li>每个“行”数组是独立分配的，它们的内存地址是不连续的。</li><li>例如：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span>[][] matrix = <span class="keyword">new</span> <span class="title class_">int</span>[<span class="number">3</span>][<span class="number">3</span>];</span><br></pre></td></tr></table></figure>这会创建一个包含 3 个元素的一维数组，每个元素是一个长度为 3 的一维数组。这些一维数组在内存中是独立分配的。</li></ul><h3 id="类比“按行优先”"><a href="#类比“按行优先”" class="headerlink" title="类比“按行优先”"></a>类比“按行优先”</h3><p>虽然 Java 的二维数组并不是严格意义上的“按行优先”存储（因为内存中是分散的），但从逻辑上和访问习惯上，它更像是“按行优先”：</p><ul><li><strong>访问顺序</strong>：<code>matrix[i][j]</code> 的访问方式更符合“按行优先”的逻辑。</li><li><strong>遍历习惯</strong>：通常我们会按行遍历二维数组：<figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">for</span> (<span class="type">int</span> <span class="variable">i</span> <span class="operator">=</span> <span class="number">0</span>; i &lt; matrix.length; i++) &#123;  <span class="comment">// 遍历行</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="type">int</span> <span class="variable">j</span> <span class="operator">=</span> <span class="number">0</span>; j &lt; matrix[i].length; j++) &#123;  <span class="comment">// 遍历列</span></span><br><span class="line">        System.out.print(matrix[i][j] + <span class="string">&quot; &quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    System.out.println();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ul><li>Java 的二维数组本质上是一个<strong>数组的数组</strong>，而不是一个连续的二维矩阵。</li><li>它的访问方式和遍历习惯类似于“按行优先”，但并不是严格意义上的“按行优先”存储，因为每行是独立分配的。</li><li>如果需要一个真正的二维矩阵（连续存储），可以使用一维数组来模拟，并通过计算偏移量访问元素（类似于 C&#x2F;C++ 的二维数组）。</li></ul><h1 id="new-Random-System-nanoTime-nextInt-int-bound"><a href="#new-Random-System-nanoTime-nextInt-int-bound" class="headerlink" title="new Random(System.nanoTime()).nextInt(int bound)"></a>new Random(System.nanoTime()).nextInt(int bound)</h1><p><strong>问题</strong><code>new Random().nextInt()</code> 使用的是没有设置种子的默认构造方法，这会根据当前时间戳等因素初始化 <code>Random</code> 实例。对于多次连续调用 <code>nextInt()</code>，因为 <code>Random</code> 是随机数生成器，默认情况下它的种子不会改变，导致 <code>nextInt()</code> 的输出可能会在短时间内返回相同的结果</p><p><strong>方案</strong><code>System.nanoTime()</code> 是一个高精度的时间戳，它基于当前系统的纳秒级时间，通常足够用来保证随机数生成器每次调用时都会使用不同的种子</p><h1 id="二维数转链表"><a href="#二维数转链表" class="headerlink" title="二维数转链表"></a>二维数转链表</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">    <span class="comment">//打印杨辉三角</span></span><br><span class="line">    <span class="keyword">public</span> List&lt;List&lt;Integer&gt;&gt; <span class="title function_">generate</span><span class="params">(<span class="type">int</span> numRows)</span> &#123;</span><br><span class="line">        Integer[][] ans = <span class="keyword">new</span> <span class="title class_">Integer</span>[numRows][numRows];</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;numRows;i++)&#123;</span><br><span class="line">            ans[i][<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">            ans[i][i] = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;i;j++)&#123;</span><br><span class="line">                ans[i][j] = ans[i-<span class="number">1</span>][j-<span class="number">1</span>]+ans[i-<span class="number">1</span>][j];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">     <span class="keyword">return</span>  Arrays.stream(ans)</span><br><span class="line">                        .map(a-&gt; Arrays.asList(a).stream().filter(b-&gt;b!=<span class="literal">null</span>).toList())</span><br><span class="line">                        .collect(Collectors.toList());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>这里filter过滤出不为null的值</li></ul><p>#扁平化.flatMap（）</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">Map&lt;String, Long&gt; tagsCountMap = tagsList.stream()</span><br><span class="line">        .flatMap(tags -&gt; JSONUtil.toList(tags, String.class).stream()) //扁平化</span><br><span class="line">        .collect(Collectors.groupingBy(tags -&gt; tags, Collectors.counting()));//分组统计</span><br></pre></td></tr></table></figure><ul><li>**<code>flatMap</code>**：将每个元素的流合并为一个单一的流，实现扁平化。这样，所有标签都被展平到一个单一的流中。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>二维数转链表</title>
      <link href="/2025/04/03/%E4%BA%8C%E7%BB%B4%E6%95%B0%E8%BD%AC%E9%93%BE%E8%A1%A8/"/>
      <url>/2025/04/03/%E4%BA%8C%E7%BB%B4%E6%95%B0%E8%BD%AC%E9%93%BE%E8%A1%A8/</url>
      
        <content type="html"><![CDATA[<h1 id="二维数转链表"><a href="#二维数转链表" class="headerlink" title="二维数转链表"></a>二维数转链表</h1><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Solution</span> &#123;</span><br><span class="line">    <span class="keyword">public</span> List&lt;List&lt;Integer&gt;&gt; <span class="title function_">generate</span><span class="params">(<span class="type">int</span> numRows)</span> &#123;</span><br><span class="line">        Integer[][] ans = <span class="keyword">new</span> <span class="title class_">Integer</span>[numRows][numRows];</span><br><span class="line">        <span class="keyword">for</span>(<span class="type">int</span> i=<span class="number">0</span>;i&lt;numRows;i++)&#123;</span><br><span class="line">            ans[i][<span class="number">0</span>] = <span class="number">1</span>;</span><br><span class="line">            ans[i][i] = <span class="number">1</span>;</span><br><span class="line">            <span class="keyword">for</span>(<span class="type">int</span> j=<span class="number">1</span>;j&lt;i;j++)&#123;</span><br><span class="line">                ans[i][j] = ans[i-<span class="number">1</span>][j-<span class="number">1</span>]+ans[i-<span class="number">1</span>][j];</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">     <span class="keyword">return</span>  Arrays.stream(ans)</span><br><span class="line">                        .map(a-&gt; Arrays.asList(a).stream().filter(b-&gt;b!=<span class="literal">null</span>).toList())</span><br><span class="line">                        .collect(Collectors.toList());</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li>这里filter过滤出不为null的值</li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Servlet,Request</title>
      <link href="/2025/03/25/Servlet,Request/"/>
      <url>/2025/03/25/Servlet,Request/</url>
      
        <content type="html"><![CDATA[<h1 id="Servlet-Request"><a href="#Servlet-Request" class="headerlink" title="Servlet,Request"></a>Servlet,Request</h1><p>在Java的Servlet编程中，<code>request.getHeader(Header.CONTENT_TYPE.getValue())</code> 和 <code>request.getContentType()</code> 都可以用来获取HTTP请求的<code>Content-Type</code>头信息，但它们在实现方式和语义上有一些区别。</p><h3 id="1-request-getHeader-Header-CONTENT-TYPE-getValue"><a href="#1-request-getHeader-Header-CONTENT-TYPE-getValue" class="headerlink" title="1. request.getHeader(Header.CONTENT_TYPE.getValue())"></a>1. <strong><code>request.getHeader(Header.CONTENT_TYPE.getValue())</code></strong></h3><ul><li><strong>作用</strong>：通过<code>request.getHeader()</code>方法获取指定的HTTP头字段值。<code>Header.CONTENT_TYPE.getValue()</code>是获取<code>Content-Type</code>头字段的名称（通常是<code>&quot;Content-Type&quot;</code>）。</li><li><strong>实现方式</strong>：<code>request.getHeader(String name)</code>是<code>HttpServletRequest</code>接口中的方法，用于获取指定名称的头字段值。它会从请求中查找名为<code>&quot;Content-Type&quot;</code>的头字段，并返回其值。</li><li><strong>优点</strong>：<ul><li>更通用：可以用于获取任何HTTP头字段的值，不仅限于<code>Content-Type</code>。</li><li>明确性：通过显式地指定头字段名称，代码的可读性更高，更容易理解。</li></ul></li><li><strong>缺点</strong>：<ul><li>多余的字符串操作：需要通过<code>Header.CONTENT_TYPE.getValue()</code>获取头字段名称，这可能会增加一些不必要的字符串操作。</li></ul></li></ul><h3 id="2-request-getContentType"><a href="#2-request-getContentType" class="headerlink" title="2. request.getContentType()"></a>2. <strong><code>request.getContentType()</code></strong></h3><ul><li><strong>作用</strong>：直接获取HTTP请求的<code>Content-Type</code>头字段值。</li><li><strong>实现方式</strong>：<code>request.getContentType()</code>是<code>HttpServletRequest</code>接口中的一个便捷方法，专门用于获取<code>Content-Type</code>头字段的值。它内部实际上是调用了<code>getHeader(&quot;Content-Type&quot;)</code>。</li><li><strong>优点</strong>：<ul><li>简洁性：直接调用<code>getContentType()</code>方法，代码更简洁，减少了字符串操作。</li><li>性能：由于是专门的方法，可能在某些实现中经过优化，性能略优于通过<code>getHeader()</code>获取。</li></ul></li><li><strong>缺点</strong>：<ul><li>专一性：只能用于获取<code>Content-Type</code>头字段，无法用于获取其他头字段。</li></ul></li></ul><h3 id="区别总结"><a href="#区别总结" class="headerlink" title="区别总结"></a><strong>区别总结</strong></h3><table><thead><tr><th>特点</th><th><code>request.getHeader(Header.CONTENT_TYPE.getValue())</code></th><th><code>request.getContentType()</code></th></tr></thead><tbody><tr><td><strong>通用性</strong></td><td>可以获取任何HTTP头字段值</td><td>仅用于获取<code>Content-Type</code></td></tr><tr><td><strong>代码简洁性</strong></td><td>需要指定头字段名称，代码稍显冗长</td><td>代码更简洁，直接获取</td></tr><tr><td><strong>性能</strong></td><td>可能稍逊于<code>getContentType()</code></td><td>可能经过优化，性能略优</td></tr><tr><td><strong>可读性</strong></td><td>明确指定头字段名称，可读性更高</td><td>依赖方法名，可读性稍低</td></tr></tbody></table><h3 id="使用场景"><a href="#使用场景" class="headerlink" title="使用场景"></a><strong>使用场景</strong></h3><ul><li>如果你只需要获取<code>Content-Type</code>，建议使用<code>request.getContentType()</code>，因为它更简洁且性能可能更好。</li><li>如果你需要获取多个不同的HTTP头字段，或者需要更明确的代码逻辑，建议使用<code>request.getHeader(String name)</code>。</li></ul><p>希望这些信息对你有帮助！</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>矩阵运算</title>
      <link href="/2025/03/25/%E7%9F%A9%E9%98%B5%E8%BF%90%E7%AE%97/"/>
      <url>/2025/03/25/%E7%9F%A9%E9%98%B5%E8%BF%90%E7%AE%97/</url>
      
        <content type="html"><![CDATA[<h1 id="矩阵运算"><a href="#矩阵运算" class="headerlink" title="矩阵运算"></a>矩阵运算</h1><p>##N*N阶右旋</p><ul><li>将二维数组看成一维再倒转一维数组的行，再按对角线（主对角线）进行转换即**<code>a[i][j]=a[j][i]</code>**</li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>String lock = String.valueOf(userId).intern();</title>
      <link href="/2025/03/18/String-lock-=-String.valueOf(userId).intern();/"/>
      <url>/2025/03/18/String-lock-=-String.valueOf(userId).intern();/</url>
      
        <content type="html"><![CDATA[<h1 id="String-lock-String-valueOf-userId-intern"><a href="#String-lock-String-valueOf-userId-intern" class="headerlink" title="String lock &#x3D; String.valueOf(userId).intern();"></a>String lock &#x3D; String.valueOf(userId).intern();</h1><p>在 Java 中，<code>String.valueOf(userId).intern()</code> 是一种常见的字符串处理方式，通常用于确保字符串的唯一性，并且可以用于同步（synchronization）场景。下面我将详细解释这段代码的作用和使用场景。</p><hr><h3 id="1-String-valueOf-userId"><a href="#1-String-valueOf-userId" class="headerlink" title="1. String.valueOf(userId)"></a>1. <strong><code>String.valueOf(userId)</code></strong></h3><p><code>String.valueOf()</code> 是一个静态方法，用于将对象转换为字符串。它的行为取决于传入的参数类型：</p><ul><li>如果 <code>userId</code> 是 <code>null</code>，<code>String.valueOf(null)</code> 会返回字符串 <code>&quot;null&quot;</code>。</li><li>如果 <code>userId</code> 是基本数据类型（如 <code>int</code>、<code>long</code> 等），它会调用对应的 <code>toString()</code> 方法将其转换为字符串。</li><li>如果 <code>userId</code> 是对象类型，它会调用对象的 <code>toString()</code> 方法。</li></ul><p>例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">int</span> <span class="variable">userId</span> <span class="operator">=</span> <span class="number">123</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">str</span> <span class="operator">=</span> String.valueOf(userId); <span class="comment">// 结果是字符串 &quot;123&quot;</span></span><br></pre></td></tr></table></figure><hr><h3 id="2-intern-方法"><a href="#2-intern-方法" class="headerlink" title="2. intern() 方法"></a>2. <strong><code>intern()</code> 方法</strong></h3><p><code>String.intern()</code> 是一个重要的字符串方法，它的作用是：</p><ul><li>将字符串对象存储到字符串常量池（String Pool）中。</li><li>如果字符串常量池中已经存在一个相同的字符串（内容完全相同），则返回常量池中的那个字符串对象。</li><li>如果不存在，则将当前字符串对象放入常量池，并返回这个对象。</li></ul><p><strong>字符串常量池的特点：</strong></p><ul><li>字符串常量池中的字符串是唯一的，即相同的字符串内容在常量池中只有一个对象。</li><li>常量池中的字符串对象是共享的，这可以节省内存。</li></ul><p><strong>示例：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="type">String</span> <span class="variable">s1</span> <span class="operator">=</span> <span class="string">&quot;hello&quot;</span>;</span><br><span class="line"><span class="type">String</span> <span class="variable">s2</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">String</span>(<span class="string">&quot;hello&quot;</span>).intern(); <span class="comment">// s2 会被放入常量池</span></span><br><span class="line">System.out.println(s1 == s2); <span class="comment">// true，因为 s1 和 s2 指向同一个对象</span></span><br></pre></td></tr></table></figure><hr><h3 id="3-String-valueOf-userId-intern-的用途"><a href="#3-String-valueOf-userId-intern-的用途" class="headerlink" title="3. String.valueOf(userId).intern() 的用途"></a>3. <strong><code>String.valueOf(userId).intern()</code> 的用途</strong></h3><p>结合 <code>String.valueOf()</code> 和 <code>intern()</code>，<code>String.valueOf(userId).intern()</code> 的主要用途是：</p><ul><li><strong>确保字符串的唯一性：</strong> 通过 <code>intern()</code> 方法，确保每次生成的字符串对象是唯一的，避免重复创建相同的字符串对象。</li><li><strong>用于同步（synchronization）：</strong> 在多线程环境中，<code>intern()</code> 方法可以用于同步锁（synchronized）的优化。由于常量池中的字符串是唯一的，可以避免因字符串重复创建而导致的锁竞争问题。</li></ul><p><strong>示例：</strong></p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">UserCache</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> Map&lt;String, User&gt; cache = <span class="keyword">new</span> <span class="title class_">ConcurrentHashMap</span>&lt;&gt;();</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">synchronized</span> User <span class="title function_">getUser</span><span class="params">(<span class="type">long</span> userId)</span> &#123;</span><br><span class="line">        <span class="type">String</span> <span class="variable">lock</span> <span class="operator">=</span> String.valueOf(userId).intern(); <span class="comment">// 确保锁对象的唯一性</span></span><br><span class="line">        <span class="keyword">synchronized</span> (lock) &#123;</span><br><span class="line">            <span class="comment">// 模拟从缓存中获取用户信息</span></span><br><span class="line">            <span class="keyword">return</span> cache.get(lock);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中：</p><ul><li><code>String.valueOf(userId)</code> 将 <code>userId</code> 转换为字符串。</li><li><code>intern()</code> 确保每次生成的字符串对象是唯一的，并且是共享的。</li><li>使用 <code>synchronized (lock)</code> 时，不同的 <code>userId</code> 会对应不同的锁对象，从而避免了不必要的锁竞争。</li></ul><hr><h3 id="4-注意事项"><a href="#4-注意事项" class="headerlink" title="4. 注意事项"></a>4. <strong>注意事项</strong></h3><ul><li><strong>性能问题：</strong> <code>intern()</code> 方法在早期版本的 Java 中（如 Java 6 及之前）可能会导致性能问题，因为字符串常量池在堆外内存中，且大小有限。但从 Java 7 开始，字符串常量池被移到了堆内存中，性能得到了显著提升。</li><li><strong>内存泄漏：</strong> 如果滥用 <code>intern()</code>，可能会导致内存泄漏，因为常量池中的字符串对象不会被垃圾回收器回收。因此，建议仅在必要时使用 <code>intern()</code>。</li></ul><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><p><code>String.valueOf(userId).intern()</code> 的主要作用是确保字符串的唯一性，并且可以用于优化同步锁的性能。它在多线程环境中非常有用，但需要谨慎使用以避免性能问题和内存泄漏。</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>spingCloud速通</title>
      <link href="/2025/03/17/spingCloud%E9%80%9F%E9%80%9A/"/>
      <url>/2025/03/17/spingCloud%E9%80%9F%E9%80%9A/</url>
      
        <content type="html"><![CDATA[<h1 id="spingCloud速通"><a href="#spingCloud速通" class="headerlink" title="spingCloud速通"></a>spingCloud速通</h1><h2 id="Nacos"><a href="#Nacos" class="headerlink" title="Nacos"></a>Nacos</h2><h3 id="服务注册"><a href="#服务注册" class="headerlink" title="服务注册"></a>服务注册</h3><p>面试问题：</p><p><img src="/img_posts/image-20250313002602436.png" alt="image-20250313002602436"></p><p>答案：</p><p><img src="/img_posts/image-20250313002721421.png" alt="image-20250313002721421"></p><h2 id="注册中心"><a href="#注册中心" class="headerlink" title="注册中心"></a>注册中心</h2><p><img src="/img_posts/image-20250313003427571.png" alt="image-20250313003427571"></p><p>注意：</p><ul><li>记得在使用nacos配置的类上加上**@RefreshScope** 实时刷新配置，而不用重新打包项目</li><li>如果某个服务导入了nacos-config依赖，但没指定配置数据集，请在配置文件中配置<strong>spring.cloud.nacos.config.import-check.enabled&#x3D;false</strong></li></ul><p><img src="/img_posts/image-20250313005038514.png" alt="image-20250313005038514"></p><p>注意：</p><ul><li><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">@Component</span><br><span class="line">@ConfigurationProperties(prefix = &quot;order&quot;)  //配置批量绑定在nacos下,可以无需配置@RefrehScope就能自动刷新</span><br></pre></td></tr></table></figure></li></ul><h2 id="监听配置"><a href="#监听配置" class="headerlink" title="监听配置"></a>监听配置</h2><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line">/**</span><br><span class="line"> * 项目运行时，自启动，监督配置变化</span><br><span class="line"> * @param nacosConfigManager</span><br><span class="line"> * @return</span><br><span class="line"> */</span><br><span class="line">@Bean</span><br><span class="line">ApplicationRunner applicationRunner(NacosConfigManager nacosConfigManager)&#123;</span><br><span class="line">    return args -&gt; &#123;</span><br><span class="line">        ConfigService configService = nacosConfigManager.getConfigService();</span><br><span class="line">        configService.addListener(&quot;server-order.properties&quot;, &quot;DEFAULT_GROUP&quot;, new Listener() &#123;</span><br><span class="line">            /**</span><br><span class="line">             * 配置线程池</span><br><span class="line">             * @return</span><br><span class="line">             */</span><br><span class="line">            @Override</span><br><span class="line">            public Executor getExecutor() &#123;</span><br><span class="line">               return Executors.newFixedThreadPool(4);</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            /**</span><br><span class="line">             * 获取变化后的配置信息</span><br><span class="line">             * @param configInfo</span><br><span class="line">             */</span><br><span class="line">            @Override</span><br><span class="line">            public void receiveConfigInfo(String configInfo) &#123;</span><br><span class="line">                System.out.println(&quot;变化配置信息是：&quot;+configInfo);</span><br><span class="line">                System.out.println(&quot;邮件发送=====&quot;);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>面试问题</p><p><img src="/img_posts/image-20250317200832450.png" alt="image-20250317200832450"></p><p><img src="/img_posts/image-20250317200850963.png" alt="image-20250317200850963"></p><ul><li>即nacos外部配置优先</li></ul><h2 id="数据隔离"><a href="#数据隔离" class="headerlink" title="数据隔离"></a>数据隔离</h2><p><img src="/img_posts/image-20250317201301533.png" alt="image-20250317201301533"></p><p>​<img src="/img_posts/image-20250317201425205.png" alt="image-20250317201425205"></p><p><strong>实际例子（springboot配置applcation.yml）</strong></p><figure class="highlight yml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">server:</span></span><br><span class="line">  <span class="attr">port:</span> <span class="number">8000</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">profiles:</span></span><br><span class="line">    <span class="attr">active:</span> <span class="string">prod</span></span><br><span class="line">  <span class="attr">application:</span></span><br><span class="line">    <span class="attr">name:</span> <span class="string">server-order</span></span><br><span class="line">  <span class="attr">cloud:</span></span><br><span class="line">    <span class="attr">nacos:</span></span><br><span class="line">      <span class="attr">server-addr:</span> <span class="number">127.0</span><span class="number">.0</span><span class="number">.1</span><span class="string">:8848</span></span><br><span class="line">      <span class="attr">config:</span></span><br><span class="line">        <span class="attr">namespace:</span> <span class="string">$&#123;spring.profiles.active:public&#125;</span></span><br><span class="line">        <span class="attr">import-check:</span></span><br><span class="line">          <span class="attr">enabled:</span> <span class="literal">false</span>  <span class="comment">#禁用默认导入</span></span><br><span class="line"></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">config:</span></span><br><span class="line">    <span class="attr">import:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:common.properties?group=order</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:databases.properties?group=order</span></span><br><span class="line">    <span class="attr">activate:</span></span><br><span class="line">      <span class="attr">on-profile:</span> <span class="string">dev</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">config:</span></span><br><span class="line">    <span class="attr">import:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:common.properties?group=order</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:databases.properties?group=order</span></span><br><span class="line">    <span class="attr">activate:</span></span><br><span class="line">      <span class="attr">on-profile:</span> <span class="string">test</span></span><br><span class="line"><span class="meta">---</span></span><br><span class="line"><span class="attr">spring:</span></span><br><span class="line">  <span class="attr">config:</span></span><br><span class="line">    <span class="attr">import:</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:common.properties?group=order</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:databases.properties?group=order</span></span><br><span class="line">      <span class="bullet">-</span> <span class="string">nacos:druid.properties?group=order</span></span><br><span class="line">    <span class="attr">activate:</span></span><br><span class="line">      <span class="attr">on-profile:</span> <span class="string">prod</span></span><br></pre></td></tr></table></figure><p><strong>Nacos总结</strong></p><p><img src="/img_posts/image-20250317213503748.png" alt="image-20250317213503748"></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>鱼皮项目欠缺知识点</title>
      <link href="/2025/03/16/%E9%B1%BC%E7%9A%AE%E9%A1%B9%E7%9B%AE%E6%AC%A0%E7%BC%BA%E7%9F%A5%E8%AF%86%E7%82%B9/"/>
      <url>/2025/03/16/%E9%B1%BC%E7%9A%AE%E9%A1%B9%E7%9B%AE%E6%AC%A0%E7%BC%BA%E7%9F%A5%E8%AF%86%E7%82%B9/</url>
      
        <content type="html"><![CDATA[<h1 id="项目欠缺知识点"><a href="#项目欠缺知识点" class="headerlink" title="项目欠缺知识点"></a>项目欠缺知识点</h1><p>####下面是ts中?、?.、?:、??、||、|、!、!.、!!的基本介绍:<br>? 表示该属性或参数为可选项<br>?. 可以理解为对象自动加上undefined<br>?: 是指可选参数或者属性<br><strong>?? 表示只有当左侧为null和undefined时, 才会返回右侧的数</strong><br>|| 表示只有当左侧为0、null和undefined时, 才会返回右侧的数<br>| 表示取值可以为多种类型中的⼀种，联合类型使⽤ | 分隔每个类型。<br>! 是指非空断言，告诉ts某个属性或者参数不能为空<br>!. 是指断言，告诉 ts 该对象里一定有某个值<br>!! 是指把表达式强行转换为boolean类型，常用于类型判断</p><p>##await async 的底层原理<br>async&#x2F;await是一种用于处理异步操作的Promise语法糖，使得编写异步代码变得更加简单和易读。通过使用async关键字声明一个函数为异步函数，并使用await关键字等待Promise的解析（完成或拒绝），以同步的方式编写异步操作的代码。async 函数:</p><p>当函数被标记为async 时，它会自动返回一个 Promise。</p><p><strong>在async 函数内部，可以使用 await 关键字来等待一个 Promise 的解决。如果 await 后面的表达式不是一个 Promise，它会被自动包装成一个 Promise。</strong></p><p><strong>await 等待成功（Promise 对象</strong></p><p>await:当遇到 await 关键字时，函数的执行会暂停，直到 await 后面的 Promise 被解决。如果 Promise 被成功解决， await 表达式的值就是 Promise 的结果;如果 Promise 被拒绝，await 会抛出一个错误。</p><p>await 只能在 async 函数内部使用</p><p>当async 函数内部执行到一个 await 表达式时，函数的执行会暂停，并将控制权返回给调用者。此时注册一个回调函数，当 await 后面的 Promise 被解决时，这个回调函数会被调用，以继续执行函数的剩余部分。</p><p>如果await 后面的 Promise 被拒绝，会抛出一个错误，这个错误会被 try&#x2F;catch 块捕获，或者如果没有被捕获，会导致整个 Promise 链被拒绝。<br><strong>1 .async函数的特点</strong></p><ul><li>async修饰的函数默认返回的是<strong>Promise对象，</strong>所以可以放到await关键字后面来调用</li><li>async修饰的函数内部return的数据需要使用 await 或者 .then() 或者.catch 来接收</li></ul><p> <strong>2 .await的特点</strong></p><ul><li>await关键字必须放在async修饰的函数中才能使用</li><li>await关键字后面是一个Promise对象，如果是一个普通值会自动转为Promise对象来执行</li><li>await会等待其后面的Promise对象的成功执行状态，将结果值赋值给 &#x3D; 号前面的变量</li></ul><p>##**router.push({ path: ‘&#x2F;‘, replace: true })**‌：</p><ul><li>‌<strong>行为</strong>‌：这个方法会在历史记录中替换当前路由，而不是添加新的历史记录。用户不能通过浏览器的回退按钮返回到前一个路由。</li><li>‌<strong>适用场景</strong>‌：适用于不需要保留历史记录，或者希望用户不能通过回退按钮返回到前一个页面的场景。</li></ul><h5 id="具体实现方式"><a href="#具体实现方式" class="headerlink" title="具体实现方式"></a>具体实现方式</h5><p>在Vue中，<code>router.push</code>方法可以接收一个对象作为参数，其中可以包含<code>path</code>和<code>replace</code>属性。例如：</p><ul><li><code>router.push({ path: &#39;/&#39; })</code> 会将新路由添加到历史记录中。</li><li><code>router.push({ path: &#39;/&#39;, replace: true })</code> 会在历史记录中替换当前路由，而不是添加新的历史记录。</li></ul><p>####注意事项</p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">// 在 Vue 组件中</span><br><span class="line">this.$router.replace(&#x27;/users/1&#x27;)</span><br><span class="line">// 带查询参数</span><br><span class="line">this.$router.replace(&#123; path: &#x27;/search&#x27;, query: &#123; q: &#x27;vue&#x27; &#125; &#125;)</span><br><span class="line">// 命名路由，并传递参数</span><br><span class="line">this.$router.replace(&#123; name: &#x27;user&#x27;, params: &#123; userId: 123 &#125; &#125;)</span><br></pre></td></tr></table></figure><p>当你使用对象作为参数传递给 router.push 或 router.replace 时，请注意 params 和 query 的区别。params 是路由的一部分，必须要在路由配置中定义；而 query 是 URL 的查询参数，不是路由的一部分。<br>如果提供了 path，params 会被忽略。你需要使用命名路由 name 来传递 params。<br>router.push 和 router.replace 都返回一个 Promise，允许你等到导航完成后执行某些操作。</p><h2 id="Ant-Desion-中的属性"><a href="#Ant-Desion-中的属性" class="headerlink" title="Ant Desion 中的属性"></a>Ant Desion 中的<a-wap>属性</h2><p>#####在 Ant Design Vue 中，<code>&lt;a-row&gt;</code> 的 <code>wrap</code> 属性控制子元素（即 <code>&lt;a-col&gt;</code>）是否换行。具体来说：</p><ul><li>**<code>wrap=&quot;false&quot;</code>**：表示不允许换行，所有子元素会在一行内排列，即使内容超出容器宽度。</li><li>**<code>wrap=&quot;true&quot;</code>**：表示允许子元素换行，超出容器宽度的子元素会自动换到下一行。</li></ul><p>#####在 <code>Ant Design</code> 中，<code>&lt;a-col&gt;</code> 使用 <code>flex</code> 属性时，默认是基于 Flex 布局的 <code>flex-grow</code> 属性来设置宽度。如果需要将内容向右对齐，可以结合 CSS 的 <code>justify-content</code> 或 Flex 布局相关样式实现。</p><p>以下是向右对齐的实现方法：</p><hr><h3 id="方法-1：使用-style-设置-margin-left-自动对齐"><a href="#方法-1：使用-style-设置-margin-left-自动对齐" class="headerlink" title="方法 1：使用 style 设置 margin-left 自动对齐"></a>方法 1：使用 <code>style</code> 设置 <code>margin-left</code> 自动对齐</h3><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a-row</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a-col</span> <span class="attr">flex</span>=<span class="string">&quot;200px&quot;</span> <span class="attr">style</span>=<span class="string">&quot;margin-left: auto;&quot;</span>&gt;</span></span><br><span class="line">    向右对齐的内容</span><br><span class="line">  <span class="tag">&lt;/<span class="name">a-col</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">a-row</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li><code>margin-left: auto</code> 会将列内容推到右边。</li></ul><hr><h3 id="方法-2：使用-a-row-的-justify-属性"><a href="#方法-2：使用-a-row-的-justify-属性" class="headerlink" title="方法 2：使用 a-row 的 justify 属性"></a>方法 2：使用 <code>a-row</code> 的 <code>justify</code> 属性</h3><p>设置父级容器（<code>&lt;a-row&gt;</code>）的 <code>justify=&quot;end&quot;</code> 属性，让其子项靠右排列：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a-row</span> <span class="attr">justify</span>=<span class="string">&quot;end&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a-col</span> <span class="attr">flex</span>=<span class="string">&quot;200px&quot;</span>&gt;</span></span><br><span class="line">    向右对齐的内容</span><br><span class="line">  <span class="tag">&lt;/<span class="name">a-col</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">a-row</span>&gt;</span></span><br></pre></td></tr></table></figure><ul><li><code>justify=&quot;end&quot;</code> 会让所有列靠右对齐。</li></ul><hr><h3 id="方法-3：在-a-col-上使用-text-align-right"><a href="#方法-3：在-a-col-上使用-text-align-right" class="headerlink" title="方法 3：在 a-col 上使用 text-align: right"></a>方法 3：在 <code>a-col</code> 上使用 <code>text-align: right</code></h3><p>如果需要内容文字也靠右对齐，可以设置文本的 <code>text-align</code>：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a-row</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a-col</span> <span class="attr">flex</span>=<span class="string">&quot;200px&quot;</span> <span class="attr">style</span>=<span class="string">&quot;text-align: right;&quot;</span>&gt;</span></span><br><span class="line">    向右对齐的内容</span><br><span class="line">  <span class="tag">&lt;/<span class="name">a-col</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">a-row</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><h3 id="方法-4：使用-Flex-布局的-align-和-justify-属性"><a href="#方法-4：使用-Flex-布局的-align-和-justify-属性" class="headerlink" title="方法 4：使用 Flex 布局的 align 和 justify 属性"></a>方法 4：使用 Flex 布局的 <code>align</code> 和 <code>justify</code> 属性</h3><p>结合 <code>a-row</code> 的 <code>justify</code> 和 <code>align</code> 属性：</p><figure class="highlight html"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a-row</span> <span class="attr">justify</span>=<span class="string">&quot;end&quot;</span> <span class="attr">align</span>=<span class="string">&quot;middle&quot;</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">a-col</span> <span class="attr">flex</span>=<span class="string">&quot;200px&quot;</span>&gt;</span></span><br><span class="line">    向右对齐的内容</span><br><span class="line">  <span class="tag">&lt;/<span class="name">a-col</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">a-row</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><h3 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h3><ul><li>若仅调整单个列向右对齐，推荐使用 <strong>方法 1</strong>（<code>margin-left: auto</code>）。</li><li>如果调整整个行的内容对齐，使用 <strong>方法 2</strong>（<code>justify=&quot;end&quot;</code>）。</li><li>需要调整文本或内容对齐时，可结合 <strong>方法 3</strong> 或 <strong>方法 4</strong>。</li></ul><p><em>‌</em>layout&#x3D;”inline”<strong>‌ 在Ant Design的表单组件中用于设置表单项的布局方式。当设置‌</strong>layout&#x3D;”inline”<strong>‌时，表单项会以内联（inline）的形式排列，即所有表单项会在一行内显示，而不是传统的垂直排列方式。这种布局方式适用于需要紧凑布局的场景，如登录页面或简单的信息录入表单。</strong></p><h2 id="Ant-desigin"><a href="#Ant-desigin" class="headerlink" title="Ant-desigin"></a>Ant-desigin</h2><h3 id="1-a-col-组件"><a href="#1-a-col-组件" class="headerlink" title="1. a-col 组件"></a>1. <strong><code>a-col</code> 组件</strong></h3><p><code>a-col</code> 是 Ant Design Vue 中的栅格系统组件，用于创建响应式布局。它允许你通过设置不同的屏幕尺寸属性（如 <code>sm</code>、<code>md</code>、<code>lg</code>、<code>xl</code> 等）来定义列的宽度。</p><ul><li>**<code>:sm=&quot;24&quot;</code>**：在小屏幕设备（<code>sm</code>）上，该列占据 24 格宽度，即占满整个屏幕。</li><li>**<code>:md=&quot;16&quot;</code>**：在中等屏幕设备（<code>md</code>）上，该列占据 16 格宽度。</li><li>**<code>:xl=&quot;18&quot;</code>**：在大屏幕设备（<code>xl</code>）上，该列占据 18 格宽度。</li></ul><p>Ant Design Vue 的栅格系统基于 24 格布局，通过这种方式可以实现灵活的响应式设计。</p><p>####2.<code>width: 100% !important;</code> 是一个CSS样式规则，通常用于网页设计中，它的作用是将某个元素的宽度设置为父元素宽度的100%，即让该元素的宽度完全占据其父容器的宽度。</p><h3 id="关键点解释："><a href="#关键点解释：" class="headerlink" title="关键点解释："></a>关键点解释：</h3><ol><li>**<code>width: 100%</code>**：<br>这是一个相对宽度设置，表示元素的宽度是其父元素宽度的100%。例如，如果父元素宽度为500像素，那么该元素的宽度也会是500像素。</li><li>**<code>!important</code>**：<br><strong>这是一个CSS优先级规则，用于强制应用某个样式，即使它与其他样式冲突</strong>。在CSS的优先级规则中，<code>!important</code> 的优先级高于类选择器、ID选择器和内联样式（除了其他带有 <code>!important</code> 的规则）。使用时需谨慎，因为它可能会导致样式难以调试。</li></ol><h2 id="VUE组件传参"><a href="#VUE组件传参" class="headerlink" title="VUE组件传参"></a>VUE组件传参</h2><p>在 Vue 3 中，组合式 API（Composition API）是实现组件逻辑的一种强大方式。它允许我们将组件的逻辑拆分为可复用的函数，使得代码更加模块化和易于维护。以下将通过组合式 API 的方式，讲解组件传参的实现。</p><hr><h3 id="1-组合式-API-的基本概念"><a href="#1-组合式-API-的基本概念" class="headerlink" title="1. 组合式 API 的基本概念"></a><strong>1. 组合式 API 的基本概念</strong></h3><p>组合式 API 的核心是 <code>setup</code> 函数。在 <code>setup</code> 函数中，我们可以访问组件的 <code>props</code>、<code>context</code>，并定义响应式数据、方法等。通过组合式 API，我们可以将逻辑拆分为多个可复用的函数（称为“组合函数”）。</p><h3 id="2-父组件向子组件传参（Props）"><a href="#2-父组件向子组件传参（Props）" class="headerlink" title="2. 父组件向子组件传参（Props）"></a><strong>2. 父组件向子组件传参（Props）</strong></h3><h4 id="2-1-定义子组件"><a href="#2-1-定义子组件" class="headerlink" title="2.1 定义子组件"></a><strong>2.1 定义子组件</strong></h4><p>在组合式 API 中，我们通过 <code>defineProps</code> 来定义子组件的 <code>props</code>。</p><p><strong>子组件（ChildComponent.vue）：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;p&gt;子组件接收到的消息：&#123;&#123; message &#125;&#125;&lt;/p&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; defineProps &#125; from &#x27;vue&#x27;;</span><br><span class="line"></span><br><span class="line">// 使用 defineProps 定义子组件的 props</span><br><span class="line">const props = defineProps(&#123;</span><br><span class="line">  message: &#123;</span><br><span class="line">    type: String,</span><br><span class="line">    required: true</span><br><span class="line">  &#125;</span><br><span class="line">&#125;);</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><h4 id="2-2-使用父组件传递的数据"><a href="#2-2-使用父组件传递的数据" class="headerlink" title="2.2 使用父组件传递的数据"></a><strong>2.2 使用父组件传递的数据</strong></h4><p>父组件通过绑定属性的方式将数据传递给子组件。</p><p><strong>父组件（ParentComponent.vue）：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;ChildComponent :message=&quot;parentMessage&quot; /&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref &#125; from &#x27;vue&#x27;;</span><br><span class="line">import ChildComponent from &#x27;./ChildComponent.vue&#x27;;</span><br><span class="line"></span><br><span class="line">// 定义父组件的响应式数据</span><br><span class="line">const parentMessage = ref(&quot;Hello from Parent!&quot;);</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p><strong>说明：</strong></p><ol><li>子组件通过 <code>defineProps</code> 定义了它需要接收的 <code>props</code>。</li><li>父组件通过绑定 <code>:message=&quot;parentMessage&quot;</code> 将数据传递给子组件。</li><li>子组件可以直接使用 <code>props.message</code>。</li></ol><hr><h3 id="3-子组件向父组件传参（事件）"><a href="#3-子组件向父组件传参（事件）" class="headerlink" title="3. 子组件向父组件传参（事件）"></a><strong>3. 子组件向父组件传参（事件）</strong></h3><h4 id="3-1-定义子组件的事件"><a href="#3-1-定义子组件的事件" class="headerlink" title="3.1 定义子组件的事件"></a><strong>3.1 定义子组件的事件</strong></h4><p>在组合式 API 中，我们通过 <code>defineEmits</code> 或 <code>emit</code> 来定义和触发事件。</p><p><strong>子组件（ChildComponent.vue）：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;button @click=&quot;handleClick&quot;&gt;点击我&lt;/button&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; defineEmits &#125; from &#x27;vue&#x27;;</span><br><span class="line"></span><br><span class="line">// 定义子组件可以触发的事件</span><br><span class="line">const emit = defineEmits([&#x27;button-clicked&#x27;]);</span><br><span class="line"></span><br><span class="line">const handleClick = () =&gt; &#123;</span><br><span class="line">  // 触发事件，并传递数据</span><br><span class="line">  emit(&#x27;button-clicked&#x27;, &#123; id: 1, text: &#x27;Button clicked!&#x27; &#125;);</span><br><span class="line">&#125;;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><h4 id="3-2-父组件监听事件"><a href="#3-2-父组件监听事件" class="headerlink" title="3.2 父组件监听事件"></a><strong>3.2 父组件监听事件</strong></h4><p>父组件通过监听子组件触发的事件来接收数据。</p><p><strong>父组件（ParentComponent.vue）：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;div&gt;</span><br><span class="line">    &lt;ChildComponent @button-clicked=&quot;handleButtonClick&quot; /&gt;</span><br><span class="line">    &lt;p&gt;按钮信息：&#123;&#123; buttonInfo.text &#125;&#125;&lt;/p&gt;</span><br><span class="line">  &lt;/div&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; ref &#125; from &#x27;vue&#x27;;</span><br><span class="line">import ChildComponent from &#x27;./ChildComponent.vue&#x27;;</span><br><span class="line"></span><br><span class="line">const buttonInfo = ref(&#123;&#125;);</span><br><span class="line"></span><br><span class="line">const handleButtonClick = (info) =&gt; &#123;</span><br><span class="line">  buttonInfo.value = info;</span><br><span class="line">&#125;;</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><p><strong>说明：</strong></p><ol><li>子组件通过 <code>defineEmits</code> 定义它可以触发的事件。</li><li>子组件通过 <code>emit</code> 触发事件，并传递数据。</li><li>父组件通过监听 <code>@button-clicked</code> 事件接收数据，并在 <code>handleButtonClick</code> 方法中处理。</li></ol><hr><h3 id="4-使用组合函数（Composition-Function）"><a href="#4-使用组合函数（Composition-Function）" class="headerlink" title="4. 使用组合函数（Composition Function）"></a><strong>4. 使用组合函数（Composition Function）</strong></h3><p>组合式 API 的强大之处在于可以将逻辑拆分为可复用的组合函数。以下是一个示例，展示如何将子组件的逻辑封装为组合函数。</p><h4 id="4-1-封装子组件的逻辑"><a href="#4-1-封装子组件的逻辑" class="headerlink" title="4.1 封装子组件的逻辑"></a><strong>4.1 封装子组件的逻辑</strong></h4><p>我们可以将子组件的逻辑封装为一个组合函数，例如 <code>useButtonEvent</code>。</p><p><strong>useButtonEvent.js：</strong></p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; defineEmits &#125; <span class="keyword">from</span> <span class="string">&#x27;vue&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">export</span> <span class="keyword">function</span> <span class="title function_">useButtonEvent</span>(<span class="params"></span>) &#123;</span><br><span class="line">  <span class="keyword">const</span> emit = <span class="title function_">defineEmits</span>([<span class="string">&#x27;button-clicked&#x27;</span>]);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> <span class="title function_">handleClick</span> = (<span class="params"></span>) =&gt; &#123;</span><br><span class="line">    <span class="title function_">emit</span>(<span class="string">&#x27;button-clicked&#x27;</span>, &#123; <span class="attr">id</span>: <span class="number">1</span>, <span class="attr">text</span>: <span class="string">&#x27;Button clicked!&#x27;</span> &#125;);</span><br><span class="line">  &#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> &#123;</span><br><span class="line">    handleClick</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="4-2-在子组件中使用组合函数"><a href="#4-2-在子组件中使用组合函数" class="headerlink" title="4.2 在子组件中使用组合函数"></a><strong>4.2 在子组件中使用组合函数</strong></h4><p><strong>子组件（ChildComponent.vue）：</strong></p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">&lt;template&gt;</span><br><span class="line">  &lt;button @click=&quot;handleClick&quot;&gt;点击我&lt;/button&gt;</span><br><span class="line">&lt;/template&gt;</span><br><span class="line"></span><br><span class="line">&lt;script setup&gt;</span><br><span class="line">import &#123; useButtonEvent &#125; from &#x27;./useButtonEvent&#x27;;</span><br><span class="line"></span><br><span class="line">// 使用组合函数</span><br><span class="line">const &#123; handleClick &#125; = useButtonEvent();</span><br><span class="line">&lt;/script&gt;</span><br></pre></td></tr></table></figure><h4 id="4-3-父组件不变"><a href="#4-3-父组件不变" class="headerlink" title="4.3 父组件不变"></a><strong>4.3 父组件不变</strong></h4><p>父组件的实现方式与之前相同，通过监听事件接收数据。</p><hr><h3 id="5-总结"><a href="#5-总结" class="headerlink" title="5. 总结"></a><strong>5. 总结</strong></h3><p>通过组合式 API，我们可以清晰地定义组件的 <code>props</code> 和事件，并将逻辑拆分为可复用的组合函数。这种方式使得代码更加模块化，便于维护和复用。</p><h4 id="使用-defineProps"><a href="#使用-defineProps" class="headerlink" title="使用 defineProps"></a><strong>使用 <code>defineProps</code></strong></h4><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> props = defineProps&lt;<span class="title class_">Props</span>&gt;();</span><br></pre></td></tr></table></figure><ul><li><code>defineProps</code> 是 Vue 3 组合式 API 提供的一个宏（在 <code>&lt;script setup&gt;</code> 中使用时，它会被编译器处理）。</li><li>它的作用是根据提供的接口或类型定义，声明组件的 <code>props</code>。</li><li>在运行时，<code>props</code> 是一个响应式对象，你可以通过 <code>props.xxx</code> 的方式访问父组件传递的值。</li></ul><h3 id="什么是回调函数？"><a href="#什么是回调函数？" class="headerlink" title="什么是回调函数？"></a><strong>什么是回调函数？</strong></h3><p><strong>回调函数</strong>是一个作为参数传递给另一个函数的函数，并在该函数内部被调用。它允许你在某个操作完成时执行特定的逻辑。</p><p>简单来说，回调函数满足以下特点：</p><ol><li><span style="color: red"><strong>作为参数传递</strong>：回调函数通常作为参数传递给另一个函数。</span></li><li><strong>在某个时间点被调用</strong>：回调函数会在某个特定的操作完成时被调用，比如异步操作完成后、事件触发时等。</li><li><strong>延迟执行</strong>：回调函数的执行通常不是立即的，而是延迟到某个条件满足时。</li></ol><h2 id="EqualsAndHasashCode"><a href="#EqualsAndHasashCode" class="headerlink" title="@EqualsAndHasashCode"></a>@EqualsAndHasashCode</h2><p><code>@EqualsAndHashCode(callSuper = true)</code> 是一个注解，通常用于 Java 编程中，特别是在使用 Lombok 库时。Lombok 是一个流行的 Java 库，它通过注解的方式简化了 Java 的代码编写，减少了样板代码的生成。下面是对这个注解的详细解释：</p><h3 id="1-EqualsAndHashCode"><a href="#1-EqualsAndHashCode" class="headerlink" title="1. @EqualsAndHashCode"></a>1. <strong>@EqualsAndHashCode</strong></h3><p>这个注解的作用是自动生成 <code>equals()</code> 和 <code>hashCode()</code> 方法。这两个方法在 Java 中非常重要，尤其是在对象需要被存储到集合（如 <code>HashSet</code>、<code>HashMap</code>）或者需要比较对象是否相等时。</p><ul><li><strong><code>equals()</code> 方法</strong>：用于判断两个对象是否相等。</li><li><strong><code>hashCode()</code> 方法</strong>：用于生成对象的哈希码，通常用于哈希表相关操作。</li></ul><h3 id="2-callSuper-true"><a href="#2-callSuper-true" class="headerlink" title="2. callSuper = true"></a>2. <strong><code>callSuper = true</code></strong></h3><p>这个参数的作用是指示 Lombok 在生成 <code>equals()</code> 和 <code>hashCode()</code> 方法时，是否调用父类的 <code>equals()</code> 和 <code>hashCode()</code> 方法。</p><ul><li>**<code>callSuper = true</code>**：表示在生成的方法中会调用父类的 <code>equals()</code> 和 <code>hashCode()</code> 方法。这通常用于继承场景，当子类需要继承父类的某些字段时，确保父类的字段也被考虑在内。</li><li>**<code>callSuper = false</code>**（默认值）：表示不调用父类的 <code>equals()</code> 和 <code>hashCode()</code> 方法，只考虑当前类的字段。</li></ul><h3 id="3-使用场景"><a href="#3-使用场景" class="headerlink" title="3. 使用场景"></a>3. <strong>使用场景</strong></h3><p>假设你有一个父类 <code>Parent</code> 和一个子类 <code>Child</code>，并且子类需要继承父类的字段，那么在子类上使用 <code>@EqualsAndHashCode(callSuper = true)</code> 是非常有用的。</p><h4 id="示例代码："><a href="#示例代码：" class="headerlink" title="示例代码："></a>示例代码：</h4><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> lombok.EqualsAndHashCode;</span><br><span class="line"></span><br><span class="line"><span class="meta">@EqualsAndHashCode(callSuper = true)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">Child</span> <span class="keyword">extends</span> <span class="title class_">Parent</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> String childField;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>在这个例子中，Lombok 会生成 <code>equals()</code> 和 <code>hashCode()</code> 方法，这些方法会考虑父类 <code>Parent</code> 的字段以及子类 <code>Child</code> 的字段。</p><h3 id="4-注意事项"><a href="#4-注意事项" class="headerlink" title="4. 注意事项"></a>4. <strong>注意事项</strong></h3><ul><li>如果父类没有使用 Lombok 的 <code>@EqualsAndHashCode</code> 注解，那么在子类中使用 <code>callSuper = true</code> 时，父类的 <code>equals()</code> 和 <code>hashCode()</code> 方法需要手动实现。</li><li>如果父类是 <code>java.lang.Object</code>（默认父类），那么 <code>callSuper = true</code> 和 <code>callSuper = false</code> 的效果是一样的，因为 <code>Object</code> 类的 <code>equals()</code> 和 <code>hashCode()</code> 方法是默认实现。</li></ul><h2 id="acs-token"><a href="#acs-token" class="headerlink" title="acs-token"></a>acs-token</h2><p><code>acs-token</code> 是一种用于验证和授权的令牌，通常用于 HTTP 请求头中，以确保请求的合法性和安全性。它类似于其他常见的认证令牌（如 <code>access-token</code> 或 <code>auth-token</code>），用于标识请求的来源和权限。</p><h3 id="acs-token-的应用场景和作用"><a href="#acs-token-的应用场景和作用" class="headerlink" title="acs-token 的应用场景和作用"></a><code>acs-token</code> 的应用场景和作用</h3><ol><li><strong>身份验证</strong>：<code>acs-token</code> 通常由服务器生成并下发给客户端，客户端在后续请求中携带该令牌，服务器通过验证令牌的有效性来确认请求是否合法。</li><li><strong>防止恶意请求</strong>：通过在请求头中携带 <code>acs-token</code>，可以有效防止未经授权的请求，增强接口的安全性。</li><li><strong>动态生成与验证</strong>：在某些场景下，<code>acs-token</code> 是通过特定的算法动态生成的，例如结合请求的 URL、时间戳或其他参数进行加密生成。</li><li><strong>防止重放攻击</strong>：令牌通常具有时效性，过期后需要重新获取，从而防止恶意用户通过重放攻击的方式重复使用旧令牌。</li></ol><h2 id="冒泡-事件传播"><a href="#冒泡-事件传播" class="headerlink" title="冒泡(事件传播)"></a>冒泡(事件传播)</h2><p><code>e.stopPropagation()</code> 是一个常用于JavaScript和TypeScript中的事件处理方法，它的作用是阻止事件继续沿着DOM树传播。在Web开发中，事件传播是指当一个事件（如点击、鼠标移动等）在DOM元素上触发时，该事件会从触发它的元素开始，按照特定的顺序传播到其他元素（通常是父元素）。</p><p>事件传播主要分为三个阶段：</p><ol><li><strong>捕获阶段（Capturing Phase）</strong>：事件从根节点（document）开始，向目标节点（事件触发的元素）传播。</li><li><strong>目标阶段（Target Phase）</strong>：事件到达目标节点，即事件触发的元素。</li><li><strong>冒泡阶段（Bubbling Phase）</strong>：事件从目标节点开始，向根节点传播。</li></ol><p><code>e.stopPropagation()</code> 方法通常在事件处理函数中使用，用于在事件的冒泡阶段或捕获阶段阻止事件进一步传播。这对于防止事件冒泡到父元素或捕获阶段的事件处理函数非常有用，尤其是在需要精确控制事件处理行为的情况下。</p><p>例如，考虑一个嵌套的列表结构，其中每个列表项都有一个点击事件处理器。如果希望点击列表项时只触发该项的事件处理器，而不希望触发其父列表的事件处理器，可以在列表项的事件处理器中调用 <code>e.stopPropagation()</code>。</p><figure class="highlight javascript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;myList&#x27;</span>).<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span>(<span class="params">e</span>) &#123;</span><br><span class="line">    <span class="comment">// 阻止事件冒泡</span></span><br><span class="line">    e.<span class="title function_">stopPropagation</span>();</span><br><span class="line">    <span class="comment">// 处理点击事件</span></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;List clicked&#x27;</span>);</span><br><span class="line">&#125;);</span><br><span class="line"></span><br><span class="line"><span class="variable language_">document</span>.<span class="title function_">getElementById</span>(<span class="string">&#x27;myItem&#x27;</span>).<span class="title function_">addEventListener</span>(<span class="string">&#x27;click&#x27;</span>, <span class="keyword">function</span>(<span class="params">e</span>) &#123;</span><br><span class="line">    <span class="comment">// 处理点击事件</span></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;Item clicked&#x27;</span>);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure><p>在这个例子中，如果点击 <code>#myItem</code>，将只执行 <code>#myItem</code> 的事件处理器，而不会触发 <code>#myList</code> 的事件处理器，因为 <code>#myItem</code> 的事件处理器调用了 <code>e.stopPropagation()</code>。</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Maven学习记录</title>
      <link href="/2025/03/10/Maven%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
      <url>/2025/03/10/Maven%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="Maven-学习记录"><a href="#Maven-学习记录" class="headerlink" title="Maven 学习记录"></a>Maven 学习记录</h1><h2 id="maven的作用"><a href="#maven的作用" class="headerlink" title="maven的作用"></a>maven的作用</h2><p><strong>1.依赖管理</strong></p><p><strong>2.构建项目</strong></p><p><img src="/img_posts/image-20241018213900272.png" alt="image-20241018213900272"></p><h2 id="maven的安装与配置"><a href="#maven的安装与配置" class="headerlink" title="maven的安装与配置"></a>maven的安装与配置</h2><p><strong>1. 设置本地仓库</strong></p><p><strong>2.设置jdk版本</strong></p><p><strong>3.设置国内镜像</strong>    </p><h2 id="maven的GAVP属性"><a href="#maven的GAVP属性" class="headerlink" title="maven的GAVP属性"></a>maven的GAVP属性</h2><img src="/img_posts/image-20241018221824814.png" alt="image-20241018221824814" style="zoom:50%;" /><p><em>这里version有默认的值</em></p><h3 id="maven创建工程"><a href="#maven创建工程" class="headerlink" title="maven创建工程"></a>maven创建工程</h3><p><strong>手动创建方式</strong></p><p><img src="/img_posts/image-20241018223117792.png" alt="image-20241018223117792"></p><p><strong>插件创建方式</strong></p><p>使用JBLtojavaweb插件</p><p><img src="/img_posts/image-20241018223356763.png" alt="image-20241018223356763"></p><h2 id="maven项目结构"><a href="#maven项目结构" class="headerlink" title="maven项目结构"></a>maven项目结构</h2><img src="/img_posts/image-20241018223926867.png" alt="image-20241018223926867" style="zoom:50%;" /><h2 id="pom-依赖管理"><a href="#pom-依赖管理" class="headerlink" title="pom 依赖管理"></a>pom 依赖管理</h2><p><img src="/img_posts/image-20241018230103853.png" alt="image-20241018230103853"></p><p>使用方法：*${Jack.version}*</p><p><strong>scope标签</strong></p><p><img src="/img_posts/image-20241018230253592.png" alt="image-20241018230253592"></p><h2 id="依赖传递"><a href="#依赖传递" class="headerlink" title="依赖传递"></a>依赖传递</h2><p><img src="/img_posts/image-20241018230709245.png" alt="image-20241018230709245"></p><h2 id="依赖冲突"><a href="#依赖冲突" class="headerlink" title="依赖冲突"></a>依赖冲突</h2><img src="/img_posts/image-20241018231636271.png" alt="image-20241018231636271" style="zoom:50%;" /><p><strong><span style="color:red;"> ！！！只要发生了依赖冲突，后续的依赖传递全部静止</span></strong></p><h2 id="导入依赖的错误场景和解决办法"><a href="#导入依赖的错误场景和解决办法" class="headerlink" title="导入依赖的错误场景和解决办法"></a>导入依赖的错误场景和解决办法</h2><p><img src="/img_posts/image-20241018232613779.png" alt="image-20241018232613779"></p><p><img src="/img_posts/image-20241018232228872.png" alt="image-20241018232228872"></p><h2 id="构建管理"><a href="#构建管理" class="headerlink" title="构建管理"></a>构建管理</h2><p><strong>命令行方式构建</strong></p><p><img src="/img_posts/image-20241018233510346.png" alt="image-20241018233510346"></p><p><strong>可视化工具方式构建</strong></p><p><img src="/img_posts/image-20241019000149605.png" alt="image-20241019000149605"></p><p><strong>要求掌握的三种主动构建场景</strong></p><p><img src="/img_posts/image-20241019000255764.png" alt="image-20241019000255764"></p><p><strong>周期，命令，插件关系</strong></p><img src="/img_posts/image-20241019001153032.png" alt="image-20241019001153032" style="zoom: 33%;" /><p>、<img src="/img_posts/image-20241019001302376.png" alt="image-20241019001302376" style="zoom: 50%;" /></p><p><strong>导入插件</strong></p><p><img src="/img_posts/image-20241019001350068.png" alt="image-20241019001350068"></p><h2 id="继承关系"><a href="#继承关系" class="headerlink" title="继承关系"></a>继承关系</h2><p><img src="/img_posts/image-20241019003148800.png" alt="image-20241019003148800"></p><p><strong>父工程pom</strong></p><img src="/img_posts/image-20241019003313756.png" alt="image-20241019003313756" style="zoom:50%;" /><p><strong>子工程pom</strong></p><img src="/img_posts/image-20241019003041024.png" alt="image-20241019003041024" style="zoom:50%;" /><h2 id="聚合关系"><a href="#聚合关系" class="headerlink" title="聚合关系"></a>聚合关系</h2><p><img src="/img_posts/image-20241019004032148.png" alt="image-20241019004032148"></p><h2 id="maven项目实战"><a href="#maven项目实战" class="headerlink" title="maven项目实战"></a>maven项目实战</h2><p><strong>项目结构</strong></p><p><img src="/img_posts/image-20241019234533328.png" alt="image-20241019234533328"></p><p><strong>需要导入的依赖</strong></p><p><img src="/img_posts/image-20241019234635845.png" alt="image-20241019234635845"></p><p><strong><span style="color:red;">！！注意点：通用模块应install部署到本地仓库再被其他模块引用,后续可以写到父工程的dependenceyManagement中统一管理</span></strong></p><h2 id="Maven核心掌握知识总结"><a href="#Maven核心掌握知识总结" class="headerlink" title="Maven核心掌握知识总结"></a>Maven核心掌握知识总结</h2><p><img src="/img_posts/image-20241019235512697.png" alt="image-20241019235512697"></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>样式</title>
      <link href="/2025/03/09/%E6%A0%B7%E5%BC%8F/"/>
      <url>/2025/03/09/%E6%A0%B7%E5%BC%8F/</url>
      
        <content type="html"><![CDATA[<h1 id="样式"><a href="#样式" class="headerlink" title="样式"></a>样式</h1><p><code>height: &#39;100vh&#39;</code>: 设置布局的高度为视口高度的 100%，即填满整个屏幕。</p>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>@Async</title>
      <link href="/2025/03/08/@Async-2/"/>
      <url>/2025/03/08/@Async-2/</url>
      
        <content type="html"><![CDATA[<p>###@ Async</p><p>“@Async”是一个在编程中常见的注解，通常用于异步编程。它主要用于标记方法，使其在执行时不会阻塞主线程，而是通过异步的方式运行。以下是一些关于它的常见信息：</p><h3 id="1-用途"><a href="#1-用途" class="headerlink" title="1. 用途"></a>1. <strong>用途</strong></h3><ul><li><strong>异步执行任务</strong>：在多线程环境中，@Async可以让方法在后台线程中执行，而不是在主线程中运行。这可以提高程序的响应速度，避免主线程被长时间运行的任务阻塞。</li><li><strong>提高性能</strong>：通过异步执行，可以更好地利用多核处理器的资源，提高程序的整体性能。</li></ul><h3 id="2-使用场景"><a href="#2-使用场景" class="headerlink" title="2. 使用场景"></a>2. <strong>使用场景</strong></h3><ul><li><strong>Web开发</strong>：在Spring框架中，@Async常用于异步处理HTTP请求，例如发送邮件、日志记录、数据处理等。</li><li><strong>后台任务处理</strong>：例如定时任务、数据同步等场景。</li></ul><h3 id="3-使用方法（以Spring为例）"><a href="#3-使用方法（以Spring为例）" class="headerlink" title="3. 使用方法（以Spring为例）"></a>3. <strong>使用方法（以Spring为例）</strong></h3><ul><li><strong>启用异步支持</strong>：在Spring配置中，需要启用异步支持，例如通过<code>@EnableAsync</code>注解。   <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Configuration</span></span><br><span class="line"><span class="meta">@EnableAsync</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncConfig</span> &#123;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><strong>标记异步方法</strong>：在需要异步执行的方法上添加<code>@Async</code>注解。   <figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@Service</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">AsyncService</span> &#123;</span><br><span class="line">    <span class="meta">@Async</span></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">void</span> <span class="title function_">asyncMethod</span><span class="params">()</span> &#123;</span><br><span class="line">        System.out.println(<span class="string">&quot;This method is running asynchronously.&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="4-注意事项"><a href="#4-注意事项" class="headerlink" title="4. 注意事项"></a>4. <strong>注意事项</strong></h3><ul><li><strong>线程池配置</strong>：默认情况下，Spring会使用一个线程池来执行异步方法。可以通过配置<code>ThreadPoolTaskExecutor</code>来自定义线程池的大小和行为。</li><li><strong>异常处理</strong>：异步方法中的异常不会直接抛到主线程，需要通过<code>Future</code>或<code>CompletableFuture</code>来捕获和处理。</li><li><strong>方法调用限制</strong>：@Async注解的方法必须是<code>public</code>的，并且不能在同一个类中直接调用，否则不会触发异步行为。</li></ul>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>mybatis缓存机制</title>
      <link href="/2025/03/08/mybatis%E7%BC%93%E5%AD%98%E6%9C%BA%E5%88%B6/"/>
      <url>/2025/03/08/mybatis%E7%BC%93%E5%AD%98%E6%9C%BA%E5%88%B6/</url>
      
        <content type="html"><![CDATA[<h1 id="mybatis缓存机制"><a href="#mybatis缓存机制" class="headerlink" title="mybatis缓存机制"></a>mybatis缓存机制</h1><p>MyBatis 提供了两级缓存机制：一级缓存和二级缓存，用于提升数据库查询性能。</p><h3 id="一级缓存"><a href="#一级缓存" class="headerlink" title="一级缓存"></a>一级缓存</h3><p>一级缓存是基于 <code>SqlSession</code> 的本地缓存，每个 <code>SqlSession</code> 实例都有自己的缓存区域。它的特点如下：</p><ul><li><strong>默认开启</strong>：无需额外配置。</li><li><strong>作用范围</strong>：仅限于同一个 <code>SqlSession</code>。</li><li><strong>失效条件</strong>：<ul><li>执行 <code>insert</code>、<code>update</code> 或 <code>delete</code> 操作。</li><li>调用 <code>SqlSession.clearCache()</code>。</li><li><code>SqlSession</code> 关闭。</li></ul></li><li><strong>工作原理</strong>：当使用同一个 <code>SqlSession</code> 执行相同的查询时，MyBatis 会先检查一级缓存是否存在结果。如果存在，则直接返回缓存结果，否则查询数据库并将结果存入缓存。</li></ul><h3 id="二级缓存"><a href="#二级缓存" class="headerlink" title="二级缓存"></a>二级缓存</h3><p>二级缓存是跨多个 <code>SqlSession</code> 的全局缓存，作用范围是同一个 Mapper。它的特点如下：</p><ul><li><strong>需要手动开启</strong>：默认情况下，二级缓存是关闭的。</li><li><strong>作用范围</strong>：同一个 Mapper 的所有 <code>SqlSession</code>。</li><li><strong>缓存实现</strong>：可以使用 MyBatis 自带的缓存，也可以集成第三方缓存（如 Ehcache、Redis）。</li><li><strong>失效条件</strong>：当执行数据修改操作（<code>insert</code>、<code>update</code>、<code>delete</code>）时，二级缓存会被清空。</li><li><strong>配置步骤</strong>：<ol><li>在 <code>mybatis-config.xml</code> 中启用二级缓存：<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">settings</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">setting</span> <span class="attr">name</span>=<span class="string">&quot;cacheEnabled&quot;</span> <span class="attr">value</span>=<span class="string">&quot;true&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">settings</span>&gt;</span></span><br></pre></td></tr></table></figure></li><li>在 Mapper 文件中配置 <code>&lt;cache&gt;</code> 标签：<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">cache</span> <span class="attr">eviction</span>=<span class="string">&quot;FIFO&quot;</span> <span class="attr">flushInterval</span>=<span class="string">&quot;60000&quot;</span> <span class="attr">size</span>=<span class="string">&quot;512&quot;</span> <span class="attr">readOnly</span>=<span class="string">&quot;true&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure></li><li>如果使用第三方缓存，需指定缓存类型，例如：<figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">cache</span> <span class="attr">type</span>=<span class="string">&quot;org.mybatis.caches.ehcache.EhcacheCache&quot;</span>/&gt;</span></span><br></pre></td></tr></table></figure></li></ol></li></ul><h3 id="缓存策略配置"><a href="#缓存策略配置" class="headerlink" title="缓存策略配置"></a>缓存策略配置</h3><ul><li>**<code>eviction</code>**：指定缓存的清理策略，如 LRU（最近最少使用）、FIFO（先进先出）。</li><li>**<code>flushInterval</code>**：缓存刷新时间间隔（单位：毫秒）。</li><li>**<code>size</code>**：缓存的最大存储对象数量。</li><li>**<code>readOnly</code>**：指定缓存是否为只读。</li></ul><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><ol><li><strong>数据一致性</strong>：二级缓存需要确保与数据库数据的一致性，尤其是在分布式环境下。</li><li><strong>对象序列化</strong>：二级缓存中的对象需要实现 <code>Serializable</code> 接口。</li><li><strong>缓存穿透</strong>：避免缓存穿透问题，对高频查询适当配置缓存。</li></ol><p>通过合理配置和使用一级缓存与二级缓存，可以显著提升 MyBatis 的查询性能。</p><h2 id="解决问题"><a href="#解决问题" class="headerlink" title="解决问题"></a>解决问题</h2><p>（1） <strong>数据一致性问题</strong>  </p><p>在使用 MyBatis 二级缓存时，确保数据一致性是一个关键问题，尤其是在高并发和分布式环境下。以下是确保二级缓存数据一致性的方法和策略：</p><h3 id="1-自动清空缓存"><a href="#1-自动清空缓存" class="headerlink" title="1. 自动清空缓存"></a>1. <strong>自动清空缓存</strong></h3><p>MyBatis 在执行 <code>INSERT</code>、<code>UPDATE</code> 或 <code>DELETE</code> 操作时，会自动清空相关 Mapper 的二级缓存。这是 MyBatis 为保证数据一致性而采取的默认机制。</p><h3 id="2-配置缓存刷新策略"><a href="#2-配置缓存刷新策略" class="headerlink" title="2. 配置缓存刷新策略"></a>2. <strong>配置缓存刷新策略</strong></h3><p>通过合理配置 <code>&lt;cache&gt;</code> 标签的属性，可以进一步优化缓存的刷新策略：</p><ul><li>**<code>flushInterval</code>**：设置缓存的自动刷新时间间隔（单位：毫秒）。例如，<code>flushInterval=&quot;60000&quot;</code> 表示每 60 秒刷新一次缓存，以确保缓存中的数据不会过期。</li><li>**<code>eviction</code>**：指定缓存的淘汰策略，如 LRU（最近最少使用）或 FIFO（先进先出）。这可以避免缓存中的数据过多导致内存问题。</li></ul><h3 id="3-使用只读缓存"><a href="#3-使用只读缓存" class="headerlink" title="3. 使用只读缓存"></a>3. <strong>使用只读缓存</strong></h3><p>将二级缓存配置为只读（<code>readOnly=&quot;true&quot;</code>），可以提高缓存的性能，同时减少因缓存数据被修改而导致的一致性问题。</p><h3 id="4-分布式缓存解决方案"><a href="#4-分布式缓存解决方案" class="headerlink" title="4. 分布式缓存解决方案"></a>4. <strong>分布式缓存解决方案</strong></h3><p>在分布式环境中，MyBatis 默认的二级缓存实现可能无法满足需求，因为它们无法跨多个应用实例同步数据。此时，可以集成第三方分布式缓存，如 Redis 或 Ehcache：</p><ul><li><strong>Redis</strong>：通过 Redis 的发布&#x2F;订阅机制或使用 Redis 的过期时间（TTL）功能，可以确保缓存数据的实时性和一致性。</li><li><strong>Ehcache</strong>：支持集群模式，可以在多个应用实例之间同步缓存数据。</li></ul><h3 id="5-手动清空缓存"><a href="#5-手动清空缓存" class="headerlink" title="5. 手动清空缓存"></a>5. <strong>手动清空缓存</strong></h3><p>在某些场景下，可能需要手动清空二级缓存，以确保数据的最新性。可以通过调用 <code>clearCache()</code> 方法或在 Mapper 文件中配置 <code>&lt;cache&gt;</code> 标签的 <code>clearCacheInterval</code> 属性。</p><h3 id="6-避免缓存穿透"><a href="#6-避免缓存穿透" class="headerlink" title="6. 避免缓存穿透"></a>6. <strong>避免缓存穿透</strong></h3><p>对于高频查询的数据，应确保缓存中始终有数据可用，避免因缓存穿透导致数据库压力过大。可以通过设置合理的缓存过期时间和使用互斥锁（如分布式锁）来解决。</p><h3 id="7-合理设计缓存策略"><a href="#7-合理设计缓存策略" class="headerlink" title="7. 合理设计缓存策略"></a>7. <strong>合理设计缓存策略</strong></h3><ul><li><strong>适用场景</strong>：二级缓存适用于查询频繁且数据更新较少的业务场景（如配置表）。</li><li><strong>多表关联查询</strong>：在涉及多表关联查询时，需要通过 <code>&lt;cache-ref&gt;</code> 配置来避免脏数据。</li></ul><p>通过以上方法和策略，可以有效确保 MyBatis 二级缓存的数据一致性，同时提升系统的性能和稳定性。</p><p>（2）<strong>对象序列化</strong></p><p>在 MyBatis 的二级缓存中，对象序列化是一个重要的概念。由于二级缓存的作用范围是跨多个 <code>SqlSession</code> 的，甚至可能跨多个应用实例（在分布式环境中），因此需要将缓存的对象序列化后存储，以便在需要时可以反序列化还原对象。这就要求存储在二级缓存中的对象必须实现 <code>java.io.Serializable</code> 接口。</p><h3 id="为什么需要序列化？"><a href="#为什么需要序列化？" class="headerlink" title="为什么需要序列化？"></a>为什么需要序列化？</h3><ol><li><strong>跨会话共享</strong>：二级缓存中的数据需要在不同的 <code>SqlSession</code> 之间共享，甚至可能在不同的 JVM 实例之间共享（例如在集群环境中）。序列化可以将对象转换为字节流，方便在不同环境之间传输和存储。</li><li><strong>持久化存储</strong>：某些缓存实现（如 Ehcache）可能将数据持久化到磁盘，或者使用分布式缓存（如 Redis）时，需要将对象序列化后存储到外部存储中。</li><li><strong>缓存一致性</strong>：序列化确保对象在存储和恢复时保持一致的状态，避免因对象状态不一致导致的错误。</li></ol><h3 id="如何实现序列化？"><a href="#如何实现序列化？" class="headerlink" title="如何实现序列化？"></a>如何实现序列化？</h3><p>要使对象可以被序列化，需要满足以下条件：</p><ol><li><p><strong>实现 <code>Serializable</code> 接口</strong>：<code>Serializable</code> 是一个标记接口，没有方法和字段。实现该接口的类表示可以被序列化。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> java.io.Serializable;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>; <span class="comment">// 序列化版本号</span></span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> String email;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 构造函数、getter 和 setter 省略</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>**添加 <code>serialVersionUID</code>**：这是可选的，但建议添加。<code>serialVersionUID</code> 是一个序列化版本号，用于在反序列化时验证类版本是否一致。如果类结构发生变化（如添加或删除字段），需要更新这个版本号。</p><ul><li>如果没有显式声明 <code>serialVersionUID</code>，JVM 会根据类的结构自动生成一个。但如果类结构发生变化，自动生成的版本号也会改变，从而导致反序列化失败。</li></ul></li><li><p><strong>处理非序列化字段</strong>：如果对象中有一些字段不需要序列化，可以使用 <code>transient</code> 关键字修饰这些字段。例如：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">User</span> <span class="keyword">implements</span> <span class="title class_">Serializable</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">long</span> <span class="variable">serialVersionUID</span> <span class="operator">=</span> <span class="number">1L</span>;</span><br><span class="line">    <span class="keyword">private</span> <span class="type">int</span> id;</span><br><span class="line">    <span class="keyword">private</span> String name;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">transient</span> String password; <span class="comment">// 不需要序列化的字段</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><p>（3） <strong>缓存击穿问题</strong></p><p>在使用 MyBatis 二级缓存时，缓存穿透是一个常见的问题，尤其是在高频查询场景下。缓存穿透指的是查询不存在的数据时，由于缓存中没有该数据，每次请求都会直接查询数据库，从而失去缓存的意义，甚至可能导致数据库压力过大。</p><h3 id="解决缓存穿透问题的方法"><a href="#解决缓存穿透问题的方法" class="headerlink" title="解决缓存穿透问题的方法"></a>解决缓存穿透问题的方法</h3><p>以下是几种常见的解决方案：</p><h4 id="1-使用布隆过滤器（Bloom-Filter）"><a href="#1-使用布隆过滤器（Bloom-Filter）" class="headerlink" title="1. 使用布隆过滤器（Bloom Filter）"></a>1. <strong>使用布隆过滤器（Bloom Filter）</strong></h4><p>布隆过滤器是一种高效的数据结构，用于快速判断某个数据是否存在。在查询之前，先通过布隆过滤器判断数据是否可能存在于缓存或数据库中。如果布隆过滤器返回不存在，则直接返回，避免查询数据库。</p><h4 id="2-缓存空对象"><a href="#2-缓存空对象" class="headerlink" title="2. 缓存空对象"></a>2. <strong>缓存空对象</strong></h4><p>对于查询结果为空的数据，可以将空对象或特殊标记缓存起来，并设置较短的过期时间（如 5 分钟）。这样，后续相同的查询可以直接从缓存中获取空结果，减少对数据库的访问。</p><h4 id="3-接口层校验"><a href="#3-接口层校验" class="headerlink" title="3. 接口层校验"></a>3. <strong>接口层校验</strong></h4><p>在查询缓存之前，对请求参数进行合法性校验，如过滤非法字符、判断参数范围等。对于明显错误的参数，直接拦截并返回，避免无效查询。</p><h4 id="4-限流与熔断"><a href="#4-限流与熔断" class="headerlink" title="4. 限流与熔断"></a>4. <strong>限流与熔断</strong></h4><p>通过限流算法（如令牌桶、漏桶算法）控制访问频率，或在检测到数据库访问异常时启动熔断机制，暂时屏蔽请求。这可以有效缓解数据库压力。</p><h3 id="示例代码"><a href="#示例代码" class="headerlink" title="示例代码"></a>示例代码</h3><p>以下是一个结合布隆过滤器和缓存空对象的示例代码：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> User <span class="title function_">getUserById</span><span class="params">(Integer userId)</span> &#123;</span><br><span class="line">    <span class="comment">// 检查缓存是否存在</span></span><br><span class="line">    <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> cache.get(userId);</span><br><span class="line">    <span class="keyword">if</span> (user != <span class="literal">null</span>) &#123;</span><br><span class="line">        <span class="keyword">return</span> user;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 使用布隆过滤器判断数据是否存在</span></span><br><span class="line">    <span class="keyword">if</span> (!bloomFilter.mightContain(userId)) &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">null</span>; <span class="comment">// 直接返回，防止缓存穿透</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 查询数据库</span></span><br><span class="line">    user = userMapper.getUserById(userId);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (user != <span class="literal">null</span>) &#123;</span><br><span class="line">        cache.put(userId, user); <span class="comment">// 缓存数据</span></span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        cache.put(userId, <span class="literal">null</span>, <span class="number">300</span>); <span class="comment">// 缓存空对象，过期时间为 300 秒</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> user;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> Java面试 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>redis</title>
      <link href="/2025/03/07/redis/"/>
      <url>/2025/03/07/redis/</url>
      
        <content type="html"><![CDATA[<h1 id="redis"><a href="#redis" class="headerlink" title="redis"></a>redis</h1><p><img src="/img_posts/image-20241225222506160.png" alt="image-20241225222506160"></p><h3 id="redis通用命令"><a href="#redis通用命令" class="headerlink" title="redis通用命令"></a>redis通用命令</h3><p><img src="/img_posts/image-20241225222204337.png" alt="image-20241225222204337"></p><p><strong>TTL &#x3D; -1 代表该key永久有效</strong></p><p><strong>TTL &#x3D; -2 代表key已失效</strong></p><h3 id="String类型"><a href="#String类型" class="headerlink" title="String类型"></a>String类型</h3><p><img src="/img_posts/image-20241225222706026.png" alt="image-20241225222706026"></p><p><img src="/img_posts/image-20241225223043112.png" alt="image-20241225223043112"></p><p><strong>INCREBYFLOAT必须指定步长</strong>*</p><p><strong>通用删除命令：del key</strong></p><p>###key的层级结构</p><p><img src="/img_posts/image-20241225223646180.png" alt="image-20241225223646180"></p><p>###Hash类型</p><p><img src="/img_posts/image-20241225224044341.png" alt="image-20241225224044341"></p><p><img src="/img_posts/image-20241225224751566.png" alt="image-20241225224751566"></p><p><img src="/img_posts/image-20241225224743174.png" alt="image-20241225224743174"></p><p><strong>HSETNX成功返回1，失败返回0</strong></p><h3 id="List类型"><a href="#List类型" class="headerlink" title="List类型"></a>List类型</h3><p><img src="/img_posts/image-20241225225020541.png" alt="image-20241225225020541"></p><p><img src="/img_posts/image-20241225225608849.png" alt="image-20241225225608849"></p><p><strong>BLOP可以阻塞队列，设置等待时间，在规定时间等待其他用户插入，并返回拿到值所花的时间</strong></p><h3 id="Setl类型"><a href="#Setl类型" class="headerlink" title="Setl类型"></a>Setl类型</h3><p><img src="/img_posts/image-20241225230310612.png" alt="image-20241225230310612"></p><p><img src="/img_posts/image-20241225230505586.png" alt="image-20241225230505586"></p><h3 id="SortedSet"><a href="#SortedSet" class="headerlink" title="SortedSet"></a>SortedSet</h3><p><img src="/img_posts/image-20241225231227556.png" alt="image-20241225231227556"></p><p><img src="/img_posts/image-20241225231238289.png" alt="image-20241225231238289"></p><p><strong>排名按照score属性排，下标从零开始</strong></p><p><img src="/img_posts/image-20241225232036111.png" alt="image-20241225232036111"></p><p>###redis客户端</p><p><img src="/img_posts/image-20241225234550341.png" alt="image-20241225234550341"></p><p><strong>Jedis连接池配置</strong></p><p><img src="/img_posts/image-20241225235725110.png" alt="image-20241225235725110"></p><p><img src="/img_posts/image-20241226000315029.png" alt="image-20241226000315029"></p><p><img src="/img_posts/image-20241226000444813.png" alt="image-20241226000444813"></p><p>##重点</p><p><img src="/img_posts/image-20241226000527236.png" alt="image-20241226000527236"></p><p><img src="/img_posts/image-20241226000742215.png" alt="image-20241226000742215"></p><p><img src="/img_posts/image-20241226000818226.png" alt="image-20241226000818226"></p><p>###<strong>难点（序列化的方式）</strong></p><p><img src="/img_posts/image-20241226001236996.png" alt="image-20241226001236996"></p><p><strong>redis是将k-v的字符根据不同编码格式转成字节存在库中</strong></p><p><strong>springDataRedis默认的序列化类是jdk的序列化，所以redis解析不出来正确的结果</strong></p><p>**指定正确的序列化方式：1.key指定String….序列化方式</p><p>​  2.value指定GenericJacon2Json…方式（object装json）</p><p><em><strong>RedisTemplate其他源码序列化方式为null</strong></em></p><p><img src="/img_posts/image-20241226002617466.png" alt="image-20241226002617466"></p><p><img src="/img_posts/image-20241226001654649.png"></p><p><strong>节约内存，不使用对象序列化的做法</strong></p><p><img src="/img_posts/image-20241226003322825.png" alt="image-20241226003322825"></p><p><img src="/img_posts/image-20241226003335934.png" alt="image-20241226003335934"></p><p><img src="/img_posts/image-20241226003440593.png" alt="image-20241226003440593"></p><p><img src="/img_posts/image-20241226003532154.png" alt="image-20241226003532154"></p><p><img src="/img_posts/image-20241226003732632.png" alt="image-20241226003732632"></p><p><strong>方法与redis命令不一致(自己摸索)</strong></p><p><img src="/img_posts/image-20241226003744493.png" alt="image-20241226003744493"></p><p>问题：缓存雪崩，缓存击穿，缓存穿透</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>大数据开发文档</title>
      <link href="/2025/01/04/%E5%A4%A7%E6%95%B0%E6%8D%AE%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/"/>
      <url>/2025/01/04/%E5%A4%A7%E6%95%B0%E6%8D%AE%E5%BC%80%E5%8F%91%E6%96%87%E6%A1%A3/</url>
      
        <content type="html"><![CDATA[<h3 id="大数据代码"><a href="#大数据代码" class="headerlink" title="大数据代码"></a>大数据代码</h3><h5 id="数据仓库建模"><a href="#数据仓库建模" class="headerlink" title="数据仓库建模"></a>数据仓库建模</h5><ul><li><p>业务:  推荐景点业务</p><ul><li><p>维度</p><p> 景点信息：| 序号 | 景点名称 | 地区 | 评分 | 评级 | 地址 | 评语 | 价格 | 销量 | 省市自治区 </p></li><li><p>事实</p></li></ul></li></ul><p>​推荐景点：最热门的景点（销量最高的十个景点），评分最高的景点（评分前十），票价最优惠的景点（按评分从高到低排序，并在给定评分范围内选择最低票价,评分大于0.78票价最低的十个景点）</p><p>具体事实：</p><p>按照地区分类</p><p>按评级分类</p><h5 id="实现流程"><a href="#实现流程" class="headerlink" title="实现流程"></a>实现流程</h5><p>#####1.导入数据源csv文件（带表头列）</p><ul><li>在hive中创建表</li></ul><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> my_table (</span><br><span class="line">  ......</span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED</span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>   <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>   <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;        <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> destination_dim (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID（可以与序号关联）</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    region STRING,               <span class="comment">-- 景点所在地区</span></span><br><span class="line">    rating <span class="type">FLOAT</span>,                <span class="comment">-- 景点评分</span></span><br><span class="line">    rating_level STRING,         <span class="comment">-- 景点评级（如：A、B级等）</span></span><br><span class="line">    address STRING,              <span class="comment">-- 景点地址</span></span><br><span class="line">    reviews STRING,              <span class="comment">-- 景点评语（如“很有历史感”，“非常适合家庭旅游”等）</span></span><br><span class="line">    price <span class="type">FLOAT</span>,                 <span class="comment">-- 景点门票价格</span></span><br><span class="line">    sales <span class="type">INT</span>,                   <span class="comment">-- 景点销量</span></span><br><span class="line">    province STRING,             <span class="comment">-- 省市自治区</span></span><br><span class="line">    <span class="operator">/</span><span class="operator">/</span><span class="keyword">PRIMARY KEY</span> (destination_id)  <span class="operator">/</span><span class="operator">/</span> Hive 不支持在表创建时使用 <span class="keyword">PRIMARY KEY</span> 或其他约束（如 <span class="keyword">FOREIGN KEY</span>）等约束功能。</span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED</span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>   <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>   <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;        <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><p>真正实现的代码</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> destination_dim (destination_id STRING, destination_name STRING, region STRING, rating <span class="type">FLOAT</span>, rating_level STRING, address STRING, reviews STRING, price <span class="type">FLOAT</span>, sales <span class="type">INT</span>, province STRING) <span class="type">ROW</span> FORMAT DELIMITED FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span> LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span> STORED <span class="keyword">AS</span> TEXTFILE;</span><br></pre></td></tr></table></figure><ul><li>使用<code>hadoop fs</code>命令来删除第一行，并将结果输出到一个新的文件中，然后再将新的文件加载到Hive。</li></ul><p><code>cat /user/hive/warehouse/my_csv_file.csv | tail -n +2 &gt; /user/hive/warehouse/my_csv_file_no_header.csv</code></p><p><code>cat /root/csvdata/data.csv | tail -n +2 &gt; /root/csvdata/data_no_header.csv  </code></p><ul><li>加载数据到Hive表</li></ul><p><code>LOAD DATA INPATH &#39;/user/hive/warehouse/my_csv_file_no_header.csv&#39; INTO TABLE my_table;</code></p><p><code>LOAD DATA LOCAL INPATH &#39;/user/hive/warehouse/my_csv_file_no_header.csv&#39; INTO TABLE my_table;</code></p><p><code>LOAD DATA LOCAL INPATH &#39;/path/to/your/file.csv&#39; OVERWRITE INTO TABLE your_table_name;</code> 覆盖表数据</p><p>实现代码</p><p><code>LOAD DATA LOCAL INPATH &#39;/root/csvdata/data_no_header.csv&#39; INTO TABLE destination_dim</code></p><p><code>LOAD DATA LOCAL INPATH &#39;/root/csvdata/data_no_header.csv&#39; OVERWRITE INTO TABLE destination_dim</code></p><h5 id="2-创建表"><a href="#2-创建表" class="headerlink" title="2.创建表"></a>2.创建表</h5><p>为了实现这三个业务需求，我们需要创建三个不同的事实表来存储最热门的景点、评分最高的景点和票价最优惠的景点的信息。以下是创建这些表的 SQL 语句，以及如何根据 <code>destination_dim</code> 表插入数据到这些表中的步骤。</p><h3 id="1-创建最热门景点表（top-sales-fact）"><a href="#1-创建最热门景点表（top-sales-fact）" class="headerlink" title="1. 创建最热门景点表（top_sales_fact）"></a><strong>1. 创建最热门景点表（<code>top_sales_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> top_sales_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    sales <span class="type">INT</span>,                   <span class="comment">-- 景点销量</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="2-创建评分最高景点表（top-rating-fact）"><a href="#2-创建评分最高景点表（top-rating-fact）" class="headerlink" title="2. 创建评分最高景点表（top_rating_fact）"></a><strong>2. 创建评分最高景点表（<code>top_rating_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> top_rating_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    rating <span class="type">FLOAT</span>,                <span class="comment">-- 景点评分</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="3-创建票价最优惠景点表（best-price-fact）"><a href="#3-创建票价最优惠景点表（best-price-fact）" class="headerlink" title="3. 创建票价最优惠景点表（best_price_fact）"></a><strong>3. 创建票价最优惠景点表（<code>best_price_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> best_price_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    rating <span class="type">FLOAT</span>,                <span class="comment">-- 景点评分</span></span><br><span class="line">    price <span class="type">FLOAT</span>,                 <span class="comment">-- 景点门票价格</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="插入数据到这些表中"><a href="#插入数据到这些表中" class="headerlink" title="插入数据到这些表中"></a><strong>插入数据到这些表中</strong></h3><h4 id="插入最热门景点数据（销量最高的十个景点）"><a href="#插入最热门景点数据（销量最高的十个景点）" class="headerlink" title="插入最热门景点数据（销量最高的十个景点）"></a><strong>插入最热门景点数据（销量最高的十个景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> top_sales_fact (destination_id, destination_name, sales, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, sales, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, sales, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> sales <span class="keyword">DESC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><h4 id="插入评分最高景点数据（评分前十的景点）"><a href="#插入评分最高景点数据（评分前十的景点）" class="headerlink" title="插入评分最高景点数据（评分前十的景点）"></a><strong>插入评分最高景点数据（评分前十的景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> top_rating_fact (destination_id, destination_name, rating, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, rating, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, rating, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> rating <span class="keyword">DESC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><h4 id="插入票价最优惠景点数据（评分大于0-78票价最低的十个景点）"><a href="#插入票价最优惠景点数据（评分大于0-78票价最低的十个景点）" class="headerlink" title="插入票价最优惠景点数据（评分大于0.78票价最低的十个景点）"></a><strong>插入票价最优惠景点数据（评分大于0.78票价最低的十个景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> best_price_fact (destination_id, destination_name, rating, price, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, rating, price, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, rating, price, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">WHERE</span> rating <span class="operator">&gt;</span> <span class="number">0.78</span></span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> price <span class="keyword">ASC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><p><strong>注意</strong></p><p>报错：FAILED: SemanticException [Error 10004]: Line 4:9 Invalid table alias or column reference ‘sales’: (possible column names are: _col0, _col1, _col2, _col3)</p><p>因为 Hive 在 <code>SELECT</code> 子句中指定的列名在 <code>ORDER BY</code> 子句中可能无法直接引用。具体来说，Hive 会生成临时的列名（如 <code>_col0</code>, <code>_col1</code> 等），因此 <code>ORDER BY</code> 和 <code>LIMIT</code> 子句可能无法解析直接的列名。</p><p>解决方法  1.使用子查询 2.建立临时表</p><p>###获取表列名（hive采用的直接数据库模式,mysql）</p><p>Hive 的元数据通常存储在 Metastore 数据库中（例如 MySQL）。要查看 Hive 表的元数据（如表的列名、数据类型等），可以通过 MySQL 访问 Metastore 数据库。以下是具体步骤：</p><hr><h3 id="1-连接到-Metastore-数据库"><a href="#1-连接到-Metastore-数据库" class="headerlink" title="1. 连接到 Metastore 数据库"></a><strong>1. 连接到 Metastore 数据库</strong></h3><ol><li><p><strong>找到 Metastore 数据库的连接信息</strong>：</p><ul><li><p>检查 Hive 的配置文件 </p><figure class="highlight plaintext"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">hive-site.xml</span><br></pre></td></tr></table></figure><p>，其中会有 Metastore 的连接信息，例如：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>javax.jdo.option.ConnectionURL<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>jdbc:mysql://hostname:port/metastore_db<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>javax.jdo.option.ConnectionUserName<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>your_username<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">property</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">name</span>&gt;</span>javax.jdo.option.ConnectionPassword<span class="tag">&lt;/<span class="name">name</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">value</span>&gt;</span>your_password<span class="tag">&lt;/<span class="name">value</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">property</span>&gt;</span></span><br></pre></td></tr></table></figure></li></ul></li><li><p><strong>登录 MySQL 数据库</strong>： 使用 MySQL 客户端或工具（如 <code>mysql</code> 命令行、phpMyAdmin、DBeaver 等）连接到 Metastore 数据库：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u your_username -p -h hostname -P port</span><br></pre></td></tr></table></figure><p>输入密码后进入数据库。</p></li></ol><hr><h3 id="2-查询表的元数据"><a href="#2-查询表的元数据" class="headerlink" title="2. 查询表的元数据"></a><strong>2. 查询表的元数据</strong></h3><p>Hive 的表结构信息存储在多个表中，以下是关键表及其作用：</p><ul><li>**<code>TBLS</code>**：存储 Hive 表的基本信息。</li><li>**<code>COLUMNS_V2</code>**：存储表的列名和数据类型。</li><li>**<code>SDS</code>**：存储表的存储描述符 ID。</li><li>**<code>DBS</code>**：存储数据库信息。</li></ul><h4 id="查询表的列名和数据类型"><a href="#查询表的列名和数据类型" class="headerlink" title="查询表的列名和数据类型"></a><strong>查询表的列名和数据类型</strong></h4><p>假设你要查询 <code>your_table_name</code> 的列名和数据类型：</p><ol><li><p>找到表的存储描述符 ID (<code>SD_ID</code>)：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">USE metastore_db;</span><br><span class="line"></span><br><span class="line"><span class="keyword">SELECT</span> SD_ID</span><br><span class="line"><span class="keyword">FROM</span> TBLS</span><br><span class="line"><span class="keyword">WHERE</span> TBL_NAME <span class="operator">=</span> <span class="string">&#x27;your_table_name&#x27;</span>;</span><br></pre></td></tr></table></figure></li><li><p>使用 <code>SD_ID</code> 获取列信息：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> COLUMN_NAME, TYPE_NAME</span><br><span class="line"><span class="keyword">FROM</span> COLUMNS_V2</span><br><span class="line"><span class="keyword">WHERE</span> CD_ID <span class="operator">=</span> (<span class="keyword">SELECT</span> SD_ID <span class="keyword">FROM</span> TBLS <span class="keyword">WHERE</span> TBL_NAME <span class="operator">=</span> <span class="string">&#x27;your_table_name&#x27;</span>);</span><br></pre></td></tr></table></figure></li></ol><hr><h3 id="3-查询表的所有元数据"><a href="#3-查询表的所有元数据" class="headerlink" title="3. 查询表的所有元数据"></a><strong>3. 查询表的所有元数据</strong></h3><p>如果需要完整的表元数据，可以结合 <code>TBLS</code>、<code>COLUMNS_V2</code>、<code>SDS</code> 和 <code>DBS</code> 进行查询：</p><h4 id="获取表所在的数据库和其他信息："><a href="#获取表所在的数据库和其他信息：" class="headerlink" title="获取表所在的数据库和其他信息："></a>获取表所在的数据库和其他信息：</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> dbs.NAME <span class="keyword">AS</span> database_name, tbls.TBL_NAME <span class="keyword">AS</span> table_name, tbls.TBL_TYPE <span class="keyword">AS</span> table_type</span><br><span class="line"><span class="keyword">FROM</span> TBLS tbls</span><br><span class="line"><span class="keyword">JOIN</span> DBS dbs <span class="keyword">ON</span> tbls.DB_ID <span class="operator">=</span> dbs.DB_ID</span><br><span class="line"><span class="keyword">WHERE</span> tbls.TBL_NAME <span class="operator">=</span> <span class="string">&#x27;your_table_name&#x27;</span>;</span><br></pre></td></tr></table></figure><h4 id="获取表的列信息："><a href="#获取表的列信息：" class="headerlink" title="获取表的列信息："></a>获取表的列信息：</h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> c.COLUMN_NAME, c.TYPE_NAME, c.COMMENT</span><br><span class="line"><span class="keyword">FROM</span> TBLS t</span><br><span class="line"><span class="keyword">JOIN</span> SDS s <span class="keyword">ON</span> t.SD_ID <span class="operator">=</span> s.SD_ID</span><br><span class="line"><span class="keyword">JOIN</span> COLUMNS_V2 c <span class="keyword">ON</span> s.CD_ID <span class="operator">=</span> c.CD_ID</span><br><span class="line"><span class="keyword">WHERE</span> t.TBL_NAME <span class="operator">=</span> <span class="string">&#x27;your_table_name&#x27;</span>;</span><br></pre></td></tr></table></figure><hr><h3 id="4-导出结果"><a href="#4-导出结果" class="headerlink" title="4. 导出结果"></a><strong>4. 导出结果</strong></h3><p>可以将查询结果导出到文件：</p><ol><li><p>在 MySQL 命令行中：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">mysql -u your_username -p -h hostname -P port -e <span class="string">&quot;SELECT COLUMN_NAME, TYPE_NAME FROM COLUMNS_V2 WHERE CD_ID = (SELECT SD_ID FROM TBLS WHERE TBL_NAME = &#x27;your_table_name&#x27;);&quot;</span> &gt; output.csv</span><br></pre></td></tr></table></figure></li><li><p>或在 MySQL 客户端工具中，选择“导出”选项，保存为 CSV、Excel 等格式。</p></li></ol><hr><h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a><strong>注意事项</strong></h3><ul><li><strong>权限要求</strong>：确保你的 MySQL 用户具有查询 Metastore 数据库的权限。</li><li><strong>表名区分大小写</strong>：Hive 默认表名不区分大小写，但查询时建议保持一致。</li><li><strong>风险提示</strong>：避免对 Metastore 数据库执行任何写操作，以免破坏元数据。</li></ul><p>通过以上步骤，你可以轻松查询和导出 Hive 表的元数据。</p><p>####导出为csv文件</p><p>实现代码</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT</span> OVERWRITE <span class="keyword">LOCAL</span> DIRECTORY <span class="string">&#x27;/root/put&#x27;</span></span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED</span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> top_sales_fact;</span><br></pre></td></tr></table></figure><pre><code class="sql">INSERT OVERWRITE LOCAL DIRECTORY &#39;/root/put&#39;ROW FORMAT DELIMITEDFIELDS TERMINATED BY &#39;,&#39;LINES TERMINATED BY &#39;\n&#39;SELECT * FROM top_rating_fact;</code></pre><pre><code class="sql">INSERT OVERWRITE LOCAL DIRECTORY &#39;/root/put&#39;ROW FORMAT DELIMITEDFIELDS TERMINATED BY &#39;,&#39;LINES TERMINATED BY &#39;\n&#39;SELECT * FROM best_price_fact;</code></pre><h4 id="可视化工具"><a href="#可视化工具" class="headerlink" title="可视化工具"></a>可视化工具</h4><p><a href="http://localhost:50401/">首页 - DataGear</a></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>事物表和维度表</title>
      <link href="/2025/01/04/%E4%BA%8B%E7%89%A9%E8%A1%A8%E5%92%8C%E7%BB%B4%E5%BA%A6%E8%A1%A8/"/>
      <url>/2025/01/04/%E4%BA%8B%E7%89%A9%E8%A1%A8%E5%92%8C%E7%BB%B4%E5%BA%A6%E8%A1%A8/</url>
      
        <content type="html"><![CDATA[<p>为了实现这三个业务需求，我们需要创建三个不同的事实表来存储最热门的景点、评分最高的景点和票价最优惠的景点的信息。以下是创建这些表的 SQL 语句，以及如何根据 <code>destination_dim</code> 表插入数据到这些表中的步骤。</p><h3 id="1-创建最热门景点表（top-sales-fact）"><a href="#1-创建最热门景点表（top-sales-fact）" class="headerlink" title="1. 创建最热门景点表（top_sales_fact）"></a><strong>1. 创建最热门景点表（<code>top_sales_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> top_sales_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    sales <span class="type">INT</span>,                   <span class="comment">-- 景点销量</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="2-创建评分最高景点表（top-rating-fact）"><a href="#2-创建评分最高景点表（top-rating-fact）" class="headerlink" title="2. 创建评分最高景点表（top_rating_fact）"></a><strong>2. 创建评分最高景点表（<code>top_rating_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> top_rating_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    rating <span class="type">FLOAT</span>,                <span class="comment">-- 景点评分</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="3-创建票价最优惠景点表（best-price-fact）"><a href="#3-创建票价最优惠景点表（best-price-fact）" class="headerlink" title="3. 创建票价最优惠景点表（best_price_fact）"></a><strong>3. 创建票价最优惠景点表（<code>best_price_fact</code>）</strong></h3><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE TABLE</span> best_price_fact (</span><br><span class="line">    destination_id STRING,       <span class="comment">-- 景点ID</span></span><br><span class="line">    destination_name STRING,     <span class="comment">-- 景点名称</span></span><br><span class="line">    rating <span class="type">FLOAT</span>,                <span class="comment">-- 景点评分</span></span><br><span class="line">    price <span class="type">FLOAT</span>,                 <span class="comment">-- 景点门票价格</span></span><br><span class="line">    province STRING              <span class="comment">-- 省市自治区</span></span><br><span class="line">)</span><br><span class="line"><span class="type">ROW</span> FORMAT DELIMITED </span><br><span class="line">FIELDS TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;,&#x27;</span>  <span class="comment">-- CSV文件是以逗号分隔</span></span><br><span class="line">LINES TERMINATED <span class="keyword">BY</span> <span class="string">&#x27;\n&#x27;</span>  <span class="comment">-- 每行数据以换行符分隔</span></span><br><span class="line">STORED <span class="keyword">AS</span> TEXTFILE;       <span class="comment">-- 存储为文本文件格式</span></span><br></pre></td></tr></table></figure><h3 id="插入数据到这些表中"><a href="#插入数据到这些表中" class="headerlink" title="插入数据到这些表中"></a><strong>插入数据到这些表中</strong></h3><h4 id="插入最热门景点数据（销量最高的十个景点）"><a href="#插入最热门景点数据（销量最高的十个景点）" class="headerlink" title="插入最热门景点数据（销量最高的十个景点）"></a><strong>插入最热门景点数据（销量最高的十个景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> top_sales_fact (destination_id, destination_name, sales, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, sales, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, sales, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> sales <span class="keyword">DESC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><h4 id="插入评分最高景点数据（评分前十的景点）"><a href="#插入评分最高景点数据（评分前十的景点）" class="headerlink" title="插入评分最高景点数据（评分前十的景点）"></a><strong>插入评分最高景点数据（评分前十的景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> top_rating_fact (destination_id, destination_name, rating, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, rating, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, rating, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> rating <span class="keyword">DESC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><h4 id="插入票价最优惠景点数据（评分大于0-78票价最低的十个景点）"><a href="#插入票价最优惠景点数据（评分大于0-78票价最低的十个景点）" class="headerlink" title="插入票价最优惠景点数据（评分大于0.78票价最低的十个景点）"></a><strong>插入票价最优惠景点数据（评分大于0.78票价最低的十个景点）</strong></h4><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">INSERT INTO</span> best_price_fact (destination_id, destination_name, rating, price, province)</span><br><span class="line"><span class="keyword">SELECT</span> destination_id, destination_name, rating, price, province</span><br><span class="line"><span class="keyword">FROM</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> destination_id, destination_name, rating, price, province</span><br><span class="line">    <span class="keyword">FROM</span> destination_dim</span><br><span class="line">    <span class="keyword">WHERE</span> rating <span class="operator">&gt;</span> <span class="number">0.78</span></span><br><span class="line">    <span class="keyword">ORDER</span> <span class="keyword">BY</span> price <span class="keyword">ASC</span></span><br><span class="line">    LIMIT <span class="number">10</span></span><br><span class="line">) temp;</span><br></pre></td></tr></table></figure><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a><strong>总结</strong></h3><p>通过以上步骤，我们创建了三个事实表来存储最热门的景点、评分最高的景点和票价最优惠的景点的信息，并根据 <code>destination_dim</code> 表插入了相应的数据。这些表可以用于进一步的分析和推荐系统。如果你有其他问题或需要进一步的帮助，请告诉我！</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Linux命令</title>
      <link href="/2025/01/01/Linux%E5%91%BD%E4%BB%A4/"/>
      <url>/2025/01/01/Linux%E5%91%BD%E4%BB%A4/</url>
      
        <content type="html"><![CDATA[<h1 id="Linux命令"><a href="#Linux命令" class="headerlink" title="Linux命令"></a>Linux命令</h1><p>删除文件</p><ul><li><code>rm 文件名</code></li></ul><p>删除文件夹</p><ol><li>文件夹为空</li></ol><ul><li><code>rmdir 文件夹</code></li><li><code>rm -d 文件夹</code></li></ul><ol start="2"><li>文件夹非空</li></ol><ul><li><code>rm  -r 文件夹</code></li><li><code>rm  -rf 文件夹</code></li><li><code>del 文件夹/* </code>  + <code>rmdir 文件夹</code></li></ul><p>#####在Linux中，可以使用sed命令来显示指定文件内容的第几行。以下是一个基本的命令示例：</p><p><code>sed -n &#39;Np&#39; filename</code></p><p>其中N是你想查看的行号，filename是你的文件名。</p><p>例如，如果你想查看文件example.txt的第10行，你可以使用：</p><p><code>sed -n &#39;10p&#39; example.txt</code></p><p>这将输出example.txt文件的第10行内容。</p><p>、</p><p>####chmod 和 chown</p><p>rwx 读|写|执行</p><p>ugo 用户|组|其他</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">chmod</span> 权限 文件路径</span><br><span class="line"><span class="built_in">chmod</span> 777 test.txt</span><br><span class="line"><span class="built_in">chmod</span> u+x test.txt</span><br><span class="line"><span class="built_in">chmod</span> o+w test.txt</span><br><span class="line"><span class="built_in">chmod</span> g-w test.txt</span><br></pre></td></tr></table></figure><p><code>chown 用户:组的名字 文件路径</code></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">rw- r-- r--</span><br><span class="line">拥有者的权限u 同组用户g 其他用户(不同组)o</span><br></pre></td></tr></table></figure><p>r: read</p><p>w:write</p><p>x:执行</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>vim</title>
      <link href="/2025/01/01/vim/"/>
      <url>/2025/01/01/vim/</url>
      
        <content type="html"><![CDATA[<h1 id="VIM命令"><a href="#VIM命令" class="headerlink" title="VIM命令"></a>VIM命令</h1><p>在 Vim 中，替换指定内容的全部匹配可以通过 <code>:s</code> 命令完成。以下是具体用法和示例：</p><hr><h3 id="1-替换整个文件中的内容"><a href="#1-替换整个文件中的内容" class="headerlink" title="1. 替换整个文件中的内容"></a>1. 替换整个文件中的内容</h3><p><strong>命令：</strong></p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧内容/新内容/g</span><br></pre></td></tr></table></figure><ul><li><code>%</code>：表示作用范围是整个文件。</li><li><code>s</code>：表示替换。</li><li><code>旧内容</code>：需要被替换的内容。</li><li><code>新内容</code>：替换后的内容。</li><li><code>g</code>：表示全局替换（如果不加 <code>g</code>，每行只会替换第一个匹配）。</li></ul><p><strong>示例：</strong> 将整个文件中所有的 <code>foo</code> 替换为 <code>bar</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/foo/bar/g</span><br></pre></td></tr></table></figure><hr><h3 id="2-替换某一行的内容"><a href="#2-替换某一行的内容" class="headerlink" title="2. 替换某一行的内容"></a>2. 替换某一行的内容</h3><p><strong>命令：</strong></p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:s/旧内容/新内容/g</span><br></pre></td></tr></table></figure><ul><li>作用范围是当前行。</li></ul><p><strong>示例：</strong> 将当前行的 <code>apple</code> 替换为 <code>orange</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:s/apple/orange/g</span><br></pre></td></tr></table></figure><hr><h3 id="3-替换特定范围内的内容"><a href="#3-替换特定范围内的内容" class="headerlink" title="3. 替换特定范围内的内容"></a>3. 替换特定范围内的内容</h3><p><strong>命令：</strong></p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:起始行号,结束行号s/旧内容/新内容/g</span><br></pre></td></tr></table></figure><ul><li>替换范围为指定的行号之间。</li></ul><p><strong>示例：</strong> 将第 10 到第 20 行中的 <code>cat</code> 替换为 <code>dog</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:<span class="number">10</span>,<span class="number">20</span>s/<span class="keyword">cat</span>/dog/g</span><br></pre></td></tr></table></figure><hr><h3 id="4-交互式确认替换"><a href="#4-交互式确认替换" class="headerlink" title="4. 交互式确认替换"></a>4. 交互式确认替换</h3><p>如果你希望在每次替换时确认，可以加上 <code>c</code> 参数：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧内容/新内容/gc</span><br></pre></td></tr></table></figure><p><strong>操作：</strong></p><ul><li>Vim 会逐一跳转到匹配位置，并询问是否替换。</li><li>输入：<ul><li><code>y</code>：替换当前匹配。</li><li><code>n</code>：跳过当前匹配。</li><li><code>a</code>：替换所有匹配。</li><li><code>q</code>：退出替换操作。</li><li><code>l</code>：仅替换当前匹配并退出。</li></ul></li></ul><hr><h3 id="5-忽略大小写替换"><a href="#5-忽略大小写替换" class="headerlink" title="5. 忽略大小写替换"></a>5. 忽略大小写替换</h3><p>如果需要忽略大小写，可以加 <code>\c</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧内容/新内容/g\<span class="keyword">c</span></span><br></pre></td></tr></table></figure><p><strong>示例：</strong> 将所有大小写形式的 <code>Hello</code> 替换为 <code>hi</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/Hello/<span class="keyword">hi</span>/g\<span class="keyword">c</span></span><br></pre></td></tr></table></figure><hr><h3 id="6-替换包含特殊字符的内容"><a href="#6-替换包含特殊字符的内容" class="headerlink" title="6. 替换包含特殊字符的内容"></a>6. 替换包含特殊字符的内容</h3><p>如果要替换的内容包含 <code>/</code> 或其他特殊字符，可以用 <code>\</code> 转义。</p><p><strong>示例：</strong> 将 <code>/path/to/old</code> 替换为 <code>/path/to/new</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/\/path\/<span class="keyword">to</span>\/old/\/path\/<span class="keyword">to</span>\/<span class="keyword">new</span>/g</span><br></pre></td></tr></table></figure><p>或者，使用 <code>#</code> 代替 <code>/</code>：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s#/path/<span class="keyword">to</span>/old#/path/<span class="keyword">to</span>/new#g</span><br></pre></td></tr></table></figure><hr><h3 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h3><ol><li><p>整个文件替换：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧内容/新内容/g</span><br></pre></td></tr></table></figure></li><li><p>范围替换：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:起始行号,结束行号s/旧内容/新内容/g</span><br></pre></td></tr></table></figure></li><li><p>交互确认替换：</p><figure class="highlight vim"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">:%s/旧内容/新内容/gc</span><br></pre></td></tr></table></figure></li></ol><p>如果有其他需求，欢迎告诉我！</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>日志框架</title>
      <link href="/2024/12/30/%E6%97%A5%E5%BF%97%E6%A1%86%E6%9E%B6/"/>
      <url>/2024/12/30/%E6%97%A5%E5%BF%97%E6%A1%86%E6%9E%B6/</url>
      
        <content type="html"><![CDATA[<h1 id="日志框架"><a href="#日志框架" class="headerlink" title="日志框架"></a>日志框架</h1><p>使用日志框架可以帮助开发者更好地记录程序运行过程中的重要信息、调试问题并监控应用行为。以下是一个关于如何使用主流日志框架（例如 Log4j 2 或 SLF4J）的完整指南：</p><hr><h2 id="1-选择日志框架"><a href="#1-选择日志框架" class="headerlink" title="1. 选择日志框架"></a><strong>1. 选择日志框架</strong></h2><p>常用的 Java 日志框架包括：</p><ul><li><strong>Log4j 2</strong>：功能强大，支持异步日志和 JSON 配置。</li><li><strong>SLF4J + 日志实现</strong>：作为日志门面，可以选择具体实现（如 Logback 或 Log4j 2）。</li><li><strong>Java 内置日志（java.util.logging）</strong>：简单但功能有限。</li></ul><p>推荐使用 <strong>SLF4J + Log4j 2</strong> 或 <strong>SLF4J + Logback</strong>，因为它们性能优越且易于集成。</p><hr><h2 id="2-添加依赖"><a href="#2-添加依赖" class="headerlink" title="2. 添加依赖"></a><strong>2. 添加依赖</strong></h2><h3 id="使用-Maven"><a href="#使用-Maven" class="headerlink" title="使用 Maven"></a><strong>使用 Maven</strong></h3><p>以 SLF4J + Log4j 2 为例：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">dependencies</span>&gt;</span></span><br><span class="line">    <span class="comment">&lt;!-- SLF4J API --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.slf4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>slf4j-api<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.0.9<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">&lt;!-- Log4j 2 实现 --&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-slf4j-impl<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.20.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">dependency</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">groupId</span>&gt;</span>org.apache.logging.log4j<span class="tag">&lt;/<span class="name">groupId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">artifactId</span>&gt;</span>log4j-core<span class="tag">&lt;/<span class="name">artifactId</span>&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">version</span>&gt;</span>2.20.0<span class="tag">&lt;/<span class="name">version</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">dependency</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">dependencies</span>&gt;</span></span><br></pre></td></tr></table></figure><h3 id="使用-Gradle"><a href="#使用-Gradle" class="headerlink" title="使用 Gradle"></a><strong>使用 Gradle</strong></h3><figure class="highlight gradle"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">implementation <span class="string">&#x27;org.slf4j:slf4j-api:2.0.9&#x27;</span></span><br><span class="line">implementation <span class="string">&#x27;org.apache.logging.log4j:log4j-slf4j-impl:2.20.0&#x27;</span></span><br><span class="line">implementation <span class="string">&#x27;org.apache.logging.log4j:log4j-core:2.20.0&#x27;</span></span><br></pre></td></tr></table></figure><hr><h2 id="3-配置日志框架"><a href="#3-配置日志框架" class="headerlink" title="3. 配置日志框架"></a><strong>3. 配置日志框架</strong></h2><h3 id="Log4j-2-配置文件"><a href="#Log4j-2-配置文件" class="headerlink" title="Log4j 2 配置文件"></a><strong>Log4j 2 配置文件</strong></h3><p>在项目的 <code>src/main/resources</code> 中添加配置文件：</p><h4 id="log4j2-xml-示例"><a href="#log4j2-xml-示例" class="headerlink" title="log4j2.xml 示例"></a><code>log4j2.xml</code> 示例</h4><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&lt;?xml version=<span class="string">&quot;1.0&quot;</span> encoding=<span class="string">&quot;UTF-8&quot;</span>?&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">Configuration</span> <span class="attr">status</span>=<span class="string">&quot;WARN&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Appenders</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 控制台输出 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">Console</span> <span class="attr">name</span>=<span class="string">&quot;Console&quot;</span> <span class="attr">target</span>=<span class="string">&quot;SYSTEM_OUT&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">PatternLayout</span> <span class="attr">pattern</span>=<span class="string">&quot;%d&#123;yyyy-MM-dd HH:mm:ss&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">Console</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">Appenders</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">Loggers</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 根日志器 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">Root</span> <span class="attr">level</span>=<span class="string">&quot;info&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">AppenderRef</span> <span class="attr">ref</span>=<span class="string">&quot;Console&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">Root</span>&gt;</span></span><br><span class="line">        <span class="comment">&lt;!-- 指定包日志器 --&gt;</span></span><br><span class="line">        <span class="tag">&lt;<span class="name">Logger</span> <span class="attr">name</span>=<span class="string">&quot;com.example&quot;</span> <span class="attr">level</span>=<span class="string">&quot;debug&quot;</span> <span class="attr">additivity</span>=<span class="string">&quot;false&quot;</span>&gt;</span></span><br><span class="line">            <span class="tag">&lt;<span class="name">AppenderRef</span> <span class="attr">ref</span>=<span class="string">&quot;Console&quot;</span>/&gt;</span></span><br><span class="line">        <span class="tag">&lt;/<span class="name">Logger</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;/<span class="name">Loggers</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">Configuration</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><h2 id="4-编写代码"><a href="#4-编写代码" class="headerlink" title="4. 编写代码"></a><strong>4. 编写代码</strong></h2><p>在代码中使用 SLF4J API 记录日志。</p><h3 id="导入和创建日志对象"><a href="#导入和创建日志对象" class="headerlink" title="导入和创建日志对象"></a><strong>导入和创建日志对象</strong></h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> org.slf4j.Logger;</span><br><span class="line"><span class="keyword">import</span> org.slf4j.LoggerFactory;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoggingExample</span> &#123;</span><br><span class="line">    <span class="keyword">private</span> <span class="keyword">static</span> <span class="keyword">final</span> <span class="type">Logger</span> <span class="variable">logger</span> <span class="operator">=</span> LoggerFactory.getLogger(LoggingExample.class);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line">        logger.debug(<span class="string">&quot;这是调试信息&quot;</span>);</span><br><span class="line">        logger.info(<span class="string">&quot;这是一般信息&quot;</span>);</span><br><span class="line">        logger.warn(<span class="string">&quot;这是警告信息&quot;</span>);</span><br><span class="line">        logger.error(<span class="string">&quot;这是错误信息&quot;</span>);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 带参数的日志</span></span><br><span class="line">        <span class="type">String</span> <span class="variable">user</span> <span class="operator">=</span> <span class="string">&quot;Alice&quot;</span>;</span><br><span class="line">        logger.info(<span class="string">&quot;用户 &#123;&#125; 登录成功&quot;</span>, user);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h2 id="5-调整日志级别"><a href="#5-调整日志级别" class="headerlink" title="5. 调整日志级别"></a><strong>5. 调整日志级别</strong></h2><p>日志框架通常支持以下日志级别：</p><ul><li><code>TRACE</code>：最详细的日志信息，用于诊断。</li><li><code>DEBUG</code>：调试信息，开发时使用。</li><li><code>INFO</code>：运行时的重要事件。</li><li><code>WARN</code>：潜在问题的警告。</li><li><code>ERROR</code>：错误事件，通常需要调查。</li></ul><p>通过修改配置文件，可以动态调整日志输出的级别。例如，将 <code>Root level</code> 改为 <code>DEBUG</code>。</p><hr><h2 id="6-运行与调试"><a href="#6-运行与调试" class="headerlink" title="6. 运行与调试"></a><strong>6. 运行与调试</strong></h2><p>确保配置文件被正确加载，运行程序后，你应该可以在控制台或指定文件中看到日志输出。</p><hr><h2 id="7-日志文件输出（可选）"><a href="#7-日志文件输出（可选）" class="headerlink" title="7. 日志文件输出（可选）"></a><strong>7. 日志文件输出（可选）</strong></h2><p>如果希望将日志保存到文件，可以在配置中添加文件 Appender：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">File</span> <span class="attr">name</span>=<span class="string">&quot;File&quot;</span> <span class="attr">fileName</span>=<span class="string">&quot;logs/app.log&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">PatternLayout</span> <span class="attr">pattern</span>=<span class="string">&quot;%d&#123;yyyy-MM-dd HH:mm:ss&#125; [%t] %-5level %logger&#123;36&#125; - %msg%n&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">File</span>&gt;</span></span><br></pre></td></tr></table></figure><p>在 <code>&lt;Root&gt;</code> 或 <code>&lt;Logger&gt;</code> 中引用：</p><figure class="highlight xml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">Root</span> <span class="attr">level</span>=<span class="string">&quot;info&quot;</span>&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">AppenderRef</span> <span class="attr">ref</span>=<span class="string">&quot;Console&quot;</span>/&gt;</span></span><br><span class="line">    <span class="tag">&lt;<span class="name">AppenderRef</span> <span class="attr">ref</span>=<span class="string">&quot;File&quot;</span>/&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">Root</span>&gt;</span></span><br></pre></td></tr></table></figure><hr><p>通过以上步骤，你就可以在项目中高效使用日志框架了！如果你在实际操作中遇到问题，随时可以寻求帮助。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>harmoyOS</title>
      <link href="/2024/12/26/harmoyOS/"/>
      <url>/2024/12/26/harmoyOS/</url>
      
        <content type="html"><![CDATA[<p>#harmoyOS</p><p>在鸿蒙系统开发中，<strong>前端和后端的交互中传递 Session 信息</strong>，通常通过以下方式在其他请求中获取和使用：</p><hr><h2 id="Session-的传递方式"><a href="#Session-的传递方式" class="headerlink" title="Session 的传递方式"></a><strong>Session 的传递方式</strong></h2><h3 id="1-使用-HTTP-Header-传递-Session-信息"><a href="#1-使用-HTTP-Header-传递-Session-信息" class="headerlink" title="1. 使用 HTTP Header 传递 Session 信息"></a><strong>1. 使用 HTTP Header 传递 Session 信息</strong></h3><p>当后端生成 <code>Session ID</code> 并返回给前端，前端需要在后续请求中通过 <code>HTTP Header</code> 发送该 Session 信息。</p><h4 id="后端示例（Java）："><a href="#后端示例（Java）：" class="headerlink" title="后端示例（Java）："></a>后端示例（Java）：</h4><p>后端通过 <code>Set-Cookie</code> 将 Session ID 返回给前端：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/api&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginController</span> &#123;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@PostMapping(&quot;/login&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">login</span><span class="params">(<span class="meta">@RequestBody</span> User user, HttpSession session)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">&quot;admin&quot;</span>.equals(user.getUsername()) &amp;&amp; <span class="string">&quot;password&quot;</span>.equals(user.getPassword())) &#123;</span><br><span class="line">            session.setAttribute(<span class="string">&quot;user&quot;</span>, user);</span><br><span class="line">            <span class="comment">// 返回 Session ID</span></span><br><span class="line">            <span class="keyword">return</span> ResponseEntity.ok()</span><br><span class="line">                    .header(<span class="string">&quot;Set-Cookie&quot;</span>, <span class="string">&quot;SESSIONID=&quot;</span> + session.getId() + <span class="string">&quot;; Path=/; HttpOnly; Secure&quot;</span>)</span><br><span class="line">                    .body(<span class="string">&quot;Login successful&quot;</span>);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(<span class="string">&quot;Invalid username or password&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/getUserInfo&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">getUserInfo</span><span class="params">(HttpSession session)</span> &#123;</span><br><span class="line">        <span class="type">User</span> <span class="variable">user</span> <span class="operator">=</span> (User) session.getAttribute(<span class="string">&quot;user&quot;</span>);</span><br><span class="line">        <span class="keyword">if</span> (user != <span class="literal">null</span>) &#123;</span><br><span class="line">            <span class="keyword">return</span> ResponseEntity.ok(<span class="string">&quot;User info: &quot;</span> + user.getUsername());</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(<span class="string">&quot;User not logged in&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><hr><h4 id="前端处理（ArkTS-示例）："><a href="#前端处理（ArkTS-示例）：" class="headerlink" title="前端处理（ArkTS 示例）："></a>前端处理（ArkTS 示例）：</h4><ol><li><p><strong>存储 Session 信息</strong><br>在用户登录时，从后端响应中提取 <code>Session ID</code>，并保存在应用的存储中（如 <code>AppStorage</code> 或 <code>LocalStorage</code>）。</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> &#123; <span class="title class_">HttpRequest</span> &#125; <span class="keyword">from</span> <span class="string">&#x27;@ohos.net.http&#x27;</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">login</span>(<span class="params"><span class="attr">username</span>: <span class="built_in">string</span>, <span class="attr">password</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title class_">HttpRequest</span>.<span class="title function_">post</span>(&#123;</span><br><span class="line">        <span class="attr">url</span>: <span class="string">&#x27;https://your-api-domain/api/login&#x27;</span>,</span><br><span class="line">        <span class="attr">data</span>: &#123; username, password &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 获取 Session ID</span></span><br><span class="line">    <span class="keyword">const</span> cookies = response.<span class="property">headers</span>[<span class="string">&#x27;Set-Cookie&#x27;</span>];</span><br><span class="line">    <span class="keyword">const</span> sessionId = cookies.<span class="title function_">match</span>(<span class="regexp">/SESSIONID=([^;]+)/</span>)[<span class="number">1</span>];</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 保存 Session ID</span></span><br><span class="line">    <span class="title class_">AppStorage</span>.<span class="title class_">SetOrCreate</span>(<span class="string">&#x27;sessionId&#x27;</span>, sessionId);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>在其他请求中使用 Session</strong><br>每次请求时，将存储的 <code>Session ID</code> 添加到请求头中。</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getUserInfo</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> sessionId = <span class="title class_">AppStorage</span>.<span class="title class_">Get</span>(<span class="string">&#x27;sessionId&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title class_">HttpRequest</span>.<span class="title function_">get</span>(&#123;</span><br><span class="line">        <span class="attr">url</span>: <span class="string">&#x27;https://your-api-domain/api/getUserInfo&#x27;</span>,</span><br><span class="line">        <span class="attr">header</span>: &#123;</span><br><span class="line">            <span class="title class_">Cookie</span>: <span class="string">`SESSIONID=<span class="subst">$&#123;sessionId&#125;</span>`</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;User Info:&#x27;</span>, response.<span class="property">data</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><hr><h3 id="2-使用-Token-替代-Session"><a href="#2-使用-Token-替代-Session" class="headerlink" title="2. 使用 Token 替代 Session"></a><strong>2. 使用 Token 替代 Session</strong></h3><p>为了简化状态管理并增强安全性，可以使用 Token（如 JWT）代替传统 Session。</p><h4 id="后端示例（Java）：-1"><a href="#后端示例（Java）：-1" class="headerlink" title="后端示例（Java）："></a>后端示例（Java）：</h4><p>使用 Token 验证代替 Session：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">@RestController</span></span><br><span class="line"><span class="meta">@RequestMapping(&quot;/api&quot;)</span></span><br><span class="line"><span class="keyword">public</span> <span class="keyword">class</span> <span class="title class_">LoginController</span> &#123;</span><br><span class="line">    <span class="meta">@PostMapping(&quot;/login&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">login</span><span class="params">(<span class="meta">@RequestBody</span> User user)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (<span class="string">&quot;admin&quot;</span>.equals(user.getUsername()) &amp;&amp; <span class="string">&quot;password&quot;</span>.equals(user.getPassword())) &#123;</span><br><span class="line">            <span class="comment">// 生成 Token</span></span><br><span class="line">            <span class="type">String</span> <span class="variable">token</span> <span class="operator">=</span> TokenUtil.generateToken(user.getUsername());</span><br><span class="line">            <span class="keyword">return</span> ResponseEntity.ok().body(token); <span class="comment">// 返回给前端</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(<span class="string">&quot;Invalid username or password&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="meta">@GetMapping(&quot;/getUserInfo&quot;)</span></span><br><span class="line">    <span class="keyword">public</span> ResponseEntity&lt;String&gt; <span class="title function_">getUserInfo</span><span class="params">(<span class="meta">@RequestHeader(&quot;Authorization&quot;)</span> String token)</span> &#123;</span><br><span class="line">        <span class="keyword">if</span> (TokenUtil.validateToken(token)) &#123;</span><br><span class="line">            <span class="type">String</span> <span class="variable">username</span> <span class="operator">=</span> TokenUtil.getUsernameFromToken(token);</span><br><span class="line">            <span class="keyword">return</span> ResponseEntity.ok(<span class="string">&quot;User info: &quot;</span> + username);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">return</span> ResponseEntity.status(HttpStatus.UNAUTHORIZED).body(<span class="string">&quot;Invalid token&quot;</span>);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="前端处理（ArkTS-示例）：-1"><a href="#前端处理（ArkTS-示例）：-1" class="headerlink" title="前端处理（ArkTS 示例）："></a>前端处理（ArkTS 示例）：</h4><ol><li><p><strong>存储 Token</strong><br>在用户登录成功后，从后端响应中获取 Token 并保存：</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">login</span>(<span class="params"><span class="attr">username</span>: <span class="built_in">string</span>, <span class="attr">password</span>: <span class="built_in">string</span></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title class_">HttpRequest</span>.<span class="title function_">post</span>(&#123;</span><br><span class="line">        <span class="attr">url</span>: <span class="string">&#x27;https://your-api-domain/api/login&#x27;</span>,</span><br><span class="line">        <span class="attr">data</span>: &#123; username, password &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> token = response.<span class="property">data</span>; <span class="comment">// 后端返回的 Token</span></span><br><span class="line">    <span class="title class_">AppStorage</span>.<span class="title class_">SetOrCreate</span>(<span class="string">&#x27;authToken&#x27;</span>, token);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p><strong>在其他请求中使用 Token</strong><br>每次请求时，将 Token 添加到 <code>Authorization</code> 头中。</p><figure class="highlight typescript"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">async</span> <span class="keyword">function</span> <span class="title function_">getUserInfo</span>(<span class="params"></span>) &#123;</span><br><span class="line">    <span class="keyword">const</span> token = <span class="title class_">AppStorage</span>.<span class="title class_">Get</span>(<span class="string">&#x27;authToken&#x27;</span>);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> response = <span class="keyword">await</span> <span class="title class_">HttpRequest</span>.<span class="title function_">get</span>(&#123;</span><br><span class="line">        <span class="attr">url</span>: <span class="string">&#x27;https://your-api-domain/api/getUserInfo&#x27;</span>,</span><br><span class="line">        <span class="attr">header</span>: &#123;</span><br><span class="line">            <span class="title class_">Authorization</span>: <span class="string">`Bearer <span class="subst">$&#123;token&#125;</span>`</span>,</span><br><span class="line">        &#125;,</span><br><span class="line">    &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="variable language_">console</span>.<span class="title function_">log</span>(<span class="string">&#x27;User Info:&#x27;</span>, response.<span class="property">data</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ol><hr><h2 id="安全性建议"><a href="#安全性建议" class="headerlink" title="安全性建议"></a><strong>安全性建议</strong></h2><ol><li><p><strong>Session 安全性</strong>：</p><ul><li>使用 <code>HttpOnly</code> 和 <code>Secure</code> 标记 Cookie，防止客户端脚本访问。</li><li>配置 HTTPS，确保数据传输加密。</li></ul></li><li><p><strong>Token 安全性</strong>：</p><ul><li>使用短期有效的 Token（如 15 分钟），并提供刷新机制。</li><li>避免将 Token 明文存储在不安全的位置（如本地存储），可以考虑加密存储。</li></ul></li><li><p><strong>防止跨站攻击</strong>：</p><ul><li>对关键操作请求启用 CSRF 防护。</li><li>在后端校验请求来源。</li></ul></li></ol><hr><p>通过以上方法，您可以在鸿蒙应用中通过 <code>Session</code> 或 <code>Token</code> 管理用户登录状态，并在后续请求中安全、灵活地使用这些状态信息。</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Markdown学习记录</title>
      <link href="/2024/12/21/Markdown%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
      <url>/2024/12/21/Markdown%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="markdown简易学习记录"><a href="#markdown简易学习记录" class="headerlink" title="markdown简易学习记录"></a>markdown简易学习记录</h1><h1 id="一级标题"><a href="#一级标题" class="headerlink" title="一级标题"></a>一级标题</h1><h2 id="二级标题"><a href="#二级标题" class="headerlink" title="二级标题"></a>二级标题</h2><p>文本<br><em>斜体文本</em> <em>斜体文本</em><br><strong>文本加粗</strong><br><em><strong>文本加粗斜体</strong></em><br><em><strong><del>混合文本</del></strong></em><br><del>中划线</del><br>分割线</p><hr><hr><hr><hr/>  <h2 id="列表"><a href="#列表" class="headerlink" title="列表"></a>列表</h2><h3 id="无序列表"><a href="#无序列表" class="headerlink" title="无序列表"></a>无序列表</h3><ul><li>第一项</li><li>第二项</li><li>第三项</li></ul><ul><li>新第一项</li><li>新第二项</li><li>新第三项</li></ul><h3 id="有序列表"><a href="#有序列表" class="headerlink" title="有序列表"></a>有序列表</h3><ol><li>第一项  </li><li>第二项</li><li>第三项</li></ol><h3 id="嵌套列表"><a href="#嵌套列表" class="headerlink" title="嵌套列表"></a>嵌套列表</h3><ol><li>第一项<ul><li>无序列表</li><li>2</li><li>3<ol><li>有序列表</li><li>2</li><li>3</li></ol></li></ul></li><li>第二项</li><li>第三项</li></ol><h3 id="勾选框"><a href="#勾选框" class="headerlink" title="勾选框"></a>勾选框</h3><ul><li><input checked="" disabled="" type="checkbox"> 第一项</li><li><input disabled="" type="checkbox"> 第二项</li><li><input disabled="" type="checkbox"> 第三项</li></ul><ol><li><input checked="" disabled="" type="checkbox"> 第一项</li><li>[] 第二项</li><li>[] 第三项</li></ol><h3 id="代码块"><a href="#代码块" class="headerlink" title="代码块"></a>代码块</h3><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"><span class="type">int</span> a, b;</span><br><span class="line"><span class="type">Scanner</span> <span class="variable">sc</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Scanner</span>(System.in);</span><br><span class="line">a = sc.nextInt();</span><br><span class="line">b = sc.nextInt();<span class="comment">//注释</span></span><br><span class="line">desparate(a, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="引用"><a href="#引用" class="headerlink" title="引用"></a>引用</h3><blockquote><p>SpringBoot 是框架的框架，在前面之上又封装了一层，<br>作用就是进一步简化开发。提出了约定大于配置，前面 SSM 中你得写大量的配置文件，SpringBoot 直接给你一套默认配置，并且你maven 导哪个包，对应的配置会自动生效，你只需要写你需要改的一小部分配置，就会自动替换掉默认配置。<br>此外还增加了指标监控 等功能。</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">public</span> <span class="keyword">static</span> <span class="keyword">void</span> <span class="title function_">main</span><span class="params">(String[] args)</span> &#123;</span><br><span class="line"><span class="type">int</span> a, b;</span><br><span class="line"><span class="type">Scanner</span> <span class="variable">sc</span> <span class="operator">=</span> <span class="keyword">new</span> <span class="title class_">Scanner</span>(System.in);</span><br><span class="line">a = sc.nextInt();</span><br><span class="line">b = sc.nextInt();<span class="comment">//注释</span></span><br><span class="line">desparate(a, b);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><ul><li><p>列表<br>1.有序列表<br>2.2  </p><p>3.3 </p></li><li><p>2</p></li><li><p>3</p></li></ul></blockquote><h3 id="链接和标注"><a href="#链接和标注" class="headerlink" title="链接和标注"></a>链接和标注</h3><p>前往网站<a href="https://www.baidu.com/">百度</a><br>多个设置链接<a href="https://www.baidu.com/">链接1</a>和<a href="https://www.baidu.com/">链接2</a></p><h3 id="脚注"><a href="#脚注" class="headerlink" title="脚注"></a>脚注</h3><p>idea这里不支持这种写法，移步到Typora<a href="%E8%BF%99%E6%98%AF%E4%B8%80%E4%B8%AA%E8%84%9A%E6%B3%A8">^1</a></p><h3 id="图片插入"><a href="#图片插入" class="headerlink" title="图片插入"></a>图片插入</h3><p>方式一   </p><p><img src="D:\program\Typora\md上传图片\image-20241013232500775.png" alt="image-20241013232500775"></p><p>方式二<br><img src="https://i0.hdslb.com/bfs/archive/aeae39c68cf66d41a1a06ae4d23c68d98ffe4611.jpg" alt="图片">  </p><p>方式三<br><img src="https://img0.baidu.com/it/u=2119299385,1997312824&fm=253&fmt=auto&app=138&f=JPEG?w=800&h=800" alt="网络图片" title="草神"></p><h3 id="表格"><a href="#表格" class="headerlink" title="表格"></a>表格</h3><table><thead><tr><th align="center">姓名</th><th>年龄</th><th>性别</th></tr></thead><tbody><tr><td align="center">小明</td><td>12</td><td>男</td></tr><tr><td align="center">小红好好吃</td><td>21</td><td>女</td></tr></tbody></table><h3 id="嵌入html语言"><a href="#嵌入html语言" class="headerlink" title="嵌入html语言"></a>嵌入html语言</h3><p><span style="color:red" >sapn标签</span>  设置字体样式</p><p>Typora甚至支持ifame标签来嵌入网页</p><iframe src="//player.bilibili.com/player.html?isOutside=true&aid=1252624739&bvid=BV1eJ4m157kC&cid=1489694376&p=1" scrolling="no" border="0" frameborder="no" framespacing="0" allowfullscreen="true" width="600" height="500"></iframe><h2 id="扩展功能"><a href="#扩展功能" class="headerlink" title="扩展功能"></a>扩展功能</h2><p>&#x3D;&#x3D;我是高亮语法&#x3D;&#x3D;</p><p>下标 H<del>2</del>O</p><p>上标 X^2^</p><p>标签 y<sub>2</sub>  x<sup>2</sup></p><h3 id="数学公式"><a href="#数学公式" class="headerlink" title="数学公式"></a>数学公式</h3><p><strong>单行公式</strong></p><p>$x_1 &#x3D; (y+1)$</p><p>多行公式<br>$$<br>\frac{1}{2} + \sqrt[3]{{(4+x_1^2)}}^2<br>$$<br><strong>数学符号</strong></p> <img src="C:\Users\朱小龙\AppData\Roaming\Typora\typora-user-images\image-20241013165448928.png" alt="image-20241013165448928" style="zoom: 50%;" /><p><strong>常用模板</strong><br>$$<br>180^\circ 度数\<br>\sin\pi 三角函数\<br>\infty 无穷\<br>\int  \iint  \iiint 积分\<br>y\prime 导数\<br>\lim 极限\<br>$$</p><p>$$<br>\int_0^2x^2dx 定积分\<br>\lim_{n\rightarrow+\infty}\frac{1}{2} 极限\<br>f(x) &#x3D; \frac{1}{x_1} + \frac{1}{x_2} + \cdots+ \frac{1}{x_n} 累加\<br>$$</p><hr/>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Spring学习记录</title>
      <link href="/2024/11/15/Spring%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
      <url>/2024/11/15/Spring%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="Spring学习记录"><a href="#Spring学习记录" class="headerlink" title="Spring学习记录"></a>Spring学习记录</h1><p><img src="/img_posts/image-20241114223033795.png" alt="image-20241114223033795"></p><p> <img src="/img_posts/image-20241114224415821.png" alt="image-20241114224415821"></p><p><img src="/img_posts/image-20241114225543426.png" alt="image-20241114225543426"></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>python考点</title>
      <link href="/2024/11/06/python%E8%80%83%E7%82%B9/"/>
      <url>/2024/11/06/python%E8%80%83%E7%82%B9/</url>
      
        <content type="html"><![CDATA[<h1 id="python考点"><a href="#python考点" class="headerlink" title="python考点"></a>python考点</h1><p>不可变数据类型：数值（Number）,字符（string）,元组（Tuple）</p><p>可变数据类型：列表（List）,集合（Set）,字典（Directory）</p><p><strong>这里的 可变与不变指代的是值的变化</strong></p><p><strong>字符不可根据下标赋值，而元组也不可 修改其值，字符可以看成是一种特殊的元组</strong></p><p><strong>与 C 字符串不同的是，Python 字符串不能被改变。向一个索引位置赋值，比如 word[0] &#x3D; ‘m’ 会导致错误。</strong></p><p>元组表示方式：（ ）；<strong>元组（tuple）与列表类似，不同之处在于元组的元素不能修改</strong></p><p>列表表示方式：[ ]；<strong>相当于C语言的数组，元素可以重复，元素能够修改</strong></p><p>集合：{ } 或set()函数 &#x3D;&#x3D;》<strong>集合中的元素不能重复，且只能存放一种类型的元素</strong></p><p>字典：{ } 里面存放的是键值对（双列集合）<strong>键(key)必须使用不可变类型,集合中的键值（key）唯一，value能够重复</strong></p><p>​</p><p>注意事项：！！！！</p><p>**如果你想创建只有一个元素的元组，需要注意在元素后面添加一个逗号，以区分它是一个元组而不是一个普通的值，这是因为在没有逗号的情况下，Python会将括号解释为数学运算中的括号，而不是元组的表示。如果不添加逗号，如下所示，它将被解释为一个普通的值而不是元组 **</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">tup1 = ()    <span class="comment"># 空元组</span></span><br><span class="line">tup2 = (<span class="number">20</span>,) <span class="comment"># 一个元素，需要在元素后添加逗号</span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>步长，前开后闭（从后往前数一样）</strong></p><p>rang(11) 生成0到10的整数</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">#!/usr/bin/python3</span></span><br><span class="line"><span class="comment">#  python 列表</span></span><br><span class="line"><span class="built_in">str</span> = <span class="string">&#x27;Runoob&#x27;</span>  <span class="comment"># 定义一个字符串变量</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>)           <span class="comment"># 打印整个字符串</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>[<span class="number">0</span>:-<span class="number">1</span>])     <span class="comment"># 打印字符串第一个到倒数第二个字符（不包含倒数第一个字符）</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>[<span class="number">0</span>])        <span class="comment"># 打印字符串的第一个字符</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>[<span class="number">2</span>:<span class="number">5</span>])      <span class="comment"># 打印字符串第三到第五个字符（包含第五个字符）</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span>[<span class="number">2</span>:])       <span class="comment"># 打印字符串从第三个字符开始到末尾</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span> * <span class="number">2</span>)       <span class="comment"># 打印字符串两次</span></span><br><span class="line"><span class="built_in">print</span>(<span class="built_in">str</span> + <span class="string">&quot;TEST&quot;</span>)  <span class="comment"># 打印字符串和&quot;TEST&quot;拼接在一起</span></span><br></pre></td></tr></table></figure><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span>a = [<span class="number">1</span>, <span class="number">2</span>, <span class="number">3</span>, <span class="number">4</span>, <span class="number">5</span>, <span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">0</span>] = <span class="number">9</span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">2</span>:<span class="number">5</span>] = [<span class="number">13</span>, <span class="number">14</span>, <span class="number">15</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">9</span>, <span class="number">2</span>, <span class="number">13</span>, <span class="number">14</span>, <span class="number">15</span>, <span class="number">6</span>]</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a[<span class="number">2</span>:<span class="number">5</span>] = []   <span class="comment"># 将对应的元素值设置为 [] </span></span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>a</span><br><span class="line">[<span class="number">9</span>, <span class="number">2</span>, <span class="number">6</span>]</span><br></pre></td></tr></table></figure><p>在 Python 中，集合使用大括号 {} 表示，元素之间用逗号 , 分隔。</p><p>另外，也可以使用 set() 函数创建集合。</p><p><strong>注意：创建一个空集合必须用 set() 而不是 { }，因为 { } 是用来创建一个空字典。</strong></p><p>构造函数 dict() 可以直接从键值对序列中构建字典如下：</p><figure class="highlight py"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="built_in">dict</span>([(<span class="string">&#x27;Runoob&#x27;</span>, <span class="number">1</span>), (<span class="string">&#x27;Google&#x27;</span>, <span class="number">2</span>), (<span class="string">&#x27;Taobao&#x27;</span>, <span class="number">3</span>)])</span><br><span class="line">&#123;<span class="string">&#x27;Runoob&#x27;</span>: <span class="number">1</span>, <span class="string">&#x27;Google&#x27;</span>: <span class="number">2</span>, <span class="string">&#x27;Taobao&#x27;</span>: <span class="number">3</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span>&#123;x: x**<span class="number">2</span> <span class="keyword">for</span> x <span class="keyword">in</span> (<span class="number">2</span>, <span class="number">4</span>, <span class="number">6</span>)&#125; <span class="comment"># python推导式</span></span><br><span class="line">&#123;<span class="number">2</span>: <span class="number">4</span>, <span class="number">4</span>: <span class="number">16</span>, <span class="number">6</span>: <span class="number">36</span>&#125;</span><br><span class="line"><span class="meta">&gt;&gt;&gt; </span><span class="built_in">dict</span>(Runoob=<span class="number">1</span>, Google=<span class="number">2</span>, Taobao=<span class="number">3</span>)</span><br><span class="line">&#123;<span class="string">&#x27;Runoob&#x27;</span>: <span class="number">1</span>, <span class="string">&#x27;Google&#x27;</span>: <span class="number">2</span>, <span class="string">&#x27;Taobao&#x27;</span>: <span class="number">3</span>&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>软考数据库章节</title>
      <link href="/2024/10/30/%E8%BD%AF%E8%80%83%E6%95%B0%E6%8D%AE%E5%BA%93%E7%AB%A0%E8%8A%82/"/>
      <url>/2024/10/30/%E8%BD%AF%E8%80%83%E6%95%B0%E6%8D%AE%E5%BA%93%E7%AB%A0%E8%8A%82/</url>
      
        <content type="html"><![CDATA[<h1 id="软考数据库章节"><a href="#软考数据库章节" class="headerlink" title="软考数据库章节"></a>软考数据库章节</h1><p><img src="/img_posts/image-20241029235824597.png" alt="image-20241029235824597"></p><p><img src="/img_posts/image-20241030000153214.png" alt="image-20241030000153214"></p><p><strong>三级模式两级映像</strong></p><p><img src="/img_posts/image-20241030000427823.png" alt="image-20241030000427823"></p><p><img src="/img_posts/image-20241030002417393.png" alt="image-20241030002417393"></p><p><img src="/img_posts/image-20241030002635844.png" alt="image-20241030002635844"></p><p><strong>数据模型</strong></p><p><img src="/img_posts/image-20241030002739175.png" alt="image-20241030002739175"></p><p><img src="/img_posts/image-20241030003025016.png" alt="image-20241030003025016"></p><h2 id="关系代数"><a href="#关系代数" class="headerlink" title="关系代数"></a>关系代数</h2><p><img src="/img_posts/image-20241030003820898.png" alt="image-20241030003820898"></p><p><img src="/img_posts/image-20241030003957752.png" alt="image-20241030003957752"></p><p><img src="/img_posts/image-20241030004251657.png" alt="image-20241030004251657"></p><p><img src="/img_posts/image-20241030004321350.png" alt="image-20241030004321350"></p><p><img src="/img_posts/image-20241030005548778.png" alt="image-20241030005548778"></p><p><strong>关系优化</strong></p><p><img src="/img_posts/image-20241030005732150.png" alt="image-20241030005732150"></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>2024下半年软考软件设计师</title>
      <link href="/2024/10/29/2024%E4%B8%8B%E5%8D%8A%E5%B9%B4%E8%BD%AF%E8%80%83%E8%BD%AF%E4%BB%B6%E8%AE%BE%E8%AE%A1%E5%B8%88/"/>
      <url>/2024/10/29/2024%E4%B8%8B%E5%8D%8A%E5%B9%B4%E8%BD%AF%E8%80%83%E8%BD%AF%E4%BB%B6%E8%AE%BE%E8%AE%A1%E5%B8%88/</url>
      
        <content type="html"><![CDATA[<h1 id="2024下半年软考软件设计师"><a href="#2024下半年软考软件设计师" class="headerlink" title="2024下半年软考软件设计师"></a>2024下半年软考软件设计师</h1><p> ##考流程与考点</p><p><strong>考试流程</strong></p><p><img src="/img_posts/%E8%80%83%E8%AF%95%E6%B5%81%E7%A8%8B.png" alt="考试流程"></p><p><strong>考点</strong></p><p><img src="/img_posts/QQ20241016-232051.png" alt="QQ20241016-232051"></p><p><img src="/img_posts/%E4%B8%8B%E5%8D%88%E8%80%83%E7%82%B9.png" alt="下午考点"></p><h2 id="第一章"><a href="#第一章" class="headerlink" title="第一章"></a>第一章</h2><p>###数值及其转换和数据的表示</p><p><strong>定点数</strong></p><img src="/img_posts/QQ20241017-001145.png" alt="QQ20241017-001145" style="zoom:50%;" /><p><strong>原反补移</strong></p><p><img src="/img_posts/%E5%8E%9F%E5%8F%8D%E8%A1%A5.png" alt="原反补"></p><p><strong>浮点数</strong></p><img src="/img_posts/image-20241022214419099.png" alt="image-20241022214419099" style="zoom:50%;" /><p><strong>海明码</strong></p><p><img src="/img_posts/%E6%B5%B7%E6%98%8E%E7%A0%81.png" alt="海明码"></p><p><strong>奇偶检验码和循环码</strong></p><img src="/img_posts/image-20241022214532915.png" alt="image-20241022214532915" style="zoom:50%;" /><h3 id="指令系统（寻址）"><a href="#指令系统（寻址）" class="headerlink" title="指令系统（寻址）"></a>指令系统（寻址）</h3><p><img src="/img_posts/image-20241022201225517.png" alt="image-20241022201225517"></p><p><img src="/img_posts/image-20241022201425167.png" alt="image-20241022201425167"></p><img src="/img_posts/image-20241022201642836.png" alt="image-20241022201642836" style="zoom: 80%;" /><p><strong>流水线</strong></p><p><img src="/img_posts/image-20241022202158939.png" alt="image-20241022202158939"></p><p><img src="/img_posts/image-20241022202310977.png" alt="image-20241022202310977"></p><p><img src="/img_posts/image-20241022202447089.png" alt="image-20241022202447089"></p><h3 id="输入输出技术"><a href="#输入输出技术" class="headerlink" title="输入输出技术"></a>输入输出技术</h3><p><img src="/img_posts/image-20241022202655501.png" alt="image-20241022202655501"></p><p><img src="/img_posts/image-20241022202808704.png" alt="image-20241022202808704"></p><h3 id="存储系统"><a href="#存储系统" class="headerlink" title="存储系统"></a>存储系统</h3><p><img src="/img_posts/image-20241022203131377.png" alt="image-20241022203131377"></p><p><img src="/img_posts/image-20241022203338650.png" alt="image-20241022203338650"></p><p><img src="/img_posts/image-20241022203635256.png" alt="image-20241022203635256"></p><p><img src="/img_posts/image-20241022203836163.png" alt="image-20241022203836163"></p><p><img src="/img_posts/image-20241022204106248.png" alt="image-20241022204106248"></p><p>###cache###</p><p><img src="/img_posts/image-20241022204321186.png" alt="image-20241022204321186"></p><p><img src="/img_posts/image-20241022204420889.png" alt="image-20241022204420889"></p><p><img src="/img_posts/image-20241022204651408.png" alt="image-20241022204651408"></p><p><img src="/img_posts/image-20241022204858135.png" alt="image-20241022204858135"></p><p><img src="/img_posts/image-20241022205054396.png" alt="image-20241022205054396"></p><p><img src="/img_posts/image-20241022205316547.png" alt="image-20241022205316547"></p><p><img src="/img_posts/image-20241022205645858.png" alt="image-20241022205645858"></p><p><img src="/img_posts/image-20241022205935568.png" alt="image-20241022205935568"></p><p><strong>主存的扩展</strong></p><p><img src="/img_posts/image-20241022210140192.png" alt="image-20241022210140192"></p><img src="/img_posts/image-20241022211143831.png" alt="image-20241022211143831" style="zoom: 33%;" /><p><strong>存储器</strong></p><p><img src="/img_posts/image-20241022211305943.png" alt="image-20241022211305943"></p><p><img src="/img_posts/image-20241022211747837.png" alt="image-20241022211747837"></p><p><strong>总线系统</strong></p><p><img src="/img_posts/image-20241022212134156.png" alt="image-20241022212134156"></p><p><strong>磁盘阵列技术</strong></p><p><img src="/img_posts/image-20241022212413708.png" alt="image-20241022212413708"></p><p><strong>计算机可靠度</strong></p><p><img src="/img_posts/image-20241022212746047.png" alt="image-20241022212746047"></p><h2 id="第二章基础概念和算法"><a href="#第二章基础概念和算法" class="headerlink" title="第二章基础概念和算法"></a>第二章基础概念和算法</h2><p><img src="/img_posts/image-20241023205228635.png" alt="image-20241023205228635"></p><p><img src="/img_posts/image-20241023205300607.png" alt="image-20241023205300607"></p><p><strong>数据结构</strong></p><p><img src="/img_posts/image-20241023205440501.png" alt="image-20241023205440501"></p><p><strong>逻辑结构</strong></p><p><img src="/img_posts/image-20241023205715921.png" alt="image-20241023205715921"></p><p><strong>物理结构（存储结构）</strong></p><p><img src="/img_posts/image-20241023205759767.png" alt="image-20241023205759767"></p><p><strong>算法</strong></p><p><img src="/img_posts/image-20241023210230416.png" alt="image-20241023210230416"></p><p><img src="/img_posts/image-20241023210411694.png" alt="image-20241023210411694"></p><p><em>空间复杂度</em></p><p><img src="/img_posts/image-20241023210954043.png" alt="image-20241023210954043"><img src="/img_posts/image-20241023211142229.png" alt="image-20241023210956171"></p><p><img src="/img_posts/image-20241023211253037.png" alt="image-20241023211253037"></p><p><img src="/img_posts/image-20241023211439371.png" alt="image-20241023211439371"></p><p><img src="/img_posts/image-20241023211544359.png" alt="image-20241023211544359"></p><p><img src="/img_posts/image-20241023211953718.png" alt="image-20241023211953718"></p><p><strong>栈</strong></p><p><img src="/img_posts/image-20241023212110788.png" alt="image-20241023212110788"></p><p><strong>队列</strong></p><p><img src="/img_posts/image-20241023212231527.png" alt="image-20241023212231527"></p><p><img src="/img_posts/image-20241023212906806.png" alt="image-20241023212906806"></p><p><strong>串</strong></p><p><img src="/img_posts/image-20241023213108286.png" alt="image-20241023213108286">Ascii码值：a值为97，A为65</p><p><img src="/img_posts/image-20241023213411246.png" alt="image-20241023213411246"></p><p><strong>数组</strong></p><p><img src="/img_posts/image-20241023213604608.png" alt="image-20241023213604608"></p><h3 id="稀疏矩阵"><a href="#稀疏矩阵" class="headerlink" title="稀疏矩阵"></a>稀疏矩阵</h3><p><img src="/img_posts/image-20241023213924952.png" alt="image-20241023213924952"></p><p>###广义表</p><p><img src="/img_posts/image-20241023214359025.png" alt="image-20241023214359025"></p><p><strong><span style="color:red;">!!!!!!注意这里：表尾代表的是去除表头后剩下的所有表元素</span></strong></p><p>###树、二叉树、图</p><p><strong>树</strong></p><p><img src="/img_posts/image-20241023215603803.png" alt="image-20241023215603803"></p><ul><li><p>结点的度：该节点子结点的个数为它的度</p></li><li><p>树的度：整个树中结点的度最大值</p></li><li><p>内部结点：除去根结点和叶子结点的所有结点</p></li><li><p>添加一条定义：树的高度，就是最长的路径中边的个数</p></li></ul><p><strong>二叉树</strong></p><p><img src="/img_posts/image-20241023220044548.png" alt="image-20241023220044548"></p><p><strong><span style="color:red;">!!!!!!注意这里：完全二叉树要求从上到下，从左到右排列</span></strong></p><p><strong>二叉树的特性，牢记</strong></p><p><img src="/img_posts/image-20241023221205164.png" alt="image-20241023221205164"></p><ul><li>对于结论3，由n<del>0</del>+n<del>1</del>+n<del>2</del>&#x3D;n<del>1</del>+2*n<del>2</del>+1 可以推出</li><li>对于结论4，可以由结论2推出</li><li>对于结论四最后三点，算法中经常用到</li></ul><p><img src="/img_posts/image-20241023223106714.png" alt="image-20241023223106714"></p><p><img src="/img_posts/image-20241023223132611.png" alt="image-20241023223132611"></p><p><strong>树转二叉树</strong></p><p><img src="/img_posts/image-20241023223552175.png" alt="image-20241023223552175"></p><p><strong>二叉排序树</strong></p><p><img src="/img_posts/image-20241023223824028.png" alt="image-20241023223824028"></p><p><strong>哈夫曼树</strong></p><p><img src="/img_posts/image-20241023224136715.png" alt="image-20241023224136715"></p><P>这里其实就是给定一个集合，每次从集合中去掉最小的两个数，加入一个这两个数的和，直到集合中只剩一个数</p><p><strong>线索二叉树</strong></p><p><img src="/img_posts/image-20241023225042907.png" alt="image-20241023225042907"></p><p><strong>平衡二叉树</strong></p><p><img src="/img_posts/image-20241023225245610.png" alt="image-20241023225245610"></p><p><strong>图</strong></p><p><img src="/img_posts/image-20241023225421301.png" alt="image-20241023225421301"></p><p><img src="/img_posts/image-20241023231511590.png" alt="image-20241023231511590"></p><p><img src="/img_posts/image-20241023231649728.png" alt="image-20241023231649728"></p><p><img src="/img_posts/image-20241023231848871.png" alt="image-20241023231848871"></p><p><img src="/img_posts/image-20241023232044092.png" alt="image-20241023232044092"></p><p><strong><span style="color:red;">最小生成树算法</span></strong> </p><p><img src="/img_posts/image-20241023232654126.png" alt="image-20241023232654126"></p><p><img src="/img_posts/image-20241023232802707.png" alt="image-20241023232802707"></p><h3 id="查找和排序"><a href="#查找和排序" class="headerlink" title="查找和排序"></a>查找和排序</h3><p><strong>查找</strong></p><p><img src="/img_posts/image-20241024001532526.png" alt="image-20241024001532526"></p><p><img src="/img_posts/image-20241024001622854.png" alt="image-20241024001622854"></p><p><img src="/img_posts/image-20241024001725138.png" alt="image-20241024001725138"></p><p><img src="/img_posts/image-20241024002047709.png" alt="image-20241024002047709"></p><p><strong>折半查找时间复杂度为O（log<del>2</del>n）</strong></p><p><img src="/img_posts/image-20241024002232482.png" alt="image-20241024002232482"></p><p><img src="/img_posts/image-20241024002551780.png" alt="image-20241024002551780"></p><p><strong>哈希查找，最好时间复杂度为O(1)，最慢为线性查找O（n）</strong></p><p><img src="/img_posts/image-20241024002721950.png" alt="image-20241024002721950"></p><p><strong>排序</strong></p><p><img src="/img_posts/image-20241024002903209.png" alt="image-20241024002903209"></p><p><img src="/img_posts/image-20241024003226930.png" alt="image-20241024003226930"></p><p><img src="/img_posts/image-20241024003400767.png" alt="image-20241024003400767"></p><p><img src="/img_posts/image-20241024003534850.png" alt="image-20241024003534850"></p><p><img src="/img_posts/image-20241024003801433.png" alt="image-20241024003801433"></p><p><img src="/img_posts/image-20241024003849757.png" alt="image-20241024003849757"></p><p><img src="/img_posts/image-20241024004005259.png" alt="image-20241024004005259"></p><p><img src="/img_posts/image-20241024004344542.png" alt="image-20241024004344542"></p><p><img src="/img_posts/image-20241024004427574.png" alt="image-20241024004427574"></p><p><img src="/img_posts/image-20241024004601017.png" alt="image-20241024004601017"></p><p><img src="/img_posts/image-20241024004716683.png" alt="image-20241024004716683"></p><p>稳定性小技巧，看排序后相同的数前后位置是否发生变化，若是，则不稳定，反之则稳定。</p><h3 id="算法设计与分析（主刷题）"><a href="#算法设计与分析（主刷题）" class="headerlink" title="算法设计与分析（主刷题）"></a>算法设计与分析（主刷题）</h3><p><img src="/img_posts/image-20241024224249005.png" alt="image-20241024224249005"></p><p><img src="/img_posts/image-20241024224345434.png" alt="image-20241024224345434"></p><p><img src="/img_posts/image-20241024224620636.png" alt="image-20241024224620636"></p><p><img src="/img_posts/image-20241024224845351.png" alt="image-20241024224845351"></p><p><img src="/img_posts/image-20241024224939877.png" alt="image-20241024224939877"></p><p><img src="/img_posts/image-20241024225032942.png" alt="image-20241024225032942"></p><p><img src="/img_posts/image-20241024230205531.png" alt="image-20241024230205531"></p><p><img src="/img_posts/image-20241024230553346.png" alt="image-20241024230553346"></p><p>##操作系统</p><p><img src="/img_posts/image-20241024232515590.png" alt="image-20241024232515590"></p><p><img src="/img_posts/image-20241024232600962.png" alt="image-20241024232600962"></p><p><img src="/img_posts/image-20241024232820262.png" alt="image-20241024232820262"></p><p><img src="/img_posts/image-20241024233034499.png" alt="image-20241024233034499"></p><p><img src="/img_posts/image-20241024233409690.png" alt="image-20241024233409690"></p><p><img src="/img_posts/image-20241024233600738.png" alt="image-20241024233600738"></p><p><img src="/img_posts/image-20241024234133262.png" alt="image-20241024234133262"></p><p><img src="/img_posts/image-20241024234246439.png" alt="image-20241024234246439"></p><p><img src="/img_posts/image-20241024234447365.png" alt="image-20241024234447365"></p><p><img src="/img_posts/image-20241024234603747.png" alt="image-20241024234603747"></p><p><img src="/img_posts/image-20241024234654659.png" alt="image-20241024234654659"></p><p><strong>信号量</strong></p><p><img src="/img_posts/image-20241024234846770.png" alt="image-20241024234846770"></p><p><img src="/img_posts/image-20241024234929604.png" alt="image-20241024234929604"></p><p><img src="/img_posts/image-20241024235335424.png" alt="image-20241024235335424"></p><p><img src="/img_posts/image-20241025000952951.png" alt="image-20241025000952951"></p><p>注：把握住将将有富裕的思想</p><p><img src="/img_posts/image-20241025001332161.png" alt="image-20241025001332161"></p><p><img src="/img_posts/image-20241025001552086.png" alt="image-20241025001552086"></p><p>###存储管理</p><p><img src="/img_posts/image-20241025001752616.png" alt="image-20241025001752616"></p><p><img src="/img_posts/image-20241025002218888.png" alt="image-20241025002218888"></p><p><img src="/img_posts/image-20241025002503345.png" alt="image-20241025002503345"></p><p><img src="/img_posts/image-20241025002650774.png" alt="image-20241025002650774"></p><p><img src="/img_posts/image-20241025002753918.png" alt="image-20241025002753918"></p><p><img src="/img_posts/image-20241025002921385.png" alt="image-20241025002921385"></p><p><img src="/img_posts/image-20241025003650171.png" alt="image-20241025003650171"></p><p><strong>页面置换算法</strong></p><p><img src="/img_posts/image-20241027004559621.png" alt="image-20241027004559621"></p><p><img src="/img_posts/image-20241027004649396.png" alt="image-20241027004649396"></p><p>*OPT算法，理论上可行，但实际上在硬件上是不可能实现的。这种算法常用作于评价其他算法好坏的标准</p><p><img src="/img_posts/image-20241027005017317.png" alt="image-20241027005017317"></p><p><img src="/img_posts/image-20241027005324575.png" alt="image-20241027005324575"></p><p><em>从当前位置往之前看就行</em></p><p>###文件管理</p><p><img src="/img_posts/image-20241027005525901.png" alt="image-20241027005525901"></p><p><img src="/img_posts/image-20241027005628165.png" alt="image-20241027005628165"></p><p><img src="/img_posts/image-20241027005810344.png" alt="image-20241027005810344"></p><p><img src="/img_posts/image-20241027010154110.png" alt="image-20241027010154110"></p><p><strong>看不懂的题？？？</strong></p><p><img src="/img_posts/image-20241027011424336.png" alt="image-20241027011424336"></p><p><img src="/img_posts/image-20241027011848278.png" alt="image-20241027011848278"></p><p><img src="/img_posts/image-20241027012454754.png" alt="image-20241027012454754"></p><p><em>一个字长32位，多少个字？</em></p><h3 id="设备管理"><a href="#设备管理" class="headerlink" title="设备管理"></a>设备管理</h3><p><img src="/img_posts/image-20241027012631499.png" alt="image-20241027012631499"></p><p><img src="/img_posts/image-20241027013000549.png" alt="image-20241027013000549"></p><p><img src="/img_posts/image-20241027013026843.png" alt="image-20241027013026843"></p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>NoSQL学习记录</title>
      <link href="/2024/10/29/NoSQL%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/"/>
      <url>/2024/10/29/NoSQL%E5%AD%A6%E4%B9%A0%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="NoSQL学习记录"><a href="#NoSQL学习记录" class="headerlink" title="NoSQL学习记录"></a>NoSQL学习记录</h1><h2 id="NoSQL概述"><a href="#NoSQL概述" class="headerlink" title="NoSQL概述"></a>NoSQL概述</h2><p>NOSQL &#x3D; Not only SQL “不仅仅是SQL”</p><p>NoSQL：重点突出解决处理<strong>速度的响应</strong>和<strong>海量数据的存储问题</strong></p><p>大数据定义为高速、巨量、且多变的数据</p><p>使用NoSQL数据库的唯一标准：<strong>只要传统关系型的数据库不撒长的，都能使用NoSQL数据库</strong></p><p>NoSQL分类：键值，文档，列簇、图、其他存储数据库</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>进程记录</title>
      <link href="/2024/10/27/%E8%BF%9B%E7%A8%8B%E8%AE%B0%E5%BD%95/"/>
      <url>/2024/10/27/%E8%BF%9B%E7%A8%8B%E8%AE%B0%E5%BD%95/</url>
      
        <content type="html"><![CDATA[<h1 id="记录"><a href="#记录" class="headerlink" title="记录"></a>记录</h1><p>ps</p><p>ps -aux</p><p>ps -af</p><p>top</p>]]></content>
      
      
      <categories>
          
          <category> 笔记 </category>
          
      </categories>
      
      
    </entry>
    
    
    
    <entry>
      <title>Zxl_first-blog</title>
      <link href="/2023/12/28/Zxl-first-blog/"/>
      <url>/2023/12/28/Zxl-first-blog/</url>
      
        <content type="html"><![CDATA[<p>大家好，我是猪教授~~</p><p>这是我发布的第一篇博客！</p><p>欢迎观看。</p>]]></content>
      
      
      <categories>
          
          <category> 随笔 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 生活 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
